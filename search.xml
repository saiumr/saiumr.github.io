<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>CMake备忘单</title>
    <url>/2025/06/11/CMake%E5%A4%87%E5%BF%98%E5%8D%95/</url>
    <content><![CDATA[<h2 id="file"><a href="#file" class="headerlink" title="file"></a>file</h2><p><code>file</code> 命令用于执行各种文件和目录操作，例如创建目录、复制文件、读取文件内容等。它的功能非常强大，支持多种操作模式。  </p>
<h3 id="file常见用法"><a href="#file常见用法" class="headerlink" title="file常见用法"></a>file常见用法</h3><ol>
<li><p><strong>创建目录</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(<span class="keyword">MAKE_DIRECTORY</span> path1 path2 ...)</span><br></pre></td></tr></table></figure>

<ul>
<li>创建一个或多个目录。如果目录已存在，则不会报错。</li>
</ul>
</li>
<li><p><strong>复制文件</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(COPY file1 file2 ... DESTINATION directory)</span><br></pre></td></tr></table></figure>

<ul>
<li>将文件从源路径复制到目标目录。</li>
</ul>
</li>
<li><p><strong>写入文件</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(WRITE <span class="keyword">file</span> <span class="string">&quot;message to write&quot;</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li>将内容写入文件。如果文件已存在，则会覆盖原有内容。</li>
</ul>
</li>
<li><p><strong>追加内容到文件</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(APPEND <span class="keyword">file</span> <span class="string">&quot;message to append&quot;</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li>将内容追加到文件末尾。</li>
</ul>
</li>
<li><p><strong>读取文件内容</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(READ <span class="keyword">file</span> variable)</span><br></pre></td></tr></table></figure>

<ul>
<li>将文件内容读取到变量中。</li>
</ul>
</li>
<li><p><strong>删除文件或目录</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(<span class="keyword">REMOVE</span> file1 file2 ...)</span><br><span class="line"><span class="keyword">file</span>(REMOVE_RECURSE directory1 directory2 ...)</span><br></pre></td></tr></table></figure>

<ul>
<li>删除文件或目录。<code>REMOVE_RECURSE</code> 可以递归删除目录及其内容。</li>
</ul>
</li>
<li><p><strong>获取文件列表</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">file</span>(GLOB variable [RELATIVE path] [globbing expressions])</span><br></pre></td></tr></table></figure>

<ul>
<li>获取匹配特定模式的文件列表。<code>RELATIVE</code> 参数可以指定相对路径。</li>
</ul>
</li>
</ol>
<h3 id="file示例"><a href="#file示例" class="headerlink" title="file示例"></a>file示例</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建目录</span></span><br><span class="line"><span class="keyword">file</span>(<span class="keyword">MAKE_DIRECTORY</span> <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 复制文件</span></span><br><span class="line"><span class="keyword">file</span>(COPY <span class="variable">$&#123;CMAKE_SOURCE_DIR&#125;</span>/input.txt DESTINATION <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 写入文件</span></span><br><span class="line"><span class="keyword">file</span>(WRITE <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output/<span class="keyword">message</span>.txt <span class="string">&quot;Hello, World!&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 追加内容到文件</span></span><br><span class="line"><span class="keyword">file</span>(APPEND <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output/<span class="keyword">message</span>.txt <span class="string">&quot;\nThis is an appended line.&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 读取文件内容</span></span><br><span class="line"><span class="keyword">file</span>(READ <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output/<span class="keyword">message</span>.txt CONTENT)</span><br><span class="line"><span class="keyword">message</span>(STATUS <span class="string">&quot;File content: $&#123;CONTENT&#125;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除文件</span></span><br><span class="line"><span class="keyword">file</span>(<span class="keyword">REMOVE</span> <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/output/<span class="keyword">message</span>.txt)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取文件列表</span></span><br><span class="line"><span class="keyword">file</span>(GLOB INPUT_FILES RELATIVE <span class="variable">$&#123;CMAKE_SOURCE_DIR&#125;</span> <span class="variable">$&#123;CMAKE_SOURCE_DIR&#125;</span>/*.txt)</span><br><span class="line"><span class="keyword">message</span>(STATUS <span class="string">&quot;Input files: $&#123;INPUT_FILES&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="configure-file"><a href="#configure-file" class="headerlink" title="configure_file"></a>configure_file</h2><p><code>configure_file</code> 命令用于将文件从源路径复制到目标路径，并可以选择性地对文件内容进行变量替换。它常用于生成配置文件或复制资源文件。</p>
<h3 id="configure-file常见用法"><a href="#configure-file常见用法" class="headerlink" title="configure_file常见用法"></a>configure_file常见用法</h3><ol>
<li><p><strong>简单复制文件</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">configure_file</span>(source destination COPYONLY)</span><br></pre></td></tr></table></figure>

<ul>
<li>将源文件直接复制到目标路径，不进行任何处理。</li>
</ul>
</li>
<li><p><strong>变量替换</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">configure_file</span>(source destination)</span><br></pre></td></tr></table></figure>

<ul>
<li>将源文件复制到目标路径，并对文件中的变量进行替换。变量格式为 <code>$&#123;VARIABLE_NAME&#125;</code>。</li>
</ul>
</li>
<li><p><strong>指定文件权限</strong>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">configure_file</span>(source destination [NEWLINE_STYLE style] [PERMISSIONS permissions...])</span><br></pre></td></tr></table></figure>

<ul>
<li>可以指定文件的换行风格（如 <code>UNIX</code> 或 <code>WIN32</code>）和权限。</li>
</ul>
</li>
</ol>
<h3 id="configure-file示例"><a href="#configure-file示例" class="headerlink" title="configure_file示例"></a>configure_file示例</h3><p>假设你有一个配置文件 <code>config.in</code>，内容如下：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># config.in</span><br><span class="line">The value of MY_VARIABLE is $&#123;MY_VARIABLE&#125;.</span><br></pre></td></tr></table></figure>

<p>在 <code>CMakeLists.txt</code> 中，你可以这样使用 <code>configure_file</code>：  </p>
<figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置变量</span></span><br><span class="line"><span class="keyword">set</span>(MY_VARIABLE <span class="string">&quot;Hello, CMake!&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置文件</span></span><br><span class="line"><span class="keyword">configure_file</span>(<span class="variable">$&#123;CMAKE_SOURCE_DIR&#125;</span>/config.in <span class="variable">$&#123;CMAKE_BINARY_DIR&#125;</span>/config.txt)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看生成的文件</span></span><br><span class="line"><span class="keyword">message</span>(STATUS <span class="string">&quot;Config file generated at $&#123;CMAKE_BINARY_DIR&#125;/config.txt&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>生成的 <code>config.txt</code> 文件内容将是：  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">The value of MY_VARIABLE is Hello, CMake!.</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>cmake</tag>
        <tag>c++</tag>
      </tags>
  </entry>
  <entry>
    <title>Conda项目管理</title>
    <url>/2026/04/17/Conda%E9%A1%B9%E7%9B%AE%E7%AE%A1%E7%90%86%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<h2 id="Conda-管理项目的核心好处"><a href="#Conda-管理项目的核心好处" class="headerlink" title="Conda 管理项目的核心好处"></a>Conda 管理项目的核心好处</h2><ol>
<li>无需单独安装 Python，Conda 自带完整 Python 环境，可直接运行所有 Python 代码；  </li>
<li>环境隔离，不同项目依赖互不冲突，彻底解决第三方库（如 pynini）编译报错问题；  </li>
<li>多 Python 版本一键切换，无需反复卸载重装；  </li>
<li>依赖统一管理，避免环境混乱、路径污染。</li>
</ol>
<h2 id="Miniconda3-安装（windows上用-Scoop-安装）"><a href="#Miniconda3-安装（windows上用-Scoop-安装）" class="headerlink" title="Miniconda3 安装（windows上用 Scoop 安装）"></a>Miniconda3 安装（windows上用 Scoop 安装）</h2><p>打开终端，执行 Scoop 安装命令（前提是已安装 Scoop）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scoop install miniconda3</span><br></pre></td></tr></table></figure>

<p>安装完成后，重启终端，输入 <code>python --version</code>，显示 Python 版本即安装成功；无需额外配置环境变量，Scoop 会自动配置，可直接使用 <code>python</code> 和 <code>pip</code> 命令。  </p>
<h2 id="Conda-与-Pip-安装依赖的区别"><a href="#Conda-与-Pip-安装依赖的区别" class="headerlink" title="Conda 与 Pip 安装依赖的区别"></a>Conda 与 Pip 安装依赖的区别</h2><table>
<thead>
<tr>
<th>方式</th>
<th>安装来源</th>
<th>核心特点</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>Conda</td>
<td>conda-forge 等 Conda 仓库</td>
<td>下载 <strong>预编译二进制包</strong>，无需本地编译，Windows 兼容性极强</td>
<td>安装带 C++ 扩展的库（如 pynini）、跨语言依赖</td>
</tr>
<tr>
<td>Pip</td>
<td>PyPI 仓库</td>
<td>下载源码，Windows 下部分库（如 pynini）需本地编译，易报错</td>
<td>安装纯 Python 库（如 flask、requests），使用习惯与单独装 Python 一致</td>
</tr>
</tbody></table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda install -c conda-forge 依赖名 -y</span><br></pre></td></tr></table></figure>

<h2 id="Conda-运行-Python-项目完整步骤"><a href="#Conda-运行-Python-项目完整步骤" class="headerlink" title="Conda 运行 Python 项目完整步骤"></a>Conda 运行 Python 项目完整步骤</h2><h3 id="创建专属环境"><a href="#创建专属环境" class="headerlink" title="创建专属环境"></a>创建专属环境</h3><p>Conda 环境独立于项目文件夹，统一由 Conda 管理，创建时可指定 Python 版本，也可使用默认版本。  </p>
<ul>
<li>默认版本（使用 Conda 自带的 Python 版本）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create -n 环境名 -y  <span class="comment"># 环境名自定义，如 moss-tts-nano</span></span><br></pre></td></tr></table></figure>

<ul>
<li>指定 Python 版本（重点说明）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create -n 环境名 python=3.12 -y  <span class="comment"># 例：指定 Python 3.12</span></span><br></pre></td></tr></table></figure>

<p>说明：若 Conda 默认 Python 版本为 3.13，指定 3.12 会 <strong>自动下载 Python 3.12 版本</strong>，与默认 3.13 共存，互不干扰；每个环境的 Python 都是独立的，不会覆盖其他环境或默认版本。  </p>
<h3 id="激活专属环境"><a href="#激活专属环境" class="headerlink" title="激活专属环境"></a>激活专属环境</h3><p>激活前需要初始化（也许只需一次，不过报错会有提示的），重点是初始化之后需要重新打开终端激活环境，相当于是添加环境变量，先用下面的命令初始化：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda init</span><br></pre></td></tr></table></figure>

<p>然后打开一个新终端，使用下述命令激活环境：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda activate 环境名  <span class="comment"># 例：conda activate moss-tts-nano</span></span><br></pre></td></tr></table></figure>

<p>如果还是提示需要init，就使用管理员权限运行初始化命令。  </p>
<p>激活后，所有 <code>conda install</code> 和 <code>pip install</code> 安装的依赖，都会存入该专属环境，不影响其他环境。  </p>
<p>我们可以使用这个命令看到当前激活的环境（查看所有环境和状态）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda info --envs</span><br></pre></td></tr></table></figure>

<p>激活成功后，终端前缀会显示 <code>(环境名)</code>，此时执行 <code>python --version</code>，显示的是该环境的 Python 版本。</p>
<h3 id="环境相关操作（退出、查看、删除）"><a href="#环境相关操作（退出、查看、删除）" class="headerlink" title="环境相关操作（退出、查看、删除）"></a>环境相关操作（退出、查看、删除）</h3><ul>
<li>退出专属环境：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda deactivate</span><br></pre></td></tr></table></figure>

<p>退出后，终端前缀的 <code>(环境名)</code> 消失，恢复使用 Conda 默认环境。  </p>
<ul>
<li>查看所有 Conda 环境（含环境路径）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda info --envs</span><br></pre></td></tr></table></figure>

<ul>
<li>删除专属环境（删除后环境内所有依赖一并删除，不可恢复）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda remove -n 环境名 --all -y</span><br></pre></td></tr></table></figure>

<h3 id="环境存放位置"><a href="#环境存放位置" class="headerlink" title="环境存放位置"></a>环境存放位置</h3><p>所有 Conda 环境（包括默认环境 base、自定义环境），均统一存放在 Conda 安装目录的 <code>envs</code> 文件夹下，默认路径（Scoop 安装的conda位置是根据scoop设置来的，默认在c盘）：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">C:\Users\你的用户名\scoop\apps\miniconda3\current\envs\环境名</span><br></pre></td></tr></table></figure>

<p>每个环境对应一个独立文件夹，内含该环境的 Python 解释器、依赖库（Lib）等，与其他环境完全隔离。  </p>
<h2 id="运行-MOSS-TTS-Nano"><a href="#运行-MOSS-TTS-Nano" class="headerlink" title="运行 MOSS-TTS-Nano"></a>运行 MOSS-TTS-Nano</h2><p>注：下面的命令不是在一个终端中连续执行的，只是说明，切忌直接复制粘贴。  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/OpenMOSS/MOSS-TTS-Nano.git</span><br><span class="line"><span class="built_in">cd</span> MOSS-TTS-Nano</span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署运行，来自项目文档</span></span><br><span class="line">conda create -n moss-tts-nano python=3.12 -y</span><br><span class="line">conda init                             <span class="comment"># 管理员权限运行init</span></span><br><span class="line">conda activate moss-tts-nano           <span class="comment"># 新开终端中执行，激活环境</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装编译型依赖（如 pynini，就是为了这个玩意儿才用的conda）</span></span><br><span class="line">conda install -c conda-forge pynini=2.1.6.post1 -y</span><br><span class="line">pip install git+https://github.com/WhizZest/WeTextProcessing.git</span><br><span class="line"><span class="comment"># 安装其他项目依赖</span></span><br><span class="line"><span class="built_in">cd</span> MOSS-TTS-Nano</span><br><span class="line">pip install -r requirements.txt</span><br><span class="line"><span class="comment"># 运行项目</span></span><br><span class="line">python app.py</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>conda</tag>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>Claude Code简明教程</title>
    <url>/2026/03/18/Claude-Code%E7%AE%80%E6%98%8E%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="智能体？"><a href="#智能体？" class="headerlink" title="智能体？"></a>智能体？</h2><p>把这个标题放在第一个，是想简单说明一下不同AI应用所处的位置，以便于我们能更好地理解和使用AI应用。我自己理解也不一定非常准确，但是用于使用工具足够了。  </p>
<p>首先要明确一件事，Clude Code，Openclaw等等这些<strong>需要配置API Key</strong>的应用，并不是真正意义上的智能体，它们是强大的AI应用工具，能<strong>处理操作</strong>，并不能分析问题，分析问题的是<strong>大模型</strong>，如果把配置了大模型的 Claude Code 比作一个人，那么 Claude Code 本身是手脚，大模型才是大脑。  </p>
<p>上面标注的<strong>处理操作</strong>，实际上就是执行Claude Code内置的一系列命令（也包括我们自己安装的命令行工具），我们在CLI中直接执行这些命令就能直观感受到它是怎么处理需求的了，就像是一套可执行程序，大模型分析完问题之后输出一个执行流程给Clude Code，调用什么命令，有什么参数等等，Clude Code只是照着执行罢了，当然，它也能根据环境反馈做简单调整，但整体缺乏主动规划能力。  </p>
<p>而真正的智能体，同时拥有大脑和身体，能够对需求自主分析，在思考中不断反思，遇到不会的自己去学，得到结果后自己验证，结果不符合预期又会rollback继续迭代，可以参考《钢铁侠》中的贾维斯，《流浪地球》中的MOSS，当然那比智能体强大得多，如果未来真的问世，可能会有别的称呼吧。  </p>
<p>装上大模型的Clude Code仍然需要我们人力验证，给提示词，他才能按照要求一步步得到最终结果，这才是它和智能体最根本的区别。  </p>
<p>随着技术发展，AI应用工具与智能体的界限会越来越模糊。或许不久的将来，我们只需给出目标，剩下的交给 AI，到那时“智能体”的定义本身也可能被重新审视。  </p>
<h2 id="scoop"><a href="#scoop" class="headerlink" title="scoop"></a>scoop</h2><p>聊安装使用之前，先介绍一下scoop工具，这是Windows上的包管理工具，linux用户可忽略此节，直接查看后面安装使用部分即可，不想用scoop统一建议使用npm+国内镜像安装。有了它就可以像使用我们熟悉的<code>sudo apt install</code>一样安装软件了，也可以管理升级，查询软件信息，我感觉比去官网下载软件安装包要方便（PS：现在大部分官方软件内置升级检查，所以也不用太纠结怎么装）。  </p>
<p>后面我会介绍使用 scoop 安装 clude code 和直接安装，介绍 scoop 的管理方式，主要是因为我个人习惯用它，也算是一种记录。  </p>
<p><a href="https://scoop.sh/">Scoop官网</a>可以看到如何通过powershell安装scoop（下方命令）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在powershell中依次执行</span></span><br><span class="line">Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser</span><br><span class="line">Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression</span><br></pre></td></tr></table></figure>

<p>注意，此时它默认安装到C盘用户目录，如果你想用其他数据盘管理安装的软件，可以直接把他的文件夹移动到你想放的位置，再把环境变量中他的路径前缀改掉。  </p>
<p>一般在<code>C:\Users\xxx\scoop</code>，比如我想移动到<code>D:\</code>，直接把那个<code>scoop</code>移动到D盘，环境变量中所有scoop相关值前缀都要改成<code>D:\scoop</code>，这样scoop本体和他以后安装的软件都在<code>D:\scoop</code>下了。  </p>
<p>在官网上我们可以直接搜索想安装的软件，在scoop软件仓库里的都会列举出来并给出相应安装命令，比如VsCode  </p>
<p><img src="/../images/post/AI_Clude_0_scoop_vscode.png" alt="scoop vscode">  </p>
<p>我们按序执行这两条命令就可以安装vscode了。  </p>
<p>这里scoop的命令就不过多解释了，感兴趣可以自行了解一下，也可以在我们配置好clude code之后问问它。  </p>
<h2 id="安装与使用"><a href="#安装与使用" class="headerlink" title="安装与使用"></a>安装与使用</h2><p>下面我会详细介绍如何在 Windows 上通过 Scoop 安装 Claude Code，以及一些日常使用中会用到的核心命令和技巧。  </p>
<p>注意，在装claude code之前，我们要安装nodejs，这是claude code依赖的环境，这也是为什么我推荐使用scoop，因为nodejs也可以用scoop安装。</p>
<h3 id="Windows上使用scoop安装Claude-Code"><a href="#Windows上使用scoop安装Claude-Code" class="headerlink" title="Windows上使用scoop安装Claude Code"></a>Windows上使用scoop安装Claude Code</h3><p>Claude Code 是基于 Node.js 开发的，所以第一步需要先安装 Node.js 运行环境。  </p>
<p>使用 Scoop 安装 Node.js 非常简单，打开 PowerShell 执行以下命令即可</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用 Scoop 安装 Node.js 的长期支持版</span></span><br><span class="line">scoop install nodejs-lts</span><br></pre></td></tr></table></figure>

<p>安装完成之后，我们使用下面的命令检查一下安装  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node --version</span><br><span class="line">npm --version</span><br></pre></td></tr></table></figure>

<p>如果不行，执行一下下面的命令配置nodejs的环境变量  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scoop reset nodejs-lts</span><br></pre></td></tr></table></figure>

<p>再检查，如果还不行，检查环境变量里面有scoop目录下可执行文件或软链接的路径。  </p>
<p>还是不行的花我也没招了，上网搜搜，或者问问AI，或者不用scoop，直接安装claude code。  </p>
<p>接下来我们安装clude code，先去scoop仓库搜一下它的安装命令  </p>
<p><img src="/../images/post/AI_Clude_1_scoop_claude.png" alt="scoop clude code">  </p>
<p>按序执行安装clude code  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scoop bucket add main</span><br><span class="line">scoop install main/claude-code</span><br></pre></td></tr></table></figure>

<h3 id="Windows上直接安装Claude-Code"><a href="#Windows上直接安装Claude-Code" class="headerlink" title="Windows上直接安装Claude Code"></a>Windows上直接安装Claude Code</h3><p>使用官方脚本安装（可能需要科学上网），powershell 和 cmd 安装命令不同，选用一个就行  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># powershell 安装命令</span></span><br><span class="line">irm https://claude.ai/install.ps1 | iex</span><br><span class="line"></span><br><span class="line"><span class="comment"># cmd 安装命令</span></span><br><span class="line">curl -fsSL https://claude.ai/install.cmd -o install.cmd &amp;&amp; install.cmd &amp;&amp; del install.cmd</span><br></pre></td></tr></table></figure>

<h3 id="Mac、Linux安装Claude-Code"><a href="#Mac、Linux安装Claude-Code" class="headerlink" title="Mac、Linux安装Claude Code"></a>Mac、Linux安装Claude Code</h3><p>先安装nodejs，对于 Ubuntu&#x2F;Debian  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y nodejs npm</span><br></pre></td></tr></table></figure>

<p>对于 CentOS&#x2F;RHEL</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> yum install -y epel-release</span><br><span class="line"><span class="built_in">sudo</span> yum install -y nodejs npm</span><br></pre></td></tr></table></figure>

<p>验证  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v</span><br><span class="line">npm -v</span><br></pre></td></tr></table></figure>

<p>接着同样使用官方脚本安装，可能需要科学上网（推荐npm+国内镜像安装，直接看下一节）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -fsSL https://claude.ai/install.sh | bash</span><br></pre></td></tr></table></figure>

<h3 id="（通用，需安装好nodejs）使用npm安装Claude-Code"><a href="#（通用，需安装好nodejs）使用npm安装Claude-Code" class="headerlink" title="（通用，需安装好nodejs）使用npm安装Claude Code"></a>（通用，需安装好nodejs）使用npm安装Claude Code</h3><p>npm使用国内镜像下载安装基本没有问题  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @anthropic-ai/claude-code --registry=https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h3 id="验证安装"><a href="#验证安装" class="headerlink" title="验证安装"></a>验证安装</h3><p>查看一下版本号输出即可  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude --version</span><br></pre></td></tr></table></figure>

<h2 id="配置-API-密钥与环境变量"><a href="#配置-API-密钥与环境变量" class="headerlink" title="配置 API 密钥与环境变量"></a>配置 API 密钥与环境变量</h2><p>安装完成后，需要配置 API 密钥才能开始使用。Claude Code 通过环境变量来读取这些配置，其实这一步配置就是设置这些环境变量，我只记录部分，让我们能快速用起来。  </p>
<p>对于怎么获取API Key，一般就是去大模型官网，注册账号登录，创建Key就行了。  </p>
<p>关于更多环境变量的设置和大模型API Key的申请、切换，请参考：<a href="https://www.runoob.com/claude-code/claude-code-setup.html">Claude Code API配置-菜鸟教程</a>  </p>
<p>window上通过PowerShell设置如下（重启终端生效）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[System.Environment]::SetEnvironmentVariable(<span class="string">&quot;ANTHROPIC_BASE_URL&quot;</span>, <span class="string">&quot;你的中转服务URL&quot;</span>, [System.EnvironmentVariableTarget]::User)</span><br><span class="line">[System.Environment]::SetEnvironmentVariable(<span class="string">&quot;ANTHROPIC_AUTH_TOKEN&quot;</span>, <span class="string">&quot;你的API密钥&quot;</span>, [System.EnvironmentVariableTarget]::User)</span><br><span class="line">[System.Environment]::SetEnvironmentVariable(<span class="string">&quot;ANTHROPIC_MODEL&quot;</span>, <span class="string">&quot;指定的大模型名称&quot;</span>, [System.EnvironmentVariableTarget]::User)</span><br></pre></td></tr></table></figure>

<p>Windows上通过CMD设置如下（重启终端生效）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setx ANTHROPIC_BASE_URL 你的中转服务URL</span><br><span class="line">setx ANTHROPIC_AUTH_TOKEN 你的API密钥</span><br><span class="line">setx ANTHROPIC_MODEL 指定的大模型名称</span><br></pre></td></tr></table></figure>

<p>Unix-like系统（Linux）上通过bash设置如下（注意，默认shell是bash，配置文件是.bashrc，如果使用其他shell，如zsh，下面的.bashrc就要改为.zshrc） </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看当前的shell是不是bash，是的话直接按照下面的命令执行</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$0</span></span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;export ANTHROPIC_BASE_URL=&quot;你的中转服务URL&quot;&#x27;</span> &gt;&gt; ~/.bashrc</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;export ANTHROPIC_AUTH_TOKEN=&quot;你的API密钥&quot;&#x27;</span> &gt;&gt; ~/.bashrc</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;export ANTHROPIC_MODEL=&quot;指定的大模型名称&quot;&#x27;</span> &gt;&gt; ~/.bashrc</span><br></pre></td></tr></table></figure>

<p>使改动生效执行命令（或重启终端）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure>

<p>查看环境变量  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># powershell</span></span><br><span class="line"><span class="variable">$env</span>:ANTHROPIC_BASE_URL</span><br><span class="line"><span class="comment"># cmd</span></span><br><span class="line"><span class="built_in">echo</span> %ANTHROPIC_BASE_URL%</span><br><span class="line"><span class="comment"># bash</span></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$ANTHROPIC_BASE_URL</span></span><br></pre></td></tr></table></figure>

<h2 id="开始使用-Claude-Code"><a href="#开始使用-Claude-Code" class="headerlink" title="开始使用 Claude Code"></a>开始使用 Claude Code</h2><p>直接启动交互式会话（CLI）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude</span><br></pre></td></tr></table></figure>

<p>在特定项目中使用，需要进入项目目录启动claude  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入你的项目目录</span></span><br><span class="line"><span class="built_in">cd</span> D:\Projects\my-awesome-app</span><br><span class="line"><span class="comment"># 启动 Claude Code</span></span><br><span class="line">claude</span><br><span class="line"><span class="comment"># 初始化配置，生成claude.md，在claude code中执行</span></span><br><span class="line">/init</span><br></pre></td></tr></table></figure>

<p>首次启动配置：第一次运行 claude 时，会有一个简单的初始化向导，一般只需要按回车确认默认选项即可。  </p>
<p>Tips：最好使用<code>claude --dangerously-skip-permissions</code>启动claude code，不然它访问任何目录都要先询问一遍你，可能会让你感觉麻烦。  </p>
<h2 id="常用命令速查表"><a href="#常用命令速查表" class="headerlink" title="常用命令速查表"></a>常用命令速查表</h2><p>Claude Code 提供了丰富的内置命令，以下是一些最常用的  </p>
<table>
<thead>
<tr>
<th>命令</th>
<th>作用</th>
<th>使用场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>/init</code></td>
<td>初始化项目，生成 <code>CLAUDE.md</code> 上下文文件</td>
<td>新项目首次使用时，让 AI 理解项目结构</td>
</tr>
<tr>
<td><code>/clear</code></td>
<td>清空当前对话历史</td>
<td>切换任务时，避免旧上下文干扰</td>
</tr>
<tr>
<td><code>/compact</code></td>
<td>压缩总结当前对话，节省 token</td>
<td>对话过长时让 AI 重新聚焦</td>
</tr>
<tr>
<td><code>!</code> + 命令</td>
<td>直接执行终端命令（如 <code>!git status</code>）</td>
<td>不退出 Claude 快速执行系统命令</td>
</tr>
<tr>
<td><code>#</code> + 信息</td>
<td>将重要信息存入长期记忆</td>
<td>告诉 AI 项目技术栈、代码规范等</td>
</tr>
<tr>
<td><code>exit</code> 或 <code>Ctrl+C</code></td>
<td>退出 Claude Code</td>
<td>结束会话</td>
</tr>
</tbody></table>
<h2 id="项目上下文文件：CLAUDE-md"><a href="#项目上下文文件：CLAUDE-md" class="headerlink" title="项目上下文文件：CLAUDE.md"></a>项目上下文文件：<code>CLAUDE.md</code></h2><p>当你执行 <code>/init</code> 命令后，Claude Code 会在项目根目录生成一个 <code>CLAUDE.md</code> 文件。这个文件非常重要，它是 AI 理解你项目的“记忆库”。</p>
<p>你可以手动编辑这个文件，加入项目相关的信息，比如：</p>
<ul>
<li>项目目标和核心业务逻辑  </li>
<li>使用的技术栈（C++17 + MSVC + SDL3 + OpenGL + Assimp + Bullet）  </li>
<li>代码规范（缩进用 2 空格、命名规范等）  </li>
<li>重要模块说明</li>
</ul>
<p>这样每次启动 Claude Code 时，AI 会自动读取这些信息，后续的回答会更贴合你的项目风格。</p>
<h2 id="升级与卸载"><a href="#升级与卸载" class="headerlink" title="升级与卸载"></a>升级与卸载</h2><h3 id="使用scoop管理"><a href="#使用scoop管理" class="headerlink" title="使用scoop管理"></a>使用scoop管理</h3><p>使用scoop更新和卸载，可以先用命令确定<code>app</code>的名字  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scoop list</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 升级命令，不指定app升级scoop本身和它安装的所有软件</span></span><br><span class="line">scoop update &lt;app&gt;</span><br><span class="line"><span class="comment"># 升级claude code</span></span><br><span class="line">scoop update claude-code</span><br><span class="line"><span class="comment"># 卸载命令</span></span><br><span class="line">scoop uninstall claude-code</span><br></pre></td></tr></table></figure>

<h3 id="直接管理"><a href="#直接管理" class="headerlink" title="直接管理"></a>直接管理</h3><p>如果是官方脚本安装，升级使用claude code内置升级命令  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">claude update</span><br></pre></td></tr></table></figure>

<p>卸载就删除相关文件（可能需手动）。  </p>
<h3 id="使用npm管理"><a href="#使用npm管理" class="headerlink" title="使用npm管理"></a>使用npm管理</h3><p>如果你用的是npm安装，最好用npm升级、卸载  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 升级命令</span></span><br><span class="line">npm update -g @anthropic-ai/claude-code --registry=https://registry.npmmirror.com</span><br><span class="line"><span class="comment"># 卸载命令</span></span><br><span class="line">npm uninstall -g @anthropic-ai/claude-code</span><br></pre></td></tr></table></figure>

<h2 id="任务进行中中断（需要从官方脚本安装）"><a href="#任务进行中中断（需要从官方脚本安装）" class="headerlink" title="任务进行中中断（需要从官方脚本安装）"></a>任务进行中中断（需要从官方脚本安装）</h2><hr>
<p>更新。</p>
<p>这个问题可能和环境无关，究其原因还是claude code不再支持从npm安装的老版本，从官方脚本安装的就没有问题。  </p>
<p>不过我确实只有wsl2中使用才有问题，windows下用scoop安装的claude code可以正常使用。  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 卸载 npm 安装的老版本</span></span><br><span class="line">npm uninstall -g u/anthropic-ai/claude-code</span><br><span class="line"><span class="comment"># 从官方脚本安装老版本</span></span><br><span class="line">curl -fsSL https://claude.ai/install.sh | bash</span><br></pre></td></tr></table></figure>

<p>claude code 对中国开发者是锁国区的，除了需要科学上网工具，还需要给wsl配置代理  </p>
<p>打开魔法工具，设置好代理端口（一般默认是7890，不同工具可能有不同启用和设置方式）。  </p>
<p>在powershell下看看自己的<code>局域网IP</code>，比如我是<code>192.168.43.34</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在wsl下配置正确代理（Windows IP：192.168.43.34）</span></span><br><span class="line"><span class="built_in">export</span> WINDOWS_IP=192.168.43.34</span><br><span class="line"><span class="built_in">export</span> http_proxy=http://<span class="variable">$WINDOWS_IP</span>:7890</span><br><span class="line"><span class="built_in">export</span> https_proxy=http://<span class="variable">$WINDOWS_IP</span>:7890</span><br></pre></td></tr></table></figure>

<p>配置完之后再用上面的命令，使用官方脚本安装（网速一般全程大致5-8分钟，并非卡死）。  </p>
<p>如果返回一大串文本最终failed，按下<code>Windows键</code>，搜索<code>Windows Defender 防火墙</code>，在<code>启用或关闭WindowsDefender 防火墙</code>中把所有防火墙都禁用，再去安装，安装完之后记得开启。  </p>
<hr>
<p>下面是这个标题原本内容。</p>
<p>在我实际使用过程中（wsl Ubuntu24.04 lts），出现过莫名奇妙的任务中断  </p>
<p><code>Interrupted · What should Claude do instead?</code>  </p>
<p>再对话也是重复中断，这时候就需要退出，恢复对话，然而这样也只是偶尔能成功  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在claude中输入/exit退出</span></span><br><span class="line">/exit</span><br><span class="line"><span class="comment"># 在shell中输入命令恢复对话</span></span><br><span class="line">claude --resume</span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">claude -r</span><br><span class="line"><span class="comment"># 接着继续之前的对话即可</span></span><br></pre></td></tr></table></figure>

<h2 id="WSL中claude输入卡顿"><a href="#WSL中claude输入卡顿" class="headerlink" title="WSL中claude输入卡顿"></a>WSL中claude输入卡顿</h2><p>我在 wsl2+ubuntu24.04 环境下运行的claude code，问题原因大体还是因为反复调用不同环境中的程序导致的，在网上找到一个解决方法，亲测有效。  </p>
<p><a href="https://linux.do/t/topic/956128">Windows 下 WSL2 运行 Claude Code 卡顿的解决方法 - 项太傅</a>  </p>
<p>记录其中推荐的一种方式，把下面的命令复制到bash执行即可  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> -p ~/bin</span><br><span class="line"></span><br><span class="line"><span class="built_in">cat</span> &lt;&lt; <span class="string">&#x27;EOF&#x27;</span> &gt; ~/bin/powershell.exe</span><br><span class="line"><span class="comment">#!/bin/bash</span></span><br><span class="line"><span class="keyword">if</span> [[ <span class="string">&quot;<span class="variable">$1</span>&quot;</span> == <span class="string">&quot;-Command&quot;</span> &amp;&amp; <span class="string">&quot;<span class="variable">$2</span>&quot;</span> == <span class="string">&quot;\$env:USERPROFILE&quot;</span> ]]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&quot;C:\\Users\\Administrator&quot;</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  /mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe <span class="string">&quot;<span class="variable">$@</span>&quot;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line"><span class="built_in">chmod</span> +x ~/bin/powershell.exe</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> [[ <span class="string">&quot;<span class="variable">$SHELL</span>&quot;</span> == */zsh ]]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&#x27;export PATH=&quot;$HOME/bin:$PATH&quot;&#x27;</span> &gt;&gt; ~/.zshrc</span><br><span class="line">  SHELL_CONFIG_FILE=~/.zshrc</span><br><span class="line"><span class="keyword">elif</span> [[ <span class="string">&quot;<span class="variable">$SHELL</span>&quot;</span> == */bash ]]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">&#x27;export PATH=&quot;$HOME/bin:$PATH&quot;&#x27;</span> &gt;&gt; ~/.bashrc</span><br><span class="line">  SHELL_CONFIG_FILE=~/.bashrc</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  SHELL_CONFIG_FILE=<span class="string">&quot;your shell config file (e.g., ~/.bashrc or ~/.zshrc)&quot;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="string">&quot;<span class="variable">$HOME</span>/bin:<span class="variable">$PATH</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;✅ 修复完成！&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h2><p>claude code 使用时很占用内存，低配设备如果不加以限制<strong>可能会爆内存</strong>，实际上，不同配置能用于不同种类的业务，大致如下  </p>
<table>
<thead>
<tr>
<th>硬件配置</th>
<th>性能优化重点</th>
<th>推荐使用场景</th>
</tr>
</thead>
<tbody><tr>
<td>低配设备</td>
<td>内存限制、禁用高级分析（4GB内存，双核CPU）</td>
<td>基础代码解释、简单命令执行</td>
</tr>
<tr>
<td>中端设备</td>
<td>平衡内存与CPU占用（8GB内存，四核CPU）</td>
<td>常规开发、代码重构辅助</td>
</tr>
<tr>
<td>高端设备</td>
<td>并行任务处理、缓存优化（16GB+内存，六核+CPU）</td>
<td>大型项目分析、批量代码生成</td>
</tr>
</tbody></table>
<p>在用户配置目录下创建或修改config.json文件，添加内存限制配置  </p>
<p>windows下在 <code>C:\Users\[你的名字]\.claude.json</code>，linux下在 <code>~/.claude.json</code>。  </p>
<p>打开配置文件，添加或编辑以下配置项  </p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;memory&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;maxHeapSize&quot;</span><span class="punctuation">:</span> <span class="string">&quot;4096MB&quot;</span><span class="punctuation">,</span>  <span class="comment">// 根据系统内存总量调整，建议不超过物理内存的50%</span></span><br><span class="line">    <span class="attr">&quot;cacheSize&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1024MB&quot;</span>      <span class="comment">// 代码分析缓存大小</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>对于超过10,000文件的大型项目，可启用增量分析模式，只处理修改过的文件  </p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;analysis&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;incrementalMode&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;excludePatterns&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;node_modules/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;dist/**&quot;</span><span class="punctuation">]</span>  <span class="comment">// 排除无需分析的目录</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>通过环境变量设置Claude Code可使用的最大线程数  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># bash</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;export CLAUDE_CODE_THREADS=4&#x27;</span> &gt;&gt; .bashrc  <span class="comment"># 通常设置为CPU核心数的50-75%</span></span><br><span class="line"><span class="comment"># powershell</span></span><br><span class="line">[System.Environment]::SetEnvironmentVariable(<span class="string">&quot;CLAUDE_CODE_THREADS&quot;</span>, <span class="string">&quot;4&quot;</span>, [System.EnvironmentVariableTarget]::User)</span><br></pre></td></tr></table></figure>

<h2 id="目前最屌的配置"><a href="#目前最屌的配置" class="headerlink" title="目前最屌的配置"></a>目前最屌的配置</h2><p>claude code “冠军”配置，至于为什么这样说，搜索这个开源项目的名字就知道了  </p>
<p><a href="https://github.com/affaan-m/everything-claude-code">everything claude code</a>  </p>
<p>不看别人的博客吹它，单凭它 90k+ 的 star 也能看出来不一般，这个配置写了你想要的一切，我就不赘述了。  </p>
<h2 id="Claude-Code-能做什么？"><a href="#Claude-Code-能做什么？" class="headerlink" title="Claude Code 能做什么？"></a>Claude Code 能做什么？</h2><p>参考文章：<a href="https://turion.ai/blog/claude-code-multi-agents-subagents-guide/">Claude Code Multi-Agents and Subagents: Complete Orchestration Guide</a><br>参考项目：<a href="https://github.com/shanraisshan/claude-code-best-practice">claude-code-best-practice</a>  </p>
<p>把这个标题放在最后，是因为参考文章和项目对初学者而言过于庞大，一般我们都是了解到 Claude Code 能读写文档，改代码，自动化一些工作流程，然后直接配置使用了，所以我想，一开始避开讨论“它能做什么”专注于“我需要它做什么”会更好。  </p>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>AI</tag>
        <tag>claude</tag>
        <tag>scoop</tag>
      </tags>
  </entry>
  <entry>
    <title>Gamma校正</title>
    <url>/2025/10/23/Gamma%E6%A0%A1%E6%AD%A3/</url>
    <content><![CDATA[<h3 id="概括理解Gamma校正"><a href="#概括理解Gamma校正" class="headerlink" title="概括理解Gamma校正"></a>概括理解Gamma校正</h3><p>人眼感受色彩变化是非线性的，对暗的颜色更有辨识度，一旦亮起来感受就不那么明显了；而我们处理光照计算是在线性空间中进行的，这样才能计算得准确，这便有了<strong>Gamma校正</strong>，帮助我们计算出“真正”的颜色。</p>
<p>现在大多数图片和显示器都遵循 sRGB 色彩空间，而 sRGB 本身就内置了Gamma校正规则（默认 γ≈2.2），即图片保存时已按此规则编码，显示器显示时也会按此规则转换光强。  </p>
<p>我们在处理图片颜色的时候，需要进行“解码”，“计算”，“编码”三个步骤。解码是把图片中已Gamma编码的非线性 RGB 值（sRGB 格式）转换为线性 RGB 值，方便我们计算光照（计算得准确），而编码是把计算完成的线性 RGB 值，按 sRGB 的Gamma规则转换为非线性 RGB 值，让人眼感官看起来更舒适自然。  </p>
<p>如果跳过解码，光照计算会用错误的非线性值，导致暗部过暗、亮部过曝；如果跳过编码，线性值会被显示器<strong>二次Gamma处理</strong>，画面整体过亮失真。  </p>
<p>总的来说，Gamma校正包含“解码”和“编码”这两部分，“解码”是为了将颜色空间从非线性转换到线性空间去“计算”，当然如果颜色空间本身就是线性的就不用解码了，这牵扯到<strong>双重校正</strong>的问题，顾名思义，就是进行了两次gamma校正（本质是进行了两次编码），下面的内容会分析并解决此问题。  </p>
<h3 id="应用与效果"><a href="#应用与效果" class="headerlink" title="应用与效果"></a>应用与效果</h3><p>(1) 直接使用OpenGL内建的sRGB帧缓冲。请看下面对比图，右图开启Gamma校正。  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line">glEnable(GL_FRAMEBUFFER_SRGB);</span><br></pre></td></tr></table></figure>

<p><img src="/../images/post/ogl-2_1-Gamma%E6%A0%A1%E6%AD%A3.png" alt="GL_FRAMEBUFFER_SRGB">  </p>
<p>需要注意的是，多渲染目标（多帧缓冲）之间传递时，必须保持线性颜色，不能提前做伽马校正。只有最后一步将颜色输出到屏幕（绑定显示器对应的帧缓冲）时，才需要开启GL_FRAMEBUFFER_SRGB；如果在中间帧缓冲传递时就开启这个选项，会导致中间结果变成非线性颜色，后续再基于这些颜色计算（比如模糊、光效），结果就会出错。  </p>
<p>(2) 自己在片段着色器中进行gamma校正。  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> main()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 在线性空间做炫酷的光照效果</span></span><br><span class="line">    [...]</span><br><span class="line">    <span class="comment">// 应用Gamma校正</span></span><br><span class="line">    <span class="type">float</span> gamma = <span class="number">2.2</span>;</span><br><span class="line">    <span class="comment">// gamma encode</span></span><br><span class="line">    fragColor.rgb = <span class="built_in">pow</span>(fragColor.rgb, <span class="type">vec3</span>(<span class="number">1.0</span>/gamma));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/../images/post/ogl-2_2-Gamma%E6%A0%A1%E6%AD%A3.png" alt="片段着色器Gamma校正">  </p>
<h3 id="sRGB纹理和解码"><a href="#sRGB纹理和解码" class="headerlink" title="sRGB纹理和解码"></a>sRGB纹理和解码</h3><p>我们上面Gamma校正之后的图，有没有感觉过于亮了？因为我们现在用的纹理图片大多是 sRGB 格式，它们在保存时就已经经过一次Gamma编码，而我们又用OpenGL进行了一次Gamma编码，导致画面过曝。也就是说，我们的光照计算实际是在非线性颜色空间中，所以我们需要先解码图片，在线性颜色空间中计算光照后再编码。  </p>
<p>解码也很简单，只需要对光照颜色进行“<strong>逆</strong>”Gamma校正就能转换其到线性颜色空间了：  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> gamma = <span class="number">2.2</span>;</span><br><span class="line"><span class="comment">// gamma decode</span></span><br><span class="line"><span class="type">vec3</span> diffuseColor = <span class="built_in">pow</span>(<span class="built_in">texture</span>(diffuse, texCoords).rgb, <span class="type">vec3</span>(gamma));</span><br></pre></td></tr></table></figure>

<p>解码之后再编码便真正只进行一次编码，效果如下：  </p>
<p><img src="/../images/post/ogl-2_3-Gamma%E6%A0%A1%E6%AD%A3.png" alt="use gamma to decode">  </p>
<p>除了手动计算，OpenGL还提供了<strong>GL_SRGB</strong>和<strong>GL_SRGB_ALPHA</strong>内部纹理格式让我们加载纹理就自动进行解码。  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line">glTexImage2D(GL_TEXTURE_2D, <span class="number">0</span>, GL_SRGB, width, height, <span class="number">0</span>, GL_RGB, GL_UNSIGNED_BYTE, image);</span><br></pre></td></tr></table></figure>

<p>(需要ALPHA就指定GL_SRGB_ALPHA)  </p>
<blockquote>
<p>因为不是所有纹理都是在sRGB空间中的，所以当你把纹理指定为sRGB纹理加载时要格外小心。比如diffuse纹理，这种为物体上色的纹理几乎都是在sRGB空间中的。而为了获取光照参数的纹理，像specular贴图和法线贴图几乎都在线性空间中，所以如果你把它们也配置为sRGB纹理的话，光照就坏掉了。指定sRGB纹理时要当心。  </p>
</blockquote>
<p>将diffuse纹理定义为sRGB纹理之后，我们将获得所期望的视觉输出，但这次每个物体都会只进行一次gamma校正。  </p>
<h3 id="光照衰减"><a href="#光照衰减" class="headerlink" title="光照衰减"></a>光照衰减</h3><p>现实中，光照衰减和光源距离平方成反比：  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> attenuation = <span class="number">1.0</span> / (<span class="built_in">distance</span> * <span class="built_in">distance</span>);</span><br></pre></td></tr></table></figure>

<p>但在显示器上效果最好的衰减方程，并不是符合物理的。  </p>
<p>不进行gamma校正（不解码），显示在监视器上的衰减方程实际上将变成：  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mo stretchy="false">(</mo><mn>1.0</mn><mi mathvariant="normal">/</mi><mi>d</mi><mi>i</mi><mi>s</mi><mi>t</mi><mi>a</mi><mi>n</mi><mi>c</mi><msup><mi>e</mi><mn>2</mn></msup><msup><mo stretchy="false">)</mo><mn>2.2</mn></msup></mrow><annotation encoding="application/x-tex">
(1.0 / distance ^ 2) ^ {2.2}
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1.1141em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord">1.0/</span><span class="mord mathnormal">d</span><span class="mord mathnormal">i</span><span class="mord mathnormal">s</span><span class="mord mathnormal">t</span><span class="mord mathnormal">an</span><span class="mord mathnormal">c</span><span class="mord"><span class="mord mathnormal">e</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight">2</span></span></span></span></span></span></span></span><span class="mclose"><span class="mclose">)</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">2.2</span></span></span></span></span></span></span></span></span></span></span></span></span>

<p>这时衰减会过于强烈，反而直接使用双曲线方程效果更好：</p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mo stretchy="false">(</mo><mn>1.0</mn><mi mathvariant="normal">/</mi><mi>d</mi><mi>i</mi><mi>s</mi><mi>t</mi><mi>a</mi><mi>n</mi><mi>c</mi><mi>e</mi><msup><mo stretchy="false">)</mo><mn>2.2</mn></msup></mrow><annotation encoding="application/x-tex">
(1.0 / distance) ^ {2.2}
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1.1141em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord">1.0/</span><span class="mord mathnormal">d</span><span class="mord mathnormal">i</span><span class="mord mathnormal">s</span><span class="mord mathnormal">t</span><span class="mord mathnormal">an</span><span class="mord mathnormal">ce</span><span class="mclose"><span class="mclose">)</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">2.2</span></span></span></span></span></span></span></span></span></span></span></span></span>

<p>下面是gamma校正和衰减方程的效果组合，可以看出，不开启gamma校正时，衰减使用双曲线方程更好，开启时选用二次方程效果更好。  </p>
<p>在Demo <a href="https://github.com/saiumr/LearnOpenGL">OpenGL27-GammaCorrection</a> 中，我们用键盘上的G和F分别控制这两个效果（有修改，效果可能不同）。</p>
<table>
<thead>
<tr>
<th><img src="/../images/post/ogl-2_4-1-Gamma%E6%A0%A1%E6%AD%A3.gif" alt="pic1"></th>
<th><img src="/../images/post/ogl-2_4-2-Gamma%E6%A0%A1%E6%AD%A3.gif" alt="pic2"></th>
</tr>
</thead>
<tbody><tr>
<td><img src="/../images/post/ogl-2_4-3-Gamma%E6%A0%A1%E6%AD%A3.gif" alt="pic3"></td>
<td><img src="/../images/post/ogl-2_4-4-Gamma%E6%A0%A1%E6%AD%A3.gif" alt="pic4"></td>
</tr>
</tbody></table>
<p>上面的效果，图片的解码是在片段着色器中进行的，后来我修改使用OpenGL内部纹理格式加载纹理，颜色效果和上面的有些区别。  </p>
<h3 id="引用小结"><a href="#引用小结" class="headerlink" title="引用小结"></a>引用小结</h3><blockquote>
<p>gamma校正使我们可以在线性空间中进行操作。因为线性空间更符合物理世界，大多数物理公式现在都可以获得较好效果，比如真实的光的衰减。你的光照越真实，使用gamma校正获得漂亮的效果就越容易。这也正是为什么当引进gamma校正时，建议只去调整光照参数的原因。  </p>
</blockquote>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt开发基础</title>
    <url>/2026/03/24/Qt%E5%BC%80%E5%8F%91%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>开发概览：<a href="https://zhuanlan.zhihu.com/p/667518320">Qt类图专业技术宝典</a>  </p>
<p>可参考我的开源代码配合博客记录的内容查看：<a href="https://github.com/saiumr/LearnQt/tree/main/003-QtDevEssential">Qt Dev Essential</a>  </p>
<p>Tips1：很多设置项，都可以在Qt Creator中搜索文档看到具体说明，不论是不是初学都该在实际开发时翻阅文档确定内容。<br>Tips2：所有 UI 操作必须在主线程，Qt 严禁在子线程操作窗口。  </p>
<p>Qt，肤浅地理解为一个做GUI的玩意儿，用C++进行开发任务，实际开发感觉不如叫Q++，C++常用的东西基本被它包了个遍。以往只有本地零散的记录，在此打算系统记录一下一些概念和设计，整理出来到博客上，有些东西搞忘记了开发的时候可能会一个头两个大，实用的内容该记还是得记。  </p>
<p>PS：有部分内容是与AI问答，自己加以修改补充的，多是使用场景一类。  </p>
<h2 id="元对象系统（Meta-Object-System）"><a href="#元对象系统（Meta-Object-System）" class="headerlink" title="元对象系统（Meta-Object System）"></a>元对象系统（Meta-Object System）</h2><p>这是对C++标准的一个扩展，为Qt提供了信号槽机制，实时类型信息，动态属性系统。  </p>
<p>使用元对象系统有三个基本条件：类必须继承自QObject，类中声明<code>Q_OBJECT</code>宏（默认私有），元对象编译器<code>moc</code>（Meta_Object Compiler）。  </p>
<p>在 Qt Creator 软件中，左下角🔎查找类信息，比如输入<code>? QDialog</code>，就可以查看QDialog的文档，可以看到他继承自哪儿。  </p>
<p>翻看QObject文档内容，开头第一句是：  </p>
<blockquote>
<p>The QObject class is the base class of all Qt objects.  </p>
</blockquote>
<p>这句话让我不得不把元对象系统放第一个标题，描述中它有种不怒自威，傲视群雄的感觉。  </p>
<p>元对象系统提供的最实用的功能特性：  </p>
<ul>
<li><p><strong>信号与槽</strong>：这是 Qt 最核心的特性。传统的 C++ 回调函数难以保证类型安全且容易产生耦合，而信号槽利用元对象系统提供的类型信息，实现了对象之间的松耦合通信。在运行时，moc 生成的代码维护了信号与槽的连接表，让调用变得安全。</p>
</li>
<li><p><strong>属性系统</strong>：通过 <code>Q_PROPERTY</code> 宏，可以在运行时动态地读写对象的属性。这是 Qt 中 QML 与 C++ 交互的基石，也是 Qt Designer 能够动态调整控件属性的原理所在。</p>
</li>
<li><p><strong>运行时类型信息</strong>：标准 C++ 虽然有 typeid，但功能有限。Qt 提供了：</p>
<ul>
<li><p><code>inherits()</code>：判断一个对象是否属于某个特定类或继承链。</p>
</li>
<li><p><code>qobject_cast()</code>：类似于 C++ 的 dynamic_cast，但不需要开启 RTTI（运行时类型信息），而且速度更快，专门用于 QObject 的转换。</p>
</li>
</ul>
</li>
<li><p><strong>动态翻译</strong>：<code>tr()</code> 函数之所以能够动态切换界面语言，是因为元对象系统在运行时记录了字符串的上下文。</p>
</li>
</ul>
<p>注意一些实际开发中的问题：  </p>
<ul>
<li><p><strong>信号和槽的语法演进</strong>：</p>
<ul>
<li><p>Qt5 之前：必须使用 <code>SIGNAL()</code> 和 <code>SLOT()</code> 宏，属于字符串匹配，运行时才报错。</p>
</li>
<li><p>Qt5 及以后：推荐使用函数指针语法（如 <code>&amp;MyClass::signalName</code>），编译期检查，性能更好，也更符合现代 C++ 风格。</p>
</li>
</ul>
</li>
<li><p><strong>moc 的局限性</strong>：</p>
<ul>
<li><p>模板类问题：QObject 不能作为模板类的基类。因为 moc 不支持模板预处理，如果你的类既是模板又继承了 QObject，moc 无法正确生成元对象代码。</p>
</li>
<li><p>多重继承：如果必须多重继承，QObject 必须出现在继承列表的第一个位置。这是因为 moc 生成的代码假设 this 指针与 QObject 子对象的偏移量为 0。</p>
</li>
</ul>
</li>
<li><p><strong>编译链接问题</strong>：如果忘记写 <code>Q_OBJECT</code> 宏，信号槽连接可能会失败，或者编译时出现“undefined reference to vtable”的错误。解决办法是：确保头文件中有 Q_OBJECT，并清理项目重新编译（因为 moc 可能未重新生成）。</p>
</li>
</ul>
<h3 id="元对象编译器MOC"><a href="#元对象编译器MOC" class="headerlink" title="元对象编译器MOC"></a>元对象编译器MOC</h3><p>前面说了，Qt设计者设计了元对象系统给C++加了一套动态特性，用的时候要在类中写上 <code>Q_OBJECT</code> 宏，所以在编译前，编译器要能够扫描源码中标记了 <code>Q_OBJECT</code> 的类，自动生成额外的 C++ 代码以实现元对象系统，之后将生成的代码和用户的代码一起编译成可执行文件，这显然只靠 C++ 编译器办不到。  </p>
<p>这就是 MOC 出现的原因，MOC 将 Qt 特有的语法（如信号、槽、属性等）转换成标准 C++ 代码，它为每个使用了 <code>Q_OBJECT</code> 的类构建了一张“元数据表”，这张表在运行时可以被查询和调用。  </p>
<table>
<thead>
<tr>
<th align="left">功能</th>
<th>MOC 生成的代码负责</th>
</tr>
</thead>
<tbody><tr>
<td align="left">信号</td>
<td>为每个信号生成一个函数，该函数调用 <code>qt_metacall</code> 来激活所有连接到该信号的槽。</td>
</tr>
<tr>
<td align="left">槽</td>
<td>在元对象表中注册槽函数的索引和签名，使得 <code>qt_metacall</code> 可以根据索引调用正确的槽函数。</td>
</tr>
<tr>
<td align="left">属性系统</td>
<td>生成 <code>qt_metacall</code> 中对 <code>Q_PROPERTY</code> 的读&#x2F;写处理，支持动态获取和设置属性。</td>
</tr>
<tr>
<td align="left">运行时类型信息</td>
<td>生成 <code>staticMetaObject</code> 静态对象，以及 <code>metaObject()</code>、<code>inherits()</code>、<code>qobject_cast</code> 所需的类型比较数据。</td>
</tr>
<tr>
<td align="left">动态翻译</td>
<td>标记 <code>tr()</code> 中的字符串上下文，使翻译系统能准确定位。</td>
</tr>
</tbody></table>
<p>MOC工作流程，其中生成的 <code>moc_xxx.cpp</code> 代码可以在 <code>build</code> 目录里找到，里面有很多”元调用”（上面表格记录的调用函数）：  </p>
<pre class="mermaid">graph TD
    A[项目源文件] --> B{头文件含有 Q_OBJECT?}
    B -->|是| C[MOC 处理]
    B -->|否| D[常规编译]
    C --> E[生成 moc_xxx.cpp]
    E --> F[与原始源文件一起编译]
    D --> F
    F --> G[链接生成可执行文件]</pre>

<p>当然，作为一个编译器，我们开发时对它的存在是“无感”的，只是了解一下可以对 <code>Q_OBJECT</code> 扩展出来的奇奇怪怪的东西获得一分通透感。  </p>
<h3 id="信号与槽"><a href="#信号与槽" class="headerlink" title="信号与槽"></a>信号与槽</h3><p>信号与槽（Signals &amp; Slots）的灵活性，概括起来就九个字：松耦合，一对多，多对一；下面把它和传统回调对比同时感受一下灵活性。  </p>
<p>信号与槽相比传统回调（如函数指针、观察者模式），主要有以下优势：</p>
<ol>
<li><strong>类型安全</strong>：编译期检查参数类型，避免回调签名不匹配导致的崩溃。</li>
<li><strong>松耦合</strong>：发送者无需知道接收者是谁，只需发射信号；接收者只需连接信号，无需修改发送者代码。</li>
<li><strong>多对多通信</strong>：一个信号可连接多个槽，一个槽可接收多个信号，自动管理连接列表。</li>
<li><strong>自动生命周期管理</strong>：当发送者或接收者被销毁时，连接自动断开，避免悬空指针。</li>
</ol>
<p>对比场景实例：按钮点击时，同时更新日志和播放声音。</p>
<h4 id="传统回调（函数指针）方式"><a href="#传统回调（函数指针）方式" class="headerlink" title="传统回调（函数指针）方式"></a>传统回调（函数指针）方式</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 回调函数类型</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*ClickCallback)</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Button</span> &#123;</span><br><span class="line">    std::vector&lt;ClickCallback&gt; callbacks;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">addCallback</span><span class="params">(ClickCallback cb)</span> </span>&#123; callbacks.<span class="built_in">push_back</span>(cb); &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">click</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span> cb : callbacks) &#123; <span class="built_in">cb</span>(); &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">updateLog</span><span class="params">()</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">playSound</span><span class="params">()</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line">Button btn;</span><br><span class="line">btn.<span class="built_in">addCallback</span>(updateLog);</span><br><span class="line">btn.<span class="built_in">addCallback</span>(playSound);</span><br><span class="line">btn.<span class="built_in">click</span>();</span><br></pre></td></tr></table></figure>

<p><strong>缺点</strong>：类型不安全（可传入任何返回void无参的函数），无法传递上下文，手动管理回调列表易出错。</p>
<h4 id="Qt-信号槽方式"><a href="#Qt-信号槽方式" class="headerlink" title="Qt 信号槽方式"></a>Qt 信号槽方式</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Button</span> : <span class="keyword">public</span> QObject &#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">click</span><span class="params">()</span> </span>&#123; <span class="function">emit <span class="title">clicked</span><span class="params">()</span></span>; &#125;</span><br><span class="line">signals:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">clicked</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">App</span> : <span class="keyword">public</span> QObject &#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"><span class="keyword">public</span> slots:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">updateLog</span><span class="params">()</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">playSound</span><span class="params">()</span> </span>&#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Button btn;</span><br><span class="line">App app;</span><br><span class="line">QObject::<span class="built_in">connect</span>(&amp;btn, &amp;Button::clicked, &amp;app, &amp;App::updateLog);</span><br><span class="line">QObject::<span class="built_in">connect</span>(&amp;btn, &amp;Button::clicked, &amp;app, &amp;App::playSound);</span><br><span class="line">btn.<span class="built_in">click</span>();</span><br></pre></td></tr></table></figure>

<p><strong>优点</strong>：类型安全（连接时签名必须匹配），发送者与接收者完全解耦，断开连接自动处理。</p>
<h4 id="例子中信号槽的连接关系"><a href="#例子中信号槽的连接关系" class="headerlink" title="例子中信号槽的连接关系"></a>例子中信号槽的连接关系</h4><pre class="mermaid">graph LR
    subgraph 发送者
        B[Button]
    end
    subgraph 接收者
        A[App]
    end
    B -- clicked() --> A
    A -->|调用 playSound 和 updateLog| A</pre>

<p>图中可见一个信号可以触发多个槽，且信号与槽之间没有直接依赖。</p>
<h4 id="connect-和-disconnect"><a href="#connect-和-disconnect" class="headerlink" title="connect 和 disconnect"></a>connect 和 disconnect</h4><p>上面我们看到了信号槽的松耦合和一对多特性，多对一也是类似，设置好 <code>connect</code> 就行了，需要注意的是，一对多 <code>connect</code> 时槽方法参数要匹配。  </p>
<p>而且，信号还能触发信号（信号 <code>connect</code> 信号），这更加验证了信号槽的灵活性。  </p>
<p>断开信号与槽连接使用 <code>disconnect</code>，可以指定需要操作的对象、信号和槽，特定参数位为<code>0</code>则表示：所有对象，所有信号，所有槽。  </p>
<p>比如，断开A对象和B对象的信号槽连接：  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">disconnect</span>(&amp;A, &amp;A::signal, &amp;B, &amp;B::slot);</span><br></pre></td></tr></table></figure>

<p>断开A对象信号和B对象所有槽的连接：  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">disconnect</span>(&amp;A, &amp;A::signal, &amp;B, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>断开A对象所有信号和所有对象所有槽的连接：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">disconnect</span>(&amp;A, <span class="number">0</span>， <span class="number">0</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>断开所有对象信号和槽的连接：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">disconnect</span>(<span class="number">0</span>, <span class="number">0</span>， <span class="number">0</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<h3 id="属性系统（Q-PROPERTY）"><a href="#属性系统（Q-PROPERTY）" class="headerlink" title="属性系统（Q_PROPERTY）"></a>属性系统（<code>Q_PROPERTY</code>）</h3><p>属性系统（Q_PROPERTY）是 Qt 元对象系统的重要组成部分，它让你能够在运行时动态地读取、写入对象的属性，并且支持属性变化通知。这在自定义控件、QML 与 C++ 交互、Qt Designer 可调属性等场景中极其常用。</p>
<p>简单来说，它把一个 C++ 类的成员变量或方法“暴露”给 Qt 的元对象系统，让外部可以：  </p>
<ul>
<li>用 <code>QObject::property()</code> 和 <code>setProperty()</code> 动态读写</li>
<li>在 <code>Qt Designer</code> 中直接修改（用于自定义控件）</li>
<li>在 <code>QML</code> 中绑定和自动更新</li>
<li>配合信号实现属性变化的自动通知</li>
</ul>
<h4 id="Q-PROPERTY-基本语法"><a href="#Q-PROPERTY-基本语法" class="headerlink" title="Q_PROPERTY 基本语法"></a>Q_PROPERTY 基本语法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyWidget</span> : <span class="keyword">public</span> QWidget &#123;</span><br><span class="line">    <span class="function">Q_OBJECT</span></span><br><span class="line"><span class="function">    <span class="title">Q_PROPERTY</span><span class="params">(<span class="type">int</span> value READ value WRITE setValue NOTIFY valueChanged)</span></span></span><br><span class="line"><span class="function">    <span class="comment">//      ↑类型 ↑属性名 ↑读方法  ↑写方法   ↑变化信号（可选）</span></span></span><br><span class="line"><span class="function"><span class="keyword">public</span>:</span></span><br><span class="line"><span class="function">    int value() const &#123;</span> <span class="keyword">return</span> m_value; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setValue</span><span class="params">(<span class="type">int</span> v)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (m_value != v) &#123;</span><br><span class="line">            m_value = v;</span><br><span class="line">            <span class="function">emit <span class="title">valueChanged</span><span class="params">(v)</span></span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">signals:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">valueChanged</span><span class="params">(<span class="type">int</span> newValue)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_value = <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>关键部分</strong>：  </p>
<ul>
<li><strong>READ</strong>：必选，提供一个 const 的 getter 方法。</li>
<li><strong>WRITE</strong>：可选，提供一个 setter 方法。如果没有，属性就是只读的。</li>
<li><strong>NOTIFY</strong>：可选，当属性值改变时发射的信号。<strong>强烈建议加上</strong>，这样外部才能监听到变化（比如 QML 自动刷新）。</li>
<li>其他可选关键字：<code>DESIGNABLE</code>（是否在 Designer 中可见）、<code>STORED</code>（是否持久化）等，但工作中最常用的就是 READ、WRITE、NOTIFY。</li>
</ul>
<h4 id="常用场景"><a href="#常用场景" class="headerlink" title="常用场景"></a>常用场景</h4><ul>
<li><strong>自定义控件 + Qt Designer</strong></li>
</ul>
<p>如果你写一个自定义控件，想让用户在设计器里调整它的外观属性，就用 <code>Q_PROPERTY</code> 声明，并加上 <code>DESIGNABLE true</code>（默认就是 true）。这样 Designer 会自动生成属性编辑器。  </p>
<ul>
<li><strong>QML 与 C++ 交互</strong></li>
</ul>
<p>将 C++ 对象注册到 QML 后，QML 可以直接读写该对象的 <code>Q_PROPERTY</code> 属性，并且当 NOTIFY 信号触发时，QML 中绑定的界面会自动更新。  </p>
<ul>
<li><strong>动态保存&#x2F;恢复状态</strong></li>
</ul>
<p>用 <code>QObject::property()</code> 和 <code>setProperty()</code> 可以在不修改类的情况下临时存储一些额外数据。比如给一个按钮附加一个 id 值：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">button-&gt;<span class="built_in">setProperty</span>(<span class="string">&quot;value&quot;</span>, <span class="number">100</span>);</span><br><span class="line"><span class="type">int</span> id = button-&gt;<span class="built_in">property</span>(<span class="string">&quot;value&quot;</span>).<span class="built_in">toInt</span>();</span><br></pre></td></tr></table></figure>

<p>这在一些动态界面、数据传递时很实用。  </p>
<h4 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h4><ul>
<li><strong>属性名不要和已有的方法名冲突</strong>，通常用小写开头。  </li>
<li><strong>NOTIFY 信号</strong>尽量在属性真正变化时才发射，避免在 setter 中无脑发射（先比较新旧值）。  </li>
<li><strong>属性类型</strong>可以是 int、bool、QString、枚举（需用 <code>Q_ENUM</code> 注册）等。如果是自定义类型，需要用 <code>Q_DECLARE_METATYPE</code> 注册。  </li>
<li>如果你只是临时想给一个对象附加点数据，<strong>不需要专门定义子类</strong>，直接用 <code>setProperty</code> 动态添加即可，元对象系统会帮你在运行时管理这些动态属性。</li>
</ul>
<h2 id="对象树"><a href="#对象树" class="headerlink" title="对象树"></a>对象树</h2><p>每个<code>QObject</code>内部有一个子对象列表，父对象析构时自动删除所有子对象，层层传递，最终由顶层对象释放整棵树。  </p>
<h3 id="核心要点"><a href="#核心要点" class="headerlink" title="核心要点"></a>核心要点</h3><ol>
<li><strong>父子关系通过构造函数参数或 <code>setParent()</code> 建立</strong>：<code>new QObject(parent)</code>。  </li>
<li><strong>有父对象，绝不手动 <code>delete</code></strong>，否则会导致双重释放。  </li>
<li><strong>所有 <code>QObject</code> 子类都支持对象树</strong>（不需要 <code>Q_OBJECT</code> 宏，但你要用信号槽就得加）。  </li>
<li><strong>顶层窗口（如 <code>QMainWindow</code>）通常没有父对象</strong>，由 <code>QApplication</code> 间接管理，一般不需要手动删除。  </li>
<li><strong>可以通过 <code>findChild&lt;T&gt;()</code> 按类型&#x2F;名字查找子对象</strong>。</li>
</ol>
<h3 id="常见错误"><a href="#常见错误" class="headerlink" title="常见错误"></a>常见错误</h3><ul>
<li>在栈上创建 <code>QObject</code> 子类（如 <code>QPushButton btn(this);</code>）→ 双重释放。  </li>
<li>给有父对象的子对象手动 <code>delete</code> → 崩溃。  </li>
<li>忘记给布局或控件指定父对象 → 内存泄漏。</li>
</ul>
<h3 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h3><p>添加析构函数声明（在 <code>public</code> 区域）：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// colorcircle.h</span></span><br><span class="line">~<span class="built_in">ColorCircle</span>();</span><br></pre></td></tr></table></figure>

<p>添加析构函数实现，并包含<code>QDebug</code>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// colorcircle.cpp</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;QDebug&gt;</span></span></span><br><span class="line"></span><br><span class="line">ColorCircle::~<span class="built_in">ColorCircle</span>() &#123;</span><br><span class="line">    <span class="built_in">qDebug</span>() &lt;&lt; <span class="string">&quot;ColorCircle 被销毁了&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关闭主窗口将会看到这个打印，实际上我们<code>new QObject[Son] &#123;this&#125;</code>的地方都会被对象树管理。  </p>
<h2 id="组件基类-QWidget"><a href="#组件基类-QWidget" class="headerlink" title="组件基类 QWidget"></a>组件基类 QWidget</h2><p>如果是非自定义控件，布局和样式还可以在设计模式中通过属性表配置。  </p>
<p><a href="https://blog.csdn.net/qq_62435866/article/details/148193525">QWidget类关系图 - ꧁坚持很酷꧂（CSDN）</a>  </p>
<p><img src="/../images/post/qt-0-QWidget%E5%85%B3%E7%B3%BB%E5%9B%BE.png" alt="QWidget类关系图">  </p>
<h3 id="窗体和控件风格"><a href="#窗体和控件风格" class="headerlink" title="窗体和控件风格"></a>窗体和控件风格</h3><p>只需掌握 <strong>最常用的几种设置</strong> 和 <strong>样式表（QSS）基础</strong>，就能应对大部分界面美化需求。  </p>
<h4 id="窗口标志（Qt-WindowFlags）——-控制窗口的“长相”和“行为”"><a href="#窗口标志（Qt-WindowFlags）——-控制窗口的“长相”和“行为”" class="headerlink" title="窗口标志（Qt::WindowFlags）—— 控制窗口的“长相”和“行为”"></a>窗口标志（Qt::WindowFlags）—— 控制窗口的“长相”和“行为”</h4><p>窗口标志决定了窗口是否有标题栏、能否最大化、是否为工具窗等。常用组合：  </p>
<table>
<thead>
<tr>
<th align="left">效果</th>
<th align="left">代码（在构造或 <code>setWindowFlags</code> 中使用）</th>
</tr>
</thead>
<tbody><tr>
<td align="left">普通窗口（默认）</td>
<td align="left"><code>Qt::Widget</code></td>
</tr>
<tr>
<td align="left">顶级窗口（带标题栏、关闭按钮）</td>
<td align="left"><code>Qt::Window</code></td>
</tr>
<tr>
<td align="left">对话框（带问号？可设置）</td>
<td align="left"><code>Qt::Dialog</code></td>
</tr>
<tr>
<td align="left"><strong>无边框窗口</strong></td>
<td align="left"><code>Qt::FramelessWindowHint</code></td>
</tr>
<tr>
<td align="left"><strong>始终置顶</strong></td>
<td align="left"><code>Qt::WindowStaysOnTopHint</code></td>
</tr>
<tr>
<td align="left"><strong>工具窗口（窄标题栏，不在任务栏显示）</strong></td>
<td align="left"><code>Qt::Tool</code></td>
</tr>
</tbody></table>
<p>实际工作中最常用的是 <strong>无边框窗口</strong>（自定义标题栏和拖动）和 <strong>置顶窗口</strong>（如悬浮工具）。  </p>
<p>比如我们可以这样设置主窗口无边框：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 MainWindow 构造函数中</span></span><br><span class="line"><span class="built_in">setWindowFlags</span>(Qt::FramelessWindowHint);</span><br></pre></td></tr></table></figure>

<p>如果同时需要多个标志，用 <code>|</code> 组合：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setWindowFlags</span>(Qt::FramelessWindowHint | Qt::WindowStaysOnTopHint);</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注意</strong>：改变窗口标志后，通常需要调用 <code>show()</code> 才能生效（或在构造函数末尾调用 <code>setWindowFlags</code> 后调用 <code>show()</code>）。如果窗口已显示，可以先 <code>hide()</code> 再 <code>show()</code>。  </p>
</blockquote>
<h4 id="样式表（QSS）——-让控件变漂亮"><a href="#样式表（QSS）——-让控件变漂亮" class="headerlink" title="样式表（QSS）—— 让控件变漂亮"></a>样式表（QSS）—— 让控件变漂亮</h4><p>Qt 样式表语法类似 CSS，可以快速改变控件的外观（背景色、边框、圆角、字体等）。  </p>
<h5 id="最常用的一些样式设置"><a href="#最常用的一些样式设置" class="headerlink" title="最常用的一些样式设置"></a>最常用的一些样式设置</h5><p><strong>给整个窗口设置背景色和圆角</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setStyleSheet</span>(<span class="string">&quot;QWidget &#123; background-color: #2c3e50; border-radius: 10px; &#125;&quot;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>给按钮设置样式</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QPushButton &#123;</span><br><span class="line">    background-color: #<span class="number">3498</span>db;</span><br><span class="line">    color: white;</span><br><span class="line">    border: none;</span><br><span class="line">    border-radius: <span class="number">5</span>px;</span><br><span class="line">    padding: <span class="number">8</span>px <span class="number">16</span>px;</span><br><span class="line">&#125;</span><br><span class="line">QPushButton:hover &#123;</span><br><span class="line">    background-color: #<span class="number">2980</span>b9;</span><br><span class="line">&#125;</span><br><span class="line">QPushButton:pressed &#123;</span><br><span class="line">    background-color: #<span class="number">1</span>c5a7a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>给一个特定控件单独设置样式</strong>（用对象名）：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">button-&gt;<span class="built_in">setObjectName</span>(<span class="string">&quot;myButton&quot;</span>);</span><br><span class="line">button-&gt;<span class="built_in">setStyleSheet</span>(<span class="string">&quot;#myButton &#123; background-color: red; &#125;&quot;</span>);</span><br></pre></td></tr></table></figure>

<h4 id="实用技巧"><a href="#实用技巧" class="headerlink" title="实用技巧"></a>实用技巧</h4><ul>
<li><strong>全局样式</strong>：通常在 <code>main()</code> 中给 <code>QApplication</code> 设置样式表，所有控件都会继承。  </li>
<li><strong>边框圆角</strong>：<code>border-radius: 5px;</code> 需要配合 <code>border</code> 使用，否则不生效。  </li>
<li><strong>背景透明</strong>：<code>background-color: transparent;</code> 或 <code>background: transparent;</code>。  </li>
<li><strong>无边框窗口的阴影</strong>：无边框窗口默认没有阴影，但可以用 <code>QGraphicsDropShadowEffect</code> 模拟。</li>
</ul>
<h4 id="无边框窗口的拖动实现"><a href="#无边框窗口的拖动实现" class="headerlink" title="无边框窗口的拖动实现"></a>无边框窗口的拖动实现</h4><p>无边框窗口没有标题栏，无法拖动。你需要自己实现鼠标拖动逻辑：  </p>
<p><strong>在 MainWindow 中添加成员变量</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    QPoint m_dragPos;</span><br><span class="line">    <span class="type">bool</span> m_dragging = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure>

<p><strong>重写鼠标事件</strong>（在 MainWindow 中）：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;QMouseEvent&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MainWindow::mousePressEvent</span><span class="params">(QMouseEvent *event)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (event-&gt;<span class="built_in">button</span>() == Qt::LeftButton) &#123;</span><br><span class="line">        m_dragging = <span class="literal">true</span>;</span><br><span class="line">        m_dragPos = event-&gt;<span class="built_in">globalPos</span>() - <span class="built_in">frameGeometry</span>().<span class="built_in">topLeft</span>();</span><br><span class="line">        event-&gt;<span class="built_in">accept</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MainWindow::mouseMoveEvent</span><span class="params">(QMouseEvent *event)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (m_dragging &amp;&amp; (event-&gt;<span class="built_in">buttons</span>() &amp; Qt::LeftButton)) &#123;</span><br><span class="line">        <span class="built_in">move</span>(event-&gt;<span class="built_in">globalPos</span>() - m_dragPos);</span><br><span class="line">        event-&gt;<span class="built_in">accept</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MainWindow::mouseReleaseEvent</span><span class="params">(QMouseEvent *event)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (event-&gt;<span class="built_in">button</span>() == Qt::LeftButton) &#123;</span><br><span class="line">        m_dragging = <span class="literal">false</span>;</span><br><span class="line">        event-&gt;<span class="built_in">accept</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="半透明窗口"><a href="#半透明窗口" class="headerlink" title="半透明窗口"></a>半透明窗口</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setWindowOpacity</span>(<span class="number">0.8</span>);   <span class="comment">// 0.0 完全透明，1.0 不透明</span></span><br></pre></td></tr></table></figure>

<p>注意：无边框窗口开启半透明后，可能需要设置 <code>setAttribute(Qt::WA_TranslucentBackground)</code> 来实现真正的透明背景。  </p>
<h4 id="样式设置-api-概览"><a href="#样式设置-api-概览" class="headerlink" title="样式设置 api 概览"></a>样式设置 api 概览</h4><table>
<thead>
<tr>
<th>需求</th>
<th>代码</th>
</tr>
</thead>
<tbody><tr>
<td>无边框</td>
<td><code>setWindowFlags(Qt::FramelessWindowHint);</code></td>
</tr>
<tr>
<td>置顶</td>
<td><code>setWindowFlags(Qt::WindowStaysOnTopHint);</code></td>
</tr>
<tr>
<td>全局样式</td>
<td><code>qApp-&gt;setStyleSheet(&quot;...&quot;);</code></td>
</tr>
<tr>
<td>单控件样式</td>
<td><code>widget-&gt;setStyleSheet(&quot;...&quot;);</code></td>
</tr>
<tr>
<td>无边框拖动</td>
<td>重写 <code>mousePressEvent</code> &#x2F; <code>mouseMoveEvent</code></td>
</tr>
<tr>
<td>半透明</td>
<td><code>setWindowOpacity(0.8);</code></td>
</tr>
</tbody></table>
<h3 id="窗口和控件尺寸和尺寸策略"><a href="#窗口和控件尺寸和尺寸策略" class="headerlink" title="窗口和控件尺寸和尺寸策略"></a>窗口和控件尺寸和尺寸策略</h3><h4 id="三种基础尺寸设置方法"><a href="#三种基础尺寸设置方法" class="headerlink" title="三种基础尺寸设置方法"></a>三种基础尺寸设置方法</h4><table>
<thead>
<tr>
<th>方法</th>
<th>作用</th>
<th>常用场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>setFixedSize(w, h)</code></td>
<td>固定大小，用户不能拖动改变</td>
<td>按钮、图标、固定大小的控件</td>
</tr>
<tr>
<td><code>setMinimumSize(w, h)</code></td>
<td>设置最小尺寸（可以更大）</td>
<td>窗口、可缩放的控件</td>
</tr>
<tr>
<td><code>setMaximumSize(w, h)</code></td>
<td>设置最大尺寸</td>
<td>限制控件过大</td>
</tr>
<tr>
<td><code>resize(w, h)</code></td>
<td>设置当前尺寸（用户仍可改变）</td>
<td>窗口初始大小</td>
</tr>
</tbody></table>
<h5 id="示例：固定按钮大小"><a href="#示例：固定按钮大小" class="headerlink" title="示例：固定按钮大小"></a>示例：固定按钮大小</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QPushButton *btn = <span class="keyword">new</span> <span class="built_in">QPushButton</span>(<span class="string">&quot;确定&quot;</span>);</span><br><span class="line">btn-&gt;<span class="built_in">setFixedSize</span>(<span class="number">80</span>, <span class="number">30</span>);</span><br></pre></td></tr></table></figure>

<h5 id="示例：让窗口最小-400x300，最大-800x600"><a href="#示例：让窗口最小-400x300，最大-800x600" class="headerlink" title="示例：让窗口最小 400x300，最大 800x600"></a>示例：让窗口最小 400x300，最大 800x600</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setMinimumSize</span>(<span class="number">400</span>, <span class="number">300</span>);</span><br><span class="line"><span class="built_in">setMaximumSize</span>(<span class="number">800</span>, <span class="number">600</span>);</span><br></pre></td></tr></table></figure>

<h4 id="布局中的尺寸策略（sizePolicy）"><a href="#布局中的尺寸策略（sizePolicy）" class="headerlink" title="布局中的尺寸策略（sizePolicy）"></a>布局中的尺寸策略（<code>sizePolicy</code>）</h4><p>当你使用布局管理器（<code>QVBoxLayout</code> 等）时，控件的大小由布局和控件的 <strong>尺寸策略</strong> 共同决定。  </p>
<h5 id="常用策略（QSizePolicy-Policy）"><a href="#常用策略（QSizePolicy-Policy）" class="headerlink" title="常用策略（QSizePolicy::Policy）"></a>常用策略（<code>QSizePolicy::Policy</code>）</h5><table>
<thead>
<tr>
<th>策略</th>
<th>含义</th>
<th>常见控件默认策略</th>
</tr>
</thead>
<tbody><tr>
<td><code>Fixed</code></td>
<td>大小固定，不拉伸</td>
<td>按钮、标签（水平方向）</td>
</tr>
<tr>
<td><code>Minimum</code></td>
<td>可以扩展，但不能小于 <code>sizeHint()</code></td>
<td>输入框</td>
</tr>
<tr>
<td><code>Expanding</code></td>
<td>优先扩展，占据多余空间</td>
<td>文本编辑、列表框</td>
</tr>
<tr>
<td><code>Preferred</code></td>
<td>按 <code>sizeHint()</code>，可以扩展或收缩</td>
<td>大多数控件</td>
</tr>
<tr>
<td><code>Maximum</code></td>
<td>可以收缩，但不能大于 <code>sizeHint()</code></td>
<td>不常用</td>
</tr>
</tbody></table>
<h5 id="让按钮水平方向自动拉伸"><a href="#让按钮水平方向自动拉伸" class="headerlink" title="让按钮水平方向自动拉伸"></a>让按钮水平方向自动拉伸</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">button-&gt;<span class="built_in">setSizePolicy</span>(QSizePolicy::Expanding, QSizePolicy::Fixed);</span><br></pre></td></tr></table></figure>

<p>第一个参数是水平策略，第二个是垂直策略。  </p>
<h5 id="重要概念：sizeHint-和-minimumSizeHint"><a href="#重要概念：sizeHint-和-minimumSizeHint" class="headerlink" title="重要概念：sizeHint() 和 minimumSizeHint()"></a>重要概念：<code>sizeHint()</code> 和 <code>minimumSizeHint()</code></h5><ul>
<li><code>sizeHint()</code>：控件的“建议大小”，基于内容（如按钮文字、图片）。  </li>
<li><code>minimumSizeHint()</code>：控件能正常显示的最小大小。</li>
</ul>
<p>你很少需要重写这些函数，但要理解布局会参考它们。</p>
<h4 id="让控件随窗口自动缩放（最常用）"><a href="#让控件随窗口自动缩放（最常用）" class="headerlink" title="让控件随窗口自动缩放（最常用）"></a>让控件随窗口自动缩放（最常用）</h4><p>当你把控件放入布局（<code>QVBoxLayout</code>、<code>QHBoxLayout</code>、<code>QGridLayout</code>）后，布局会自动调整控件大小。想让某个控件在窗口放大时“抢”到更多空间，有两个办法：  </p>
<h5 id="方法1：设置-sizePolicy-为-Expanding"><a href="#方法1：设置-sizePolicy-为-Expanding" class="headerlink" title="方法1：设置 sizePolicy 为 Expanding"></a>方法1：设置 <code>sizePolicy</code> 为 <code>Expanding</code></h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">widget-&gt;<span class="built_in">setSizePolicy</span>(QSizePolicy::Expanding, QSizePolicy::Expanding);</span><br></pre></td></tr></table></figure>

<h5 id="方法2：设置布局的拉伸因子（stretch）"><a href="#方法2：设置布局的拉伸因子（stretch）" class="headerlink" title="方法2：设置布局的拉伸因子（stretch）"></a>方法2：设置布局的拉伸因子（<code>stretch</code>）</h5><p>在 <code>addWidget</code> 时指定拉伸系数：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">layout-&gt;<span class="built_in">addWidget</span>(widget1, <span class="number">1</span>);  <span class="comment">// 拉伸因子1</span></span><br><span class="line">layout-&gt;<span class="built_in">addWidget</span>(widget2, <span class="number">2</span>);  <span class="comment">// 拉伸因子2 → widget2 获得两倍多余空间</span></span><br></pre></td></tr></table></figure>

<h4 id="尺寸设置-api-概览"><a href="#尺寸设置-api-概览" class="headerlink" title="尺寸设置 api 概览"></a>尺寸设置 api 概览</h4><table>
<thead>
<tr>
<th>需求</th>
<th>代码</th>
</tr>
</thead>
<tbody><tr>
<td>固定大小</td>
<td><code>setFixedSize(w, h)</code></td>
</tr>
<tr>
<td>最小&#x2F;最大限制</td>
<td><code>setMinimumSize(w, h)</code> &#x2F; <code>setMaximumSize(w, h)</code></td>
</tr>
<tr>
<td>布局中让控件可扩展</td>
<td><code>setSizePolicy(Expanding, Expanding)</code></td>
</tr>
<tr>
<td>布局中按比例分配空间</td>
<td><code>addWidget(widget, stretch)</code></td>
</tr>
<tr>
<td>窗口初始大小</td>
<td><code>resize(w, h)</code></td>
</tr>
</tbody></table>
<h2 id="常用控件-QDialog"><a href="#常用控件-QDialog" class="headerlink" title="常用控件 QDialog"></a>常用控件 QDialog</h2><p>只需要掌握 <strong>模态 vs 非模态</strong>、<strong>标准对话框</strong> 和 <strong>如何做自定义对话框</strong>，就能应对大部分场景。  </p>
<h3 id="QDialog-是什么？"><a href="#QDialog-是什么？" class="headerlink" title="QDialog 是什么？"></a>QDialog 是什么？</h3><p><code>QDialog</code> 是 <code>QWidget</code> 的子类，专门用来做 <strong>临时交互窗口</strong>（比如提示、设置、选择）。它的核心特点是 <strong>可以以“模态”方式运行</strong>，阻塞其他窗口直到对话框关闭。  </p>
<h3 id="模态-vs-非模态（最重要）"><a href="#模态-vs-非模态（最重要）" class="headerlink" title="模态 vs 非模态（最重要）"></a>模态 vs 非模态（最重要）</h3><table>
<thead>
<tr>
<th>类型</th>
<th>效果</th>
<th>常用场景</th>
</tr>
</thead>
<tbody><tr>
<td><strong>模态（Modal）</strong></td>
<td>弹出后，用户必须关闭对话框才能操作其他窗口</td>
<td>文件选择、确认删除、登录框</td>
</tr>
<tr>
<td><strong>非模态（Modeless）</strong></td>
<td>弹出后，可以同时操作主窗口</td>
<td>查找替换、属性面板、工具窗口</td>
</tr>
</tbody></table>
<h3 id="代码写法"><a href="#代码写法" class="headerlink" title="代码写法"></a>代码写法</h3><p><strong>模态（阻塞）</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">QDialog <span class="title">dialog</span><span class="params">(<span class="keyword">this</span>)</span></span>;</span><br><span class="line">dialog.<span class="built_in">exec</span>();  <span class="comment">// 阻塞，直到对话框关闭</span></span><br></pre></td></tr></table></figure>

<p><strong>非模态（不阻塞）</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QDialog *dialog = <span class="keyword">new</span> <span class="built_in">QDialog</span>(<span class="keyword">this</span>);</span><br><span class="line">dialog-&gt;<span class="built_in">setAttribute</span>(Qt::WA_DeleteOnClose); <span class="comment">// 关闭时自动删除</span></span><br><span class="line">dialog-&gt;<span class="built_in">show</span>();  <span class="comment">// 不阻塞</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>工作中 <strong>模态对话框更常用</strong>，因为它简单且符合大多数用户预期。  </p>
</blockquote>
<p>在 Qt 中，设置对话框的模态&#x2F;非模态行为有多种方式。下表总结了最常用的方法及其效果：  </p>
<table>
<thead>
<tr>
<th>方式</th>
<th>代码示例</th>
<th>模态&#x2F;非模态</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>exec()</code></td>
<td><code>dialog.exec();</code></td>
<td><strong>模态</strong>（阻塞）</td>
<td>启动独立的事件循环，阻塞所有同应用窗口，直到对话框关闭。返回值 <code>Accepted</code> &#x2F; <code>Rejected</code>。</td>
</tr>
<tr>
<td><code>setModal(true)</code> + <code>show()</code></td>
<td><code>dialog.setModal(true); dialog.show();</code></td>
<td><strong>模态</strong>（非阻塞）</td>
<td>设置为模态后调用 <code>show()</code>，不阻塞代码执行，但对话框模态阻止其他窗口交互。</td>
</tr>
<tr>
<td><code>setModal(false)</code> + <code>show()</code></td>
<td><code>dialog.setModal(false); dialog.show();</code></td>
<td><strong>非模态</strong></td>
<td>对话框不阻塞其他窗口，可同时操作。</td>
</tr>
<tr>
<td><code>open()</code></td>
<td><code>dialog.open();</code></td>
<td><strong>模态</strong>（非阻塞）</td>
<td>相当于 <code>setModal(true)</code> + <code>show()</code>，且提供了 <code>finished</code> 信号（可连接槽接收结果）。不阻塞代码。</td>
</tr>
<tr>
<td><code>setWindowModality()</code></td>
<td><code>dialog.setWindowModality(Qt::ApplicationModal); dialog.show();</code></td>
<td>按参数决定</td>
<td>细粒度控制：<code>Qt::NonModal</code>（非模态）、<code>Qt::WindowModal</code>（阻塞父窗口及祖先）、<code>Qt::ApplicationModal</code>（阻塞整个应用）。</td>
</tr>
<tr>
<td><code>show()</code> 默认</td>
<td>如果父窗口存在且未设置 <code>WA_ShowModal</code> 属性</td>
<td>通常<strong>非模态</strong></td>
<td>默认 <code>QDialog</code> 的 <code>show()</code> 是非模态的，除非设置了模态属性或调用了 <code>exec()</code>。</td>
</tr>
</tbody></table>
<h3 id="Qt-内置的标准对话框（拿来即用）"><a href="#Qt-内置的标准对话框（拿来即用）" class="headerlink" title="Qt 内置的标准对话框（拿来即用）"></a>Qt 内置的标准对话框（拿来即用）</h3><table>
<thead>
<tr>
<th>对话框</th>
<th>静态方法</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>消息框</td>
<td><code>QMessageBox::information()</code>, <code>warning()</code>, <code>critical()</code>, <code>question()</code></td>
<td>提示信息、询问确认</td>
</tr>
<tr>
<td>文件对话框</td>
<td><code>QFileDialog::getOpenFileName()</code>, <code>getSaveFileName()</code></td>
<td>打开&#x2F;保存文件</td>
</tr>
<tr>
<td>颜色对话框</td>
<td><code>QColorDialog::getColor()</code></td>
<td>选择颜色</td>
</tr>
<tr>
<td>字体对话框</td>
<td><code>QFontDialog::getFont()</code></td>
<td>选择字体</td>
</tr>
<tr>
<td>输入对话框</td>
<td><code>QInputDialog::getText()</code>, <code>getInt()</code>, <code>getDouble()</code></td>
<td>获取用户输入</td>
</tr>
</tbody></table>
<h3 id="示例：消息框询问"><a href="#示例：消息框询问" class="headerlink" title="示例：消息框询问"></a>示例：消息框询问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QMessageBox::StandardButton reply = QMessageBox::<span class="built_in">question</span>(<span class="keyword">this</span>, <span class="string">&quot;确认&quot;</span>, <span class="string">&quot;确定要退出吗？&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (reply == QMessageBox::Yes) &#123;</span><br><span class="line">    <span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="示例：获取颜色"><a href="#示例：获取颜色" class="headerlink" title="示例：获取颜色"></a>示例：获取颜色</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QColor color = QColorDialog::<span class="built_in">getColor</span>(Qt::red, <span class="keyword">this</span>, <span class="string">&quot;选择颜色&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (color.<span class="built_in">isValid</span>()) &#123;</span><br><span class="line">    <span class="comment">// 使用 color</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="自定义对话框（最实用）"><a href="#自定义对话框（最实用）" class="headerlink" title="自定义对话框（最实用）"></a>自定义对话框（最实用）</h3><p>当标准对话框不能满足需求时，自己做一个对话框很简单：  </p>
<h4 id="1-新建一个类继承-QDialog"><a href="#1-新建一个类继承-QDialog" class="headerlink" title="1. 新建一个类继承 QDialog"></a>1. 新建一个类继承 <code>QDialog</code></h4><p><strong>myDialog.h</strong>  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;QDialog&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;QLineEdit&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;QPushButton&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyDialog</span> : <span class="keyword">public</span> QDialog &#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">MyDialog</span><span class="params">(QWidget *parent = <span class="literal">nullptr</span>)</span></span>;</span><br><span class="line">    <span class="function">QString <span class="title">getInputText</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> m_lineEdit-&gt;<span class="built_in">text</span>(); &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    QLineEdit *m_lineEdit;</span><br><span class="line">    QPushButton *m_okBtn;</span><br><span class="line">    QPushButton *m_cancelBtn;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>myDialog.cpp</strong>  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">MyDialog::<span class="built_in">MyDialog</span>(QWidget *parent) : <span class="built_in">QDialog</span>(parent) &#123;</span><br><span class="line">    <span class="built_in">setWindowTitle</span>(<span class="string">&quot;自定义输入&quot;</span>);</span><br><span class="line">    QVBoxLayout *layout = <span class="keyword">new</span> <span class="built_in">QVBoxLayout</span>(<span class="keyword">this</span>);</span><br><span class="line">    m_lineEdit = <span class="keyword">new</span> <span class="built_in">QLineEdit</span>(<span class="keyword">this</span>);</span><br><span class="line">    m_okBtn = <span class="keyword">new</span> <span class="built_in">QPushButton</span>(<span class="string">&quot;确定&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line">    m_cancelBtn = <span class="keyword">new</span> <span class="built_in">QPushButton</span>(<span class="string">&quot;取消&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line">    layout-&gt;<span class="built_in">addWidget</span>(m_lineEdit);</span><br><span class="line">    layout-&gt;<span class="built_in">addWidget</span>(m_okBtn);</span><br><span class="line">    layout-&gt;<span class="built_in">addWidget</span>(m_cancelBtn);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">connect</span>(m_okBtn, &amp;QPushButton::clicked, <span class="keyword">this</span>, &amp;QDialog::accept);</span><br><span class="line">    <span class="built_in">connect</span>(m_cancelBtn, &amp;QPushButton::clicked, <span class="keyword">this</span>, &amp;QDialog::reject);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-使用自定义对话框"><a href="#2-使用自定义对话框" class="headerlink" title="2. 使用自定义对话框"></a>2. 使用自定义对话框</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">MyDialog <span class="title">dlg</span><span class="params">(<span class="keyword">this</span>)</span></span>;</span><br><span class="line"><span class="keyword">if</span> (dlg.<span class="built_in">exec</span>() == QDialog::Accepted) &#123;</span><br><span class="line">    QString text = dlg.<span class="built_in">getInputText</span>();</span><br><span class="line">    <span class="comment">// 处理输入</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关键点：  </p>
<ul>
<li>使用 <code>exec()</code> 模态运行。  </li>
<li>点击确定调用 <code>accept()</code>，点击取消调用 <code>reject()</code>。  </li>
<li>通过 <code>exec()</code> 的返回值判断用户操作。</li>
</ul>
<h4 id="作用机制"><a href="#作用机制" class="headerlink" title="作用机制"></a>作用机制</h4><p>一、exec() 做了什么？  </p>
<p>当你调用 dialog.exec() 时，Qt 会：  </p>
<ol>
<li><p>显示对话框（如果还没显示）。  </p>
</li>
<li><p>启动一个新的事件循环（QEventLoop），这个循环会不断处理窗口消息（鼠标点击、键盘输入等）。  </p>
</li>
<li><p>阻塞当前代码（模态），直到事件循环退出。  </p>
</li>
<li><p>返回一个结果：QDialog::Accepted 或 QDialog::Rejected。</p>
</li>
</ol>
<p>那事件循环什么时候退出？当对话框内部调用了 <code>accept()</code> 或 <code>reject()</code> 时。  </p>
<p>二、accept() 和 reject() 的作用  </p>
<p>accept()：告诉对话框“用户确认了”，内部会设置结果为 Accepted，然后退出事件循环。</p>
<p>reject()：告诉对话框“用户取消了”，内部设置结果为 Rejected，然后退出事件循环。</p>
<p>这两个函数是 QDialog 的槽，也是普通的成员函数。你可以随时调用它们。  </p>
<p>所以我们需要事先connect信号槽（clicked和accept&#x2F;reject），后面用 <code>dialog.exec()</code> 调用就可以生效了。  </p>
<h3 id="QDialog-api-概览"><a href="#QDialog-api-概览" class="headerlink" title="QDialog api 概览"></a>QDialog api 概览</h3><table>
<thead>
<tr>
<th>需求</th>
<th>代码</th>
</tr>
</thead>
<tbody><tr>
<td>模态对话框</td>
<td><code>dlg.exec()</code></td>
</tr>
<tr>
<td>非模态对话框</td>
<td><code>dlg.show()</code> + <code>setAttribute(Qt::WA_DeleteOnClose)</code></td>
</tr>
<tr>
<td>标准消息框</td>
<td><code>QMessageBox::question(...)</code></td>
</tr>
<tr>
<td>标准颜色对话框</td>
<td><code>QColorDialog::getColor(...)</code></td>
</tr>
<tr>
<td>自定义对话框</td>
<td>继承 <code>QDialog</code>，使用 <code>accept()</code> &#x2F; <code>reject()</code></td>
</tr>
<tr>
<td>判断用户确认</td>
<td><code>if (dlg.exec() == QDialog::Accepted) &#123;...&#125;</code></td>
</tr>
</tbody></table>
<h2 id="常用控件-QLabel"><a href="#常用控件-QLabel" class="headerlink" title="常用控件 QLabel"></a>常用控件 QLabel</h2><p><code>QLabel</code> 是最常用的显示控件之一，用来显示文本、数字、图片或富文本。它不能编辑，但可以响应点击等事件（需额外处理）。  </p>
<h3 id="基础用法：显示文本"><a href="#基础用法：显示文本" class="headerlink" title="基础用法：显示文本"></a>基础用法：显示文本</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLabel *label = <span class="keyword">new</span> <span class="built_in">QLabel</span>(<span class="string">&quot;Hello Qt&quot;</span>, <span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>

<p>常用设置：  </p>
<ul>
<li><code>setText(QString)</code>：改变显示内容。  </li>
<li><code>text()</code>：获取当前文本。  </li>
<li><code>setAlignment(Qt::AlignCenter)</code>：设置对齐（居中、左、右等）。  </li>
<li><code>setWordWrap(bool)</code>：是否自动换行。</li>
</ul>
<p><strong>示例：创建并设置标签</strong>  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLabel *info = <span class="keyword">new</span> <span class="built_in">QLabel</span>(<span class="keyword">this</span>);</span><br><span class="line">info-&gt;<span class="built_in">setText</span>(<span class="string">&quot;用户名：&quot;</span>);</span><br><span class="line">info-&gt;<span class="built_in">setAlignment</span>(Qt::AlignRight | Qt::AlignVCenter);</span><br></pre></td></tr></table></figure>

<p>对齐方式常用组合：  </p>
<ul>
<li><code>Qt::AlignLeft</code> &#x2F; <code>AlignRight</code> &#x2F; <code>AlignHCenter</code>（水平）  </li>
<li><code>Qt::AlignTop</code> &#x2F; <code>AlignBottom</code> &#x2F; <code>AlignVCenter</code>（垂直）  </li>
<li>可以用 <code>|</code> 组合，如 <code>AlignCenter</code> 即水平垂直居中。</li>
</ul>
<h3 id="显示数字或其它类型"><a href="#显示数字或其它类型" class="headerlink" title="显示数字或其它类型"></a>显示数字或其它类型</h3><p><code>QLabel</code> 只接受 <code>QString</code>，所以需要将数字转换：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> count = <span class="number">10</span>;</span><br><span class="line">label-&gt;<span class="built_in">setText</span>(QString::<span class="built_in">number</span>(count));          <span class="comment">// &quot;10&quot;</span></span><br><span class="line">label-&gt;<span class="built_in">setText</span>(<span class="built_in">QString</span>(<span class="string">&quot;数值：%1&quot;</span>).<span class="built_in">arg</span>(count));   <span class="comment">// &quot;数值：10&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="显示图片（QPixmap）"><a href="#显示图片（QPixmap）" class="headerlink" title="显示图片（QPixmap）"></a>显示图片（QPixmap）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLabel *imageLabel = <span class="keyword">new</span> <span class="built_in">QLabel</span>(<span class="keyword">this</span>);</span><br><span class="line"><span class="function">QPixmap <span class="title">pixmap</span><span class="params">(<span class="string">&quot;./images/logo.png&quot;</span>)</span></span>;   <span class="comment">// 资源文件或绝对路径</span></span><br><span class="line">imageLabel-&gt;<span class="built_in">setPixmap</span>(pixmap);</span><br><span class="line"><span class="comment">// 可选：缩放图片以适应标签大小（会改变像素）</span></span><br><span class="line">imageLabel-&gt;<span class="built_in">setScaledContents</span>(<span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<p>如果你想保持图片比例，可以调整标签大小或使用 <code>QPixmap::scaled()</code>。</p>
<h3 id="富文本（HTML-子集）"><a href="#富文本（HTML-子集）" class="headerlink" title="富文本（HTML 子集）"></a>富文本（HTML 子集）</h3><p><code>QLabel</code> 支持简单的 HTML 标签，如 <code>&lt;b&gt;</code>、<code>&lt;i&gt;</code>、<code>&lt;font color=...&gt;</code>、<code>&lt;img&gt;</code> 等。  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">label-&gt;<span class="built_in">setText</span>(<span class="string">&quot;&lt;b&gt;粗体&lt;/b&gt; 和 &lt;i&gt;斜体&lt;/i&gt; 以及 &lt;font color=red&gt;红色&lt;/font&gt;&quot;</span>);</span><br><span class="line">label-&gt;<span class="built_in">setText</span>(<span class="string">&quot;&lt;a href=&#x27;http://qt.io&#x27;&gt;Qt官网&lt;/a&gt;&quot;</span>);</span><br></pre></td></tr></table></figure>

<p><strong>处理链接点击</strong>：需要设置 <code>setOpenExternalLinks(true)</code> 才会自动打开浏览器。  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">label-&gt;<span class="built_in">setText</span>(<span class="string">&quot;&lt;a href=&#x27;https://www.qt.io&#x27;&gt;Qt官网&lt;/a&gt;&quot;</span>);</span><br><span class="line">label-&gt;<span class="built_in">setOpenExternalLinks</span>(<span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<h3 id="常用信号（QLabel-本身信号很少）"><a href="#常用信号（QLabel-本身信号很少）" class="headerlink" title="常用信号（QLabel 本身信号很少）"></a>常用信号（QLabel 本身信号很少）</h3><p>QLabel 不直接提供点击信号。要检测鼠标点击，需要重写 <code>mousePressEvent</code> 或使用事件过滤器。但简单做法是用一个按钮替代，或者将 <code>QLabel</code> 放在一个 <code>QPushButton</code> 内部（不推荐）。  </p>
<p>如果你需要可点击的文本，可以用 <code>QPushButton</code> 设置扁平样式，或用 <code>QLabel</code> + 事件过滤器。新手阶段可以先用按钮。  </p>
<h3 id="QLabel-api-概览"><a href="#QLabel-api-概览" class="headerlink" title="QLabel api 概览"></a>QLabel api 概览</h3><table>
<thead>
<tr>
<th>功能</th>
<th>代码</th>
</tr>
</thead>
<tbody><tr>
<td>设置文本</td>
<td><code>setText(QString)</code></td>
</tr>
<tr>
<td>获取文本</td>
<td><code>text()</code></td>
</tr>
<tr>
<td>设置对齐</td>
<td><code>setAlignment(Qt::AlignCenter)</code></td>
</tr>
<tr>
<td>显示数字</td>
<td><code>setText(QString::number(num))</code></td>
</tr>
<tr>
<td>显示图片</td>
<td><code>setPixmap(QPixmap(...))</code></td>
</tr>
<tr>
<td>缩放图片适应</td>
<td><code>setScaledContents(true)</code></td>
</tr>
<tr>
<td>支持HTML</td>
<td>直接 <code>setText(&quot;&lt;b&gt;bold&lt;/b&gt;&quot;)</code></td>
</tr>
<tr>
<td>自动打开链接</td>
<td><code>setOpenExternalLinks(true)</code></td>
</tr>
</tbody></table>
<h2 id="常用控件-Button"><a href="#常用控件-Button" class="headerlink" title="常用控件 Button"></a>常用控件 Button</h2><p>在 Qt 中，所有按钮都继承自 <code>QAbstractButton</code>，它提供了按钮的通用功能（点击、状态、文本等）。实际工作中最常用的按钮子类有四种：<code>QPushButton</code>（普通按钮）、<code>QToolButton</code>（工具按钮）、<code>QRadioButton</code>（单选按钮）、<code>QCheckBox</code>（复选框）。  </p>
<h3 id="按钮家族总览"><a href="#按钮家族总览" class="headerlink" title="按钮家族总览"></a>按钮家族总览</h3><table>
<thead>
<tr>
<th>按钮类型</th>
<th>典型用途</th>
<th>是否互斥</th>
<th>是否可选中</th>
</tr>
</thead>
<tbody><tr>
<td><code>QPushButton</code></td>
<td>执行命令（确定、取消、提交）</td>
<td>否</td>
<td>通常不可选中（除非设置 checkable）</td>
</tr>
<tr>
<td><code>QToolButton</code></td>
<td>工具栏中的图标按钮</td>
<td>否</td>
<td>同左</td>
</tr>
<tr>
<td><code>QRadioButton</code></td>
<td>单选选项（性别、模式）</td>
<td>是（同一父控件内）</td>
<td>是，选中后保持</td>
</tr>
<tr>
<td><code>QCheckBox</code></td>
<td>多选选项（兴趣爱好）</td>
<td>否</td>
<td>是，独立选中&#x2F;取消</td>
</tr>
</tbody></table>
<h3 id="逐个击破（实用级）"><a href="#逐个击破（实用级）" class="headerlink" title="逐个击破（实用级）"></a>逐个击破（实用级）</h3><h4 id="1-QPushButton：最常用的按钮"><a href="#1-QPushButton：最常用的按钮" class="headerlink" title="1. QPushButton：最常用的按钮"></a>1. <code>QPushButton</code>：最常用的按钮</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QPushButton *btn = <span class="keyword">new</span> <span class="built_in">QPushButton</span>(<span class="string">&quot;点击我&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line"><span class="comment">// 连接点击信号</span></span><br><span class="line"><span class="built_in">connect</span>(btn, &amp;QPushButton::clicked, <span class="keyword">this</span>, []()&#123;</span><br><span class="line">    <span class="built_in">qDebug</span>() &lt;&lt; <span class="string">&quot;按钮被点了&quot;</span>;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><strong>常用设置</strong>：  </p>
<ul>
<li><code>setEnabled(bool)</code>：禁用&#x2F;启用（变灰）。</li>
<li><code>setCheckable(true)</code>：变成开关按钮（按下&#x2F;弹起状态）。</li>
<li><code>setIcon(QIcon(&quot;:/icon.png&quot;))</code>：添加图标。</li>
</ul>
<p><strong>工作中</strong>：<code>QPushButton</code> 占按钮使用的 80% 以上，记住它的点击信号就够了。</p>
<h4 id="2-QToolButton：工具栏按钮（通常只显示图标）"><a href="#2-QToolButton：工具栏按钮（通常只显示图标）" class="headerlink" title="2. QToolButton：工具栏按钮（通常只显示图标）"></a>2. <code>QToolButton</code>：工具栏按钮（通常只显示图标）</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QToolButton *toolBtn = <span class="keyword">new</span> <span class="built_in">QToolButton</span>(<span class="keyword">this</span>);</span><br><span class="line">toolBtn-&gt;<span class="built_in">setIcon</span>(<span class="built_in">QIcon</span>(<span class="string">&quot;:/save.png&quot;</span>));</span><br><span class="line">toolBtn-&gt;<span class="built_in">setToolTip</span>(<span class="string">&quot;保存&quot;</span>);  <span class="comment">// 悬停提示</span></span><br><span class="line"><span class="built_in">connect</span>(toolBtn, &amp;QToolButton::clicked, <span class="keyword">this</span>, &amp;saveFile);</span><br></pre></td></tr></table></figure>

<p><strong>区别</strong>：<code>QToolButton</code> 默认没有文字边框，适合放在工具栏；<code>QPushButton</code> 适合放在窗口主体。  </p>
<h4 id="3-QRadioButton：单选按钮"><a href="#3-QRadioButton：单选按钮" class="headerlink" title="3. QRadioButton：单选按钮"></a>3. <code>QRadioButton</code>：单选按钮</h4><p>同一组内只能选一个。要自动互斥，需要将它们放在同一个父控件（如 <code>QWidget</code>）或使用 <code>QButtonGroup</code>。  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QRadioButton *male = <span class="keyword">new</span> <span class="built_in">QRadioButton</span>(<span class="string">&quot;男&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line">QRadioButton *female = <span class="keyword">new</span> <span class="built_in">QRadioButton</span>(<span class="string">&quot;女&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line">male-&gt;<span class="built_in">setChecked</span>(<span class="literal">true</span>);   <span class="comment">// 默认选中男</span></span><br></pre></td></tr></table></figure>

<p><strong>获取选中状态</strong>：  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (male-&gt;<span class="built_in">isChecked</span>()) &#123; <span class="comment">/* 选中的是男 */</span> &#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>：如果不希望它们互斥（比如两组单选按钮），用 <code>QButtonGroup</code> 分组。</p>
<h4 id="4-QCheckBox：复选框"><a href="#4-QCheckBox：复选框" class="headerlink" title="4. QCheckBox：复选框"></a>4. <code>QCheckBox</code>：复选框</h4><p>可以独立选中&#x2F;取消，也支持“三态”（半选，用于树形节点）。  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QCheckBox *agree = <span class="keyword">new</span> <span class="built_in">QCheckBox</span>(<span class="string">&quot;我同意协议&quot;</span>, <span class="keyword">this</span>);</span><br><span class="line"><span class="comment">// QCheckBox::stateChanged 已弃用</span></span><br><span class="line"><span class="built_in">connect</span>(agree, &amp;QCheckBox::checkStateChanged, <span class="keyword">this</span>, [](<span class="type">int</span> state)&#123;</span><br><span class="line">    <span class="comment">// state: Qt::Checked, Qt::Unchecked, Qt::PartiallyChecked</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>常用信号：<code>checkStateChanged</code> 或 <code>toggled</code>。  </p>
<h3 id="Button-核心要点"><a href="#Button-核心要点" class="headerlink" title="Button 核心要点"></a>Button 核心要点</h3><ul>
<li><strong>90% 的按钮需求</strong>：<code>QPushButton</code> + <code>clicked</code> 信号。</li>
<li><strong>开关效果</strong>：<code>setCheckable(true)</code> + <code>toggled</code> 信号。</li>
<li><strong>单选按钮组</strong>：把几个 <code>QRadioButton</code> 放在同一个父控件里就自动互斥。</li>
<li><strong>复选框</strong>：<code>stateChanged</code> 信号，<code>isChecked()</code> 判断。</li>
<li><strong>工具按钮</strong>：通常用于工具栏，可以只放图标。</li>
</ul>
<h2 id="常用控件-QLineEdit"><a href="#常用控件-QLineEdit" class="headerlink" title="常用控件 QLineEdit"></a>常用控件 QLineEdit</h2><p><code>QLineEdit</code> 是最常用的输入控件，用于让用户输入一行文本（用户名、密码、数字等）。工作中 90% 的单行输入场景都由它完成。  </p>
<h3 id="基础用法"><a href="#基础用法" class="headerlink" title="基础用法"></a>基础用法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLineEdit *lineEdit = <span class="keyword">new</span> <span class="built_in">QLineEdit</span>(<span class="keyword">this</span>);</span><br><span class="line">lineEdit-&gt;<span class="built_in">setPlaceholderText</span>(<span class="string">&quot;请输入用户名&quot;</span>);  <span class="comment">// 灰色提示文字</span></span><br><span class="line">QString text = lineEdit-&gt;<span class="built_in">text</span>();              <span class="comment">// 获取输入内容</span></span><br><span class="line">lineEdit-&gt;<span class="built_in">setText</span>(<span class="string">&quot;初始值&quot;</span>);                  <span class="comment">// 设置内容</span></span><br></pre></td></tr></table></figure>

<h3 id="常用设置（实用级）"><a href="#常用设置（实用级）" class="headerlink" title="常用设置（实用级）"></a>常用设置（实用级）</h3><table>
<thead>
<tr>
<th>功能</th>
<th>代码</th>
</tr>
</thead>
<tbody><tr>
<td>清空按钮（右侧X）</td>
<td><code>lineEdit-&gt;setClearButtonEnabled(true);</code></td>
</tr>
<tr>
<td>密码模式（显示圆点）</td>
<td><code>lineEdit-&gt;setEchoMode(QLineEdit::Password);</code></td>
</tr>
<tr>
<td>只读模式</td>
<td><code>lineEdit-&gt;setReadOnly(true);</code></td>
</tr>
<tr>
<td>限制最大输入长度</td>
<td><code>lineEdit-&gt;setMaxLength(16);</code></td>
</tr>
<tr>
<td>输入掩码（如手机号）</td>
<td><code>lineEdit-&gt;setInputMask(&quot;999-9999-9999&quot;);</code></td>
</tr>
</tbody></table>
<p><strong>常用回声模式</strong>：  </p>
<ul>
<li><code>QLineEdit::Normal</code>：正常显示  </li>
<li><code>QLineEdit::Password</code>：显示密码圆点  </li>
<li><code>QLineEdit::NoEcho</code>：完全不显示（密码类，防偷窥）</li>
</ul>
<p>注：手机号、IP 地址、MAC 地址、日期格式、产品序列号等固定格式输入，掩码比较有用，需要用的时候查表编写代码就好。  </p>
<p><a href="https://blog.csdn.net/MrHHHHHH/article/details/134608918">QLineEdit设置掩码ip - FreeLikeTheWind.(CSDN)</a>  </p>
<h3 id="常用信号"><a href="#常用信号" class="headerlink" title="常用信号"></a>常用信号</h3><table>
<thead>
<tr>
<th>信号</th>
<th>触发时机</th>
<th>常用场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>textChanged(const QString &amp;)</code></td>
<td>内容改变（包括代码设置）</td>
<td>实时搜索、输入统计</td>
</tr>
<tr>
<td><code>textEdited(const QString &amp;)</code></td>
<td>用户编辑触发（代码设置不触发）</td>
<td>用户输入验证</td>
</tr>
<tr>
<td><code>returnPressed()</code></td>
<td>按下回车键</td>
<td>登录、搜索提交</td>
</tr>
<tr>
<td><code>editingFinished()</code></td>
<td>编辑完成（焦点离开或回车）</td>
<td>保存数据</td>
</tr>
</tbody></table>
<p><strong>示例：实时显示输入长度</strong>  </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">connect</span>(lineEdit, &amp;QLineEdit::textChanged, <span class="keyword">this</span>, [](<span class="type">const</span> QString &amp;text)&#123;</span><br><span class="line">    <span class="built_in">qDebug</span>() &lt;&lt; <span class="string">&quot;当前长度:&quot;</span> &lt;&lt; text.<span class="built_in">length</span>();</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="输入验证（常用）"><a href="#输入验证（常用）" class="headerlink" title="输入验证（常用）"></a>输入验证（常用）</h3><ul>
<li><strong>用 <code>QIntValidator</code> 限制整数范围</strong>：</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLineEdit *ageEdit = <span class="keyword">new</span> <span class="built_in">QLineEdit</span>(<span class="keyword">this</span>);</span><br><span class="line">ageEdit-&gt;<span class="built_in">setValidator</span>(<span class="keyword">new</span> <span class="built_in">QIntValidator</span>(<span class="number">0</span>, <span class="number">150</span>, <span class="keyword">this</span>));</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>用 <code>QDoubleValidator</code> 限制浮点数</strong>：</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QLineEdit *doubleEdit = <span class="keyword">new</span> <span class="built_in">QLineEdit</span>(<span class="keyword">this</span>);</span><br><span class="line">doubleEdit-&gt;<span class="built_in">setValidator</span>(<span class="keyword">new</span> <span class="built_in">QDoubleValidator</span>(<span class="number">0.0</span>, <span class="number">100.0</span>, <span class="number">2</span>, <span class="keyword">this</span>));</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>用 <code>QRegularExpressionValidator</code> 自定义规则</strong>（如手机号）：</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">QRegularExpression <span class="title">regex</span><span class="params">(<span class="string">&quot;^1[3-9]\\d&#123;9&#125;$&quot;</span>)</span></span>;</span><br><span class="line">QRegularExpressionValidator *validator = <span class="keyword">new</span> <span class="built_in">QRegularExpressionValidator</span>(regex, <span class="keyword">this</span>);</span><br><span class="line">phoneEdit-&gt;<span class="built_in">setValidator</span>(validator);</span><br></pre></td></tr></table></figure>

<h3 id="LineEdit-核心要点"><a href="#LineEdit-核心要点" class="headerlink" title="LineEdit 核心要点"></a>LineEdit 核心要点</h3><ul>
<li>获取文本：<code>text()</code></li>
<li>设置提示：<code>setPlaceholderText()</code></li>
<li>清空按钮：<code>setClearButtonEnabled(true)</code></li>
<li>密码模式：<code>setEchoMode(QLineEdit::Password)</code></li>
<li>输入限制：<code>setValidator()</code></li>
<li>常用信号：<code>textChanged</code>（实时）、<code>returnPressed</code>（回车）</li>
</ul>
<script type="module"> import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.esm.min.mjs';	mermaid.initialize({startOnLoad: true, flowchart: {curve: 'linear'}}); </script>]]></content>
      <categories>
        <category>Qt</category>
      </categories>
      <tags>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>SSAO核心思想</title>
    <url>/2025/11/25/SSAO%E6%A0%B8%E5%BF%83%E6%80%9D%E6%83%B3/</url>
    <content><![CDATA[<p>LearnOpenGL高级光照，SSAO章节，对我个人而言很难理解，半知半解抄完所有代码之后突然顿悟了，故做个总结。  </p>
<p>原文：<a href="https://learnopengl-cn.github.io/05%20Advanced%20Lighting/09%20SSAO/#ssao">SSAO</a>  </p>
<h3 id="核心思想"><a href="#核心思想" class="headerlink" title="核心思想"></a>核心思想</h3><p>对于屏幕上的每一个像素（它对应着 3D 世界中的一个点 fragPos），我们都在以该点为中心、以法线 normal 为方向的半球内，随机选取 N 个采样点（比如 64 个）。  </p>
<p>然后，我们去检查这 N 个采样点所对应的位置，在真实的场景中是否被其他物体占据。  </p>
<p>如果一个采样点的位置被占据了（即有物体比采样点更靠近相机），我们就认为这个采样点对当前像素造成了一次 “遮挡”。  </p>
<p>最后，通过统计这 N 个采样点中有多少个造成了遮挡，我们就能计算出当前像素的环境光被遮蔽的程度（occlusion）。遮挡的采样点越多，occlusion 值越高，该像素就越暗。</p>
<h3 id="碎碎念、作图、分析"><a href="#碎碎念、作图、分析" class="headerlink" title="碎碎念、作图、分析"></a>碎碎念、作图、分析</h3><p>这个算法本身不难理解，关键就是“遮挡”二字，只是原文里的图越琢磨脑子越乱。  </p>
<p>想象一下，正在上课，面前有块黑板，假设你在黑板上画了个圈，你想模拟出那个圈里面的部分黑板表面明暗程度，再假设有个“聚光玻璃罩”，能完全罩住你画的圈，聚集的光完全落在你画的圈里，平时看那块儿是很亮的，但老师来了往前面一站，挡住了一部分光线，那里就暗不少了，因为落进去的光线少了。  </p>
<p><img src="/../images/post/ogl-3_1-ssao.png" alt="SSAO理解图"></p>
<p>能想象出这个场景就离理解原文很接近了，按照原文的理解方式，我们以一个像素为球心作一个半球（切面和法线表面法线垂直），在里面均匀放上一些采样点，然后判断遮挡关系决定明暗。相当于把一个点扩散成一片（还是3D的），让这一片被遮挡的程度决定一个点的明暗而已，这样在我们的视线所及之处，邻近物体被遮挡的一方就会暗一些了（多近取决于我们采样半球空间的大小）。</p>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>VAO,VBO,EBO三者的关系</title>
    <url>/2025/09/12/VAO-VBO-EBO%E4%B8%89%E8%80%85%E7%9A%84%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<p>始终要牢记一点，OpenGL是一个巨大的状态机，当我们绑定VAO，VBO或者EBO时，实际上是在告诉OpenGL当前哪个对象处于活动状态，然后进行的操作都是和这个对象相关联的。  </p>
<p>先看一张图有个大致理解：  </p>
<p><img src="/../images/post/ogl-1_1-VAO%E4%B8%8EBUFFER%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%85%B3%E7%B3%BB.png" alt="VAO与BUFFER对象的关系">  </p>
<p>VAO和VBO相对独立，他们的绑定互不影响，EBO与VAO强相关，它的绑定必须在绑定了一个VAO之后，VAO之中要记录EBO的状态信息，意思就是，EBO要在设置哪个VAO时激活（绑定）。  </p>
<p>之所以这样设计，是因为索引信息和我们的顶点对象强相关，也就是说，顶点对象是个什么玩意儿某种意义上就是看EBO的，VBO是可以复用的，他就是一堆不同坐标顶点而已。  </p>
<p>同理，EBO解绑也必须要在他相关的VAO解绑之后进行，因为如果先解绑EBO，VAO中索引信息就不是激活状态了，失效了，这时候渲染找不到索引信息会出现问题，程序甚至卡死。  </p>
<p>当然，解绑VBO，EBO的操作不是必须的，后面会说明。  </p>
<p>总体的关系说完了，接下来讲讲细节。  </p>
<p>生成VAO，VBO和EBO，就是生成不同对象的句柄，这些对象逻辑都在状态机中，绑定不同句柄就能切换不同对象（数据）。</p>
<p>绑定VAO，就是告诉状态机，我们接下来要设置或应用这个顶点对象了，设置中肯定会有的操作是，设置顶点属性指针，即<code>glVertexAttribPointer</code>，它的四个参数是描述读取顶点的方式：读多少，数据类型，步长（读取完一块每隔多少再读），起始位置；所以这个操作一定在绑定VBO之后（必须得有一个激活的VBO），这些与VBO中的数据息息相关但是对它本身毫无影响，说白了就像是设置一个指向数据的指针，按照设置的方式去读取数据，我们绑定不同的VBO可以应用不同的数据，只是不切换绑定VAO的话读取方式是固定的；  </p>
<p>绑定VBO，EBO，顾名思义，Buffer Object，我们所使用的数据真正存在于此，肯定会有的操作是，从CPU上传顶点数据到GPU，即<code>glBufferData</code>，不同的是EBO必须在绑定VAO之后绑定，解绑同样如此。如果说VAO是通过指针应用VBO的数据，那么对EBO数据的使用，就如同直接使用，不需要任何媒介，所以并不需要设置一系列信息告诉状态机怎么用EBO中的数据，它们俩是强相关的，原因前面解释过了不再赘述。</p>
<p>然后是解绑操作，所有的显式解绑都不是必须的，因为绑定到另一个VAO，VBO或者EBO，旧的绑定会自动解绑，但我们仍然推崇显式解绑，因为这样代码的边界更清晰。  </p>
<p>下面是设置两个VAO一般的步骤，可以尝试自行调换函数执行顺序看看自己是否理解到位，至少，在程序执行上不会有逻辑错误，也许细节方面理解会不到位。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">glGenVertexArrays</span>(<span class="number">1</span>, &amp;VAO_);</span><br><span class="line"><span class="built_in">glGenVertexArrays</span>(<span class="number">1</span>, &amp;LightVAO_);</span><br><span class="line"><span class="built_in">glGenBuffers</span>(<span class="number">1</span>, &amp;VBO_);</span><br><span class="line"><span class="built_in">glGenBuffers</span>(<span class="number">1</span>, &amp;EBO_);</span><br><span class="line"></span><br><span class="line"><span class="comment">// bind first VAO</span></span><br><span class="line"><span class="built_in">glBindVertexArray</span>(VAO_);</span><br><span class="line"></span><br><span class="line"><span class="comment">// bind and set VBO</span></span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ARRAY_BUFFER, VBO_);</span><br><span class="line"><span class="built_in">glBufferData</span>(GL_ARRAY_BUFFER, <span class="built_in">sizeof</span>(vertices), vertices, GL_STATIC_DRAW);</span><br><span class="line"></span><br><span class="line"><span class="comment">// set vertex attribute pointers to VAO</span></span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">0</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">11</span> * <span class="built_in">sizeof</span>(GL_FLOAT), (<span class="type">void</span>*)<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">1</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">11</span> * <span class="built_in">sizeof</span>(GL_FLOAT), (<span class="type">void</span>*)(<span class="number">3</span> * <span class="built_in">sizeof</span>(GL_FLOAT)));</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">1</span>);</span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">2</span>, <span class="number">2</span>, GL_FLOAT, GL_FALSE, <span class="number">11</span> * <span class="built_in">sizeof</span>(GL_FLOAT), (<span class="type">void</span>*)(<span class="number">6</span> * <span class="built_in">sizeof</span>(GL_FLOAT)));</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">2</span>);</span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">3</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">11</span> * <span class="built_in">sizeof</span>(GL_FLOAT), (<span class="type">void</span>*)(<span class="number">8</span> * <span class="built_in">sizeof</span>(GL_FLOAT)));</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// bind and set EBO</span></span><br><span class="line"><span class="comment">// EBO must bind after VAO bind</span></span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ELEMENT_ARRAY_BUFFER, EBO_);</span><br><span class="line"><span class="built_in">glBufferData</span>(GL_ELEMENT_ARRAY_BUFFER, <span class="built_in">sizeof</span>(indices), indices, GL_STATIC_DRAW);</span><br><span class="line"></span><br><span class="line"><span class="comment">// unbind VAO, VBO, EBO</span></span><br><span class="line"><span class="built_in">glBindVertexArray</span>(<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ARRAY_BUFFER, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ELEMENT_ARRAY_BUFFER, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// bind second VAO</span></span><br><span class="line"><span class="built_in">glBindVertexArray</span>(LightVAO_);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ARRAY_BUFFER, VBO_);  <span class="comment">// VBO_ and EBO_ has included buffer data</span></span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ELEMENT_ARRAY_BUFFER, EBO_);</span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">0</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">11</span> * <span class="built_in">sizeof</span>(GL_FLOAT), (<span class="type">void</span>*)<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glBindVertexArray</span>(<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ARRAY_BUFFER, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ELEMENT_ARRAY_BUFFER, <span class="number">0</span>);  <span class="comment">// EBO must unbind after VAO unbind</span></span><br></pre></td></tr></table></figure>

<p>代码中30和35行可以同时去除，因为VAO和VBO绑定独立，只要不解绑VBO，后面用的数据还是原先VBO的，但是，31和36行同时去除将会执行失败，因为VAO中要存储EBO的绑定状态，所以36行不能去除，每设置一个VAO就要绑定一个EBO。  </p>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>socket网络编程</title>
    <url>/2026/03/31/socket%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="OSI七层网络模型"><a href="#OSI七层网络模型" class="headerlink" title="OSI七层网络模型"></a>OSI七层网络模型</h2><p>先了解网络模型，更有利于后续学习和理解。  </p>
<p>七层网络模型，实际上可以简化到四层（TCP&#x2F;IP模型）。  </p>
<p><img src="/../images/post/net-0-OSI%E4%B8%83%E5%B1%82%E7%BD%91%E7%BB%9C%E6%A8%A1%E5%9E%8B.jpg" alt="网络模型"><br>（图片来源：<a href="https://c.biancheng.net/view/2126.html">C语言中文网-socket</a>，如果是从基础学习，推荐看看大佬的文章）  </p>
<p>需要注意的是，数据只能逐层传输不能跃层，每一层都可以使用下层提供的服务，并为上层提供服务。  </p>
<p>了解这个之后，我们对开发中不理解的地方，先搞清楚数据传输经历了哪些层或许更好理解为什么代码那么写。  </p>
<h2 id="TCP-IP协议族"><a href="#TCP-IP协议族" class="headerlink" title="TCP&#x2F;IP协议族"></a>TCP&#x2F;IP协议族</h2><p>上面图其实已经标注了TCP&#x2F;IP模型包含四层，所谓简化就是简化成TCP&#x2F;IP模型。  </p>
<p>TCP&#x2F;IP 模型包含了 TCP、IP、UDP、Telnet、FTP、SMTP 等上百个互为关联的协议，其中 TCP 和 IP 是最常用的两种底层协议，所以把它们统称为“TCP&#x2F;IP 协议族”。  </p>
<p>注意，是“TCP&#x2F;IP模型”包含很多协议，我们把这些协议统称为“TCP&#x2F;IP协议族”。  </p>
<p>本文记录socket编程也是基于TCP和UDP协议的，说“也是”，是因为大部分文章，大部分人都是从TCP&#x2F;UDP记录学习的。  </p>
<p>TCP和UDP协议的层级关系如下  </p>
<p><img src="/../images/post/net-1-tcp_udp%E5%8D%8F%E8%AE%AE.gif" alt="TCP和UDP协议的层级关系">  </p>
<h2 id="socket套接字类型"><a href="#socket套接字类型" class="headerlink" title="socket套接字类型"></a>socket套接字类型</h2><p>面向连接的套接字和无连接的套接字，专业点叫流格式套接字（SOCK_STREAM）和数据报格式套接字（SOCK_DGRAM）。  </p>
<p>SOCK_STREAM，底层基于TCP协议，提供一种可靠的、双向的通信数据流，数据可以准确无误地到达另一台计算机，如果损坏或丢失，可以重新发送。  </p>
<p>SOCK_DGRAM，底层基于UDP协议，是一种不可靠的、不按顺序传递的、以追求速度为目的的套接字。  </p>
<table>
<thead>
<tr>
<th>对比维度</th>
<th>面向连接（TCP）</th>
<th>无连接（UDP）</th>
</tr>
</thead>
<tbody><tr>
<td>连接建立</td>
<td>必须先三次握手建立专属连接，通信结束四次挥手断开</td>
<td>无需建立连接，直接发数据包，随时收发</td>
</tr>
<tr>
<td>可靠性</td>
<td>可靠：丢包重传、差错校验、应答确认</td>
<td>不可靠：不保证送达、不纠错、丢包不重发</td>
</tr>
<tr>
<td>数据有序性</td>
<td>保证按发送顺序到达，无乱序</td>
<td>可能乱序、重复，数据包独立无关联</td>
</tr>
<tr>
<td>数据形式</td>
<td>字节流（无数据边界，会粘包）</td>
<td>独立数据报（有边界，一次发送对应一次接收）</td>
</tr>
<tr>
<td>拥塞 &#x2F; 流量控制</td>
<td>自带拥塞控制、滑动窗口流量控制</td>
<td>无任何控制，发送过快会直接丢包</td>
</tr>
<tr>
<td>头部开销</td>
<td>TCP 头部大（20~60 字节），开销高</td>
<td>UDP 头部极小（8 字节），转发快、延迟低</td>
</tr>
<tr>
<td>通信模式</td>
<td>一对一点对点通信</td>
<td>支持一对一、广播、组播</td>
</tr>
<tr>
<td>编程流程</td>
<td>复杂：listen&#x2F;accept&#x2F;connect 缺一不可</td>
<td>简单：无需监听、连接，直接 sendto&#x2F;recvfrom</td>
</tr>
</tbody></table>
<p>PS: Linux 下可以用<code>read/recv</code>，Windows 下使用<code>recv/recvfrom</code>  </p>
<p>TCP 基于字节流传输，没有数据边界，会出现粘包（多包合并）、拆包（一包拆分）。简单解决思路：  </p>
<ul>
<li>固定长度：收发约定相同消息长度  </li>
<li>分隔符结尾：用特殊符号标记消息结束  </li>
<li>包头 + 长度：消息前加长度字段，按长度读取</li>
</ul>
<p><strong>面向连接（TCP）流程</strong>  </p>
<p>服务端：<br>socket() → bind() → listen() → accept() → send()&#x2F;recv() → close()  </p>
<p>客户端：<br>socket() → connect() → send()&#x2F;recv() → close()</p>
<p><strong>无连接（UDP）流程</strong>  </p>
<p>服务端 &#x2F; 客户端通用：<br>socket() → bind(可选) → sendto()&#x2F;recvfrom()  </p>
<h2 id="IP、MAC和端口号"><a href="#IP、MAC和端口号" class="headerlink" title="IP、MAC和端口号"></a>IP、MAC和端口号</h2><p>公网 IP 可以定位到一台主机（或 NAT 网关），但内网 IP 需要结合 NAT 才能唯一标识终端。在互联网上跑数据的IP都是公网IP，能定位到一个路由器，但一个路由器下还有局域网，并不能确定数据报来自哪个用户。  </p>
<p>只有MAC地址能唯一确定一台计算机（网卡），每张网卡的MAC都是独一无二的，虽然也有修改MAC地址的技术手段，但我们讨论正常情况。  </p>
<p>端口号是网络程序的网络接口。前面说了MAC能确定计算机，那还要精确到数据报来自计算机上的哪个网络程序，就要看端口号了，系统会为每个开启的网络应用分配一个端口号，我们自己写网络应用也可以指定端口号，但需要注意<strong>0-1024</strong>是知名端口，避开他们。  </p>
<p>有IP地址、MAC地址和端口号，才能唯一确定是哪个网络应用要通信。  </p>
<h2 id="阻塞与唤醒"><a href="#阻塞与唤醒" class="headerlink" title="阻塞与唤醒"></a>阻塞与唤醒</h2><p>我之所以用阻塞与唤醒的标题，是因为这里的阻塞实际上是休眠。</p>
<p>以前我只知道阻塞是应用程序<strong>看起来</strong>卡住，之后触发什么条件从而继续运行的这么一个行为表现，但实际上这个话题比我想象的高深得多。  </p>
<p>首先说明，我们可以用 <strong>fcntl&#x2F;ioctl</strong> 把 Socket 设置为非阻塞模式，但默认情况我们socket编程处于阻塞模式。  </p>
<p>当我们编写基于tcp的服务端时，调用 <strong>accept</strong> 就会阻塞，我好奇过系统是怎么知道我客户端发了消息服务端就知道继续往下执行的，一开始我以为是accept内部在轮询检查，不断循环检查是不是有消息，这样子处理的，但实际上，我的想法过于幼稚。  </p>
<p>阻塞调用（accept&#x2F;recv&#x2F;read&#x2F;epoll_wait）会把当前线程彻底移出 CPU 就绪 &#x2F; 运行任务队列，进入<strong>休眠</strong>等待队列，完全不占用CPU资源；这实际上对了解操作系统的开发者而言很容易想到，只在应用程序层面考虑可能不会太深。  </p>
<p>既然是休眠，那与之对应的就是<strong>唤醒</strong>了。唤醒靠的是硬件中断+事件驱动，当客户端发送信息之后，服务端网卡收到信号触发硬件中断，把消息写入内核缓冲区，接着内核标<strong>fd就绪</strong>或<strong>把fd放进epoll就绪链表</strong>，再之后内核把之前休眠挂起的服务端线程，重新丢回 CPU 执行调度队列，最后，线程分到 CPU 时间片 → 阻塞函数返回，业务代码接着跑。</p>
<blockquote>
<p>epoll 的事件通知机制与 select 不同，前者在内核维护就绪链表，后者是每次扫描（在后面的示例有所体现），所以epoll比select更高效；阻塞 I&#x2F;O 时进程状态为 TASK_INTERRUPTIBLE（可中断睡眠），可以被信号唤醒。  </p>
</blockquote>
<h2 id="客户端与服务端示例（Linux-TCP）"><a href="#客户端与服务端示例（Linux-TCP）" class="headerlink" title="客户端与服务端示例（Linux &amp;&amp; TCP）"></a>客户端与服务端示例（Linux &amp;&amp; TCP）</h2><p>客户端由于是通用的就不过多解释，主要解释服务端。  </p>
<p>把服务端 &#x3D; 奶茶店店员，客户端 &#x3D; 客人  </p>
<ul>
<li><strong>单线程阻塞</strong>：店员一次只给一个客人做奶茶，其他人站在门口等死，后面的人根本进不来（玩具项目，只能连接一个客户端）；  </li>
<li><strong>多线程</strong>：来一个客人雇一个店员，店里挤满店员，工资（内存）付不起，店直接倒闭（开辟线程服务，本质还是一对一，占用资源）；  </li>
<li><strong>select&#x2F;epoll</strong>：一个店员盯着所有客人，谁举手说「我要下单 &#x2F; 我要取餐」，店员就立刻过去服务，效率拉满（内核调度）。</li>
</ul>
<p>总结一下  </p>
<p><strong>不用 select&#x2F;epoll</strong>：<br>服务端要么只能连 1 个客户端，要么线程太多把服务器干崩；<br><strong>用 select&#x2F;epoll</strong>：<br>单线程同时管理海量连接，实现高并发；</p>
<p>用最低的资源，处理最多的客户端，这就是网络服务器的核心。  </p>
<p>下面看看echo服务端和客户端的具体实现，客户端发送消息给服务端，服务端原封不动把客户端发送的消息返回。  </p>
<h3 id="通用客户端"><a href="#通用客户端" class="headerlink" title="通用客户端"></a>通用客户端</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8888</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> sock = <span class="number">0</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line">    <span class="type">char</span> send_buffer[BUFFER_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">char</span> buffer[BUFFER_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((sock = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;socket 创建失败\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将服务器 IP 地址从点分十进制转换为二进制</span></span><br><span class="line">    <span class="keyword">if</span> (inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;serv_addr.sin_addr) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;地址无效\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (connect(sock, (<span class="keyword">struct</span> sockaddr *)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;连接失败\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;成功连接服务端！输入消息发送，输入 exit 退出\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 发送消息</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">// 清空缓冲区</span></span><br><span class="line">        <span class="built_in">memset</span>(send_buffer, <span class="number">0</span>, BUFFER_SIZE);</span><br><span class="line">        <span class="built_in">memset</span>(buffer, <span class="number">0</span>, BUFFER_SIZE);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从控制台读取输入</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;请输入消息：&quot;</span>);</span><br><span class="line">        fgets(send_buffer, BUFFER_SIZE, <span class="built_in">stdin</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 去掉fgets自动读取的换行符</span></span><br><span class="line">        send_buffer[<span class="built_in">strcspn</span>(send_buffer, <span class="string">&quot;\n&quot;</span>)] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 输入exit，退出循环</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">strcmp</span>(send_buffer, <span class="string">&quot;exit&quot;</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;客户端退出\n&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 发送消息</span></span><br><span class="line">        send(sock, send_buffer, <span class="built_in">strlen</span>(send_buffer), <span class="number">0</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 接收服务端回显</span></span><br><span class="line">        <span class="type">int</span> len = read(sock, buffer, BUFFER_SIZE);</span><br><span class="line">        <span class="keyword">if</span> (len &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;服务端断开连接\n&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;服务端回显: %s\n&quot;</span>, buffer);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="单连接服务端（玩具）"><a href="#单连接服务端（玩具）" class="headerlink" title="单连接服务端（玩具）"></a>单连接服务端（玩具）</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8888</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> server_fd, client_fd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line">    <span class="type">char</span> buffer[BUFFER_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">int</span> addr_len = <span class="keyword">sizeof</span>(serv_addr);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 创建服务端socket</span></span><br><span class="line">    <span class="keyword">if</span> ((server_fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>)) == <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;socket 创建失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 端口复用（解决重启报错问题）</span></span><br><span class="line">    <span class="type">int</span> opt = <span class="number">1</span>;</span><br><span class="line">    setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &amp;opt, <span class="keyword">sizeof</span>(opt));</span><br><span class="line"></span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">    serv_addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 绑定端口</span></span><br><span class="line">    <span class="keyword">if</span> (bind(server_fd, (<span class="keyword">struct</span> sockaddr *)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;绑定失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 监听</span></span><br><span class="line">    <span class="keyword">if</span> (listen(server_fd, <span class="number">3</span>) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;监听失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;服务端启动成功，监听端口 %d，等待客户端连接...\n&quot;</span>, PORT);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 接收客户端连接</span></span><br><span class="line">    <span class="keyword">if</span> ((client_fd = accept(server_fd, (<span class="keyword">struct</span> sockaddr *)&amp;serv_addr, (<span class="type">socklen_t</span>*)&amp;addr_len)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;接收连接失败&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;客户端已连接！\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5. 循环接收消息 + 回显（核心逻辑）</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">memset</span>(buffer, <span class="number">0</span>, BUFFER_SIZE);</span><br><span class="line">        <span class="comment">// 读取客户端消息</span></span><br><span class="line">        <span class="type">int</span> len = read(client_fd, buffer, BUFFER_SIZE);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 客户端断开或出错</span></span><br><span class="line">        <span class="keyword">if</span> (len &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;客户端断开连接\n&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;收到客户端消息: %s\n&quot;</span>, buffer);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 回显给客户端</span></span><br><span class="line">        send(client_fd, buffer, <span class="built_in">strlen</span>(buffer), <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 6. 关闭套接字</span></span><br><span class="line">    close(client_fd);</span><br><span class="line">    close(server_fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="IO复用多连接服务端（select）"><a href="#IO复用多连接服务端（select）" class="headerlink" title="IO复用多连接服务端（select）"></a>IO复用多连接服务端（select）</h3><p>使用select，老式方案。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// select</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8888</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_CLIENTS 10</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUFFER_SIZE 1024</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 select 实现多客户端 echo 服务器</span></span><br><span class="line"><span class="comment">// 缺点：一般最多也就1024个fd，还要遍历检查，每次都要将整个fd_set从用户态拷贝到内核态</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">  <span class="type">int</span> server_fd, new_socket, client_socket[MAX_CLIENTS], max_sd, sd, activity, valread;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">address</span>;</span></span><br><span class="line">  fd_set readfds;</span><br><span class="line">  <span class="type">char</span>   buffer[BUFFER_SIZE];</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAX_CLIENTS; ++i) client_socket[i] = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 创建新监听套接字</span></span><br><span class="line">  <span class="keyword">if</span> ((server_fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>)) == <span class="number">0</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;socket failed&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 允许端口复用，tcp连接断开后不必强制等待1-2分钟再用同一端口开启服务端，可以立刻开</span></span><br><span class="line">  <span class="type">int</span> opt = <span class="number">1</span>;</span><br><span class="line">  setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &amp;opt, <span class="keyword">sizeof</span>(address));</span><br><span class="line"></span><br><span class="line">  address.sin_family = AF_INET;</span><br><span class="line">  address.sin_port = htons(PORT);</span><br><span class="line">  address.sin_addr.s_addr = INADDR_ANY;  <span class="comment">// 监听所有地址8888端口</span></span><br><span class="line">  bind(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;address, <span class="keyword">sizeof</span>(address));</span><br><span class="line">  listen(server_fd, <span class="number">3</span>);   <span class="comment">// 等待区至多3个客户端连接，多出则拒绝连接，accept之后等待区才会空闲（一般写128,1024大值）</span></span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;服务器监听端口 %d\n&quot;</span>, PORT);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">// FD_ZERO(&amp;readfds) 把所有开关全部关掉，所有位 → 0</span></span><br><span class="line">    <span class="comment">// FD_SET(fd, &amp;readfds) 把编号为 fd 的那个开关打开，第 fd 位 → 1</span></span><br><span class="line">    <span class="comment">// FD_ISSET(fd, &amp;readfds) 看看编号 fd 的开关是不是打开的，判断第 fd 位是否为 1</span></span><br><span class="line">    FD_ZERO(&amp;readfds);</span><br><span class="line">    FD_SET(server_fd, &amp;readfds);</span><br><span class="line">    max_sd = server_fd;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将客户端套接字加入集合</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAX_CLIENTS; ++i) &#123;</span><br><span class="line">      sd = client_socket[i];</span><br><span class="line">      <span class="keyword">if</span> (sd &gt; <span class="number">0</span>) FD_SET(sd, &amp;readfds);</span><br><span class="line">      <span class="keyword">if</span> (sd &gt; max_sd) max_sd = sd;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 阻塞等待活动（最后一个time_out参数为NULL）</span></span><br><span class="line">    activity = select(max_sd + <span class="number">1</span>, &amp;readfds, <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (activity &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;select error&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 检查监听套接字</span></span><br><span class="line">    <span class="keyword">if</span> (FD_ISSET(server_fd, &amp;readfds)) &#123;</span><br><span class="line">      <span class="type">int</span> addrlen = <span class="keyword">sizeof</span>(address);</span><br><span class="line">      new_socket = accept(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;address, (<span class="type">socklen_t</span>*)&amp;addrlen);</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;新连接: fd %d, IP %s, 端口 %d\n&quot;</span>, new_socket, inet_ntoa(address.sin_addr), ntohs(address.sin_port));</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 将新的套接字存入数组</span></span><br><span class="line">      <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAX_CLIENTS; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span> (client_socket[i] == <span class="number">0</span>) &#123;</span><br><span class="line">          client_socket[i] = new_socket;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 检查客户端套接字</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAX_CLIENTS; ++i) &#123;</span><br><span class="line">      sd = client_socket[i];</span><br><span class="line">      <span class="keyword">if</span> (FD_ISSET(sd, &amp;readfds)) &#123;</span><br><span class="line">        valread = read(sd, buffer, BUFFER_SIZE);</span><br><span class="line">        <span class="keyword">if</span> (valread == <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">dc_address</span>;</span></span><br><span class="line">          <span class="type">int</span> dc_addrlen = <span class="keyword">sizeof</span>(dc_address);</span><br><span class="line">          getpeername(sd, (<span class="keyword">struct</span> sockaddr*)&amp;dc_address, (<span class="type">socklen_t</span>*)&amp;dc_addrlen);  <span class="comment">// 获取已连接客户端的地址和端口号</span></span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;客户端断开：IP %s, 端口 %d\n&quot;</span>, inet_ntoa(dc_address.sin_addr), ntohs(dc_address.sin_port));</span><br><span class="line">          close(sd);</span><br><span class="line">          client_socket[i] = <span class="number">0</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          buffer[valread] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;收到：%s\n&quot;</span>, buffer);</span><br><span class="line">          send(sd, buffer, <span class="built_in">strlen</span>(buffer), <span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="IO复用多连接服务端（epoll）"><a href="#IO复用多连接服务端（epoll）" class="headerlink" title="IO复用多连接服务端（epoll）"></a>IO复用多连接服务端（epoll）</h3><p>epoll是Linux专属的，在Windows中有另一种机制，IOCP，都是实现高并发服务的必学机制。  </p>
<p>epoll分水平触发（LT，默认）和边缘触发（ET），意思如同单片机引脚信号是稳定电平一直触发，还是上升沿&#x2F;下降沿触发，所以<strong>二者处理事务的具体实现也不一样</strong>。  </p>
<p>水平触发因为一直会有事件通知，所以照常收发数据就好，边缘触发在客户端发数据到服务端来的时候，只会调用一次通知，所以需要循环读，直到读到<strong>EAGAIN</strong>，表示数据收干净了。  </p>
<p>先展示水平触发（LT）的具体实现。  </p>
<p>注意，创建epoll实例：  </p>
<ul>
<li><code>epoll_create(size)</code>：旧版接口，size 参数已废弃  </li>
<li><code>epoll_create1(0)</code>：推荐使用，现代 Linux 标准写法，参数传0即可</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// epoll LT</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/epoll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8888</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_EVENTS 1024</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1）水平触发（LT）—— 默认模式（本代码）</span></span><br><span class="line"><span class="comment">// 缓冲区有数据就一直通知你</span></span><br><span class="line"><span class="comment">// 最稳定、最好用</span></span><br><span class="line"><span class="comment">// 适合新手</span></span><br><span class="line"><span class="comment">// 2）边缘触发（ET）—— 高性能（采用边缘触发需循环read/recv直到接收到EAGAIN）</span></span><br><span class="line"><span class="comment">// 只在状态变化时通知一次</span></span><br><span class="line"><span class="comment">// 必须非阻塞 + 循环读</span></span><br><span class="line"><span class="comment">// Nginx 用的就是 ET</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// epoll_create 创建一个 epoll 实例</span></span><br><span class="line"><span class="comment">// epoll_ctl 向 epoll 注册(ADD) / 修改(MOD) / 删除(DEL) fd</span></span><br><span class="line"><span class="comment">// epoll_wait 等待就绪事件</span></span><br><span class="line"><span class="comment">// epoll_event 事件结构体</span></span><br><span class="line"><span class="comment">// EPOLLIN / EPOLLOUT 事件类型</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">  <span class="type">int</span> server_fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">  <span class="type">int</span> opt = <span class="number">1</span>;</span><br><span class="line">  setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &amp;opt, <span class="keyword">sizeof</span>(opt));</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">  addr.sin_family = AF_INET;</span><br><span class="line">  addr.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">  addr.sin_port = htons(PORT);</span><br><span class="line">  bind(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line"></span><br><span class="line">  listen(server_fd, <span class="number">128</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 创建epoll实例</span></span><br><span class="line">  <span class="type">int</span> epoll_fd = epoll_create1(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 编码和select类似，把服务器fs加入（监听新连接）</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span>;</span></span><br><span class="line">  event.events = EPOLLIN;     <span class="comment">// 监听可读事件</span></span><br><span class="line">  event.data.fd = server_fd;</span><br><span class="line">  epoll_ctl(epoll_fd, EPOLL_CTL_ADD, server_fd, &amp;event);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 事件数组（fd就绪之后存在此处，对服务端来说是有新连接，对客户端是发消息）</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[<span class="title">MAX_EVENTS</span>];</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">// 等待就绪事件</span></span><br><span class="line">    <span class="type">int</span> n = epoll_wait(epoll_fd, events, MAX_EVENTS, <span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">      <span class="type">int</span> fd = events[i].data.fd;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 新客户端连接</span></span><br><span class="line">      <span class="keyword">if</span> (fd == server_fd) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">client_addr</span>;</span></span><br><span class="line">        <span class="type">socklen_t</span> len = <span class="keyword">sizeof</span>(client_addr);</span><br><span class="line">        <span class="type">int</span> client_fd = accept(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;client_addr, &amp;len);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 加入epoll</span></span><br><span class="line">        event.events = EPOLLIN;</span><br><span class="line">        event.data.fd = client_fd;</span><br><span class="line">        epoll_ctl(epoll_fd, EPOLL_CTL_ADD, client_fd, &amp;event);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;新客户端连接：fd %d\n&quot;</span>, client_fd);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 客户端发来数据</span></span><br><span class="line">        <span class="type">char</span> buf[<span class="number">1024</span>];</span><br><span class="line">        <span class="type">int</span> n = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="comment">// 客户端断开</span></span><br><span class="line">          close(fd);</span><br><span class="line">          epoll_ctl(epoll_fd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;客户端断开：%d\n&quot;</span>, fd);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="comment">// 收到数据</span></span><br><span class="line">          buf[n] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">          <span class="built_in">printf</span>(<span class="string">&quot;收到：%s\n&quot;</span>, buf);</span><br><span class="line">          <span class="comment">// 回显</span></span><br><span class="line">          send(fd, buf, n, <span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是边缘触发（ET）的具体实现，可以看到在处理新连接和处理接收消息时都放在了循环<code>while(1) &#123;...&#125;</code>内，因为边缘触发的信号只会触发一次，有新连接来的时候，有一次信号，倘若此时前面客户端的连接尚未完成，他会进入等待连接的队列中，也不会再有信号了，所以我们需要循环accept，直到所有客户端完成连接；同理，收取消息也是一样，消息来的时候只会有一个信号，但我们可能没法一次收取完整的消息，剩余数据就会一直堵在缓冲区里。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/epoll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> PORT 8888</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAX_EVENTS 1024</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1）水平触发（LT）—— 默认模式（本代码）</span></span><br><span class="line"><span class="comment">// 缓冲区有数据就一直通知你</span></span><br><span class="line"><span class="comment">// 最稳定、最好用</span></span><br><span class="line"><span class="comment">// 适合新手</span></span><br><span class="line"><span class="comment">// 2）边缘触发（ET）—— 高性能（采用边缘触发需循环read/recv直到接收到EAGAIN）</span></span><br><span class="line"><span class="comment">// 只在状态变化时通知一次</span></span><br><span class="line"><span class="comment">// 必须非阻塞 + 循环读</span></span><br><span class="line"><span class="comment">// Nginx 用的就是 ET</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// epoll_create 创建一个 epoll 实例</span></span><br><span class="line"><span class="comment">// epoll_ctl 向 epoll 注册(ADD) / 修改(MOD) / 删除(DEL) fd</span></span><br><span class="line"><span class="comment">// epoll_wait 等待就绪事件</span></span><br><span class="line"><span class="comment">// epoll_event 事件结构体</span></span><br><span class="line"><span class="comment">// EPOLLIN / EPOLLOUT 事件类型</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置非阻塞模式</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">set_nonblocking</span><span class="params">(<span class="type">int</span> fd)</span> &#123;</span><br><span class="line">    <span class="type">int</span> flag = fcntl(fd, F_GETFL, <span class="number">0</span>);</span><br><span class="line">    fcntl(fd, F_SETFL, flag | O_NONBLOCK);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    <span class="type">int</span> server_fd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (server_fd == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;socket failed&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 端口复用</span></span><br><span class="line">    <span class="type">int</span> opt = <span class="number">1</span>;</span><br><span class="line">    setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &amp;opt, <span class="keyword">sizeof</span>(opt));</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">    addr.sin_port = htons(PORT);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;addr, <span class="keyword">sizeof</span>(addr)) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;bind failed&quot;</span>);</span><br><span class="line">        close(server_fd);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (listen(server_fd, <span class="number">128</span>) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;listen failed&quot;</span>);</span><br><span class="line">        close(server_fd);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建epoll实例</span></span><br><span class="line">    <span class="type">int</span> epoll_fd = epoll_create1(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (epoll_fd == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;epoll_create1 failed&quot;</span>);</span><br><span class="line">        close(server_fd);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 服务端fd非阻塞（ET模式必须）</span></span><br><span class="line">    set_nonblocking(server_fd);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 添加服务端fd到epoll（ET模式）</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span>;</span></span><br><span class="line">    event.events = EPOLLIN | EPOLLET;  <span class="comment">// 边缘触发</span></span><br><span class="line">    event.data.fd = server_fd;</span><br><span class="line">    <span class="keyword">if</span> (epoll_ctl(epoll_fd, EPOLL_CTL_ADD, server_fd, &amp;event) == <span class="number">-1</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;epoll_ctl add server failed&quot;</span>);</span><br><span class="line">        close(server_fd);</span><br><span class="line">        close(epoll_fd);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">events</span>[<span class="title">MAX_EVENTS</span>];</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Epoll ET 服务端启动成功，监听端口 %d\n&quot;</span>, PORT);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">// 等待事件</span></span><br><span class="line">        <span class="type">int</span> n = epoll_wait(epoll_fd, events, MAX_EVENTS, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="number">-1</span>) &#123;</span><br><span class="line">            perror(<span class="string">&quot;epoll_wait failed&quot;</span>);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">            <span class="type">int</span> fd = events[i].data.fd;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 处理新连接（ET模式：必须循环accept）</span></span><br><span class="line">            <span class="keyword">if</span> (fd == server_fd) &#123;</span><br><span class="line">                <span class="comment">// 循环accept，直到EAGAIN</span></span><br><span class="line">                <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">client_addr</span>;</span></span><br><span class="line">                    <span class="type">socklen_t</span> len = <span class="keyword">sizeof</span>(client_addr);</span><br><span class="line">                    <span class="type">int</span> client_fd = accept(server_fd, (<span class="keyword">struct</span> sockaddr*)&amp;client_addr, &amp;len);</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (client_fd == <span class="number">-1</span>) &#123;</span><br><span class="line">                        <span class="comment">// 没有新连接了，退出循环</span></span><br><span class="line">                        <span class="keyword">if</span> (errno == EAGAIN || errno == EWOULDBLOCK) &#123;</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            perror(<span class="string">&quot;accept failed&quot;</span>);</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;新客户端连接：fd %d\n&quot;</span>, client_fd);</span><br><span class="line">                    set_nonblocking(client_fd);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 添加客户端到epoll（ET模式）</span></span><br><span class="line">                    event.events = EPOLLIN | EPOLLET;</span><br><span class="line">                    event.data.fd = client_fd;</span><br><span class="line">                    epoll_ctl(epoll_fd, EPOLL_CTL_ADD, client_fd, &amp;event);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 处理客户端消息（ET模式：循环read）</span></span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">1024</span>];</span><br><span class="line">                <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="comment">// 每次read前清空缓冲区（防止乱码）</span></span><br><span class="line">                    <span class="built_in">memset</span>(buf, <span class="number">0</span>, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">                    <span class="type">ssize_t</span> read_len = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (read_len &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;收到客户端%d数据：%s\n&quot;</span>, fd, buf);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// send只发送有效数据长度，不发空字符</span></span><br><span class="line">                        send(fd, buf, read_len, <span class="number">0</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 客户端断开连接</span></span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (read_len == <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;客户端%d断开连接\n&quot;</span>, fd);</span><br><span class="line">                        <span class="comment">// 注意此处，先删epoll，再关闭fd</span></span><br><span class="line">                        epoll_ctl(epoll_fd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">                        close(fd);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 读取完毕（EAGAIN）或错误</span></span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="keyword">if</span> (errno == EAGAIN || errno == EWOULDBLOCK) &#123;</span><br><span class="line">                            <span class="comment">// printf(&quot;数据读取完成\n&quot;);</span></span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            perror(<span class="string">&quot;read error&quot;</span>);</span><br><span class="line">                            epoll_ctl(epoll_fd, EPOLL_CTL_DEL, fd, <span class="literal">NULL</span>);</span><br><span class="line">                            close(fd);</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 资源释放（理论上到不了这里）</span></span><br><span class="line">    close(server_fd);</span><br><span class="line">    close(epoll_fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="客户端与服务端示例（Linux-UDP）"><a href="#客户端与服务端示例（Linux-UDP）" class="headerlink" title="客户端与服务端示例（Linux &amp;&amp; UDP）"></a>客户端与服务端示例（Linux &amp;&amp; UDP）</h2><p>因为 <strong>UDP没有请求连接和受理连接（没有connect、listen和accept）</strong> 的过程，所以严格意义上来说没有办法区分服务器端和客户端，在这里只能将提供服务的称为服务器端  </p>
<p>udp发送的每个数据包自带目标ip和端口号，使用sendto&#x2F;recvfrom通信，如果在sendto之前不bind地址，第一次调用 sendto 时，内核自动做<strong>隐式Bind</strong>，内核根据路由表，自动选一个本机出口IP当源IP，自动分配一个系统临时随机端口当源端口，之后每次sendto都用这个目标地址和端口。  </p>
<h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// udp_server_echo.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 32</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">  <span class="type">int</span> serv_sock;</span><br><span class="line">  <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">  <span class="type">int</span> str_len;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>, <span class="title">clnt_addr</span>;</span></span><br><span class="line">  <span class="type">socklen_t</span> clnt_addr_size;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">  <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  serv_sock = socket(PF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (serv_sock == <span class="number">-1</span>)</span><br><span class="line">  error_handling(<span class="string">&quot;UDP socket creation error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">  serv_addr.sin_family      = PF_INET;</span><br><span class="line">  serv_addr.sin_port        = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line">  serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">  error_handling(<span class="string">&quot;bind() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    clnt_addr_size = <span class="keyword">sizeof</span>(clnt_addr);</span><br><span class="line">    str_len = recvfrom(serv_sock, message, BUF_SIZE, <span class="number">0</span>, </span><br><span class="line">                       (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, &amp;clnt_addr_size);</span><br><span class="line">    sendto(serv_sock, message, str_len, <span class="number">0</span>, </span><br><span class="line">            (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, clnt_addr_size);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  close(serv_sock);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">  <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">  fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">  <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h3><pre><code class="language-c">// udp_client_echo.c
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
#include &lt;unistd.h&gt;      // write  read  声明的地方
#include &lt;arpa/inet.h&gt;
#include &lt;sys/socket.h&gt;
#define BUF_SIZE 32
void error_handling(char* message);

int main(int argc, char** argv) &#123;
  int clnt_sock;
  char message[BUF_SIZE];
  int str_len;
  socklen_t addr_size;
  struct sockaddr_in serv_addr, from_addr;

  if (argc != 3) &#123;
  printf(&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;, argv[0]);
  exit(1);
  &#125;

  clnt_sock = socket(PF_INET, SOCK_DGRAM, 0);
  if (clnt_sock == -1)
  error_handling(&quot;socket() error!&quot;);

  memset(&amp;serv_addr, 0, sizeof(serv_addr));
  serv_addr.sin_family = PF_INET;
  serv_addr.sin_port   = htons(atoi(argv[2]));
  if (inet_pton(AF_INET, argv[1], &amp;serv_addr.sin_addr) == 0)  // 会自动转换字节序
  error_handling(&quot;inet_pton() error!&quot;);

  while(1) &#123;
    fputs(&quot;Insert message(q to quit): &quot;, stdout);
    fgets(message, sizeof(message), stdin);
    if (!strcmp(message, &quot;q\n&quot;) || !strcmp(message, &quot;Q\n&quot;)) break;
    sendto (clnt_sock, message, strlen(message), 0,
            (struct sockaddr*)&amp;serv_addr, sizeof(serv_addr));
    addr_size = sizeof(serv_addr);
    str_len = recvfrom(clnt_sock, message, BUF_SIZE, 0,
                (struct sockaddr*)&amp;from_addr, &amp;addr_size);
    message[str_len] = 0;
    printf(&quot;Message from server: %s\n&quot;, message);
    &#125;
    close(clnt_sock);
    return 0;
  &#125;

void error_handling(char* message) &#123;
  fputs(message, stderr);
  fputc(&#39;\n&#39;, stderr);
  exit(1);
&#125;
</code></pre>
]]></content>
      <categories>
        <category>网络编程</category>
      </categories>
      <tags>
        <tag>socket</tag>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>上拉电阻和下拉电阻</title>
    <url>/2026/03/26/%E4%B8%8A%E6%8B%89%E7%94%B5%E9%98%BB%E5%92%8C%E4%B8%8B%E6%8B%89%E7%94%B5%E9%98%BB/</url>
    <content><![CDATA[<p>先上图，然后再了解上拉电阻和下拉电阻  </p>
<p><img src="/../images/post/mcu-1-%E4%B8%8A%E6%8B%89%E7%94%B5%E9%98%BB%E4%B8%8B%E6%8B%89%E7%94%B5%E9%98%BB.png" alt="上拉电阻下拉电阻">  </p>
<p>上面电路图的外围电路就是我们耳熟能详的上拉电阻和下拉电阻的电路，至于内部的，当然不是我画的那样，但是是那么个意思。  </p>
<p>输入引脚接到内部检测电压的电路，超大阻抗和那个”电压表”共同组成一个<strong>检测器</strong>，当电压表的值超过某一阈值时，向输出引脚输送电平信号1，否则输送0。  </p>
<p>上拉电阻和下拉电阻说白了还是电阻，在了解为什么需要他们之前，我们先要了解一件事：<strong>悬空引脚电平信号不稳定</strong>。  </p>
<p>悬空引脚意思就是没有输入引脚旁边的一堆外围电路，输入引脚不接任何线，这时候我们从输出引脚测量电平信号，往往是1010011010…这样混乱不堪的结果，并不是我们想象中的稳定输出电平信号0，至于原因，可能要了解MCU内部真正的电路结构，有MOS电路（超大阻抗就是因为接了栅极），有小电容，有施密特触发器这些东西。  </p>
<p>（讨教了一下AI为啥悬空引脚信号不稳定）  </p>
<blockquote>
<p>这是因为引脚内部的MOSFET栅极像一根“天线”，会感应周围空间的电磁干扰，同时内部微小的漏电流也会让电压随机漂移，所以检测器读出的结果毫无规律。</p>
</blockquote>
<p>总之人类设计的MCU，在物理世界的规则就有“悬空引脚电平信号不稳定”的问题，你说该怎么办吧。  </p>
<p>聪明的你一定想到了：我在外围接个电源不就完事儿了。  </p>
<p>确实，我照着做，把引脚直接接到电源正极（VCC），再把开关接在引脚和地（GND）之间。这样开关断开时，引脚一直有高电平；开关闭合时，引脚直接接地，变成低电平。<br>结果，我闭合开关的瞬间导线烧了…  </p>
<p>后来有人告诉我：在电源和引脚之间串一个电阻就行了。  </p>
<p>这个电阻在开关断开时，让引脚仍然能通过它得到高电平；在开关闭合时，它限制了从电源到地的电流，不会短路。这就是上拉电阻的由来。  </p>
<p>上面的情景讨论，最终产物就是我们的上拉电阻电路，从最终结果上看，加上他只是为了防短路，但确实必不可少。  </p>
<p>下拉电阻电路同理，只是开关闭合时才是高电平，断开时低电平，这更符合人类直觉，闭合开关才是开灯的那种直觉。（PS：工程中上拉更常用，很多MCU内部集成了上拉电阻）  </p>
<p>所以，“上拉”“下拉”只是描述电路接法的专业术语——电阻接在VCC和引脚之间就叫上拉，接在GND和引脚之间就叫下拉。  </p>
<p>说电阻“把引脚拉到高电平”，其实是省略了中间过程的结果描述。真正干活的还是电源，电阻还是用来防止烧电路。  </p>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>MCU</tag>
      </tags>
  </entry>
  <entry>
    <title>函数对象</title>
    <url>/2026/03/10/%E5%87%BD%E6%95%B0%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h2 id="函数对象"><a href="#函数对象" class="headerlink" title="函数对象"></a>函数对象</h2><p>实际上就是类中重载了<code>()</code>，以至于使用类对象能像调用函数一样。  </p>
<p>先看个最基础的了解一下怎么使用。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Adder</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Adder</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Adder add;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">add</span>(<span class="number">1</span>, <span class="number">1</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;  <span class="comment">// 打印 2</span></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">add</span>(<span class="number">2</span>, <span class="number">3</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;  <span class="comment">// 打印 5</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，就是把对象当做函数调用。  </p>
<h2 id="lambda函数是匿名函数对象的语法糖"><a href="#lambda函数是匿名函数对象的语法糖" class="headerlink" title="lambda函数是匿名函数对象的语法糖"></a>lambda函数是匿名函数对象的语法糖</h2><p>函数对象可以结合泛型编程使用，这很正常。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;array&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ListArr</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">ListArr</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">typename</span> ArrT&gt;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(ArrT&amp; arr)</span> </span>&#123;</span><br><span class="line">        std::for_each(arr.<span class="built_in">begin</span>(), arr.<span class="built_in">end</span>(), </span><br><span class="line">                [](<span class="type">const</span> <span class="keyword">auto</span>&amp; e) &#123; std::cout &lt;&lt; e &lt;&lt; <span class="string">&#x27; &#x27;</span>; &#125; );</span><br><span class="line">        std::cout &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt;    a &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line">    std::vector&lt;<span class="type">double</span>&gt; b &#123;<span class="number">4.5</span>, <span class="number">5.5</span>, <span class="number">6.5</span>&#125;;</span><br><span class="line">    std::array&lt;<span class="type">int</span>, 3&gt;  c &#123;<span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>&#125;;</span><br><span class="line"></span><br><span class="line">    ListArr la;</span><br><span class="line">    <span class="built_in">la</span>(a);   <span class="comment">// 输出 1 2 3</span></span><br><span class="line">    <span class="built_in">la</span>(b);   <span class="comment">// 输出 4.5 5.5 6.5</span></span><br><span class="line">    <span class="built_in">la</span>(c);   <span class="comment">// 输出 7 8 9</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码用到了c++14支持的<strong>lambda泛型</strong>，所以需要使用c++14和以上版本的标准编译，现在我基本都使用c++17标准编译自己的代码。  </p>
<p>我们上面在函数对象中写了个lambda函数，实际上lambda函数本质上就是个匿名函数对象，lambda函数是个匿名函数对象的语法糖。  </p>
<p>我们写的lambda表达式：  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">[](<span class="type">const</span> <span class="keyword">auto</span>&amp; e) &#123; std::cout &lt;&lt; e &lt;&lt; <span class="string">&#x27; &#x27;</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>编译器生成的匿名函数对象大概是：  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">__AnonymousLambda</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">    <span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> T&amp; e)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; e &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>实际上编译器会生成更完整的内容（局部类或者结构体），让我们的调用能够生效。  </p>
<h2 id="函数对象利于内联优化"><a href="#函数对象利于内联优化" class="headerlink" title="函数对象利于内联优化"></a>函数对象利于内联优化</h2><p>众所周知，编译器内联优化就是把函数源码填充到调用的地方，这样我们的代码就少了一些地址跳转，函数返回，达到优化的目的，但这个前提是编译器能够知道调用的源码是什么才行。  </p>
<p>倘若我们使用函数指针，那编译器在编译期间基本没法知道函数地址，就没法内联优化，而使用函数对象在编译期间是可以确定内容的，当然，用lambda函数也一样。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Func&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">repeat</span><span class="params">(<span class="type">int</span> n, Func f)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) <span class="built_in">f</span>(i);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Printer</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> x)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; x &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">repeat</span>(<span class="number">5</span>, <span class="built_in">Printer</span>());                <span class="comment">// 使用函数对象</span></span><br><span class="line">    <span class="built_in">repeat</span>(<span class="number">5</span>, [](<span class="type">int</span> x)&#123; std::cout &lt;&lt; x &lt;&lt; <span class="string">&#x27; &#x27;</span>; &#125;); <span class="comment">// lambda 同理</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="写一个小学二年级都会的斐波那契数列"><a href="#写一个小学二年级都会的斐波那契数列" class="headerlink" title="写一个小学二年级都会的斐波那契数列"></a>写一个小学二年级都会的斐波那契数列</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="type">int</span> N&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Fib</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">constexpr</span> <span class="type">int</span> <span class="title">operator</span><span class="params">()</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(N == <span class="number">0</span>)</span> <span class="keyword">return</span> 0</span>;</span><br><span class="line">        <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(N == <span class="number">1</span> || N == <span class="number">2</span>)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">Fib</span>&lt;N<span class="number">-1</span>&gt;()() + <span class="built_in">Fib</span>&lt;N<span class="number">-2</span>&gt;()();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">Fib</span>&lt;<span class="number">3</span>&gt;()() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">Fib</span>&lt;<span class="number">5</span>&gt;()() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">Fib</span>&lt;<span class="number">7</span>&gt;()() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">Fib</span>&lt;<span class="number">9</span>&gt;()() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="comment">// std::cout &lt;&lt; Fib&lt;999&gt;()() &lt;&lt; &#x27;\n&#x27;;  // don&#x27;t do this</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用非类型模板和函数对象可以达到一种诡异的效果，其实也没什么神秘的，就是编译期计算和函数对象调用罢了。  </p>
<p>要注意<code>if constexpr</code>是c++17引入的，编译标准需要改到c++17。  </p>
<p>上面代码有意思的点在于，去掉<code>&#123;&#125;</code>就会编译错误。  </p>
<p>究其原因就是因为这还是在编译期，return之后还是要编译后面的语句，而我们的运算也是在编译期发生的，所以就会发生无限递归，越界。  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="type">int</span> N&gt;</span><br><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="type">int</span> <span class="title">fib</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(N == <span class="number">1</span>)</span> <span class="keyword">return</span> 1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">fib</span>&lt;N<span class="number">-1</span>&gt;() + <span class="built_in">fib</span>&lt;N<span class="number">-2</span>&gt;();  <span class="comment">// 即使 N == 1 时永远不执行，也必须编译，而编译时又发生计算，便会无穷尽也</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>c++</tag>
      </tags>
  </entry>
  <entry>
    <title>打包发布Qt应用</title>
    <url>/2026/03/23/%E6%89%93%E5%8C%85%E5%8F%91%E5%B8%83Qt%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>参考我的学习项目：<a href="https://github.com/saiumr/LearnQt/tree/main/002-releaseQtApp">Release Qt App</a>  </p>
<p>里面有两个目录，分别是绿色版本和安装包版本。  </p>
<h2 id="Enigma-Virtual-Box"><a href="#Enigma-Virtual-Box" class="headerlink" title="Enigma Virtual Box"></a>Enigma Virtual Box</h2><p>因为要用这个工具，就问了问AI了解了一下。  </p>
<p>Enigma Virtual Box 是一个能把你的程序及其所有“配件”（DLL文件、图片、数据库等）打包成一个单独 .exe 文件的免费工具。  </p>
<p>对 Qt 开发者来说，它的核心价值在于：解决了分发程序时必须带着一堆 DLL 文件的尴尬，让用户收到的只是一个清爽的、双击就能运行的单文件。  </p>
<p>当用户运行这个打包后的程序时，它并不会像普通压缩包一样把文件解压到电脑的临时文件夹里，而是直接在内存中“虚拟”出这些文件。对程序来说，它“看到”的依赖都在，但对用户来说，磁盘上干干净净，没有任何临时文件被释放出来。这对于追求“绿色软件”体验的开发者来说，是一个巨大的吸引力。  </p>
<p>但是它的加载方式并不是全量加载，而是动态的，所以对资源和性能的影响很小。  </p>
<h2 id="打包成绿色版本"><a href="#打包成绿色版本" class="headerlink" title="打包成绿色版本"></a>打包成绿色版本</h2><p>有一篇博客写的好啊：<a href="https://zhuanlan.zhihu.com/p/678981419">QT程序打包发布详细步骤 - 程序员在路上（知乎）</a>  </p>
<p>注意：请先甄别自己使用的Qt版本，我使用的<code>6.8.3</code>版本，亲测可行，原文提到<code>4.x</code>版本是不行的。  </p>
<p>下面简单记录一下原文章：  </p>
<ol>
<li>编译运行release版本应用；  </li>
<li>将二进制包和资源文件复制到单独的目录下（比如<code>resQtApp</code>，注意目录结构）；</li>
<li>在开始菜单中打开Qt命令行窗口（可能需要注意自己的编译环境，比如我是用Mingw编译的，用的就是 <code>Qt 6.8.3(MinGW 13.1.0 64-bit)</code>）；  </li>
<li>补充：先试试 <code>windeployqt --version</code> 能不能执行（注意这里似乎不能tab补全，输入全名执行，以下操作都是如此），不行的话要先配置环境变量；  </li>
<li>补充：运行Qt自带的配置环境变量的脚本 <code>.\bin\qtenv2.bat</code>；  </li>
<li>输入命令跳转到打包的文件夹：<code>cd /d resQtApp</code>，接着输入命令执行打包流程：<code>windeployqt 程序名</code> （也可能是<code>windeployqt6</code>，暂时不清楚有何区别，我用的带6的），这样我们所需要的所有动态链接库和其他资源都会复制过来；  </li>
<li>在上一步骤执行完命令就可以双击运行App了；  </li>
<li>接下来使用 <code>Enigma Virtual Box</code> 打包App，只需要在 <code>Input File Name</code> 里选中我们之前处理好的.exe应用就好了，然后 <code>Add -&gt; Add Folder Recursive</code>，选中.exe文件所在目录，然后 <code>Files Option -&gt; 勾选 Compress Files</code>，最后点击 <code>Process</code>；  </li>
<li>上一步得到的打包文件(.exe)默认带有 <code>_boxed</code> 后缀，我们可以把这单个文件放在任意目录执行。</li>
</ol>
<p>实际上在执行完<code>第6步</code>之后我们就可以把整个目录压缩发布了，但是打包成单个可执行文件对使用者来说更简洁干净，而且我亲测打包 <code>Compress</code> 之后可执行文件体积对比整个包小了近一半，可谓利远远大于弊。  </p>
<h2 id="打包成安装包程序（Setup-exe）"><a href="#打包成安装包程序（Setup-exe）" class="headerlink" title="打包成安装包程序（Setup.exe）"></a>打包成安装包程序（Setup.exe）</h2><p>参考博客：<a href="https://zhuanlan.zhihu.com/p/144079072">使用NSIS打包程序 - 佳瑞Jarrett（知乎）</a>  </p>
<p>我们需要使用安装脚本制作安装包，首先需要按序安装：  </p>
<p><a href="https://nsis.sourceforge.io/Download">NSIS脚本</a><br><a href="https://hmne.sourceforge.net/index.php#Download">HM NIS Edit</a>  </p>
<p>NSIS（Nullsoft Scriptable Install System）是一个开源的 Windows 安装程序制作工具。它的特点是基于脚本、体积小巧、逻辑灵活且完全免费，非常适合用来打包软件并生成一个轻量级的 .exe 安装包。</p>
<p>常用编辑器是 HM NIS Edit。它是一个图形化的 IDE（集成开发环境），通过向导功能和语法高亮，帮助用户更直观地编写 NSIS 脚本，无需完全手动敲代码即可快速生成安装程序。</p>
<p>我们把App打包成安装包，有四样不可或缺的东西：  </p>
<ol>
<li>应用程序本体和完整的可执行环境；  </li>
<li>安装.exe；</li>
<li>卸载.exe；  </li>
<li>安装过程中显示的描述信息（License.txt）</li>
</ol>
<p>为此，除了工具之外，我们还需要完成以下准备：  </p>
<ol>
<li>打包绿色版本的步骤1~7（准备好完整的可执行环境）；  </li>
<li>安装应用的ico图标；  </li>
<li>卸载应用的ico图标；  </li>
<li>License.txt</li>
</ol>
<p>准备好之后需要使用<code>HM NIS Edit</code>应用工具制作安装包。  </p>
<ol>
<li>运行<code>HM NIS Edit</code>（索引工具直接运行<code>nisedit</code>），选择<code>文件-&gt;新建脚本向导</code>，主要设置<code>应用程序名称</code>，可以任意写，其他的可以不用改；  </li>
<li>下一步，设置安装程序的图标和语言（简体中文），如果没有语言选项可能是编译器配置路径错了，需要手动配置一下’NSIS’路径；  </li>
<li>下一步，把授权文件改成自己准备好的<code>License.txt</code>；  </li>
<li>下一步，删除默认的文件列表，然后添加文件，先把所有安装包中<code>非目录文件</code>添加进来；  </li>
<li>再次添加文件，选择 <code>imageformats\qico.dll</code>，注意路径，需要在 <code>目的目录</code> 后面加上 <code>\imageformats</code>；  </li>
<li>再次添加文件，选择 <code>platforms\qwindows.dll</code>，注意路径，需要在 <code>目的目录</code> 后面加上 <code>\platforms</code>；  </li>
<li>在已添加的文件列表中选择我们的可执行文件本体，选中之后选择下一步；  </li>
<li>现在到了<code>应用程序图标</code>，基本不用设置，直接下一步；  </li>
<li>修改<code>自述</code>，改成我们的<code>License.txt</code>作演示，到时候安装会出现这个文本信息；  </li>
<li>下一步，设置卸载应用的图标；  </li>
<li>现在到了<code>向导已完成</code>，把三个选项全都勾选上：保存脚本，转换文件路径到关键路径，编译脚本；  </li>
<li>完成，选择保存文件夹，去报编译无误即可。</li>
</ol>
<p>在我们最后选择的文件夹中会生成<code>nsi脚本</code>，编译没问题的话会生成<code>Setup.exe</code>，可以把这个安装文件单独发布，利用这个安装程序即可安装使用主体应用。  </p>
]]></content>
      <categories>
        <category>Qt</category>
      </categories>
      <tags>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>利用github搭建个人博客（使用hexo）</title>
    <url>/2025/02/23/%E5%88%A9%E7%94%A8github%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<p>写在前面  </p>
<p>实际上现在github建立博客十分简单了，建立特定名称仓库后，在settings-&gt;pages里面就可以进行部署的操作，参考视频：<a href="https://www.bilibili.com/video/BV12H4y1N7Q4">Github王炸功能Pages</a>  </p>
<p>配置next主题，可以查看此视频：<a href="https://www.bilibili.com/video/BV16W411t7mq">[馨客栈分享]使用Hexo博客搭建的个人博客</a>  </p>
<h2 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h2><p>下面正式开始本文  </p>
<p>参考<a href="https://zhuanlan.zhihu.com/p/102592286">从零开始搭建个人博客（超详细）[枫叶 - 知乎]</a>  </p>
<p><strong>感谢每一位无私分享知识的笔者</strong>  </p>
<h3 id="注册git账号并绑定到本地"><a href="#注册git账号并绑定到本地" class="headerlink" title="注册git账号并绑定到本地"></a>注册git账号并绑定到本地</h3><p>因为我已经有了，所以这一步骤省略，网上也都是注册和使用教程  </p>
<h3 id="安装node-js和hexo"><a href="#安装node-js和hexo" class="headerlink" title="安装node.js和hexo"></a>安装node.js和hexo</h3><p><a href="https://nodejs.org/zh-cn">NodeJs官网</a>  </p>
<p>在官网下载完之后直接安装就了，最好改个非C盘的路径安装，安装完成后在命令行用下面的命令检查一下是否安装成功了  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v</span><br><span class="line">npm -v</span><br></pre></td></tr></table></figure>

<p>然后在nodejs安装目录里新建两个文件夹<code>node_cache</code>和<code>node_global</code>，接着我们用下面的命令设置npm安装模块的默认路径（注意要把命令里的路径改成自己的）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> prefix <span class="string">&quot;D:\Software\NodeJS\node_global&quot;</span></span><br><span class="line">npm config <span class="built_in">set</span> cache <span class="string">&quot;D:\Software\NodeJS\node_cache&quot;</span></span><br></pre></td></tr></table></figure>

<p>打开文件管理器，右击此电脑，选择属性，选择高级系统设置配置环境变量，新增一个系统变量<code>NODE_PATH</code>，值是<code>D:\Software\NodeJS\node_modules</code>  </p>
<p>再修改用户变量里<code>Path</code>中<code>npm</code>的路径，改成<code>D:\Software\NodeJS\node_global</code>  </p>
<p>这样我们安装模块就会默认安装到node_global里面了  </p>
<h3 id="修改npm安装源"><a href="#修改npm安装源" class="headerlink" title="修改npm安装源"></a>修改npm安装源</h3><p>查询安装源：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config get registry</span><br></pre></td></tr></table></figure>

<p>设置安装源（下面的是淘宝镜像）：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm config <span class="built_in">set</span> registry https://registry.npmmirror.com</span><br></pre></td></tr></table></figure>

<h3 id="开启一个管理员命令行窗口"><a href="#开启一个管理员命令行窗口" class="headerlink" title="开启一个管理员命令行窗口"></a>开启一个管理员命令行窗口</h3><p>后面安装包的命令都需要管理员权限，单独把这个作为一个步骤列举出来，用管理员权限打开命令行窗口  </p>
<h3 id="安装webpack和hexo-cli"><a href="#安装webpack和hexo-cli" class="headerlink" title="安装webpack和hexo-cli"></a>安装webpack和hexo-cli</h3><p>安装webpack</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install webpack -g</span><br></pre></td></tr></table></figure>

<p>如果前面设置没有问题，我们在自己设置的<code>node_modules</code>目录下应该能看到安装的包，，否则就应该在默认目录下：<code>C:\Users\[用户名]\AppData\Roaming\npm\node_modules</code>  </p>
<p>如果错了也没关系，用命令<code>npm uninstall webpack</code>就能删除干净  </p>
<p>接下来安装hexo-cli  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-cli</span><br></pre></td></tr></table></figure>

<p>然后在你想要建立博客工作目录的地方使用下面的命令初始化，比如我的目录位置是<code>D:\root\blog</code>  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo init</span><br></pre></td></tr></table></figure>

<p>这一步可能会clone仓库失败，github访问在国内挺随缘的，只能自行上网解决了  </p>
<p>静态部署网页  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo g</span><br></pre></td></tr></table></figure>

<p>查看静态网页（可能不会自动打开，在浏览器查看命令行给的地址）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>检查完毕后ctrl+c终止运行  </p>
<h3 id="建立博客远程仓库"><a href="#建立博客远程仓库" class="headerlink" title="建立博客远程仓库"></a>建立博客远程仓库</h3><p>建立github仓库，需要注意仓库的名称得是<code>[用户名].github.io</code>，这也是我们个人博客的域名  </p>
<h3 id="将Hexo部署到仓库"><a href="#将Hexo部署到仓库" class="headerlink" title="将Hexo部署到仓库"></a>将Hexo部署到仓库</h3><p>回到blog文件夹，打开<code>_config.yml</code>文件，将最后的<code>deploy</code>部分替换成下面的内容  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repository:</span> <span class="string">https://github.com/[用户名]/[用户名].github.io.git</span>  <span class="comment">#你的仓库地址</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>

<p>再安装git部署插件  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>然后依次执行下面三条命令  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean   <span class="comment">#清除缓存文件 db.json 和已生成的静态文件 public</span></span><br><span class="line">hexo g       <span class="comment">#生成网站静态文件到默认设置的 public 文件夹(hexo generate 的缩写)</span></span><br><span class="line">hexo d       <span class="comment">#自动生成网站静态文件，并部署到设定的仓库(hexo deploy 的缩写)</span></span><br></pre></td></tr></table></figure>

<p>部署完成后就可以输入你的域名访问自己的博客网站了：[用户名].github.io  </p>
<h2 id="美化"><a href="#美化" class="headerlink" title="美化"></a>美化</h2><p>hexo主题众多，其中next深得我心，简约大气，我们建立博客是为了写文章做笔记，某种意义上什么主题并不重要，当然你可以找一个自己喜欢的  </p>
<p>这个是next的<a href="https://theme-next.js.org/">示例站点</a>  </p>
<p>你可以拖到最下面查看他的源代码，找找那些你喜欢的地方是如何修改的  </p>
<p>我们将主题仓库clone到blog&#x2F;themes&#x2F;next目录下（或者clone之后放到next目录里）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/next-theme/hexo-theme-next</span><br></pre></td></tr></table></figure>

<p>完成后我们修改之前改过的配置文件<code>_config.yml</code>  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Site</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">cola&#x27;s</span> <span class="string">blog</span></span><br><span class="line"><span class="attr">subtitle:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">&#x27;hello world&#x27;</span></span><br><span class="line"><span class="attr">keywords:</span></span><br><span class="line"><span class="attr">author:</span> <span class="string">cola</span></span><br><span class="line"><span class="attr">language:</span> <span class="string">zh-CN</span>           <span class="comment">#主题语言</span></span><br><span class="line"><span class="attr">timezone:</span> <span class="string">&#x27;Asia/Shanghai&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">theme:</span> <span class="string">hexo-theme-next</span>  <span class="comment">#主题</span></span><br></pre></td></tr></table></figure>

<p>打开<code>hexo-theme-next</code>下的配置文件<code>_config.yml</code>，对主题本身作一些修改  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Schemes</span></span><br><span class="line"><span class="comment">#scheme: Muse</span></span><br><span class="line"><span class="comment">#scheme: Mist</span></span><br><span class="line"><span class="comment">#scheme: Pisces</span></span><br><span class="line"><span class="attr">scheme:</span> <span class="string">Gemini</span>    <span class="comment">#这是我选的主题</span></span><br></pre></td></tr></table></figure>

<p>然后我们再在命令行输入  </p>
<p>清理  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br></pre></td></tr></table></figure>

<p>静态部署  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo g</span><br></pre></td></tr></table></figure>

<p>本地查看（先检查再部署是好习惯）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>部署（上传到github）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>针对主题的修改可以发挥自己的想象力，不只是修改他本身的配置文件，虽然这已经可以做到很多事情了，你可以设置头像，设置座右铭，你也可以写自己的前端代码，定制主题样式，接下来就交给你自己探索吧。  </p>
<h2 id="文章相关功能"><a href="#文章相关功能" class="headerlink" title="文章相关功能"></a>文章相关功能</h2><h3 id="如何写文章"><a href="#如何写文章" class="headerlink" title="如何写文章"></a>如何写文章</h3><p>我们的文章默认都在<code>blog/public/_post</code>下面，默认有一篇<code>helloworld.md</code>  </p>
<p>新建一篇文章  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new post 新文章名</span><br></pre></td></tr></table></figure>

<p>新建一篇草稿（不会显示在站点上）  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new draft 草稿文章名</span><br></pre></td></tr></table></figure>

<p>查看草稿</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo s --draft</span><br></pre></td></tr></table></figure>

<p>草稿转换成正式文章  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo publish 草稿文章名</span><br></pre></td></tr></table></figure>

<p>简化命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新建正式文章</span></span><br><span class="line">hexo n 文章名</span><br><span class="line"><span class="comment"># 新建草稿文章</span></span><br><span class="line">hexo n draft 草稿文章名</span><br><span class="line"><span class="comment"># 启动本地服务器并显示草稿</span></span><br><span class="line">hexo s --draft</span><br></pre></td></tr></table></figure>

<p>写完文章之后用<code>hexo d</code>部署，就可以在网页端看到我们的文章了，当然，先检查再部署才是好习惯。  </p>
<h3 id="将正式文章转化为草稿"><a href="#将正式文章转化为草稿" class="headerlink" title="将正式文章转化为草稿"></a>将正式文章转化为草稿</h3><p>正常我们新建一个草稿文章，会在<code>source/_drafts</code>目录下，我们只需要手动将文章移动到这里即可。最好是用<code>hexo n draft xxx</code>让hexo自动创建这个目录。草稿文章和正式文章，只是所在目录不同罢了，<code>hexo publish</code>也只是把草稿文章从那个<code>source/_drafts</code>移动到<code>source/_posts</code>中。  </p>
<h3 id="显示所有文章"><a href="#显示所有文章" class="headerlink" title="显示所有文章"></a>显示所有文章</h3><p><code>hexo list post --draft</code>  </p>
<p>这会显示所有文章（包括草稿），在你需要转换草稿为正式文章时直接用此命令列出来，复制他的名字<code>publish</code>就行。  </p>
<h3 id="如何删除文章"><a href="#如何删除文章" class="headerlink" title="如何删除文章"></a>如何删除文章</h3><p>如果文章还没有发布到git，直接删除_post文件夹下的源文件之后，执行  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>如果你的文章已经发布到git，还需要删除<code>.deploy_git</code>文件夹，再执行上面的命令，究其原因就是hexo clean并没有将发布内容清除  </p>
<h3 id="如何创建页面"><a href="#如何创建页面" class="headerlink" title="如何创建页面"></a>如何创建页面</h3><p>我们可以打开next主题的menu，比如下面我启用了四个页面  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">home:</span> <span class="string">/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-home</span></span><br><span class="line">  <span class="comment">#about: /about/ || fa fa-user</span></span><br><span class="line">  <span class="attr">tags:</span> <span class="string">/tags/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-tags</span></span><br><span class="line">  <span class="attr">categories:</span> <span class="string">/categories/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-th</span></span><br><span class="line">  <span class="attr">archives:</span> <span class="string">/archives/</span> <span class="string">||</span> <span class="string">fa</span> <span class="string">fa-archive</span></span><br><span class="line">  <span class="comment">#schedule: /schedule/ || fa fa-calendar</span></span><br><span class="line">  <span class="comment">#sitemap: /sitemap.xml || fa fa-sitemap</span></span><br><span class="line">  <span class="comment">#commonweal: /404/ || fa fa-heartbeat</span></span><br></pre></td></tr></table></figure>

<p>但是实际点击会发现<code>标签</code>和<code>分类</code>页面会错误，因为我们还没有创建这两个页面  </p>
<p>创建标签页  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new page tags</span><br></pre></td></tr></table></figure>

<p>创建分类页  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new page categories</span><br></pre></td></tr></table></figure>

<p>接下来我们就会看到<code>_posts</code>所在目录下多了两个文件夹，并且其中都有<code>index.md</code>文件，我们修改一下  </p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 标签</span><br><span class="line">date: 2025-02-23 18:38:08</span><br><span class="line"><span class="section">type: &quot;tags&quot;</span></span><br><span class="line"><span class="section">---</span></span><br></pre></td></tr></table></figure>

<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 分类</span><br><span class="line">date: 2025-02-23 18:38:08</span><br><span class="line"><span class="section">type: &quot;categories&quot;</span></span><br><span class="line"><span class="section">---</span></span><br></pre></td></tr></table></figure>

<p>我们在文章头信息可以自定义标签和分类，这也是本文章的分类和标签，你可以在相关的地方找到，文章头中我们写了描述信息，这会自动折叠文章，打开时会自动为文章创建目录  </p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 利用github搭建个人博客（使用hexo）</span><br><span class="line">date: 2025-02-23 18:10:52</span><br><span class="line">categories: 随想</span><br><span class="line">tags:</span><br><span class="line"><span class="bullet">  -</span> hexo</span><br><span class="line"><span class="section">description: 介绍如何在windows上使用hexo框架和github部署自己的静态博客网站，配置next主题，并简单介绍了怎么使用它写文章</span></span><br><span class="line"><span class="section">---</span></span><br></pre></td></tr></table></figure>

<h3 id="增加搜索功能"><a href="#增加搜索功能" class="headerlink" title="增加搜索功能"></a>增加搜索功能</h3><p>安装hexo-generator-searchdb，在站点根目录下执行  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>

<p>编辑站点配置文件，在最后新增下面内容  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">search:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">search.xml</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">post</span></span><br><span class="line">  <span class="attr">format:</span> <span class="string">html</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">10000</span></span><br></pre></td></tr></table></figure>

<p>编辑主题配置文件，启用本地搜索  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">local_search:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line"><span class="string">```</span>  </span><br><span class="line"></span><br><span class="line"><span class="comment">#### 配置显示mermaid流程图  </span></span><br><span class="line"></span><br><span class="line"><span class="string">当然那还是通过配置插件解决（注意，我是NexT主题，不同主题需要的配置方式可能不一致）</span>  </span><br><span class="line"></span><br><span class="line"><span class="string">在博客根目录下安装插件：</span>  </span><br><span class="line"></span><br><span class="line"><span class="string">```bash</span></span><br><span class="line"><span class="string">npm</span> <span class="string">install</span> <span class="string">hexo-mermaid-lastest</span> <span class="string">--save</span></span><br></pre></td></tr></table></figure>

<p>修改主题的配置文件<code>_config.yaml</code>:  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Mermaid tag</span></span><br><span class="line"><span class="attr">mermaid:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>然后重新生成博客：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean</span><br><span class="line">hexo g</span><br></pre></td></tr></table></figure>

<p>如果你习惯用暗色主题，可能会看到流程图的连线颜色和背景色过于相似导致看不清，这时候我们打开 <code>next</code> 主题中的自定义样式：  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">custom_file_path:</span></span><br><span class="line">  <span class="comment"># 把这个注释取消即可</span></span><br><span class="line">  <span class="attr">style:</span> <span class="string">source/_data/styles.styl</span></span><br></pre></td></tr></table></figure>

<p>重点来了，这个文件不是索引的 <code>next</code> 目录，而是hexo主目录，所以我们要在hexo主目录的 <code>source</code> 添加文件：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span>/_data/styles.styl</span><br></pre></td></tr></table></figure>

<p>然后在文件中写上样式配置：  </p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.mermaid</span> <span class="selector-class">.flowchart-link</span> &#123;</span><br><span class="line">  <span class="attribute">stroke</span>: <span class="number">#aaa</span> <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.mermaid</span> <span class="selector-class">.edgePath</span> <span class="selector-class">.path</span> &#123;</span><br><span class="line">  <span class="attribute">stroke</span>: <span class="number">#aaa</span> <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.mermaid</span> <span class="selector-class">.arrowheadPath</span> &#123;</span><br><span class="line">  <span class="attribute">fill</span>: <span class="number">#aaa</span> <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.mermaid</span> <span class="attribute">marker</span> <span class="selector-tag">path</span> &#123;</span><br><span class="line">  <span class="attribute">fill</span>: <span class="number">#aaa</span> <span class="meta">!important</span>;</span><br><span class="line">  <span class="attribute">stroke</span>: <span class="number">#aaa</span> <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.mermaid</span> <span class="selector-tag">text</span> &#123;</span><br><span class="line">  <span class="attribute">fill</span>: <span class="number">#eee</span> <span class="meta">!important</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样，在默认的暗色主题下也能看清mermaid连线了（如果是亮色主题并不需要这个配置）。</p>
<h2 id="保存你的配置和源文件"><a href="#保存你的配置和源文件" class="headerlink" title="保存你的配置和源文件"></a>保存你的配置和源文件</h2><p>当我们完成配置hexo之后，最重要的就是我们的配置文件和源文件，防止主机丢失，或者在多台机器上搭建相同环境，这样我们在哪里都能写文章。  </p>
<p>配置文件自然就是根目录下的<code>_config.yml</code>，源文件是<code>source</code>目录中所有内容。  </p>
<p>我们可以直接在根目录<code>git init</code>建立git仓库，然后连接到远程仓库（你可以保存源文件到私有仓库），根目录下已经有了一个<code>.gitignore</code>，帮我们排除了所有不需要的文件，如果没有的话直接新建一个，然后写上：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br><span class="line">db.json</span><br><span class="line">*.log</span><br><span class="line">node_modules/</span><br><span class="line">public/</span><br><span class="line">.deploy*/</span><br><span class="line">_multiconfig.yml</span><br><span class="line">.vscode</span><br><span class="line">.github</span><br><span class="line">themes</span><br><span class="line">package-lock.json</span><br><span class="line">package.json</span><br></pre></td></tr></table></figure>

<p>我们用<code>git status</code>查看一下会上传哪些文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.gitignore</span><br><span class="line">_config.yml</span><br><span class="line">scaffolds/</span><br><span class="line"><span class="built_in">source</span>/</span><br></pre></td></tr></table></figure>

<p>完美。  </p>
<p>注意，我把themes排除了，因为配置得不多，而且文件太大，需要配置再clone吧。  </p>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>tcp/ip编程</title>
    <url>/2026/04/13/tcp-ip%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>这篇文章是《TCP&#x2F;IP 网络编程》的阅读笔记，一开始我记录得很详细，几乎是抄书，因为前面都是讲解原理，我想越详细越好，但是到后面我不会事无巨细了，我只记录感兴趣的、重要的和不懂的  </p>
<p>作者同时讲解了linux和windows下的网络编程，笔记中写到的函数声明，其后面的注释一般都是解释返回值的，S代表Success，函数执行成功时的返回值，F代表Failure，函数执行失败时的返回值，但并不是所有的都用S、F这种记法  </p>
<p>在第一章会给出Linux和Windows下可用的服务端和客户端应用代码，不要过分纠结其中看不懂的东西，因为前四章可以说就是为了说明白第一章的代码而存在的，直接往下看就好  </p>
<ul>
<li>Part1：ch1~ch14  开始网络编程</li>
<li>Part2：ch15~ch18 基于Linux的编程</li>
<li>Part3：ch19~ch23 基于Windows的编程</li>
<li>Part4：ch24~ch25 结束网络编程</li>
</ul>
<h2 id="Chapter1-理解网络编程和套接字"><a href="#Chapter1-理解网络编程和套接字" class="headerlink" title="Chapter1 理解网络编程和套接字"></a>Chapter1 理解网络编程和套接字</h2><h3 id="1-1-理解网络编程和套接字"><a href="#1-1-理解网络编程和套接字" class="headerlink" title="1.1 理解网络编程和套接字"></a>1.1 理解网络编程和套接字</h3><h4 id="网络编程和套接字概要"><a href="#网络编程和套接字概要" class="headerlink" title="网络编程和套接字概要"></a>网络编程和套接字概要</h4><p><strong>网络编程</strong> 就是编写程序使两台计算机之间交换数据，<strong>套接字（socket）</strong> 是网络数据传输用的软件设备，是由操作系统提供的部件  </p>
<h4 id="构建“接电话”套接字"><a href="#构建“接电话”套接字" class="headerlink" title="构建“接电话”套接字"></a>构建“接电话”套接字</h4><p>作者形象地将接受连接请求的套接字创建比喻成了接电话，其过程是：  </p>
<ol>
<li>调用<em>socket</em>函数创建套接字（安装电话机）</li>
<li>调用<em>bind</em>函数分配ip地址和端口号（分配电话号码）</li>
<li>调用<em>listen</em>函数转化为可接收请求状态（连接电话线）</li>
<li>调用<em>accept</em>函数受理连接请求（拿起话筒）</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">socket</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol)</span>                    <span class="comment">// 成功返回文件描述符，失败返回-1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">bind</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr* myaddr, <span class="type">socklen_t</span> addrlen)</span>  <span class="comment">// 成功0，失败-1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">listen</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">int</span> backlog)</span>                               <span class="comment">// 成功0，失败-1</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">accept</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> *addrlen)</span> <span class="comment">// 成功返回文件描述符，失败返回-1</span></span><br></pre></td></tr></table></figure>

<h4 id="编写“Hello-World-”服务器端"><a href="#编写“Hello-World-”服务器端" class="headerlink" title="编写“Hello World!”服务器端"></a>编写“Hello World!”服务器端</h4><p>下面的代码只要看看之前提到的四个函数的调用过程就好  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// hello_server.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> serv_sock;</span><br><span class="line">    <span class="type">int</span> clnt_sock;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">clnt_addr</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> clnt_addr_size;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> message[]=<span class="string">&quot;Hello World!&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage : %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (serv_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    serv_addr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*) &amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (listen(serv_sock, <span class="number">5</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    clnt_addr_size = <span class="keyword">sizeof</span>(clnt_addr);</span><br><span class="line">    clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, &amp;clnt_addr_size);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;accept() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    write(clnt_sock, message, <span class="keyword">sizeof</span>(message));</span><br><span class="line">    close(clnt_sock);</span><br><span class="line">    close(serv_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>24：socket</li>
<li>33：bind</li>
<li>36：listen</li>
<li>40：accept</li>
</ul>
<h4 id="构建打电话（请求连接）套接字（客户端）"><a href="#构建打电话（请求连接）套接字（客户端）" class="headerlink" title="构建打电话（请求连接）套接字（客户端）"></a>构建打电话（请求连接）套接字（客户端）</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">connect</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr *serv_addr, <span class="type">socklen_t</span> addrlen)</span>;  <span class="comment">// 成功0，失败-1</span></span><br></pre></td></tr></table></figure>

<p>客户端只需要“调用socket函数创建套接字”和“调用connect函数向服务器发送连接请求”这两个步骤  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// hello_client.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    <span class="type">int</span> sock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line">    <span class="type">char</span> message[<span class="number">30</span>];</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;IP&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_addr.s_addr = inet_addr(argv[<span class="number">1</span>]);</span><br><span class="line">    serv_addr.sin_port = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (connect(sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    str_len = read(sock, message, <span class="keyword">sizeof</span>(message)<span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">if</span> (str_len == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;read() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Message from server : %s \n&quot;</span>, message);</span><br><span class="line">    close(sock);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>21：socket创建套接字，后面调用bind、listen将成为服务端套接字，调用connect将成为客户端套接字</li>
<li>30：connect向服务端发送连接请求</li>
</ul>
<h4 id="在Linux平台下运行"><a href="#在Linux平台下运行" class="headerlink" title="在Linux平台下运行"></a>在Linux平台下运行</h4><p>假设编译出来的可执行文件为<code>hserver</code>和<code>hclient</code>，那么开<strong>两个终端</strong>这样运行  </p>
<p>先开服务端监听  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./hserver 9190</span><br></pre></td></tr></table></figure>

<p>再运行客户端请求连接</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./hclient 127.0.0.1 9190</span><br></pre></td></tr></table></figure>

<p>此时在客户端终端界面应该会传送过来一条信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Message from server : Hello World!</span><br></pre></td></tr></table></figure>

<p>运行完之后会服务端和客户端都会停止，而且无法使用同一个端口号立即重新运行，这个以后再讲  </p>
<p>使用IP地址为<code>127.0.0.1</code>是因为客户端和服务端都运行在同一台计算机上，如果运行在不同的计算机上，则要将IP换成服务端程序所在的计算机IP  </p>
<h3 id="1-2-基于Linux的文件操作"><a href="#1-2-基于Linux的文件操作" class="headerlink" title="1.2 基于Linux的文件操作"></a>1.2 基于Linux的文件操作</h3><p>在Linux中，socket也被认为是文件的一种，因此在网络数据传输的过程中自然可以使用文件I&#x2F;O的函数，Windows和Linux不同，要区分socket和文件，因此在Windows下传输网络数据需要使用特殊的数据传输相关函数  </p>
<h4 id="底层文件访问（Low-Level-File-Access）和文件描述符（File-Descriptor）"><a href="#底层文件访问（Low-Level-File-Access）和文件描述符（File-Descriptor）" class="headerlink" title="底层文件访问（Low-Level File Access）和文件描述符（File Descriptor）"></a>底层文件访问（Low-Level File Access）和文件描述符（File Descriptor）</h4><p>“底层”可以理解为“与标准无关的、操作系统独立提供的”，稍后讲解的都是非标准的函数  </p>
<p><strong>文件描述符</strong>（文件句柄：Windows中的叫法）是为了方便“称呼”文件或套接字而赋予的<strong>整数</strong>，在Linux系统中，C语言的标准输入输出和标准错误也被赋予了文件描述符，只不过它们不需要我们手动创建，而是程序开始后自动分配的  </p>
<table>
<thead>
<tr>
<th align="center">File Descriptor</th>
<th align="center">Object</th>
</tr>
</thead>
<tbody><tr>
<td align="center">0</td>
<td align="center">Standard Input</td>
</tr>
<tr>
<td align="center">1</td>
<td align="center">Standard Output</td>
</tr>
<tr>
<td align="center">2</td>
<td align="center">Standard Error</td>
</tr>
</tbody></table>
<p>文件和套接字的文件描述符来源不同，文件的是通过<code>open</code>函数打开它而创建，套接字的则是通过<code>socket</code>函数创建，但对文件描述符的操作并无区别  </p>
<h4 id="打开文件"><a href="#打开文件" class="headerlink" title="打开文件"></a>打开文件</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/type.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span>    <span class="comment">// file control</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// path 文件名的字符串路径</span></span><br><span class="line"><span class="comment">// flag 文件的打开模式</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">open</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> flag)</span>;  <span class="comment">// 成功返回文件描述符，失败返回-1</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th align="left">打开模式（flag）</th>
<th align="left">含义</th>
</tr>
</thead>
<tbody><tr>
<td align="left">O_CREAT</td>
<td align="left">必要时创建文件</td>
</tr>
<tr>
<td align="left">O_TRUNC</td>
<td align="left">删除全部现有数据</td>
</tr>
<tr>
<td align="left">O_APPEND</td>
<td align="left">维持现有数据，将添加内容保存到其后面</td>
</tr>
<tr>
<td align="left">O_RDONLY</td>
<td align="left">只读</td>
</tr>
<tr>
<td align="left">O_WRONLY</td>
<td align="left">只写</td>
</tr>
<tr>
<td align="left">O_RDWR</td>
<td align="left">读写</td>
</tr>
</tbody></table>
<p>可以使用或运算以多种模式打开文件  </p>
<h4 id="关闭文件"><a href="#关闭文件" class="headerlink" title="关闭文件"></a>关闭文件</h4><p><code>close</code>函数用于关闭打开的文件，作用对象是文件描述符  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// fd 需要关闭的文件或套接字的文件描述符</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">close</span><span class="params">(<span class="type">int</span> fd)</span>;  <span class="comment">// 成功时返回0，失败返回-1</span></span><br></pre></td></tr></table></figure>

<h4 id="将数据写入文件"><a href="#将数据写入文件" class="headerlink" title="将数据写入文件"></a>将数据写入文件</h4><p><code>write</code>函数用于向文件输出（传输）数据，作用对象也是文件描述符  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// fd      显示数据传输对象的文件描述符  </span></span><br><span class="line"><span class="comment">// buf     保存要传输数据的缓冲地址</span></span><br><span class="line"><span class="comment">// nbytes  要传输数据的字节数</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">write</span><span class="params">(<span class="type">int</span> fd, <span class="type">const</span> <span class="type">void</span>* buf, <span class="type">size_t</span> nbytes)</span>;  <span class="comment">// 成功时返回写入的字节数，失败时返回-1</span></span><br></pre></td></tr></table></figure>

<p><code>size_t</code>是<code>unsigned int</code>，<code>ssize_t</code>是<code>signed int</code>，它们是使用<code>typedef</code>定义的类型，不同时代的系统<code>int</code>位数是不一样的，而使用<code>size_t</code>就能很方便进行修改，只需要在<code>typedef</code>处更改即可  </p>
<h4 id="读文件中的数据"><a href="#读文件中的数据" class="headerlink" title="读文件中的数据"></a>读文件中的数据</h4><p><code>read</code>函数用于输入（接收）数据，作用对象依然是文件描述符  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// fd      显示数据接收对象的文件描述符</span></span><br><span class="line"><span class="comment">// buf     要保存接收数据的缓冲地址</span></span><br><span class="line"><span class="comment">// nbytes  要接收数据的最大字节数</span></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">read</span><span class="params">(<span class="type">int</span> fd, <span class="type">void</span>* buf, <span class="type">size_t</span> nbytes)</span>;  <span class="comment">// 成功时返回接收的字节数（但是直接遇到文件结尾则返回0），失败时返回-1</span></span><br></pre></td></tr></table></figure>

<h4 id="文件描述符与套接字"><a href="#文件描述符与套接字" class="headerlink" title="文件描述符与套接字"></a>文件描述符与套接字</h4><p>作者这里写了个同时创建文件和套接字的程序，然后打印出文件描述符，文件描述符从3开始从小到大整数递增，因为0、1、2已经分配给标准I&#x2F;O了  </p>
<h3 id="1-3-基于Windows平台的实现"><a href="#1-3-基于Windows平台的实现" class="headerlink" title="1.3 基于Windows平台的实现"></a>1.3 基于Windows平台的实现</h3><p>Windows套接字和Linux的在很多地方都很类似  </p>
<h4 id="同时学习Windows和Linux的原因"><a href="#同时学习Windows和Linux的原因" class="headerlink" title="同时学习Windows和Linux的原因"></a>同时学习Windows和Linux的原因</h4><p>大多数项目的服务端在Linux上而客户端在Windows上，而且它们的套接字编程很类似  </p>
<h4 id="为Windows套接字编程设置头文件和库"><a href="#为Windows套接字编程设置头文件和库" class="headerlink" title="为Windows套接字编程设置头文件和库"></a>为Windows套接字编程设置头文件和库</h4><p>在<code>Winsock</code>的基础上开发网络程序，需要：</p>
<ul>
<li>导入头文件<code>winsock2.h</code></li>
<li>连接<code>ws2_32.lib</code>库</li>
</ul>
<p>至此只需要在源文件中添加头文件<code>winsock2.h</code>就能调用Winsock相关函数了  </p>
<h4 id="Winsock的初始化"><a href="#Winsock的初始化" class="headerlink" title="Winsock的初始化"></a>Winsock的初始化</h4><p>调用<code>WSAStartup</code>函数，设置使用的Winsock版本（Winsock有多个版本，需要手动设置一下），并初始化相应版本的库  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// wVersionRequested  程序中用到的winsock版本</span></span><br><span class="line"><span class="comment">// lpWSAData          WSADATA结构体变量指针</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">WSAStartup</span><span class="params">(WORD wVersionRequested, LPWSADATA lpWSAData)</span>;  <span class="comment">// 成功时返回0，失败时返回非0的错误代码</span></span><br></pre></td></tr></table></figure>

<p>使用<code>MAKEWORD</code>传递版本信息  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">MAKEWORD(<span class="number">1</span>, <span class="number">2</span>);  <span class="comment">// 主版本为1，副版本为2，返回0x0201</span></span><br><span class="line">MAKEWORD(<span class="number">2</span>, <span class="number">2</span>);  <span class="comment">// 主版本为2，副版本为2，返回0x0202</span></span><br></pre></td></tr></table></figure>

<p>“Winsock初始化公式”  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注销Winsock  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">WSACleanup</span><span class="params">(<span class="type">void</span>)</span>;  <span class="comment">// 成功时返回0，失败时返回SOCKET_ERROR</span></span><br></pre></td></tr></table></figure>

<p>调用该函数之后，Winsock的相关函数便无法再使用，一般在程序将要结束前调用  </p>
<h3 id="1-4-基于Windows的套接字相关函数及示例"><a href="#1-4-基于Windows的套接字相关函数及示例" class="headerlink" title="1.4 基于Windows的套接字相关函数及示例"></a>1.4 基于Windows的套接字相关函数及示例</h3><p>只是介绍，体会一下和Linux的相似性  </p>
<h4 id="基于Windows的套接字相关函数"><a href="#基于Windows的套接字相关函数" class="headerlink" title="基于Windows的套接字相关函数"></a>基于Windows的套接字相关函数</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 开始前需要WSAStartup初始化</span></span><br><span class="line">SOCKET <span class="title function_">socket</span><span class="params">(<span class="type">int</span> af, <span class="type">int</span> type, <span class="type">int</span> protocol)</span>;                       <span class="comment">// S: 套接字句柄  F：INVALID_SOCKET</span></span><br><span class="line"><span class="type">int</span>    <span class="title function_">bind</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="keyword">struct</span> sockaddr* name, <span class="type">int</span> namelen)</span>;     <span class="comment">// S：0  F：SOCKET_ERROR</span></span><br><span class="line"><span class="type">int</span>    <span class="title function_">listen</span><span class="params">(SOCKET s, <span class="type">int</span> backlog)</span>;                                <span class="comment">// S：0  F：SOCKET_ERROR</span></span><br><span class="line">SOCKET <span class="title function_">accept</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="keyword">struct</span> sockaddr* addr, <span class="type">int</span> addrlen)</span>;   <span class="comment">// S: 套接字句柄  F：INVALID_SOCKET</span></span><br><span class="line"><span class="type">int</span>    <span class="title function_">connect</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="keyword">struct</span> sockaddr* name, <span class="type">int</span> namelen)</span>;  <span class="comment">// S：0  F：SOCKET_ERROR</span></span><br><span class="line"><span class="type">int</span>    <span class="title function_">closesocket</span><span class="params">(SOCKET s)</span>;                                        <span class="comment">// S：0  F：SOCKET_ERROR</span></span><br><span class="line"><span class="comment">// 结束后需要WSACleanup注销</span></span><br></pre></td></tr></table></figure>

<p>在Linux中关闭文件和套接字都是使用<code>close</code>函数，而在windows中关闭套接字有专用函数<code>closesocket</code>，其他的并没有太大差别  </p>
<h4 id="Windows中的文件句柄和套接字句柄"><a href="#Windows中的文件句柄和套接字句柄" class="headerlink" title="Windows中的文件句柄和套接字句柄"></a>Windows中的文件句柄和套接字句柄</h4><p>Windows中的文件句柄和套接字句柄是有区别的，不像Linux中操作文件和套接字都是文件描述符，可以用完全一样的函数操作，Windows中操作文件句柄和套接字句柄会使用不同的函数  </p>
<h4 id="创建基于Windows的服务器端和客户端"><a href="#创建基于Windows的服务器端和客户端" class="headerlink" title="创建基于Windows的服务器端和客户端"></a>创建基于Windows的服务器端和客户端</h4><p>目前只需要理解套接字相关函数的调用过程，套接字库的初始化过程和注销即可  </p>
<p>我使用 VisualStudio 2022 进行学习，新建好项目之后会生成一个解决方案，一个解决方案底下可以新建多个项目，可以把server和client项目都建立在同一个解决方案下  </p>
<p>在<strong>项目名称</strong>上右键-&gt;<strong>属性（可以看到打开属性有个快捷键Alt+Enter）-&gt;链接器-&gt;附件依赖项</strong>填写<code>ws2_32.lib</code>，然后在<strong>调试-&gt;命令参数</strong>里面填写命令行参数（服务端是一个端口号，我填的是9091；客户端是ip和端口号，我填的是 127.0.0.1 9091），以空格分隔多个参数  </p>
<p>在<strong>解决方案名称</strong>上右键，打开属性菜单，设置<strong>启动项目</strong>，因为我们要分服务端和客户端执行，并且服务端要先执行，所以肯定不能是“单启动项目”（默认是这个），把它设置为“当前选定内容”，这样你选中哪个项目执行就是执行那个项目生成的可执行文件；或者设置为“多个启动项目”，同时在<strong>项目依赖项</strong>里面“项目”栏选中为客户端，勾选“依赖于”栏的服务端，这样你开始执行的时候vs就会自动先执行服务端，然后执行客户端  </p>
<p>如我创建的项目结构，解决方案为<strong>vsWindowsNet</strong>，服务端项目为<strong>vsWindowsNetServer</strong>，客户端项目为<strong>vsWindowsNetClient</strong>  </p>
<p><img src="/../images/post/tcp-0_vsProject.png" alt="项目结构">  </p>
<p>当然也可以用gcc编译（我使用的是MinGW64），使用命令行  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc .\hello_server_win.c -o server.exe -lws2_32</span><br><span class="line">gcc .\hello_client_win.c -o client.exe -lws2_32</span><br></pre></td></tr></table></figure>

<p>执行服务端  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.\server.exe 9091</span><br></pre></td></tr></table></figure>

<p>执行客户端与结果  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.\client.exe 127.0.0.1 9091</span><br><span class="line">Message from server: Hello Windows Network Program!</span><br></pre></td></tr></table></figure>

<p>其实我也非常喜欢轻量化的环境，我使用vs是因为之前没有用过这款神一般的IDE，有必要学习一下，利用它开发很方便，可以省去很多不必要的麻烦，而且它的调试器很好用，不过我目前还不太会  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// hello_server_win.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    SOCKET hServSock, hClntSock;</span><br><span class="line">    SOCKADDR_IN servAddr, clntAddr;</span><br><span class="line">    <span class="type">int</span> szClntAddr;</span><br><span class="line">    <span class="type">char</span> message[] = <span class="string">&quot;Hello Windows Network Program!&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    hServSock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (hServSock == INVALID_SOCKET)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;servAddr, <span class="number">0</span>, <span class="keyword">sizeof</span>(servAddr));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;servaddr size: %zu\n&quot;</span>, <span class="keyword">sizeof</span>(servAddr));</span><br><span class="line">    servAddr.sin_family = AF_INET;</span><br><span class="line">    servAddr.sin_addr.S_un.S_addr = htonl(INADDR_ANY);</span><br><span class="line">    servAddr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(hServSock, (SOCKADDR*)&amp;servAddr, <span class="keyword">sizeof</span>(servAddr)) == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;bind() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (listen(hServSock, <span class="number">5</span>) == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;listen() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    szClntAddr = <span class="keyword">sizeof</span>(clntAddr);</span><br><span class="line">    hClntSock = accept(hServSock, (SOCKADDR*)&amp;clntAddr, &amp;szClntAddr);</span><br><span class="line">    <span class="keyword">if</span> (hClntSock == INVALID_SOCKET)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;accept() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    send(hClntSock, message, <span class="keyword">sizeof</span>(message), <span class="number">0</span>);</span><br><span class="line">    closesocket(hClntSock);</span><br><span class="line">    closesocket(hServSock);</span><br><span class="line">    WSACleanup();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>19：<code>WSAStartup</code>初始化套接字库</li>
<li>22、31：<code>socket</code>创建套接字、<code>bind</code>给套接字分配IP地址和端口号</li>
<li>35：<code>listen</code>使创建的套接字成为服务器套接字</li>
<li>39：<code>accept</code>受理客户端连接请求</li>
<li>43：<code>send</code>向第39行连接的客户端传输数据</li>
<li>46：<code>WSACleanup</code>注销19行初始化的套接字库</li>
</ul>
<p>可以看到除了需要初始化和注销，其余的基本都是类似的  </p>
<p>注：第29行的结构体成员和原书写的不一样，我这个是正确的，可能是结构体有变动或者是原书笔误  </p>
<p>下面是客户端代码，需要注意的是，原书里用到的一些函数被弃用了，第29行的<code>inet_pton(...)</code>是新的函数，将字符串参数转换为ip地址，它在头文件<code>ws2tcpip.h</code>中被声明  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// hello_client_win.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ws2tcpip.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    SOCKET hSocket;</span><br><span class="line">    SOCKADDR_IN servAddr;</span><br><span class="line"></span><br><span class="line">    <span class="type">char</span> message[<span class="number">32</span>];</span><br><span class="line">    <span class="type">int</span> strLen;</span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    hSocket = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (hSocket == INVALID_SOCKET)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;servAddr, <span class="number">0</span>, <span class="keyword">sizeof</span>(servAddr));</span><br><span class="line">    servAddr.sin_family = AF_INET;</span><br><span class="line">    inet_pton(AF_INET, argv[<span class="number">1</span>], &amp;servAddr.sin_addr.S_un.S_addr);</span><br><span class="line">    servAddr.sin_port = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (connect(hSocket, (SOCKADDR*)&amp;servAddr, <span class="keyword">sizeof</span>(servAddr)) == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    strLen = recv(hSocket, message, <span class="keyword">sizeof</span>(message) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (strLen == <span class="number">-1</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;recv() error!&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s\n&quot;</span>, message);</span><br><span class="line"></span><br><span class="line">    closesocket(hSocket);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>20：<code>WSAStartup</code>初始化Winsock库</li>
<li>23、32：<code>socket</code>创建套接字、<code>connect</code>通过套接字向服务器发出连接请求</li>
<li>35：<code>recv</code>接收服务器发来的数据</li>
<li>42：<code>WSACleanup</code>注销Winsock库</li>
</ul>
<h4 id="基于Windows的I-O函数"><a href="#基于Windows的I-O函数" class="headerlink" title="基于Windows的I&#x2F;O函数"></a>基于Windows的I&#x2F;O函数</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// s      数据传输对象的套接字句柄值</span></span><br><span class="line"><span class="comment">// buf    保存待传输的数据地址值</span></span><br><span class="line"><span class="comment">// len    要传输的字节数</span></span><br><span class="line"><span class="comment">// flags  传输数据时用到的多种选项信息</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">send</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="type">char</span>* buf, <span class="type">int</span> len, <span class="type">int</span> flags)</span>;  <span class="comment">// S：传输字节数  F：SOCKET_ERROR</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// s      数据接收对象的套接字句柄值</span></span><br><span class="line"><span class="comment">// buf    保存接收数据的缓冲地址值</span></span><br><span class="line"><span class="comment">// len    能够接收的最大字节数</span></span><br><span class="line"><span class="comment">// flags  接收数据时用到的多种选项信息</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">revc</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="type">char</span>* buf, <span class="type">int</span> len, <span class="type">int</span> flags)</span>;  <span class="comment">// S：传输字节数（收到EOF则返回0）  F：SOCKET_ERROR</span></span><br></pre></td></tr></table></figure>

<p>windows中套接字句柄需要使用专门的函数传输&#x2F;接收数据，而linux中文件和套接字都能用相同的函数进行数据I&#x2F;O，但是linux中也有函数专门操作套接字（同windows有send、recv），这个以后再说  </p>
<h2 id="Chapter2-套接字类型与协议设置"><a href="#Chapter2-套接字类型与协议设置" class="headerlink" title="Chapter2 套接字类型与协议设置"></a>Chapter2 套接字类型与协议设置</h2><p>这一章是原理篇，会相对比较枯燥，不过很重要，一点点来吧，仅仅关注创建套接字时使用的socket函数就好  </p>
<h3 id="2-1-套接字协议及其数据传输特性"><a href="#2-1-套接字协议及其数据传输特性" class="headerlink" title="2.1 套接字协议及其数据传输特性"></a>2.1 套接字协议及其数据传输特性</h3><h4 id="关于协议（protocol）"><a href="#关于协议（protocol）" class="headerlink" title="关于协议（protocol）"></a>关于协议（protocol）</h4><p>计算机间对话必备的通信规则、完成数据交换而制定的约定  </p>
<h4 id="创建套接字"><a href="#创建套接字" class="headerlink" title="创建套接字"></a>创建套接字</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// domain    套接字中使用的协议族（Protocol Family）信息  </span></span><br><span class="line"><span class="comment">// type      套接字中数据传输类型信息</span></span><br><span class="line"><span class="comment">// protocol  计算机间通信使用的协议信息</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">socket</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol)</span>;  <span class="comment">// S：文件描述符  // F：-1</span></span><br></pre></td></tr></table></figure>

<h4 id="协议族（Protocol-Family）"><a href="#协议族（Protocol-Family）" class="headerlink" title="协议族（Protocol Family）"></a>协议族（Protocol Family）</h4><p><code>socket</code>的第一个参数<code>domain</code>是协议族，它规定了第三个参数<code>protocol</code>的范围  </p>
<p><code>sys/socket.h</code>中声明的PF  </p>
<table>
<thead>
<tr>
<th align="left">名称</th>
<th align="left">协议族</th>
</tr>
</thead>
<tbody><tr>
<td align="left">PF_INET</td>
<td align="left">IPv4互联网协议</td>
</tr>
<tr>
<td align="left">PF_INET6</td>
<td align="left">IPv6互联网协议</td>
</tr>
<tr>
<td align="left">PF_LOCAL</td>
<td align="left">本地通信的UNIX协议族</td>
</tr>
<tr>
<td align="left">PF_PACKET</td>
<td align="left">底层套接字的协议族</td>
</tr>
<tr>
<td align="left">PF_IPX</td>
<td align="left">IPX Novell协议族</td>
</tr>
</tbody></table>
<h4 id="套接字类型（type）"><a href="#套接字类型（type）" class="headerlink" title="套接字类型（type）"></a>套接字类型（type）</h4><p>套接字类型指的是套接字的数据传输方式，通过<code>socket</code>第二个参数传递，没错，一个协议族也存在多种数据传输方式  </p>
<p>下面介绍最具代表性的两种数据传输方式，一定要掌握</p>
<h4 id="套接字类型1：面向连接的套接字（SOCK-STREAM）"><a href="#套接字类型1：面向连接的套接字（SOCK-STREAM）" class="headerlink" title="套接字类型1：面向连接的套接字（SOCK_STREAM）"></a>套接字类型1：面向连接的套接字（SOCK_STREAM）</h4><p>如同发货点和收货点间的传送带，货物（数据）通过传送带传输，它的特征如下  </p>
<ul>
<li>传输过程中数据不会消失</li>
<li>按序传输数据</li>
<li>传输的数据不存在数据边界（Boundary）</li>
</ul>
<p>前两条好理解，最后一条说明一下帮助理解  </p>
<p><strong>收发数据的套接字内部有缓冲（buffer）</strong>, 简而言之就是字节数组，通过套接字传输的数据将保存到该数组，因此read&#x2F;write的调用次数实际上并没有太大意义，比如要接收的数据可能是等缓冲区填满了一次性read出来或者分多次read，所以说它传输的数据没有数据边界  </p>
<p>另外，即使缓冲区满了数据也不会丢失，因为一端套接字缓冲区满了再也无法接收数据的时候，另一端套接字就会停止传输，也就是说，<strong>面向连接的套接字会根据接收端的状态传输数据</strong>，如果传输出错还能提供重传服务，除了特殊情况面向连接的套接字不会发生数据丢失  </p>
<p>除此之外，套接字连接必须要一一对应（服务端和客户端）  </p>
<p>作者的总结：可靠的、按序传递的、基于字节的、面向连接的数据传输方式的套接字  </p>
<h4 id="套接字类型2：面向消息的套接字（SOCK-DGRAM）"><a href="#套接字类型2：面向消息的套接字（SOCK-DGRAM）" class="headerlink" title="套接字类型2：面向消息的套接字（SOCK_DGRAM）"></a>套接字类型2：面向消息的套接字（SOCK_DGRAM）</h4><p>如同高速摩托运送快递（数据），特征如下  </p>
<ul>
<li>强调快速传输而非传输顺序</li>
<li>传输的数据可能丢失也可能损毁</li>
<li>传输的数据有数据边界</li>
<li>限制每次传输的数据大小</li>
</ul>
<p>存在数据边界意味着接收数据的次数应和传输次数相同  </p>
<p>作者的总结：不可靠的、不按序传递的、以数据的高速传输为目的的套接字  </p>
<p>另外，面向消息的套接字不存在连接的概念，这个以后再说  </p>
<h4 id="协议的最终选择"><a href="#协议的最终选择" class="headerlink" title="协议的最终选择"></a>协议的最终选择</h4><p>实际上在大部分情况下，确定了协议族和数据传输方式后（<code>socket</code>函数的前两个参数），就能确定具体的协议了，所以<code>socket</code>函数的第三个参数大部分情况下可以直接写”0”，但是<strong>有一些协议族中有数据传输方式相同的协议</strong>，这就需要protocol参数确定使用的协议了  </p>
<p>面向连接的IPv4协议族协议只有<code>IPPROTO_TCP</code>，面向消息的IPv4协议族协议只有<code>IPPROTO_UDP</code>，下面创建TCP套接字和UDP套接字，实际上第三个参数可以直接写0，因为前两个参数确定了唯一的协议  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> tcp_socket = socket(PF_INET, SOCK_STREAM, IPPROTO_TCP);</span><br><span class="line"><span class="type">int</span> udp_socket = socket(PF_INET, SOCK_DGRAM, IPPROTO_UDP);</span><br></pre></td></tr></table></figure>

<h4 id="面向连接的套接字：TCP套接字示例"><a href="#面向连接的套接字：TCP套接字示例" class="headerlink" title="面向连接的套接字：TCP套接字示例"></a>面向连接的套接字：TCP套接字示例</h4><p>用前面章节的代码验证面向连接的套接字传输的“数据不存在边界”，只需将write和read调用次数不同即可，可以让只增加客户端read调用次数，分多次接收服务端发送的数据  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="keyword">while</span>(read_len = read(sock, &amp;message[idx++], <span class="number">1</span>)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (read_len == <span class="number">-1</span>)</span><br><span class="line">        error_handing(<span class="string">&quot;read() error!&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>

<p>Windows上和Linux的改法一样，就不赘述了  </p>
<h2 id="Chapter3-地址族与数据序列"><a href="#Chapter3-地址族与数据序列" class="headerlink" title="Chapter3 地址族与数据序列"></a>Chapter3 地址族与数据序列</h2><h3 id="分配套接字的IP地址和端口号"><a href="#分配套接字的IP地址和端口号" class="headerlink" title="分配套接字的IP地址和端口号"></a>分配套接字的IP地址和端口号</h3><p>IP（Internet Protocol）是为了收发网络数据而分配给计算机的值，端口号是为了区分程序中的套接字而分配给套接字的序号  </p>
<h4 id="网络地址（Internet-Address）"><a href="#网络地址（Internet-Address）" class="headerlink" title="网络地址（Internet Address）"></a>网络地址（Internet Address）</h4><ul>
<li>IPv4（Internet Protocol version 4）  4字节地址族   AF_INET</li>
<li>IPv6（Internet Protocol version 6）  16字节地址族  AF_INET6</li>
</ul>
<p>注：IP地址分A类、B类、C类、D类（多播IP地址），其4字节由<code>网络ID</code>和<code>主机ID</code>占用，ABCD类IP地址的网络ID分别占1、2、3、4字节  </p>
<p>数据传输并不是一步到位的，而是先传输到与数据报中IP地址相同网络地址的路由器或者交换机，再由路由器或者交换机传输到相应的主机（当然，路由器或交换机也能传到路由器或交换机）  </p>
<p>可以看看这篇文章补一补网络的基础知识：<a href="https://www.zhihu.com/question/367146504/answer/2440447640">如何学习网络编程</a>  </p>
<p>未来链接失效也没关系，你只需要知道数据是怎么通过IP实现正确转发的，以及网络模型每一层的功能（虽然这个不知道也没关系），这些应该有很多资源可供学习  </p>
<p>提一下上面文章重要的知识点：<br>如果机器A给B发送数据，发现B和它不在同一个网段（ip和子网掩码进行与运算），就会把它发送给默认网关（默认网关是路由器的ip），最终通过物理地址将数据传输给确定的机器，网络层（IP协议）本身没有传输包的功能，数据包的实际传输是委托给数据链路层（以太网中的交换机）来实现的  </p>
<p>至于如何获取用于识别各种机器的ip、MAC（物理地址），以及确定ip和MAC的映射关系、ip&#x2F;MAC和端口的映射关系，都是有一定方法的，这个建议看文章了解  </p>
<h4 id="网络地址分类与主机地址边界"><a href="#网络地址分类与主机地址边界" class="headerlink" title="网络地址分类与主机地址边界"></a>网络地址分类与主机地址边界</h4><p>通过IP地址的第一个字节就能判断网络地址占用的字节数，因为我们根据IP地址的边界区分网络地址  </p>
<ul>
<li>A类地址的首字节范围：0~127</li>
<li>B类地址的首字节范围：128~191</li>
<li>C类地址的首字节范围：192~223</li>
</ul>
<p>以及下面的表述方式（从左到右是二进制最高位-&gt;低位）：  </p>
<ul>
<li>A类地址首位以0开始</li>
<li>B类地址前2位以10开始</li>
<li>C类地址前3位以110开始</li>
</ul>
<h4 id="用于区分套接字的端口号"><a href="#用于区分套接字的端口号" class="headerlink" title="用于区分套接字的端口号"></a>用于区分套接字的端口号</h4><p>IP用于区分计算机，只要有IP地址就能向目标主机发送数据，但是无法向确定的应用程序传输数据，这时候就要用到端口号了  </p>
<p>计算机中一般配有网卡（NIC，Network Interface Card）这个数据传输设备，通过NIC接收的数据内有端口号，操作系统正是参考此端口号把数据传输给相应端口的套接字  </p>
<p>端口号就是在同一操作系统内为区分不同的套接字而设置的，因此无法将同一个端口号分配给不同的套接字，<strong>端口号由16位构成，可分配的端口号范围是0-65535，但0~1023是知名端口（Well-Known PORT），一般分配给特定的应用程序，所以应当分配此范围之外的值</strong>  </p>
<p>另外，虽然端口号不能重复，但是TCP套接字和UDP套接字不会共用端口号，所以允许端口号重复，只是协议相同的不允许罢了  </p>
<p>总之，数据传输目标地址要同时包含IP地址和端口号，只有这样数据才能被传输到最终的目标应用程序（即应用程序套接字）  </p>
<h3 id="3-2-地址信息的表示"><a href="#3-2-地址信息的表示" class="headerlink" title="3.2 地址信息的表示"></a>3.2 地址信息的表示</h3><h4 id="表示IPv4地址的结构体"><a href="#表示IPv4地址的结构体" class="headerlink" title="表示IPv4地址的结构体"></a>表示IPv4地址的结构体</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">sa_family_t</span>        sin_family;    <span class="comment">// 地址族（Address Family）</span></span><br><span class="line">    <span class="type">uint16_t</span>           sin_port;      <span class="comment">// 16位TCP/UDP端口号</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span>     <span class="title">sin_addr</span>;</span>      <span class="comment">// 32位IP地址</span></span><br><span class="line">    <span class="type">char</span>               sin_zero[<span class="number">8</span>];   <span class="comment">// 不使用</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">in_addr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">in_addr_t</span>          s_addr;        <span class="comment">// 32位IPv4地址</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>使用自定义类型的好处在于无论在多少位的机器上它们都可以是一样的位数，或根据需要变动位数而无需使用不同类型  </p>
<h4 id="结构体sockaddr-in成员分析"><a href="#结构体sockaddr-in成员分析" class="headerlink" title="结构体sockaddr_in成员分析"></a>结构体sockaddr_in成员分析</h4><ul>
<li><code>sin_family</code>：每种协议适用的地址族均不同，IPv4使用4字节地址族，IPv6使用16字节地址族  </li>
<li><code>sin_port</code>：保存16位端口号，重点在于他以网络字节序保存，也有<code>in_port_t</code>定义为<code>uint16_t</code></li>
<li><code>sin_addr</code>：保存32位IP地址，也以网络字节序保存，<code>in_addr_t</code>实际上是<code>uint32_t</code></li>
<li><code>sin_zero</code>：8字节占位符，为了与结下来要讲的<code>sockaddr</code>结构体保持一致而用</li>
</ul>
<p>之前我们调用bind是这样的</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*) &amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">    error_handling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>第二个参数强转成了<code>sockaddr</code>指针，实际上bind的第二个参数也确实是那样，但我们传参使用的是<code>sockaddr_in</code>结构体地址，为什么不直接用<code>sockaddr</code>结构体呢？看一下就会明白了  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">sa_family_t</span>    sin_family;         <span class="comment">// 地址族（Adress Family）</span></span><br><span class="line">    cahr           sa_data[<span class="number">14</span>];        <span class="comment">// 地址信息</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>对比一下<code>sockaddr_in</code>和<code>sockaddr</code>结构体不难发现为什么前者要用8个字节的占位符，而后者则是将端口号和IP地址都保存在了<code>sa_data</code>里面，这样我们填写地址信息就比较麻烦，至于为什么用<code>sockaddr</code>而不直接用<code>sockaddr_in</code>，因为我们的协议族不止有IPv4，它要能够作为一个通用的结构体存储地址信息  </p>
<h3 id="3-3-网络字节序与地址变换"><a href="#3-3-网络字节序与地址变换" class="headerlink" title="3.3 网络字节序与地址变换"></a>3.3 网络字节序与地址变换</h3><p>这涉及到CPU大小端的问题：<a href="https://blog.csdn.net/spaceyqy/article/details/39227455?spm=1001.2101.3001.6650.1&utm_medium=distribute.pc_relevant.none-task-blog-2~default~CTRLIST~Rate-1-39227455-blog-118912066.235%5Ev35%5Epc_relevant_increate_t0_download_v2_base&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~CTRLIST~Rate-1-39227455-blog-118912066.235%5Ev35%5Epc_relevant_increate_t0_download_v2_base&utm_relevant_index=2">CPU中的大端与小端</a>  </p>
<p>简单来说就是  </p>
<ul>
<li>Little endian：将低位字节存储在低位地址</li>
<li>Big endian：将高位字节存储在低位地址</li>
</ul>
<p>这篇文章里提到的一个判别CPU是大端还是小端的方法我感觉很好  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">isBigEndian</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">union</span>&#123;</span></span><br><span class="line">        <span class="type">long</span> i;</span><br><span class="line">        <span class="type">char</span> c[<span class="keyword">sizeof</span>(<span class="type">long</span>)];</span><br><span class="line">    &#125;un;</span><br><span class="line">    un.i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> un.c[<span class="keyword">sizeof</span>(<span class="type">long</span>)<span class="number">-1</span>]==<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其实用union的特性就能判断了  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">union</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="type">short</span>  x;</span><br><span class="line">    <span class="type">char</span>   a[<span class="number">2</span>];</span><br><span class="line">&#125; U;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    U.a[<span class="number">0</span>]=<span class="number">0x01</span>;</span><br><span class="line">    U.a[<span class="number">1</span>]=<span class="number">0x02</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%x\n&quot;</span>, U.x);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// X86 OUTPUT: 0201, it&#x27;s Little Endian</span></span><br></pre></td></tr></table></figure>

<h4 id="字节序（Order）与网络字节库"><a href="#字节序（Order）与网络字节库" class="headerlink" title="字节序（Order）与网络字节库"></a>字节序（Order）与网络字节库</h4><p>CPU向内存中保存数据的方式有2种（大小端），那么CPU解析数据的方式也有两种  </p>
<p>由此可见不同主机保存数据的方式可能所不同，这样在进行网络数据传输的时候就牵扯到字节序的问题  </p>
<p>我们<strong>约定通过网络传输数据的的时候统一为大端序</strong>，意即先把数据数组转化成大端序格式再进行网络传输  </p>
<h4 id="字节序转换（Endian-Conversion）"><a href="#字节序转换（Endian-Conversion）" class="headerlink" title="字节序转换（Endian Conversion）"></a>字节序转换（Endian Conversion）</h4><p>帮助转换字节序的函数  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">short</span> <span class="title function_">htons</span><span class="params">(<span class="type">unsigned</span> <span class="type">short</span>)</span>;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span> <span class="title function_">ntohs</span><span class="params">(<span class="type">unsigned</span> <span class="type">short</span>)</span>;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span> <span class="title function_">htonl</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span>)</span>;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">short</span> <span class="title function_">ntohl</span><span class="params">(<span class="type">unsigned</span> <span class="type">long</span>)</span>;</span><br></pre></td></tr></table></figure>

<p><code>h</code>表示host，代表主机字节序；<code>n</code>表示net代表网络字节序；<code>s</code>和<code>l</code>表示short和long，代表需要转化的是什么类型数据  </p>
<p>除了向<code>socket_in</code>结构体填充数据外，其余情况无需转换字节序，收发数据的字节序转换是自动的  </p>
<h3 id="3-4-网络地址的初始化与分配"><a href="#3-4-网络地址的初始化与分配" class="headerlink" title="3.4 网络地址的初始化与分配"></a>3.4 网络地址的初始化与分配</h3><h4 id="将字符串信息转化为网络字节序的整数型"><a href="#将字符串信息转化为网络字节序的整数型" class="headerlink" title="将字符串信息转化为网络字节序的整数型"></a>将字符串信息转化为网络字节序的整数型</h4><p><code>inet_addr</code>将点分十进制（Dotted Decimal Notation）的字符串形式IP地址转换成32位整型数据，它在转换的同时进行网络字节序的转换<br><strong>注意</strong>：目前已知<code>inet_addr</code>在 windows 中被弃用，使用<code>inet_pton</code>，其用处和使用方式和下面说到的<code>inet_aton</code>一样  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">in_addr_t</span> <span class="title function_">inet_addr</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* <span class="built_in">string</span>)</span>; <span class="comment">// S：32位大端序整数类型值  F：INADDR_NONE</span></span><br></pre></td></tr></table></figure>

<p><code>inet_aton</code>的使用频率更高，它将字符串形式IP转换为网络字节序整型IP地址，保存地址使用了<code>in_addr</code>结构体  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// string  点分十进制形式字符串IP地址</span></span><br><span class="line"><span class="comment">// addr    将转换结果保存到这里</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">inet_aton</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* <span class="built_in">string</span>, <span class="keyword">struct</span> in_addr* addr)</span>;  <span class="comment">// S：1(true)  F：0(false)</span></span><br></pre></td></tr></table></figure>

<p><code>inet_ntoa</code>与上面的作用正好相反，将网络字节序整型IP地址转换为字符串形式IP地址  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span>* <span class="title function_">inet_ntoa</span><span class="params">(<span class="keyword">struct</span> in_addr adr)</span>;  <span class="comment">// S：字符串地址 F：-1</span></span><br></pre></td></tr></table></figure>

<p>注意此函数不要求我们分配空间，所以我们拿到字符串地址之后应该<code>strcpy</code>到自己能掌握的空间，因为<code>inet_ntoa</code>再一次调用可能就会覆盖之前的空间  </p>
<h4 id="网络地址初始化"><a href="#网络地址初始化" class="headerlink" title="网络地址初始化"></a>网络地址初始化</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line"><span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">serv_addr.sin_family = AF_INET;</span><br><span class="line"><span class="comment">//sin_addr.s_addr = inet_addr(argv[1]);</span></span><br><span class="line">sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br></pre></td></tr></table></figure>

<p>服务器端和客户端的连接需求不同，使用的函数也不同  </p>
<p>服务器：请把进入IP 211.217.168.13、9190端口的数据给我，使用<code>bind</code>函数<br>客户端：请连接到IP 211.217.168.13、9190端口，使用<code>connect</code>函数  </p>
<h4 id="INADDR-ANY"><a href="#INADDR-ANY" class="headerlink" title="INADDR_ANY"></a>INADDR_ANY</h4><p>上面的代码中我们用到了  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sin_addr.s_addr = htonl(INADDR_ANY);</span><br></pre></td></tr></table></figure>

<p>一般在服务端会这样写，表示监听本机所有可用的网络 IP 地址，如果存在多个IP地址，只要端口号正确便可以正确的收发数据；在客户端一般不会这样写  </p>
<p>并不是一张 NIC 就只有一个 IP，有时你可能只想用一个确定的 IP 通信，就要手动分配，如果只有一个 IP 或者只要能通信就行，直接使用<code>INADDR_ANY</code>即可  </p>
<h4 id="向套接字分配网络地址"><a href="#向套接字分配网络地址" class="headerlink" title="向套接字分配网络地址"></a>向套接字分配网络地址</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sockfd  套接字文件描述符</span></span><br><span class="line"><span class="comment">// myaddr  存有地址信息的结构体变量</span></span><br><span class="line"><span class="comment">// addrlen 第二个结构体变量长度</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">bind</span><span class="params">(<span class="type">int</span> sockfd, <span class="keyword">struct</span> sockaddr* myaddr, <span class="type">socklen_t</span> addrlen)</span>;  <span class="comment">// S：0 F：-1</span></span><br></pre></td></tr></table></figure>

<h4 id="Windows和Linux使用的函数"><a href="#Windows和Linux使用的函数" class="headerlink" title="Windows和Linux使用的函数"></a>Windows和Linux使用的函数</h4><p>上述函数均为Linux下的函数，但是windows也可使用，除了<code>inet_addr</code>，它在windows中已被弃用，取而代之的是<code>inet_pton</code>（实际上它就是新函数，以后尽可能用它）  </p>
<p>除此之外，windows下还有两个专用函数<code>WSAAddressToString</code>和<code>WSAStringToAddress</code>，它们和<code>inet_ntoa</code>以及<code>inet_addr</code>功能一样，只是了解一下，用它们的话会降低程序的可移植性和兼容性，说这话的意思就是能不用就不用  </p>
<h2 id="Chapter4-基于TCP的服务器端-客户端（1）"><a href="#Chapter4-基于TCP的服务器端-客户端（1）" class="headerlink" title="Chapter4 基于TCP的服务器端&#x2F;客户端（1）"></a>Chapter4 基于TCP的服务器端&#x2F;客户端（1）</h2><h3 id="4-1-理解TCP和UDP"><a href="#4-1-理解TCP和UDP" class="headerlink" title="4.1 理解TCP和UDP"></a>4.1 理解TCP和UDP</h3><p>TCP是Transmission Control Protocol，传输控制协议，意为对数据传输过程的控制  </p>
<p>这篇文章简单讲述了OSI七层网络模型：<a href="https://www.zhihu.com/tardis/zm/art/296670054?source_id=1005">OSI七层网络模型</a>  </p>
<p>不过我们只探讨四层（对编程来讲足矣）  </p>
<p><img src="/../images/post/tcp-1_tcpipVesusOsi.png" alt="TCPIPvsOSI">  </p>
<h4 id="TCP-IP协议栈"><a href="#TCP-IP协议栈" class="headerlink" title="TCP&#x2F;IP协议栈"></a>TCP&#x2F;IP协议栈</h4><p>数据传输会根据我们选择的协议而走TCP层或UDP层  </p>
<p><img src="/../images/post/tcp-2_TCP-IP_Protocol_Stack.png" alt="TCP&#x2F;IP协议栈">  </p>
<h4 id="链路层"><a href="#链路层" class="headerlink" title="链路层"></a>链路层</h4><p>计算机间通信通过设备（路由器、交换机等）进行物理连接的标准  </p>
<h4 id="TCP-UDP层"><a href="#TCP-UDP层" class="headerlink" title="TCP&#x2F;UDP层"></a>TCP&#x2F;UDP层</h4><p>IP层解决的是数据传输过程中的路径选择问题，但仅靠IP层传输数据不可靠（无序，丢失等等），因此在其上加了一个传输层（TCP层+UDP层），现在只简单讲解TCP，UDP后面再说，TCP协议规定了<strong>确认机制</strong>和<strong>超时重传</strong>，即A给B发送数据，B在收到一个数据包之后就要给A反馈已收到，要是A没有收到确认就会重传该数据包（这里讲的太简单了，实际上内容不少的）  </p>
<h4 id="应用层"><a href="#应用层" class="headerlink" title="应用层"></a>应用层</h4><p>到这里就是我们熟悉的套接字编程了，上面讲的那些都已经向开发人员隐藏起来了，我们进行套接字编程无需关注那些细枝末节的问题  </p>
<h3 id="4-2-实现基于TCP的服务器端-客户端"><a href="#4-2-实现基于TCP的服务器端-客户端" class="headerlink" title="4.2 实现基于TCP的服务器端&#x2F;客户端"></a>4.2 实现基于TCP的服务器端&#x2F;客户端</h3><h4 id="TCP服务器端默认的函数调用顺序"><a href="#TCP服务器端默认的函数调用顺序" class="headerlink" title="TCP服务器端默认的函数调用顺序"></a>TCP服务器端默认的函数调用顺序</h4><p><img src="/../images/post/tcp-3_TCP_server_func_order.png" alt="函数调用顺序">  </p>
<h4 id="进入等待连接请求状态"><a href="#进入等待连接请求状态" class="headerlink" title="进入等待连接请求状态"></a>进入等待连接请求状态</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sock     将进入监听状态的套接字</span></span><br><span class="line"><span class="comment">// backlog  连接请求队列（Queue）的长度，若为5，队列长度为5，表示最多使5个连接请求进入队列</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">listen</span><span class="params">(<span class="type">int</span> sock, <span class="type">int</span> backlog)</span>;  <span class="comment">// S：0  F：-1</span></span><br></pre></td></tr></table></figure>

<p>sock像是门卫，接待客人（连接请求），如果有正在受理的事物就让他们在等候室等着，backlog是等候室的大小  </p>
<p>必须服务端<code>listen</code>之后客户端<code>connect</code>才会产生数据交互  </p>
<h4 id="受理客户端请求"><a href="#受理客户端请求" class="headerlink" title="受理客户端请求"></a>受理客户端请求</h4><p><code>listen</code>之后就要准备按序受理请求了，受理请求也是利用套接字进行的，但<strong>不是服务器套接字</strong>，毕竟人家都是门卫了，所以我们要创建一个新的套接字受理请求，用<code>accept</code>便可创建  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sock     服务器端的套接字文件描述符</span></span><br><span class="line"><span class="comment">// addr     保存发起连接请求的客户端地址信息的变量地址</span></span><br><span class="line"><span class="comment">// addrlen  第二个参数addr结构体的长度</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">accept</span><span class="params">(<span class="type">int</span> sock, <span class="keyword">struct</span> sockaddr* addr, <span class="type">socklen_t</span>* addrlen)</span>;  <span class="comment">// S：返回创建的套接字文件描述符  F：-1</span></span><br></pre></td></tr></table></figure>

<p>调用<code>accept</code>函数受理连接请求时服务器端进入阻塞（blocking）状态，这时候来的连接请求都会被加入等待队列  </p>
<p>这样，连接请求相当于是插头，服务器套接字和accept生成的套接字共同组成一个完整的插座（插座的两个孔），连接请求插到这两部分socket组成的完整插座便可正常通信了（这样比喻也许不太合理，毕竟服务器socket充当的是“门卫”的角色）  </p>
<h4 id="TCP客户器端默认的函数调用顺序"><a href="#TCP客户器端默认的函数调用顺序" class="headerlink" title="TCP客户器端默认的函数调用顺序"></a>TCP客户器端默认的函数调用顺序</h4><p><img src="/../images/post/tcp-4_TCP_client_func_order.png" alt="客户端函数调用顺序">  </p>
<p>发送连接请求是通过<code>connect</code>函数完成的  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="comment">// sock      客户端套接字文件描述符</span></span><br><span class="line"><span class="comment">// servaddr  保存目标服务器端地址信息的变量地址值</span></span><br><span class="line"><span class="comment">// addrlen   第二个参数servaddr的长度</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">connect</span><span class="params">(<span class="type">int</span> sock, <span class="keyword">struct</span> sockaddr* servaddr, <span class="type">socklen_t</span> addrlen)</span>;  <span class="comment">// S：0  F：-1</span></span><br></pre></td></tr></table></figure>

<p>客户端调用<code>connect</code>之后，发生以下情况之一才会返回（完成函数调用）  </p>
<ul>
<li>服务器端接收连接请求  </li>
<li>发生断网等异常情况而中断连接请求</li>
</ul>
<p>服务器端“接收连接请求”并不是调用<code>accept</code>函数，而是<strong>指把客户端连接请求的信息加入等待队列</strong>；而调用<code>accept</code>函数是“受理连接请求”，要区别开“接收”和“受理”</p>
<p>我们创建客户端套接字的时候并没有手动分配IP和端口，但只要是网络数据交换，就必须分配IP和端口，那么客户端套接字是何时、何地、如何分配地址的呢？  </p>
<ul>
<li>何时？调用<code>connect</code>函数时</li>
<li>何地？操作系统内核</li>
<li>如何？IP用主机IP，端口随机</li>
</ul>
<p>客户端的IP和端口在调用<code>connect</code>函数时自动分配，端口随机  </p>
<h3 id="4-3-实现迭代服务器端-客户端"><a href="#4-3-实现迭代服务器端-客户端" class="headerlink" title="4.3 实现迭代服务器端&#x2F;客户端"></a>4.3 实现迭代服务器端&#x2F;客户端</h3><p>接下来要实现回声（echo）服务器端&#x2F;客户端，服务器端将客户端传送的字符串数据原封不动地传回客户端，在此之前先要了解一下迭代服务器  </p>
<h4 id="实现迭代服务器端"><a href="#实现迭代服务器端" class="headerlink" title="实现迭代服务器端"></a>实现迭代服务器端</h4><p>之前我们写的小程序在完成一次数据传输之后，服务端和客户端就终止了，但是现实情况是服务端不该终止，要能源源不断处理客户端的连接请求，而客户端在运行期间也不该是发送一次请求就终止的，其实我们加个循环就能解决这个问题了  </p>
<p><img src="/../images/post/tcp-5_recursive_server.png" alt="迭代服务器端">  </p>
<p>调用上图的close(client)就意味着结束了对某个客户端的服务，但这个程序在某一时刻只能针对单一个客户端提供服务，以后学完进程和线程之后就能做到同时服务多个客户端了  </p>
<h4 id="迭代回声服务器端-客户端"><a href="#迭代回声服务器端-客户端" class="headerlink" title="迭代回声服务器端&#x2F;客户端"></a>迭代回声服务器端&#x2F;客户端</h4><ul>
<li>服务器端在同一时刻只与一个客户端连接，并提供回声服务</li>
<li>服务器端依次向五个客户端提供服务并退出（指五次connect，并不是一次连接请求收发五条信息）</li>
<li>客户端接收用户收到的字符串并发送到服务器端</li>
<li>服务器端将接收的字符串数据传回客户端</li>
<li>服务器端与客户端之间的数据传输一直到用户输入<code>Q</code>为止</li>
</ul>
<p>服务端  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// server_echo.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 1024</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> serv_sock, clnt_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len, i;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>, <span class="title">clnt_addr</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> clnt_addr_size;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (serv_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family      = PF_INET;</span><br><span class="line">    serv_addr.sin_port        = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line">    serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (listen(serv_sock, <span class="number">5</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    clnt_addr_size = <span class="keyword">sizeof</span>(clnt_addr);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">        clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, &amp;clnt_addr_size);</span><br><span class="line">        <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">            error_handling(<span class="string">&quot;accept() error!&quot;</span>);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Connected client %d\n&quot;</span>, i+<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>( (str_len = read(clnt_sock, message, BUF_SIZE)) != <span class="number">0</span> )</span><br><span class="line">            write(clnt_sock, message, str_len);</span><br><span class="line"></span><br><span class="line">        close(clnt_sock);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(serv_sock);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>客户端</p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// client_echo.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span>      <span class="comment">// write  read  声明的地方</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 1024</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> clnt_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    clnt_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = PF_INET;</span><br><span class="line">    serv_addr.sin_port   = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line">    <span class="keyword">if</span> (inet_aton(argv[<span class="number">1</span>], &amp;serv_addr.sin_addr) == <span class="number">0</span>)  <span class="comment">// 会自动转换字节序</span></span><br><span class="line">        error_handling(<span class="string">&quot;inet_aton() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 建立连接</span></span><br><span class="line">    <span class="keyword">if</span> (connect(clnt_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;Connected........&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">fputs</span>(<span class="string">&quot;Input message(Q to quit): &quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">        fgets(message, BUF_SIZE, <span class="built_in">stdin</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(message, <span class="string">&quot;q\n&quot;</span>) || !<span class="built_in">strcmp</span>(message, <span class="string">&quot;Q\n&quot;</span>))</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        write(clnt_sock, message, <span class="built_in">strlen</span>(message));      <span class="comment">// 通过clnt_sock发送数据到服务端</span></span><br><span class="line">        str_len = read(clnt_sock, message, BUF_SIZE<span class="number">-1</span>);  <span class="comment">// 通过clnt_sock从服务端接收数据</span></span><br><span class="line">        message[str_len] = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s&quot;</span>, message);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    close(clnt_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="echo客户端存在的问题"><a href="#echo客户端存在的问题" class="headerlink" title="echo客户端存在的问题"></a>echo客户端存在的问题</h4><p>上面的代码中 line 45~48  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">write(clnt_sock, message, <span class="built_in">strlen</span>(message));</span><br><span class="line">str_len = read(clnt_sock, message, BUF_SIZE<span class="number">-1</span>);</span><br><span class="line">message[str_len] = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s&quot;</span>, message);</span><br></pre></td></tr></table></figure>

<p>因为TCP是面向连接的传输方式，不存在数据边界（服务端和客户端调用read&#x2F;write无需一一对应），这样如果服务端那边想要传输的数据太大，可能会分多次发送，客户端这边可能就会在尚未接收到所有数据就调用read函数，接下来就完美实现这个客户端  </p>
<h2 id="Chapter5-基于TCP的服务器端-客户端（2）"><a href="#Chapter5-基于TCP的服务器端-客户端（2）" class="headerlink" title="Chapter5 基于TCP的服务器端&#x2F;客户端（2）"></a>Chapter5 基于TCP的服务器端&#x2F;客户端（2）</h2><h3 id="5-1-echo客户端的完美实现"><a href="#5-1-echo客户端的完美实现" class="headerlink" title="5.1 echo客户端的完美实现"></a>5.1 echo客户端的完美实现</h3><p>为什么说是客户端的问题呢？因为数据多大就需要分批次传输不是服务器端程序能决定的（大概），它已经做好了它该做的了，也就是回传信息，关键在于客户端想一口一头猪，想一次把信息全部接收，殊不知服务器那边可能会分批传输数据（TCP面向连接传输），所以问题出在客户端而不是服务器端  </p>
<p>而解决方法也很简单，在客户端接收服务器传回的数据之前计算好应该接收的字符串长度，长度不够就一直<code>read</code>，直到长度够了，说明数据完整了，这时就能继续接下来的事情了  </p>
<p>修改上面客户端的程序  </p>
<figure class="highlight c"><figcaption><span>&#123;.line-numbers&#125;</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// client_echo2.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span>      <span class="comment">// write  read  声明的地方</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 1024</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> clnt_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len, recv_total_len, recv_one_len;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    clnt_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = PF_INET;</span><br><span class="line">    serv_addr.sin_port   = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line">    <span class="keyword">if</span> (inet_aton(argv[<span class="number">1</span>], &amp;serv_addr.sin_addr) == <span class="number">0</span>)  <span class="comment">// 会自动转换字节序</span></span><br><span class="line">        error_handling(<span class="string">&quot;inet_aton() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 建立连接</span></span><br><span class="line">    <span class="keyword">if</span> (connect(clnt_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;Connected........&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">fputs</span>(<span class="string">&quot;Input message(Q to quit): &quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">        fgets(message, BUF_SIZE, <span class="built_in">stdin</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(message, <span class="string">&quot;q\n&quot;</span>) || !<span class="built_in">strcmp</span>(message, <span class="string">&quot;Q\n&quot;</span>))</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        str_len = write(clnt_sock, message, <span class="built_in">strlen</span>(message));      <span class="comment">// 通过clnt_sock发送数据到服务端</span></span><br><span class="line">        recv_total_len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (recv_total_len &lt; str_len) &#123;</span><br><span class="line">            recv_one_len = read(clnt_sock, message + recv_total_len, BUF_SIZE<span class="number">-1</span>);  <span class="comment">// 通过clnt_sock从服务端接收数据</span></span><br><span class="line">            <span class="keyword">if</span> (recv_one_len == <span class="number">-1</span>)</span><br><span class="line">                error_handling(<span class="string">&quot;read() error!&quot;</span>);</span><br><span class="line">            recv_total_len += recv_one_len;</span><br><span class="line">        &#125;</span><br><span class="line">        message[str_len] = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s&quot;</span>, message);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    close(clnt_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>line 46~52，解决了上面的问题  </p>
<h4 id="如果问题不在客户端，定义引用层协议"><a href="#如果问题不在客户端，定义引用层协议" class="headerlink" title="如果问题不在客户端，定义引用层协议"></a>如果问题不在客户端，定义引用层协议</h4><p>上面解决问题的前提是我们知道了接收数据的大小，这显然在现实中不大可能，所以，我们可以在应用层制定一些协议，即一些收发数据的规则，比如数据大小，发送格式等等  </p>
<p>这里收发数据用到的write&#x2F;read（Windows下是send&#x2F;recv）要注意缓冲区刷新，字节边界的问题，我暂时不想深究这些细节，先继续学习  </p>
<p>作者在代码里写了一个挺有意思的东西，用char数组存储int整数，因为输入是按照<code>%d</code>进行的，所以可存储的数据是4字节的，至于为什么这么做，毕竟Socket只能收发字节数组，具体我也不太清楚，要说原因也只能想到和字节序转换有关，也许这就是神圣的字节流吧  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SZ 4</span></span><br><span class="line"><span class="type">char</span>* arr[<span class="number">1024</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, (<span class="type">int</span>*)&amp;arr[i*SZ]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-2-Windows-echo客户端实现"><a href="#5-2-Windows-echo客户端实现" class="headerlink" title="5.2 Windows echo客户端实现"></a>5.2 Windows echo客户端实现</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// client_echo2_win.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    SOCKET clnt_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len, recv_total_len, recv_one_len;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    clnt_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == INVALID_SOCKET)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    serv_addr.sin_port = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line">    <span class="comment">// Windows 无 inet_aton，替换为 inet_addr（功能一致）</span></span><br><span class="line">    serv_addr.sin_addr.s_addr = inet_addr(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">if</span> (serv_addr.sin_addr.s_addr == INADDR_NONE)</span><br><span class="line">        error_handling(<span class="string">&quot;inet_addr() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 建立连接（函数名与 Linux 一致）</span></span><br><span class="line">    <span class="keyword">if</span> (connect(clnt_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == SOCKET_ERROR)</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;Connected........&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">fputs</span>(<span class="string">&quot;Input message(Q to quit): &quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">        fgets(message, BUF_SIZE, <span class="built_in">stdin</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(message, <span class="string">&quot;q\n&quot;</span>) || !<span class="built_in">strcmp</span>(message, <span class="string">&quot;Q\n&quot;</span>))</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        str_len = <span class="built_in">strlen</span>(message);</span><br><span class="line">        send(clnt_sock, message, str_len, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        recv_total_len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (recv_total_len &lt; str_len) &#123;</span><br><span class="line">            recv_one_len = recv(clnt_sock, message + recv_total_len, BUF_SIZE - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">if</span> (recv_one_len == SOCKET_ERROR)</span><br><span class="line">                error_handling(<span class="string">&quot;recv() error!&quot;</span>);</span><br><span class="line">            recv_total_len += recv_one_len;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        message[str_len] = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s&quot;</span>, message);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    closesocket(clnt_sock);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-3-TCP原理"><a href="#5-3-TCP原理" class="headerlink" title="5.3 TCP原理"></a>5.3 TCP原理</h3><h4 id="TCP套接字中的I-O缓冲"><a href="#TCP套接字中的I-O缓冲" class="headerlink" title="TCP套接字中的I&#x2F;O缓冲"></a>TCP套接字中的I&#x2F;O缓冲</h4><p>TCP套接字数据收发无边界，即服务端write和客户端read不需要一一对应，因为在调用它们时，数据会移至缓冲区（输出缓冲和输入缓冲）  </p>
<p>I&#x2F;O缓冲的特性如下  </p>
<ul>
<li>I&#x2F;O缓冲在每个TCP套接字中单独存在  </li>
<li>I&#x2F;O缓冲在创建套接字时自动生成  </li>
<li>即使关闭套接字也会继续输出套接字中的遗留数据</li>
<li>关闭套接字将丢失输入缓冲中的数据</li>
</ul>
<p>在TCP传输中不会发生超过输入缓冲大小的数据传输，因为TCP会控制数据流，TCP中有滑动窗口（Sliding Window）协议，它的工作原理大概是同发送端进行对话，告诉它接收端最多可以接收多少数据，那么服务端就知道自己该发送多少数据了  </p>
<h4 id="TCP内部工作原理"><a href="#TCP内部工作原理" class="headerlink" title="TCP内部工作原理"></a>TCP内部工作原理</h4><p>TCP通信分为三步  </p>
<ol>
<li>建立连接  </li>
<li>进行数据交互  </li>
<li>断开连接</li>
</ol>
<p>每一步都由不同的过程进行  </p>
<p><strong>建立连接</strong>是通过 <em><strong>三次握手（Three-way handshaking）</strong></em> 进行的。套接字是以全双工（Full-duplx）方式工作的，意思是可以双向传输数据  </p>
<p>(1) 建立连接时，首先A向B发送  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[SYN] SEQ: 1000, ACK: -</span><br></pre></td></tr></table></figure>

<p>SYN表示进行数据交互前的同步消息，其中SEQ（序列号）为1000，ACK（确认消息）为空  </p>
<p>SEQ为1000指：现在传输的数据包序列号为1000，确认无误后通知我发送1001号数据包  </p>
<p>(2) 然后B向A发送  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[SYN+ACK] SEQ: 2000, ACK: 1001</span><br></pre></td></tr></table></figure>

<p>SYN+ACK表示这既是一条用于建立连接的同步消息，又是一条确认消息  </p>
<p>SEQ为2000指：现在传输的数据包序列号为2000，确认无误后通知我发送2001号数据包  </p>
<p>ACK为1001指：刚才你传输的1000号数据包接收无误，请发送1001号数据包</p>
<p>收发数据前向向数据包分配序号，并向对方通报序号，以便丢包重传  </p>
<p>(3) 最后A向B发送  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[SYN] SEQ: 1001 ACK: 2001</span><br></pre></td></tr></table></figure>

<p>通过此条消息确认之后A和B便可建立连接，这个过程也成称为三次握手</p>
<p><strong>进行数据交互</strong>也像上面那样互相传输数据，只不过服务端只需考虑怎么样让发送端知道接收端收到的数据丢失了，并且发送端要从丢失的数据那里开始继续传输，解决这些问题只要修改一下ACK的计算公式就好了  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ACK = SEQ + 传递成功的字节数 + 1</span><br></pre></td></tr></table></figure>

<p>这样就能很方便地进行失败重传了，因为确认号会告诉发送端下一次从这个序号的数据开始传输  </p>
<p><strong>断开连接</strong>是通过 <em><strong>四次握手（Three-way handshaking）</strong></em> 完成的，在这过程中A请求断开连接（包含FIN），然后B会发送一条确认消息给A，A不会回应，B触发超时重传（这条重传消息中包含FIN）告诉A它也可以断开连接了，然后A发送确认消息确认断开连接，这个过程有四次数据传送  </p>
<p><img src="/../images/post/tcp-6_fourshanks.png" alt="四次握手">  </p>
<h2 id="Chapter6-基于UDP的服务器端-客户端"><a href="#Chapter6-基于UDP的服务器端-客户端" class="headerlink" title="Chapter6 基于UDP的服务器端&#x2F;客户端"></a>Chapter6 基于UDP的服务器端&#x2F;客户端</h2><p>IP 协议是无连接、不可靠的底层传输；TCP 基于 IP 封装了确认应答、丢包重传（保障可靠性）、流量控制（匹配收发速率）与拥塞控制（规避网络拥堵），适合文件传输等要求数据完整的场景。UDP 不提供重传与可靠保障，协议开销小、转发延迟低，因此直播、实时语音、游戏对战等优先低延迟、可容忍少量丢包的实时业务，通常采用 UDP  </p>
<p>TCP比UDP慢的原因在于：  </p>
<ul>
<li>收发数据前后进行的连接设置及清除过程  </li>
<li>收发数据过程中为保证可靠性而添加的流控制</li>
</ul>
<p>UDP中服务器端和客户端没有连接，两端均只需一个套接字传输数据，使用UDP进行数据传输<strong>需要每次都添加目标地址信息（bind函数）</strong>，这相当于每次写信都要填写地址，UDP套接字相当于邮箱，UDP数据传输就像写好信丢到邮箱，他就能去往信上的目标地址，虽然不一定能到达，但大部分情况下还是可靠的  </p>
<p>数据传输主要利用下面两个函数  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sock      用于传输数据的套接字fd</span></span><br><span class="line"><span class="comment">// buff      保存待传输数据缓冲的地址</span></span><br><span class="line"><span class="comment">// nbytes    buff的字节大小</span></span><br><span class="line"><span class="comment">// flags     可选参数，没有则为0</span></span><br><span class="line"><span class="comment">// to        目标地址</span></span><br><span class="line"><span class="comment">// addrlen   to的大小</span></span><br><span class="line"><span class="type">ssizt_t</span> <span class="title function_">sendto</span><span class="params">(<span class="type">int</span> socket, <span class="type">void</span>* buff, <span class="type">size_t</span> nbytes, <span class="type">int</span> flags, </span></span><br><span class="line"><span class="params">               <span class="keyword">struct</span> sockaddr* to, <span class="type">socklen_t</span> addrlen)</span>;  <span class="comment">// S: 成功传输的字节数  F: -1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sock      用于接收数据的套接字fd</span></span><br><span class="line"><span class="comment">// buff      保存接收数据缓冲的地址</span></span><br><span class="line"><span class="comment">// nbytes    buff的字节大小</span></span><br><span class="line"><span class="comment">// flags     可选参数，没有则为0</span></span><br><span class="line"><span class="comment">// from      目标地址</span></span><br><span class="line"><span class="comment">// addrlen   to的大小</span></span><br><span class="line"><span class="type">ssizt_t</span> <span class="title function_">recvfrom</span><span class="params">(<span class="type">int</span> socket, <span class="type">void</span>* buff, <span class="type">size_t</span> nbytes, <span class="type">int</span> flags, </span></span><br><span class="line"><span class="params">               <span class="keyword">struct</span> sockaddr* from, <span class="type">socklen_t</span> addrlen)</span>;  <span class="comment">// S: 成功接收的字节数  F: -1</span></span><br></pre></td></tr></table></figure>

<p>因为 <strong>UDP没有请求连接和受理连接（没有connect、listen和accept）</strong> 的过程，所以严格意义上来说没有办法区分服务器端和客户端，在这里只能将提供服务的称为服务器端  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// udp_server_echo.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 32</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> serv_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>, <span class="title">clnt_addr</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> clnt_addr_size;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (serv_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;UDP socket creation error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family      = PF_INET;</span><br><span class="line">    serv_addr.sin_port        = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line">    serv_addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        clnt_addr_size = <span class="keyword">sizeof</span>(clnt_addr);</span><br><span class="line">        str_len = recvfrom(serv_sock, message, BUF_SIZE, <span class="number">0</span>, </span><br><span class="line">                           (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, &amp;clnt_addr_size);</span><br><span class="line">        sendto(serv_sock, message, str_len, <span class="number">0</span>, </span><br><span class="line">               (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, clnt_addr_size);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    close(serv_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// udp_client_echo.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span>      <span class="comment">// write  read  声明的地方</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 32</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> clnt_sock;</span><br><span class="line">    <span class="type">char</span> message[BUF_SIZE];</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line">    <span class="type">socklen_t</span> addr_size;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>, <span class="title">from_addr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;ip&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    clnt_sock = socket(PF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = PF_INET;</span><br><span class="line">    serv_addr.sin_port   = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line">    <span class="keyword">if</span> (inet_aton(argv[<span class="number">1</span>], &amp;serv_addr.sin_addr) == <span class="number">0</span>)  <span class="comment">// 会自动转换字节序</span></span><br><span class="line">        error_handling(<span class="string">&quot;inet_aton() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">fputs</span>(<span class="string">&quot;Insert message(q to quit): &quot;</span>, <span class="built_in">stdout</span>);</span><br><span class="line">        fgets(message, <span class="keyword">sizeof</span>(message), <span class="built_in">stdin</span>);</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(message, <span class="string">&quot;q\n&quot;</span>) || !<span class="built_in">strcmp</span>(message, <span class="string">&quot;Q\n&quot;</span>)) <span class="keyword">break</span>;</span><br><span class="line">        sendto (clnt_sock, message, <span class="built_in">strlen</span>(message), <span class="number">0</span>,</span><br><span class="line">                (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">        addr_size = <span class="keyword">sizeof</span>(serv_addr);</span><br><span class="line">        str_len = recvfrom(clnt_sock, message, BUF_SIZE, <span class="number">0</span>,</span><br><span class="line">                           (<span class="keyword">struct</span> sockaddr*)&amp;from_addr, &amp;addr_size);</span><br><span class="line">        message[str_len] = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s\n&quot;</span>, message);</span><br><span class="line">    &#125;</span><br><span class="line">    close(clnt_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用<code>bind()</code>函数可以给套接字绑定地址信息，未绑定的话调用<code>sendto()</code>函数时会将本机ip分配给套接字，也分配一个随机端口号  </p>
<p>UDP传输是存在数据边界的，服务端和客户端的IO函数必须一一对应，数量一致  </p>
<p>UDP套接字调用sendto函数传输数据有三个阶段：  </p>
<ol>
<li>向UDP套接字注册目标IP和端口号  </li>
<li>传输数据</li>
<li>删除UDP套接字中注册的目标地址信息</li>
</ol>
<p>每次都变更目标地址，因此可以使用同一个UDP套接字向不同的目标传输数据，这种未注册目标地址的套接字称为<strong>未连接套接字</strong>，反之则是<strong>connected套接字</strong>，UDP默认属于未连接套接字，如果使用UDP套接字长时间与一套主机通信，那么应该将它变为connected套接字，不然每次传输数据都要进行上面三个过程，很浪费时间  </p>
<p>UDP套接字绑定默认目标（简化发送），只需调用connect向套接字注册目标 IP 和端口；UDP 永远不存在「创建连接」这个行为！</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sock = socket(PF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line"><span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">addr.sin_family = AF_INET;</span><br><span class="line">addr.sin_addr.s_addr = ...</span><br><span class="line">addr.sin_port = ...</span><br><span class="line">connect(sock, (<span class="keyword">struct</span> sockaddr*)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br></pre></td></tr></table></figure>

<p>针对UDP套接字调用<code>connect</code>函数并不意味着与另一个套接字建立连接，这只是向UDP套接字注册目标IP和端口信息  </p>
<p>但其实完全没必要这么做，我们填好address之后第一次sendto内核就自动帮我们绑定目标地址了  </p>
<p>之后就可以调用sendto&#x2F;recvfrom、write&#x2F;read收发数据（已经注册了目标IP和端口号）  </p>
<p>在Windows上UDP用的函数和Linux上的类似  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sendto</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="type">char</span>* buf, <span class="type">int</span> len, <span class="type">int</span> flags, </span></span><br><span class="line"><span class="params">           <span class="type">const</span> <span class="keyword">struct</span> sockaddr* to, <span class="type">int</span> tolen)</span>;</span><br><span class="line"><span class="comment">// S: 成功传输的字节数  F: SOCKET_ERROR</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">rrecvfrom</span><span class="params">(SOCKET s, <span class="type">const</span> <span class="type">char</span>* buf, <span class="type">int</span> len, <span class="type">int</span> flags, </span></span><br><span class="line"><span class="params">              <span class="type">const</span> <span class="keyword">struct</span> sockaddr* from, <span class="type">int</span> fromlen)</span>;</span><br><span class="line"><span class="comment">// S: 成功传输的字节数  F: SOCKET_ERROR</span></span><br></pre></td></tr></table></figure>

<h2 id="Chapter7-优雅地断开套接字连接"><a href="#Chapter7-优雅地断开套接字连接" class="headerlink" title="Chapter7 优雅地断开套接字连接"></a>Chapter7 优雅地断开套接字连接</h2><p>之前的代码里最后都是直接调用<code>close</code>或<code>closesocket</code>关闭套接字，这种是完全断开连接，无法进行数据IO，就连IO缓冲里面的数据也会销毁，无法接收或传输，这样可能会造成数据丢失，为了避免这种情况，我们可以使用<strong>半关闭（Half-close）</strong>，只关闭一部分数据交换中的流，让套接字只能接收数据或只能发送数据  </p>
<p>两个套接字建立连接相当于打通管道，数据流相当于水流，只能朝一个方向移动，建立连接后每台主机都拥有两个流，一个输入流，一个输出流，半关闭就是关闭这两个流其中的一个  </p>
<p>“流”是套接字建立连接形成的概念，所以只有TCP中存在流，UDP中没有  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// sock   需要断开的套接字fd</span></span><br><span class="line"><span class="comment">// howto  传递断开方式信息</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shutdown</span><span class="params">(<span class="type">int</span> sock, <span class="type">int</span> howto)</span>;  <span class="comment">// S: 0 F: -1</span></span><br></pre></td></tr></table></figure>

<p>howto的三个值：SHUT_RD、SHUT_WR和SHUT_RDWR，关闭输入流，输入缓冲中有数据也会被抹去，关闭输出流，输出缓冲中的数据会继续传递至目标主机  </p>
<p>Windows下与Linux下半关闭相似  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">shutdown</span><span class="params">(SOCKET sock, <span class="type">int</span> howto)</span>;</span><br></pre></td></tr></table></figure>

<p>howto的三个值: SD_RECEIVE、SD_SEND和SD_BOTH  </p>
<p>下面是一个传输文件的demo  </p>
<p>Linux服务端，将名称为hello.txt的文件和服务端放置在同一目录下再运行  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// linux_FileTransmitServer.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 64</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MULTI_CLIENT 1</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> filename[] = <span class="string">&quot;hello.txt&quot;</span>;</span><br><span class="line">    FILE* fp;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">char</span> message[<span class="number">32</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">long</span> recv_len = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> serv_sock, clnt_sock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_addr</span>, <span class="title">clnt_addr</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> clnt_addr_size = <span class="number">0</span>;</span><br><span class="line">    <span class="type">size_t</span> read_count = <span class="number">0</span>, file_size = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// check input</span></span><br><span class="line">    <span class="keyword">if</span> (argc &lt; <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;-------- Start ----\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// open file by &quot;rb&quot;</span></span><br><span class="line">    fp = fopen(filename, <span class="string">&quot;rb&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (!fp)</span><br><span class="line">        error_handling(<span class="string">&quot;fopen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// init sock, addr</span></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (serv_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family        = AF_INET;</span><br><span class="line">    serv_addr.sin_port          = htons(atoi(argv[<span class="number">1</span>]));  <span class="comment">// short</span></span><br><span class="line">    <span class="comment">//serv_addr.sin_addr.s_addr   = htonl(INADDR_ANY);</span></span><br><span class="line">    <span class="keyword">if</span> (inet_aton(<span class="string">&quot;127.0.0.1&quot;</span>, &amp;serv_addr.sin_addr) == <span class="number">0</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;inet_aton() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// host info</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;HIOST IP: %s\n&quot;</span>, inet_ntoa(serv_addr.sin_addr));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// bind addr, listen request, accept to get clnt_sock</span></span><br><span class="line">    <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (listen(serv_sock, <span class="number">5</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    clnt_addr_size = <span class="keyword">sizeof</span>(clnt_addr);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> MULTI_CLIENT</span></span><br><span class="line"><span class="type">char</span>* clnt_ip[<span class="number">3</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="type">uint8_t</span> ips = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> client_i = <span class="number">0</span>; client_i &lt; <span class="number">3</span>; client_i++) </span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_addr, &amp;clnt_addr_size);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;accept() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// print client info</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;IP %s connected.\n&quot;</span>, inet_ntoa(clnt_addr.sin_addr));</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> MULTI_CLIENT</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; ips; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> ( <span class="built_in">strcmp</span>(clnt_ip[i], inet_ntoa(clnt_addr.sin_addr)) == <span class="number">0</span> ) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;FORBIDDEN_SAME\n&quot;</span>);</span><br><span class="line">        write(clnt_sock, <span class="string">&quot;FORBIDDEN_SAME&quot;</span>, <span class="number">14</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">clnt_ip[client_i] = inet_ntoa(clnt_addr.sin_addr);</span><br><span class="line">ips++;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// transmission, calculate file size</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;len filename: %lu\n&quot;</span>, <span class="keyword">sizeof</span>(filename));</span><br><span class="line">    write (clnt_sock, (<span class="type">void</span>*)filename, <span class="keyword">sizeof</span>(filename));</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        read_count = fread(buf, <span class="number">1</span>, BUF_SIZE, fp);</span><br><span class="line">        file_size = file_size + read_count;</span><br><span class="line">        <span class="keyword">if</span> (read_count &lt; BUF_SIZE) &#123;</span><br><span class="line">            write(clnt_sock, buf, read_count);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        write(clnt_sock, buf, BUF_SIZE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;File data transmis done.\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// attempt to use dialogue to indicate that you want to transmit another message</span></span><br><span class="line">    recv_len = read(clnt_sock, message, <span class="keyword">sizeof</span>(message)<span class="number">-1</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;File info: %s\n&quot;</span>, message);</span><br><span class="line">    <span class="keyword">if</span> (recv_len == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;read() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(message, <span class="string">&quot;FILE_SIZE&quot;</span>)) &#123;</span><br><span class="line">        <span class="built_in">sprintf</span>(message, <span class="string">&quot;%ld&quot;</span>, file_size);</span><br><span class="line">        write(clnt_sock, message, <span class="keyword">sizeof</span>(file_size));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// half-close transmission stream, serv_sock still could receive data</span></span><br><span class="line">    shutdown(serv_sock, SHUT_WR);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// receive feedback message by clnt_sock</span></span><br><span class="line">    recv_len = read(clnt_sock, message, <span class="keyword">sizeof</span>(message)<span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">if</span> (recv_len == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;read() error!&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from client: %s\n&quot;</span>, message);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">if</span> MULTI_CLIENT</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="comment">// close file and socks completely</span></span><br><span class="line">    fclose(fp);</span><br><span class="line">    close(clnt_sock);</span><br><span class="line">    close(serv_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;!&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Windows客户端  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// windows_FileTransmitClient.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ws2tcpip.h&gt;</span>  <span class="comment">// inet_pton</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 64</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    SOCKET  clnt_sock;</span><br><span class="line">    SOCKADDR_IN serv_addr;</span><br><span class="line">    FILE* fp = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="type">char</span>  message[<span class="number">32</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="type">char</span>  buf[BUF_SIZE] = &#123; <span class="number">0</span> &#125;;</span><br><span class="line">    <span class="type">int</span> recv_len;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// check input</span></span><br><span class="line">    <span class="comment">//if (argc &lt; 3) &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;Usage: %s &lt;ip&gt; &lt;port&gt;&quot;, argv[0]);</span></span><br><span class="line">    <span class="comment">//&#125;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// init windows env, socket, serv_addr</span></span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;WSAStarup() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    clnt_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (clnt_sock == SOCKET_ERROR)</span><br><span class="line">        error_handling(<span class="string">&quot;socket() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_addr));</span><br><span class="line">    serv_addr.sin_family = AF_INET;</span><br><span class="line">    <span class="comment">//inet_pton(AF_INET, argv[1], &amp;serv_addr.sin_addr.S_un.S_addr);</span></span><br><span class="line">    inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;serv_addr.sin_addr.S_un.S_addr);</span><br><span class="line">    <span class="comment">//serv_addr.sin_port = htons(atoi(argv[2]));</span></span><br><span class="line">    serv_addr.sin_port = htons(<span class="number">9098</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// connect</span></span><br><span class="line">    <span class="keyword">if</span> ( connect(clnt_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_addr, <span class="keyword">sizeof</span>(serv_addr)) == SOCKET_ERROR )</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// receive file name and data, use the name to create file</span></span><br><span class="line">    recv_len = recv(clnt_sock, message, <span class="keyword">sizeof</span>(message) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (recv_len == SOCKET_ERROR)</span><br><span class="line">        error_handling(<span class="string">&quot;recv() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strcmp</span>(message, <span class="string">&quot;FORBIDDEN_SAME&quot;</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;又是你小子，滚蛋\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;File name: %s\n&quot;</span>, message);</span><br><span class="line"></span><br><span class="line">    <span class="type">errno_t</span> err = fopen_s(&amp;fp, message, <span class="string">&quot;wb&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (err != <span class="number">0</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;fopen_s() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        recv_len = recv(clnt_sock, buf, BUF_SIZE, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (recv_len &lt; BUF_SIZE) &#123;</span><br><span class="line">            fwrite(buf, <span class="number">1</span>, recv_len, fp);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        fwrite(buf, <span class="number">1</span>, BUF_SIZE, fp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Receive data done.\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// send &quot;FILE_SIZE&quot; to server for requesting file size</span></span><br><span class="line">    send(clnt_sock, <span class="string">&quot;FILE_SIZE&quot;</span>, <span class="number">9</span>, <span class="number">0</span>);</span><br><span class="line">    recv(clnt_sock, message, <span class="keyword">sizeof</span>(message) - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;File size: %s&quot;</span>, message);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// send terminator message</span></span><br><span class="line">    send(clnt_sock, <span class="string">&quot;Thank you!&quot;</span>, <span class="number">10</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// close file pointer and socket completely</span></span><br><span class="line">    fclose(fp);</span><br><span class="line">    closesocket(clnt_sock);</span><br><span class="line">    WSACleanup();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Chapter8-域名及网络系统"><a href="#Chapter8-域名及网络系统" class="headerlink" title="Chapter8 域名及网络系统"></a>Chapter8 域名及网络系统</h2><p>DNS（Domain Name System，域名系统）是IP地址和域名进行转换的系统，核心是DNS服务器。DNS的工作系统是一种层次化管理的分布式系统，本地转换未知域名时，局域网DNS服务器会逐级向上请求到根DNS服务器，然后由根DNS服务器逐级向下请求从而找到要转换的域名  </p>
<p>在网络编程中使用域名是很有必要的，因为IP地址可能会变更，甚至经常变更，但是域名几乎不会改变，可以说一个域名会跟随一个网站（相同的服务，是服务不是服务器）一生  </p>
<h3 id="利用域名获取IP地址"><a href="#利用域名获取IP地址" class="headerlink" title="利用域名获取IP地址"></a>利用域名获取IP地址</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> hostent* <span class="title function_">gethostbyname</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* hostname)</span>;  <span class="comment">// S: hostent结构体  F: NULL</span></span><br></pre></td></tr></table></figure>

<p>返回的结构体存储了域名和IP，所以只利用这个结构体我们就能获得域名和IP的信息  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">hostent</span>  </span></span><br><span class="line"><span class="class">&#123;</span>  </span><br><span class="line">    <span class="type">char</span> *h_name;         <span class="comment">// official name  </span></span><br><span class="line">    <span class="type">char</span> **h_aliases;     <span class="comment">// alias list  </span></span><br><span class="line">    <span class="type">int</span> h_addrtype;       <span class="comment">// host address type, IPV4-AF_INET  </span></span><br><span class="line">    <span class="type">int</span> h_length;         <span class="comment">// address length  </span></span><br><span class="line">    <span class="type">char</span> **h_addr_list;   <span class="comment">// address list  </span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>h_name</strong> - 官方域名（Official domain name），但并不一定有  </li>
<li><strong>h_aliases</strong> - 一个IP可以绑定多个域名，这里存储的是可以访问同一IP的所有域名  </li>
<li><strong>h_addrtype</strong> - 此函数支持IPv4和IPv6，这个值就指示使用的IP版本，是IPv4的话其值等于AF_INET  </li>
<li><strong>h_length</strong> - IP地址长度，单位为字节，IPv4值为4，IPv6值为16  </li>
<li><strong>h_addr_list</strong> - 此变量以整数形式保存域名对应的IP地址，有些网站用户数大，可能会将多个IP与一个域名绑定，利用多台服务器进行负载均衡</li>
</ul>
<p><img src="/../images/post/tcp-7_hostent.png" alt="hostent结构体">  </p>
<p>利用下面这段代码测试<code>gethostbyname</code>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// getip.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hostent</span>* <span class="title">host</span>;</span></span><br><span class="line">    <span class="type">size_t</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;domain&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    host = gethostbyname(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="keyword">if</span> (!host)</span><br><span class="line">        error_handling(<span class="string">&quot;gethostbyname() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Official name: %s\n&quot;</span>, host-&gt;h_name);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; host-&gt;h_aliases[i]; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Alias[%lu]: %s\n&quot;</span>, i, host-&gt;h_aliases[i]);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Address type: %s\n&quot;</span>, </span><br><span class="line">           host-&gt;h_addrtype == AF_INET ? <span class="string">&quot;AF_INET&quot;</span> : <span class="string">&quot;AF_INET6&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Address length: %d\n&quot;</span>, host-&gt;h_length);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; host-&gt;h_addr_list[i]; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;IP[%lu]: %s\n&quot;</span>, </span><br><span class="line">               i, inet_ntoa(*(<span class="keyword">struct</span> in_addr*)host-&gt;h_addr_list[i]));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;net IP: %s\n&quot;</span>, host-&gt;h_addr_list[<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="利用IP获取域名"><a href="#利用IP获取域名" class="headerlink" title="利用IP获取域名"></a>利用IP获取域名</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="keyword">struct</span> hostent* <span class="title function_">gethostbyaddr</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* addr, <span class="type">socklen_t</span> len, <span class="type">int</span> family)</span>;</span><br></pre></td></tr></table></figure>

<p>addr参数实际上不是<code>char*</code>类型，而是一个指向存放IPv4地址的某个in_addr结构的指针，以前没有<code>void*</code>这种东西，后来的标准里这种不定类型的指针都用<code>void*</code>代替了</p>
<p>下面的程序是正确的，使用127.0.0.1便可测试，但是使用外网IP不一定能成功，需要在<code>/etc/hosts</code>文件里写上ip和域名对应关系才能解析，毕竟应该没有人用一台域名服务器在学习  </p>
<p>详情请见：<a href="https://www.cnblogs.com/wunaozai/p/3753731.html">无脑仔的小明的博客–gethostbyaddr函数</a>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// getdomain.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hostent</span>* <span class="title">host</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    <span class="type">size_t</span> i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;addr&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    inet_aton(argv[<span class="number">1</span>], (<span class="keyword">struct</span> in_addr*)&amp;addr.sin_addr);</span><br><span class="line"></span><br><span class="line">    host = gethostbyaddr((<span class="type">void</span>*)&amp;addr.sin_addr, <span class="number">4</span>, AF_INET);</span><br><span class="line">    <span class="keyword">if</span> (!host)</span><br><span class="line">        error_handling(<span class="string">&quot;gethostbyaddr() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Official name: %s\n&quot;</span>, host-&gt;h_name);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; host-&gt;h_aliases[i]; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Alias[%lu]: %s\n&quot;</span>, i, host-&gt;h_aliases[i]);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Address type: %s\n&quot;</span>,</span><br><span class="line">           host-&gt;h_addrtype == AF_INET ? <span class="string">&quot;AF_INET&quot;</span> : <span class="string">&quot;AF_INET6&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Address length: %d\n&quot;</span>, host-&gt;h_length);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; host-&gt;h_addr_list[i]; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;IP[%lu]: %s\n&quot;</span>,</span><br><span class="line">               i, inet_ntoa(*(<span class="keyword">struct</span> in_addr*)host-&gt;h_addr_list[i]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Windows上也有类似的函数  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> hostent* <span class="title function_">gethostbyname</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* name)</span>;  <span class="comment">// S: address of hostent  F: NULL</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span>  <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> hostent* <span class="title function_">gethostbyaddr</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* addr, <span class="type">int</span> len, <span class="type">int</span> type)</span>;</span><br></pre></td></tr></table></figure>

<h2 id="Chapter9-套接字的多种可选项"><a href="#Chapter9-套接字的多种可选项" class="headerlink" title="Chapter9 套接字的多种可选项"></a>Chapter9 套接字的多种可选项</h2><p>套接字有多种特性，这些特性可以通过设置可选项更改（Linux和Windows几乎一样）<br>（注：实际可设置的可选项比下表多得多）  </p>
<p><img src="/../images/post/tcp-8_Options.png" alt="Options">  </p>
<ul>
<li>SOL_SOCKET：套接字相关通用设置  </li>
<li>IPPROTO_TCP：TCP协议相关  </li>
<li>IPPROTO_IP：IP协议相关</li>
</ul>
<h3 id="get-set-套接字特性"><a href="#get-set-套接字特性" class="headerlink" title="get&#x2F;set 套接字特性"></a>get&#x2F;set 套接字特性</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 查看套接字可选项</span></span><br><span class="line"><span class="comment">// sock     要查看的套接字</span></span><br><span class="line"><span class="comment">// level    可选项的协议层</span></span><br><span class="line"><span class="comment">// optname  可选项名</span></span><br><span class="line"><span class="comment">// optval   保存结果缓冲地址的变量</span></span><br><span class="line"><span class="comment">// optlen   optval的大小，调用此get方法之后它变为缓冲内保存的字节数，set不会这样</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getsockopt</span><span class="params">(<span class="type">int</span> sock, <span class="type">int</span> level, <span class="type">int</span> optname, <span class="type">void</span>* optval, <span class="type">socklen_t</span>* optlen)</span>;  <span class="comment">// S: 0  F: -1</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置套接字可选项</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setsockopt</span><span class="params">(<span class="type">int</span> sock, <span class="type">int</span> level, <span class="type">int</span> optname, <span class="type">const</span> <span class="type">void</span>* optval, <span class="type">socklen_t</span> optlen)</span>;  <span class="comment">//S: 0  F: -1</span></span><br></pre></td></tr></table></figure>

<p>套接字类型只能在创建时决定，之后不可更改<br>验证套接字类型：协议层为SOL_SOCKET，选项名为SO_TYPE（典型的只读可选项）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// getype.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> sock_tcp, sock_udp;</span><br><span class="line">    <span class="type">int</span> sock_type;</span><br><span class="line">    <span class="type">socklen_t</span> optlen;</span><br><span class="line">    <span class="type">int</span> state;</span><br><span class="line"></span><br><span class="line">    optlen = <span class="keyword">sizeof</span>(sock_type);</span><br><span class="line">    sock_tcp = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    sock_udp = socket(PF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;SOCK_STREAM: %d\n&quot;</span>, SOCK_STREAM);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;SOCK_DGRAM: %d\n&quot;</span>, SOCK_DGRAM);</span><br><span class="line"></span><br><span class="line">    state = getsockopt(sock_tcp, SOL_SOCKET, SO_TYPE, (<span class="type">void</span>*)&amp;sock_type, &amp;optlen);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt() error!&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Socket type of sock_tcp: %d \n&quot;</span>, sock_type);</span><br><span class="line"></span><br><span class="line">    state = getsockopt(sock_udp, SOL_SOCKET, SO_TYPE, (<span class="type">void</span>*)&amp;sock_type, &amp;optlen);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt() error!&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Socket type of sock_udp: %d \n&quot;</span>, sock_type);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">SOCK_STREAM: 1</span><br><span class="line">SOCK_DGRAM: 2</span><br><span class="line">Socket type of sock_tcp: 1</span><br><span class="line">Socket type of sock_udp: 2</span><br></pre></td></tr></table></figure>

<h3 id="控制I-O缓冲大小"><a href="#控制I-O缓冲大小" class="headerlink" title="控制I&#x2F;O缓冲大小"></a>控制I&#x2F;O缓冲大小</h3><p>第五章说过创建套接字的同时会生成I&#x2F;O缓冲，而这个缓冲区大小是可以设置的  </p>
<p>使用选项 <strong>SO_SNDBUF</strong> 和 <strong>SO_RCVBUF</strong> 设置  </p>
<ul>
<li>SO_SNDBUF：输出缓冲区大小设置&#x2F;读取</li>
<li>SO_RCVBUF：输入缓冲区大小设置&#x2F;读取</li>
</ul>
<p>读取一下前面写的那么些个程序中创建的默认tcp socket的缓冲区大小  </p>
<p>获取输入&#x2F;输出缓冲大小  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// get_buf.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> send_buf, recv_buf, state;</span><br><span class="line">    <span class="type">int</span> sock_tcp;</span><br><span class="line">    <span class="type">socklen_t</span> len;</span><br><span class="line"></span><br><span class="line">    sock_tcp = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    len = <span class="keyword">sizeof</span>(send_buf);</span><br><span class="line">    state = getsockopt(sock_tcp, SOL_SOCKET, SO_SNDBUF, (<span class="type">void</span>*)&amp;send_buf, &amp;len);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    len = <span class="keyword">sizeof</span>(recv_buf);</span><br><span class="line">    state = getsockopt(sock_tcp, SOL_SOCKET, SO_RCVBUF, (<span class="type">void</span>*)&amp;recv_buf, &amp;len);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Default input buffer  size: %d (B)\n&quot;</span>, recv_buf);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Default output buffer size: %d (B)\n&quot;</span>, send_buf);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Default input buffer  size: 1048576 (B)</span><br><span class="line">Default output buffer size: 524288 (B)</span><br></pre></td></tr></table></figure>

<p>设置输入&#x2F;输出缓冲大小  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set_buf.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>** argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> send_buf = <span class="number">1024</span>*<span class="number">3</span>, recv_buf = <span class="number">1024</span> * <span class="number">3</span>, state;</span><br><span class="line">    <span class="type">int</span> sock_tcp;</span><br><span class="line">    <span class="type">socklen_t</span> len;</span><br><span class="line"></span><br><span class="line">    sock_tcp = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    state = setsockopt(sock_tcp, SOL_SOCKET, SO_SNDBUF, (<span class="type">void</span>*)&amp;send_buf, <span class="keyword">sizeof</span>(send_buf));</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;setsockopt(snd) error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    state = setsockopt(sock_tcp, SOL_SOCKET, SO_RCVBUF, (<span class="type">void</span>*)&amp;recv_buf, <span class="keyword">sizeof</span>(recv_buf));</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;setsockopt(rcv) error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    send_buf = <span class="number">0</span>;</span><br><span class="line">    recv_buf = <span class="number">0</span>;</span><br><span class="line">    len = <span class="keyword">sizeof</span>(send_buf);</span><br><span class="line">    state = getsockopt(sock_tcp, SOL_SOCKET, SO_SNDBUF, (<span class="type">void</span>*)&amp;send_buf, &amp;len);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt(snd) error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    len = <span class="keyword">sizeof</span>(recv_buf);</span><br><span class="line">    state = getsockopt(sock_tcp, SOL_SOCKET, SO_RCVBUF, (<span class="type">void</span>*)&amp;recv_buf, &amp;len);</span><br><span class="line">    <span class="keyword">if</span> (state)</span><br><span class="line">        error_handling(<span class="string">&quot;getsockopt(rcv) error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Set input buffer  size: %d (B)\n&quot;</span>, recv_buf);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Set output buffer size: %d (B)\n&quot;</span>, send_buf);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* msg)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(msg, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行结果  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Set input buffer  size: 6144 (B)</span><br><span class="line">Set output buffer size: 6144 (B)</span><br></pre></td></tr></table></figure>

<h3 id="Time-Out状态和地址再分配（SO-REUSEADDR）"><a href="#Time-Out状态和地址再分配（SO-REUSEADDR）" class="headerlink" title="Time_Out状态和地址再分配（SO_REUSEADDR）"></a>Time_Out状态和地址再分配（SO_REUSEADDR）</h3><p>之前讲过服务端和客户端断开连接的操作（<a href="#tcp%E5%86%85%E9%83%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86">四次握手</a>），最后是客户端先向服务端发送一条FIN消息，经过四次握手之后断开连接，之后再重启服务器都很正常  </p>
<p>但是如果强制关闭服务器端，即让服务器端先向客户端发送FIN消息，然后很快用统一端口号重启服务器，那么会发生<code>bind() error!</code>，无法再次运行，这种情况下要等几分钟才能重新运行服务器端  </p>
<p>上面的区别仅在于谁先发送FIN消息，但是结果却很不同，究其原因先要了解<strong>Time-Out状态</strong><br>（假设下图A是Server，B是Client）  </p>
<p><img src="/../images/post/tcp-9_TimeWait.png" alt="TimeWait">  </p>
<p><strong>先断开连接（先发送FIN消息）的主机才会经过Time-Wait状态</strong>，这个状态就是为了确保最后的确认消息不会丢失而存在的，如果没有Time-Wait状态，最后A发送给B的确认消息丢失了，A不会重传ACK消息给B，而B会一直重传FIN消息给A  </p>
<p>而在Time-Wait的过程中，实际上socket还存在，端口号也正在使用，这也就是为什么服务端先断开连接会出问题的原因了，而之所以<strong>对客户端没有影响</strong>，是因为客户端使用的端口号都是动态分配的，服务端是固定的  </p>
<p>Time-Wait状态开始于先发送FIN消息的主机收到FIN消息时，而且每一次收到FIN消息都会重新进入Time-Wait状态（还是用AB主机举例），如果网络状态不理想，那么B一直重传FIN消息给A，A就会一直处于Time-Wait状态，这肯定不行，所以有时候可以使用<strong>地址再分配</strong>解决这个问题  </p>
<p>更改套接字可选项<strong>SO_REUSEADDR</strong>，可以<strong>将Time-Wait状态下的套接字端口号重新分配给新的套接字</strong>，其默认值为0（false）  </p>
<p>修改一下前面写过的echo服务端，在创建好socket之后加上下面这么几行，然后在服务端CTRL+C关闭服务端之后再重启试试，是能够做到立即重启服务端的  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> option = <span class="number">1</span>; <span class="comment">// true</span></span><br><span class="line"><span class="type">socklen_t</span> optlen;</span><br><span class="line"><span class="type">int</span> state;</span><br><span class="line">...</span><br><span class="line">serv_sock = socket(...);</span><br><span class="line">...</span><br><span class="line">optlen = <span class="keyword">sizeof</span>(option);</span><br><span class="line">state = setsockopt(serv_sock, SOL_SOCKET, SO_REUSEADDR，(<span class="type">void</span>*)&amp;option，optlen);</span><br></pre></td></tr></table></figure>

<h3 id="Nagle算法（TCP-NODELAY控制）"><a href="#Nagle算法（TCP-NODELAY控制）" class="headerlink" title="Nagle算法（TCP_NODELAY控制）"></a>Nagle算法（TCP_NODELAY控制）</h3><p>Nagle算法<strong>TCP连接专有，默认启用</strong>，核心目的是减少小数据包数量、节省带宽、避免网络拥塞  </p>
<p>规则：未收到前一数据的 ACK 确认时，会将新数据暂存输出缓冲，攒包再发。意思就是不着急发小数据，先存缓冲，等 ACK 或凑满包再发送，减少网络小碎片包  </p>
<p>现代开发用法  </p>
<ul>
<li>大文件传输：数据塞满缓冲才发（因为传入输出缓冲比网络传输快得多），开 &#x2F; 关 Nagle 数据包数量无明显差异，所以<strong>应该禁用Nagle</strong>，这样等待ACK的时间就节省下来了；</li>
<li>小数据场景：省带宽可开，<strong>要低延迟必须禁用</strong>；</li>
</ul>
<p>4202年了，现代人的网络带宽充足，实际开发几乎默认关闭  </p>
<p>在开发中需要TCP 可靠性 + UDP 低延迟（游戏、实时聊天、远程控制），直接关闭 Nagle即可，但这是针对对低延迟没那么高的要求的情况，要知道tcp自己建立连接，三次握手啥的一套机制还是有的，要是对低延迟要求很高，老老实实UDP  </p>
<p>举个栗子，需要可靠不丢包、又要即时响应的策略游戏，TCP 关 Nagle 完美适配；UDP 只适合帧率 &#x2F; 实时性压倒一切的快节奏游戏  </p>
<p><strong>禁用Nagle</strong>使用TCP协议选项<strong>TCP_NODELAY</strong>，将它改为 <strong>1（true）</strong> 即可  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> opt_val = <span class="number">1</span>;</span><br><span class="line">setsockopt(sock, IPPROTO_TCP, TCP_NODELAY, (<span class="type">void</span>*)&amp;optval, <span class="keyword">sizeof</span>(optval));</span><br></pre></td></tr></table></figure>

<p>查看  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">opt_len = <span class="keyword">sizeof</span>(opt_val);</span><br><span class="line">getsocket(sock, IPPROTO_TCP, TCP_NODELAY, (<span class="type">void</span>*)&amp;optval, &amp;opt_len);</span><br></pre></td></tr></table></figure>

<h3 id="Windows-下-getsockopt-setsockopt-详解"><a href="#Windows-下-getsockopt-setsockopt-详解" class="headerlink" title="Windows 下 getsockopt &#x2F; setsockopt 详解"></a>Windows 下 <code>getsockopt</code> &#x2F; <code>setsockopt</code> 详解</h3><p>函数使用与Linux下并无差异，Windows 下函数声明（标准格式）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ws2tcpip.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置套接字选项</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">setsockopt</span><span class="params">(SOCKET s, <span class="type">int</span> level, <span class="type">int</span> optname, <span class="type">const</span> <span class="type">char</span>* optval, <span class="type">int</span> optlen)</span>;  </span><br><span class="line"><span class="comment">// 成功返回 0，失败返回 SOCKET_ERROR(-1)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取套接字选项</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">getsockopt</span><span class="params">(SOCKET s, <span class="type">int</span> level, <span class="type">int</span> optname, <span class="type">char</span>* optval, <span class="type">int</span>* optlen)</span>;      </span><br><span class="line"><span class="comment">// 成功返回 0，失败返回 SOCKET_ERROR(-1)</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>SOCKET s</code></td>
<td>要操作的<strong>套接字句柄</strong>（服务端&#x2F;客户端socket都可）</td>
</tr>
<tr>
<td><code>int level</code></td>
<td>选项所属层级（核心两个）：1. <code>SOL_SOCKET</code>：套接字通用层（IP&#x2F;端口相关）；2. <code>IPPROTO_TCP</code>：TCP协议层（TCP算法相关）</td>
</tr>
<tr>
<td><code>int optname</code></td>
<td>具体选项名（本次用两个：<code>SO_REUSEADDR</code> &#x2F; <code>TCP_NODELAY</code>）</td>
</tr>
<tr>
<td><code>const char* optval</code></td>
<td>指向<strong>选项值</strong>的指针（传入要设置的参数）</td>
</tr>
<tr>
<td><code>int optlen</code></td>
<td>选项值的<strong>字节长度</strong>（<code>setsockopt</code> 为入参）</td>
</tr>
<tr>
<td><code>char* optval</code></td>
<td>接收选项值的缓冲区（<code>getsockopt</code> 为出参）</td>
</tr>
<tr>
<td><code>int* optlen</code></td>
<td>入参：缓冲区大小；出参：实际返回长度（仅<code>getsockopt</code>）</td>
</tr>
</tbody></table>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// set sendbuf/recvbuf</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">ShowSocketBufSize</span><span class="params">(SOCKET sock)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    SOCKET hSock;</span><br><span class="line">    <span class="type">int</span> sndBuf, rcvBuf, state;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(WSAStartup(MAKEWORD(<span class="number">2</span>,<span class="number">2</span>),&amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line">    hSock=socket(PF_INET,SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    </span><br><span class="line">    ShowSocketBufSize(hSock);</span><br><span class="line">    sndBuf = <span class="number">1024</span> * <span class="number">3</span>,rcvBuf = <span class="number">1024</span> * <span class="number">3</span>;</span><br><span class="line">    state=setsockopt(hSock, SOL_SOCKET, SO_SNDBUF, (<span class="type">char</span>*)&amp;sndBuf, <span class="keyword">sizeof</span>(sndBuf));</span><br><span class="line">    <span class="keyword">if</span>(state==SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;setsockopt() error!&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    state=setsockopt(hSock,SOL_SOCKET, SO_RCVBUF, (<span class="type">char</span>*)&amp;rcvBuf, <span class="keyword">sizeof</span>(rcvBuf));</span><br><span class="line">    <span class="keyword">if</span>(state==SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;setsockopt() error!&quot;</span>);</span><br><span class="line">    ShowSocketBufSize(hSock);</span><br><span class="line">    </span><br><span class="line">    closesocket(hSock);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ShowSocketBufSize</span><span class="params">(SOCKET sock)</span> &#123;</span><br><span class="line">    <span class="type">int</span> sndBuf, rcvBuf, state, len;</span><br><span class="line"></span><br><span class="line">    len = <span class="keyword">sizeof</span>(sndBuf);</span><br><span class="line">    state = getsockopt(sock, SOL_SOCKET, SO_SNDBUF,(<span class="type">char</span>*)&amp;sndBuf,&amp;len);</span><br><span class="line">    <span class="keyword">if</span>(state == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;getsockopt() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    len=<span class="keyword">sizeof</span>(rcvBuf);</span><br><span class="line">    state=getsockopt(sock,SOL_SOCKET, SO_RCVBUF,(<span class="type">char</span>*)&amp;rcvBuf, &amp;len);</span><br><span class="line">    <span class="keyword">if</span>(state==SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;getsockopt() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Input buffer size:%d \n&quot;</span>, rcvBuf);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Output buffer size: %d \n&quot;</span>, sndBuf);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span> *message)</span> &#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Chapter10-多进程服务器端"><a href="#Chapter10-多进程服务器端" class="headerlink" title="Chapter10 多进程服务器端"></a>Chapter10 多进程服务器端</h2><p>多进程服务器端，核心思路就是<strong>主进程专门负责监听客户端连接</strong>，每有一个新客户端连入，就创建一个子进程专门为这个客户端提供服务，父进程继续监听新连接。  </p>
<p>依靠操作系统对进程的独立调度，实现<strong>同时处理多个客户端</strong>的并发效果；重点要搞懂进程创建、僵尸进程的产生与避免、信号机制处理子进程退出，最终用这套模型写出能同时响应多个客户端的TCP服务器，还可以拆分读写逻辑让通信更高效。  </p>
<h3 id="进程的概念和应用"><a href="#进程的概念和应用" class="headerlink" title="进程的概念和应用"></a>进程的概念和应用</h3><p>进程是操作系统进行资源分配和调度的最小单位，通俗来说，就是<strong>正在运行的程序实例</strong>——一个程序（比如.exe文件、可执行脚本）被加载到内存中执行，就变成了一个进程，每个进程都有自己独立的内存空间、PID（进程ID），互不干扰，即使是同一个程序，多次运行也会产生多个独立进程。  </p>
<p>在Linux系统中，我们可以用<code>ps au</code>命令查看当前系统中所有正在运行的进程，这个命令能直观展示进程的关键信息：比如PID（进程唯一标识）、USER（进程所属用户）、%CPU（进程占用CPU比例）、%MEM（占用内存比例）、COMMAND（启动进程的命令），日常开发中，我们常用这个命令查看进程是否正常运行、定位占用资源过高的进程，也是排查多进程服务器问题的基础工具。  </p>
<p>重点掌握用<code>fork()</code>函数创建进程——这是多进程服务器的核心操作。<code>fork()</code>函数由父进程调用，调用后会创建一个与父进程<strong>几乎</strong>完全相同的子进程，子进程会复制父进程的内存空间、代码段、数据段等资源，唯一的区别是<code>fork()</code>的返回值：<strong>父进程中返回子进程的PID，子进程中返回0</strong>，我们可以通过这个返回值区分父子进程，从而让它们执行不同的逻辑（比如父进程继续监听客户端连接，子进程负责与单个客户端通信）。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fork.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> gval = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">  <span class="type">pid_t</span> pid;</span><br><span class="line">  <span class="type">int</span> lval = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">  gval++, lval += <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">  pid = fork();</span><br><span class="line">  <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// child process</span></span><br><span class="line">    gval += <span class="number">2</span>, lval += <span class="number">2</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// parent process</span></span><br><span class="line">    gval -= <span class="number">2</span>, lval -= <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;child proc: [%d, %d] \n&quot;</span>, gval, lval);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;parent proc: [%d, %d] \n&quot;</span>, gval, lval);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./fork</span></span><br><span class="line">parent proc: [9, 23] </span><br><span class="line">child proc: [13, 27] </span><br></pre></td></tr></table></figure>

<p>父进程和子进程内存完全独立。  </p>
<h3 id="进程和僵尸进程"><a href="#进程和僵尸进程" class="headerlink" title="进程和僵尸进程"></a>进程和僵尸进程</h3><p>子进程先于父进程终止时，如果<strong>父进程没有主动回收子进程的退出状态信息</strong>，子进程就会变成<strong>僵尸进程</strong>。僵尸进程不会释放占用的进程ID等系统资源，大量产生会耗尽系统资源，导致无法创建新进程。  </p>
<p>子进程终止后会释放内存、文件描述符等大部分资源，但会保留包含自身PID、退出状态等信息的进程结构体，等待父进程通过系统函数读取后才会彻底销毁；若<strong>父进程一直未执行回收操作</strong>，这个残留的子进程就会以僵尸状态存在。  </p>
<p>这种情况在三次元称之为，自己的孩子自己要负责，操作系统是个宽容不管事的大平台，会持久保存子进程状态但不会主动释放子进程。  </p>
<p>我们可以通过一段简单代码制造僵尸进程：父进程调用<code>fork()</code>创建子进程后，进入长时间休眠状态；子进程创建完成后直接退出，此时<strong>父进程未回收子进程资源，子进程就会变为僵尸进程</strong>，通过<code>ps au</code>命令可以查看到该进程状态为<code>Z</code>（僵尸状态）。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// zombie.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;子进程(%d) 已终止，等待父进程回收...\n&quot;</span>, getpid());</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;父进程(%d) 休眠中，未回收子进程资源\n&quot;</span>, getpid());</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;父进程中打印子进程pid：%d \n&quot;</span>, pid);</span><br><span class="line">    sleep(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>执行结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./zombie </span></span><br><span class="line">父进程(5011) 休眠中，未回收子进程资源</span><br><span class="line">父进程中打印子进程pid：5012 </span><br><span class="line">子进程(5012) 已终止，等待父进程回收...</span><br></pre></td></tr></table></figure>

<p>使用 <code>ps au | grep zombie</code> 查看僵尸进程：  </p>
<p><img src="/../images/post/tcp-10_zombie.png" alt="僵尸进程">  </p>
<p>10秒后父进程睡眠结束，回收子进程资源，程序自动结束。  </p>
<p>为了销毁僵尸进程、回收系统资源，父进程必须调用<code>wait()</code>或<code>waitpid()</code>函数。  </p>
<p>下面进行详细解释和示例演示。  </p>
<p><code>wait()</code> 函数的作用详解：<strong>阻塞父进程，等待任意一个子进程终止，同时回收子进程资源（销毁僵尸进程），并获取子进程的退出状态值</strong>。  </p>
<p>函数声明：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">wait</span><span class="params">(<span class="type">int</span> *status)</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>参数 <code>status</code>：指向整型变量的指针，用于<strong>存储子进程的退出状态信息</strong>  </li>
<li>返回值：成功返回<strong>终止的子进程PID</strong>，失败返回 <code>-1</code></li>
</ul>
<p>通过 <code>wait</code> 拿到的状态值 <code>status</code> 不能直接使用，需要用宏解析：  </p>
<ol>
<li><code>WIFEXITED(status)</code>  <ul>
<li>作用：判断子进程是否<strong>正常终止</strong>（<code>return</code> 退出 &#x2F; <code>exit</code> 退出）  </li>
<li>返回值：正常终止 → 非0；异常终止 → 0</li>
</ul>
</li>
<li><code>WEXITSTATUS(status)</code>  <ul>
<li>作用：获取子进程<strong>正常退出时的返回值</strong>（子进程 <code>return</code> 的数值）  </li>
<li>前提：必须先通过 <code>WIFEXITED</code> 确认子进程正常终止</li>
</ul>
</li>
</ol>
<p>wait.c 示例代码（回收僵尸进程 + 获取子进程返回值）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// wait.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> status;</span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 第一个子进程：return 3</span></span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;CHILD PID: %d \n&quot;</span>, pid);</span><br><span class="line">        pid = fork();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 第二个子进程：exit(7)</span></span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">7</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;CHILD PID: %d \n&quot;</span>, pid);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 第一次等待：回收任意子进程，获取状态</span></span><br><span class="line">            wait(&amp;status);</span><br><span class="line">            <span class="keyword">if</span>(WIFEXITED(status))</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;Child send one: %d \n&quot;</span>,WEXITSTATUS(status));</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 第二次等待：回收另一个子进程，获取状态</span></span><br><span class="line">            wait(&amp;status);</span><br><span class="line">            <span class="keyword">if</span>(WIFEXITED(status))</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;Child send two: %d \n&quot;</span>, WEXITSTATUS(status));</span><br><span class="line"></span><br><span class="line">            sleep(<span class="number">10</span>); <span class="comment">// 休眠10秒，父进程保持运行</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./wait</span></span><br><span class="line">CHILD PID: 9126 </span><br><span class="line">CHILD PID: 9127 </span><br><span class="line">Child send one: 3 </span><br><span class="line">Child send two: 7 </span><br></pre></td></tr></table></figure>

<p>当我们用<code>ps au | grep wait</code>查看进程信息时，并没有僵尸进程产生，子进程资源被完全回收，父进程成功获取子进程返回值。  </p>
<p>下面讲 <code>waitpid</code>。  </p>
<p>函数声明：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="type">pid_t</span> <span class="title function_">waitpid</span><span class="params">(<span class="type">pid_t</span> pid, <span class="type">int</span> *status, <span class="type">int</span> options)</span>;</span><br></pre></td></tr></table></figure>

<p>核心参数 &amp; 返回值：  </p>
<ol>
<li><strong>pid_t pid</strong><ul>
<li><code>-1</code>：等待<strong>任意子进程</strong>退出（和 <code>wait</code> 作用一致）</li>
<li>正数：等待指定PID的子进程退出</li>
</ul>
</li>
<li>*<em>int <em>status</em></em>：存储子进程的退出状态（和 <code>wait</code> 用法一致）</li>
<li><strong>int options</strong>：核心选项<ul>
<li><code>WNOHANG</code>：<strong>非阻塞模式</strong>（重点），没有子进程退出时，函数立即返回 <code>0</code>，不阻塞父进程</li>
</ul>
</li>
<li><strong>返回值</strong><ul>
<li>成功：返回退出的子进程PID</li>
<li>无子进程退出（仅<code>WNOHANG</code>）：返回 <code>0</code></li>
<li>失败：返回 <code>-1</code></li>
</ul>
</li>
</ol>
<p>关键宏（沿用之前的用法）：  </p>
<ul>
<li><code>WIFEXITED(status)</code>：判断子进程是否<strong>正常退出</strong></li>
<li><code>WEXITSTATUS(status)</code>：获取子进程的退出返回值</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// waitpid.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> status;</span><br><span class="line">    <span class="type">pid_t</span> pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 子进程：休眠15秒后退出，返回值24</span></span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        sleep(<span class="number">15</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">24</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 父进程：非阻塞轮询回收子进程</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// waitpid返回0时，循环继续；返回子进程PID时，循环结束</span></span><br><span class="line">        <span class="keyword">while</span>(!waitpid(<span class="number">-1</span>, &amp;status, WNOHANG))</span><br><span class="line">        &#123;</span><br><span class="line">            sleep(<span class="number">3</span>);</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;sleep 3sec.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 解析子进程退出状态</span></span><br><span class="line">        <span class="keyword">if</span>(WIFEXITED(status))</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Child send %d \n&quot;</span>, WEXITSTATUS(status));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./waitpid </span></span><br><span class="line"><span class="built_in">sleep</span> 3sec.</span><br><span class="line"><span class="built_in">sleep</span> 3sec.</span><br><span class="line"><span class="built_in">sleep</span> 3sec.</span><br><span class="line"><span class="built_in">sleep</span> 3sec.</span><br><span class="line"><span class="built_in">sleep</span> 3sec.</span><br><span class="line">Child send 24</span><br></pre></td></tr></table></figure>

<p>同样，并没有僵尸进程产生。  </p>
<h3 id="信号处理"><a href="#信号处理" class="headerlink" title="信号处理"></a>信号处理</h3><p>本节核心解决<strong>子进程终止时机未知、主动wait&#x2F;waitpid会阻塞&#x2F;浪费资源</strong>的问题：我们无法预知子进程何时终止，不能让父进程无休止等待，也不能循环轮询，而<strong>信号</strong>是操作系统的主动通知机制——事件发生时系统自动发信号，进程绑定处理函数响应，完美解决该问题。本节先提出核心问题，依次讲解<code>signal</code>（过时）、<code>alarm</code>函数，再讲解标准的<code>sigaction</code>函数与结构体，最终用信号实现<strong>自动销毁僵尸进程</strong>，是多进程服务器的核心技术。  </p>
<p>我们已经知道了进程创建及销毁方法，但还有一个问题没解决。“子进程究竟何时终止？调用waitpid函数后要无休止地等待吗？” 答案就是信号处理。  </p>
<p><strong>signal 函数（过时，仅了解）</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="type">void</span> (*signal(<span class="type">int</span> sig, <span class="type">void</span> (*handler)(<span class="type">int</span>)))(<span class="type">int</span>);</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>  </p>
<ul>
<li><code>int sig</code>：要注册的信号编号（如 <code>SIGALRM</code>：已到通过调用alarm函数注册的时间，<code>SIGCHLD</code>：子进程终止，<code>SIGINT</code>：输入Ctrl + C）</li>
<li><code>void (*handler)(int)</code>：信号处理函数指针，收到信号时自动执行该函数</li>
</ul>
<p><strong>返回值</strong>  </p>
<ul>
<li>成功：返回之前的信号处理函数指针</li>
<li>失败：返回 <code>SIG_ERR</code></li>
</ul>
<p><strong>缺陷</strong>：不同UNIX&#x2F;Linux系统实现不一致，兼容性差、不稳定，已被弃用。  </p>
<p><strong>alarm 函数（定时发送信号）</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> <span class="title function_">alarm</span><span class="params">(<span class="type">unsigned</span> <span class="type">int</span> seconds)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>  </p>
<ul>
<li><code>unsigned int seconds</code>：秒数，到达时间后系统向进程发送 <code>SIGALRM</code> 信号</li>
</ul>
<p><strong>返回值</strong>  </p>
<ul>
<li>之前设置的闹钟剩余秒数，无闹钟则返回0</li>
</ul>
<p><strong>作用</strong>：定时触发信号，配合信号处理函数使用。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// signal.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// SIGALRM: 超时</span></span><br><span class="line"><span class="comment">// SIGINT: 用户中断</span></span><br><span class="line"><span class="comment">// SIGCHLD: 子进程终止</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">timeout</span><span class="params">(<span class="type">int</span> sig)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (sig = SIGALRM)</span><br><span class="line">    <span class="built_in">puts</span>(<span class="string">&quot;Time out!&quot;</span>);</span><br><span class="line">  alarm(<span class="number">2</span>);  <span class="comment">// 预约2秒后产生SIGALRM信号</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">keycontrol</span><span class="params">(<span class="type">int</span> sig)</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (sig == SIGINT) </span><br><span class="line">    <span class="built_in">puts</span>(<span class="string">&quot;CTRL+C pressed!&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> &#123;</span><br><span class="line">  <span class="type">int</span> i;</span><br><span class="line">  <span class="comment">// 注册信号</span></span><br><span class="line">  signal(SIGALRM, timeout);</span><br><span class="line">  signal(SIGINT, keycontrol);</span><br><span class="line">  <span class="comment">// 预约2秒后产生SIGALRM信号，开始信号</span></span><br><span class="line">  alarm(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">3</span>; ++i) &#123;</span><br><span class="line">    <span class="built_in">puts</span>(<span class="string">&quot;wait...&quot;</span>);</span><br><span class="line">    sleep(<span class="number">100</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 不干预执行，会输出：</span></span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"></span><br><span class="line"><span class="comment"># 干预执行（用 ctrl + c 终止）：</span></span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">^CCTRL+C pressed!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br></pre></td></tr></table></figure>

<p><strong>struct sigaction 结构体（信号配置核心）</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="comment">// 结构体声明</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> &#123;</span></span><br><span class="line">    <span class="type">void</span>     (*sa_handler)(<span class="type">int</span>);   <span class="comment">// 信号处理函数</span></span><br><span class="line">    <span class="type">sigset_t</span> sa_mask;              <span class="comment">// 信号屏蔽集</span></span><br><span class="line">    <span class="type">int</span>      sa_flags;             <span class="comment">// 附加标志</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>成员说明</strong>  </p>
<ul>
<li><code>sa_handler</code>：指定信号处理函数，同signal的handler参数</li>
<li><code>sa_mask</code>：处理信号时需要屏蔽的信号集合，用<code>sigemptyset</code>初始化为空集</li>
<li><code>sa_flags</code>：附加特性，默认填0即可满足常规使用</li>
</ul>
<p><strong>sigaction 函数（现代标准信号注册函数）</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="comment">// 函数声明</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sigaction</span><span class="params">(<span class="type">int</span> sig, <span class="type">const</span> <span class="keyword">struct</span> sigaction *act, <span class="keyword">struct</span> sigaction *oldact)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>  </p>
<ul>
<li><code>int sig</code>：与signal函数相同，传递信号信息</li>
<li><code>const struct sigaction *act</code>：对应于第一个参数的信号处理函数信息（目前只需要了解这里保存信号处理函数的函数指针即可）</li>
<li><code>struct sigaction *oldact</code>：保存旧的信号配置（之前注册的），不需要则传0</li>
</ul>
<p><strong>返回值</strong>  </p>
<ul>
<li>成功返回0，失败返回-1</li>
</ul>
<p><strong>优势</strong>：所有Linux系统行为完全一致，稳定可靠，替代signal函数。  </p>
<p><strong>sigaction + SIGALRM 示例（修正可运行）</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 信号处理函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">timeout</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span>(sig == SIGALRM)</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;Time out!&quot;</span>);</span><br><span class="line">    alarm(<span class="number">2</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc,<span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">act</span>;</span></span><br><span class="line">    act.sa_handler = timeout;</span><br><span class="line">    sigemptyset(&amp;act.sa_mask);</span><br><span class="line">    act.sa_flags = <span class="number">0</span>;</span><br><span class="line">    sigaction(SIGALRM, &amp;act, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    alarm(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;<span class="number">3</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;wait...&quot;</span>);</span><br><span class="line">        sleep(<span class="number">100</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./sigaction </span></span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Time out!</span><br></pre></td></tr></table></figure>

<p><strong>信号处理消灭僵尸进程示例</strong>  </p>
<p>子进程终止时，系统自动向父进程发送<code>SIGCHLD</code>信号，注册该信号的处理函数，在函数中调用<code>waitpid</code>自动回收子进程，彻底杜绝僵尸进程。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// remove_zombie.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// SIGCHLD信号处理函数：自动回收子进程</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">read_childproc</span><span class="params">(<span class="type">int</span> sig)</span>&#123;</span><br><span class="line">    <span class="type">int</span> status;</span><br><span class="line">    <span class="type">pid_t</span> id = waitpid(<span class="number">-1</span>, &amp;status, WNOHANG);</span><br><span class="line">    <span class="keyword">if</span>(WIFEXITED(status))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Removed proc id: %d\n&quot;</span>, id);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Child send: %d\n&quot;</span>, WEXITSTATUS(status));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">act</span>;</span></span><br><span class="line">    act.sa_handler = read_childproc;</span><br><span class="line">    sigemptyset(&amp;act.sa_mask);</span><br><span class="line">    act.sa_flags = <span class="number">0</span>;</span><br><span class="line">    sigaction(SIGCHLD, &amp;act, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建第一个子进程</span></span><br><span class="line">    pid = fork();</span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">puts</span>(<span class="string">&quot;Hi! I&#x27;m child process&quot;</span>);</span><br><span class="line">        sleep(<span class="number">10</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">12</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Child proc id: %d\n&quot;</span>, pid);</span><br><span class="line">        <span class="comment">// 创建第二个子进程</span></span><br><span class="line">        pid = fork();</span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;Hi! I&#x27;m child process&quot;</span>);</span><br><span class="line">            sleep(<span class="number">10</span>);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">24</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> i;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Child proc id: %d\n&quot;</span>, pid);</span><br><span class="line">            <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">puts</span>(<span class="string">&quot;wait...&quot;</span>);</span><br><span class="line">                sleep(<span class="number">5</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./remove_zombie     </span></span><br><span class="line">Child proc <span class="built_in">id</span>: 30503</span><br><span class="line">Child proc <span class="built_in">id</span>: 30504</span><br><span class="line">Hi! I<span class="string">&#x27;m child process</span></span><br><span class="line"><span class="string">wait...</span></span><br><span class="line"><span class="string">Hi! I&#x27;</span>m child process</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line">Removed proc <span class="built_in">id</span>: 30503</span><br><span class="line">Child send: 12</span><br><span class="line">Removed proc <span class="built_in">id</span>: 30504</span><br><span class="line">Child send: 24</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line"><span class="built_in">wait</span>...</span><br><span class="line"><span class="built_in">wait</span>...</span><br></pre></td></tr></table></figure>

<h3 id="基于多任务的并发服务器"><a href="#基于多任务的并发服务器" class="headerlink" title="基于多任务的并发服务器"></a>基于多任务的并发服务器</h3><p>我们之前学的单进程服务器同一时间只能服务一个客户端，而<strong>多任务并发服务器</strong>就是解决这个问题：由<strong>父进程专门负责监听客户端连接</strong>，每有一个新客户端连接，就调用 <code>fork()</code> 创建一个子进程，让子进程单独和这个客户端通信，父进程则回到监听状态，等待下一个客户端。这样就能实现<strong>多人同时连接、同时通信</strong>，配合之前学的 <code>SIGCHLD</code> 信号自动回收子进程，彻底避免僵尸进程，这是Linux下经典的并发服务器模型。  </p>
<p>多任务并发回声服务器示例（客户端我们使用<a href="#51-echo%E5%AE%A2%E6%88%B7%E7%AB%AF%E7%9A%84%E5%AE%8C%E7%BE%8E%E5%AE%9E%E7%8E%B0">5.1 echo客户端的完美实现</a>）：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo_mpserv.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">read_childproc</span><span class="params">(<span class="type">int</span> sig)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> serv_sock, clnt_sock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_adr</span>, <span class="title">clnt_adr</span>;</span></span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">act</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> adr_sz;</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(argc!=<span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage : %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 注册SIGCHLD信号处理函数，自动回收子进程</span></span><br><span class="line">    act.sa_handler = read_childproc;</span><br><span class="line">    sigemptyset(&amp;act.sa_mask);</span><br><span class="line">    act.sa_flags = <span class="number">0</span>;</span><br><span class="line">    sigaction(SIGCHLD, &amp;act, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建监听套接字</span></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_adr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_adr));</span><br><span class="line">    serv_adr.sin_family = AF_INET;</span><br><span class="line">    serv_adr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    serv_adr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 绑定+监听</span></span><br><span class="line">    <span class="keyword">if</span>(bind(serv_sock, (<span class="keyword">struct</span> sockaddr*) &amp;serv_adr, <span class="keyword">sizeof</span>(serv_adr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span>(listen(serv_sock, <span class="number">5</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 无限循环监听客户端连接</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        adr_sz = <span class="keyword">sizeof</span>(clnt_adr);</span><br><span class="line">        <span class="comment">// 接收客户端连接</span></span><br><span class="line">        clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_adr, &amp;adr_sz);</span><br><span class="line">        <span class="keyword">if</span>(clnt_sock == <span class="number">-1</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;new client connected...&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建子进程</span></span><br><span class="line">        pid = fork();</span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">-1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            close(clnt_sock);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 子进程：负责和客户端通信</span></span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            close(serv_sock);  <span class="comment">// 子进程关闭监听套接字文件描述符</span></span><br><span class="line">            <span class="comment">// 回声服务：读取客户端数据并回传</span></span><br><span class="line">            <span class="keyword">while</span>((str_len = read(clnt_sock, buf, BUF_SIZE)) != <span class="number">0</span>)</span><br><span class="line">                write(clnt_sock, buf, str_len);</span><br><span class="line"></span><br><span class="line">            close(clnt_sock);</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;client disconnected...&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 父进程：关闭客户端套接字文件描述符，继续监听</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            close(clnt_sock);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    close(serv_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子进程退出信号处理函数：回收僵尸进程</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">read_childproc</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="type">int</span> status;</span><br><span class="line">    pid = waitpid(<span class="number">-1</span>, &amp;status, WNOHANG);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Removed proc id: %d \n&quot;</span>, pid);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误处理函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 终端中运行服务器 ./echo_mpserv 9527</span></span><br><span class="line"><span class="comment"># 多开几个终端模拟多客户端连接 ./client_echo2 127.0.0.1 9527</span></span><br><span class="line">Connected........</span><br><span class="line">Input message(Q to quit): hello</span><br><span class="line">Message from server: hello</span><br><span class="line">Input message(Q to quit): 人类的本质是</span><br><span class="line">Message from server: 人类的本质是</span><br><span class="line">Input message(Q to quit): 复读鸡你太美babyoh~</span><br><span class="line">Message from server: 复读鸡你太美babyoh~</span><br><span class="line">Input message(Q to quit): Q</span><br><span class="line"></span><br><span class="line"><span class="comment"># 客户端建立连接和断开连接，服务器端显示</span></span><br><span class="line">new client connected...</span><br><span class="line">new client connected...</span><br><span class="line">new client connected...</span><br><span class="line">new client connected...</span><br><span class="line">client disconnected...</span><br><span class="line">Removed proc <span class="built_in">id</span>: 11184 </span><br><span class="line">client disconnected...</span><br><span class="line">Removed proc <span class="built_in">id</span>: 11405 </span><br><span class="line">client disconnected...</span><br><span class="line">Removed proc <span class="built_in">id</span>: 11132 </span><br><span class="line">client disconnected...</span><br><span class="line">Removed proc <span class="built_in">id</span>: 11074 </span><br><span class="line">^C</span><br></pre></td></tr></table></figure>

<p><strong>文件描述符 和 套接字 的关系</strong>（注意第3、4点）：  </p>
<ol>
<li><strong>套接字（Socket）</strong> 是<strong>操作系统内核</strong>创建的<strong>网络通信资源</strong>，属于内核对象，存在于系统内核中，进程<strong>不能直接操作</strong>套接字本身。</li>
<li><strong>文件描述符</strong> 是<strong>进程</strong>分配的一个整数编号，本质是<strong>进程对内核套接字的引用&#x2F;句柄</strong>，进程通过文件描述符来间接使用内核中的套接字。</li>
<li>调用 <code>fork()</code> 创建子进程时，<strong>绝对不会复制内核套接字</strong>，内核中始终只有一个套接字对象；子进程只会复制父进程的<strong>文件描述符表</strong>，同时内核会给这个套接字的<strong>引用计数 +1</strong>。</li>
<li>代码中父子进程都要关闭多余文件描述符的原因：子进程不需要监听客户端，所以关闭 <code>serv_sock</code>（监听套接字的文件描述符）；父进程不需要和客户端通信，所以关闭 <code>clnt_sock</code>（客户端套接字的文件描述符），这是为了保证引用计数正确。</li>
</ol>
<p>最后是最核心的规则：<br><strong>内核中的套接字，只有当引用它的所有文件描述符都被关闭（引用计数变为0）时，操作系统才会真正销毁这个套接字，释放系统资源</strong>。<br>只要还有一个进程持有该套接字的文件描述符没有关闭，套接字就会一直存在，不会被销毁。  </p>
<h3 id="分割TCP的I-O程序"><a href="#分割TCP的I-O程序" class="headerlink" title="分割TCP的I&#x2F;O程序"></a>分割TCP的I&#x2F;O程序</h3><p>分割TCP的I&#x2F;O程序，核心就是<strong>将数据的读取（Read）和写入（Write）拆分为两个独立的执行流程</strong>，本节课我们通过父子进程实现分离：父进程只负责接收服务端数据，子进程只负责向服务端发送数据。书中特意说明，<strong>回声客户端本身完全不需要I&#x2F;O分割</strong>，这样做反而会增加编程复杂度，本节的核心目的只是<strong>演示I&#x2F;O分离的思想</strong>，以及解决多进程场景下的关键问题——<strong>如何正确发送EOF断开连接</strong>，这也是<code>shutdown</code>函数的核心用法。  </p>
<p>I&#x2F;O分割的通用优势（复杂业务场景适用）：读写操作互不干扰，避免单线程中<code>read</code>阻塞导致无法发送数据，或<code>write</code>阻塞导致无法接收数据，让数据收发更流畅，是高性能网络程序的常用设计。  </p>
<p>TCP I&#x2F;O分割客户端代码：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo_mpclient.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">read_routine</span><span class="params">(<span class="type">int</span> sock, <span class="type">char</span> *buf)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">write_routine</span><span class="params">(<span class="type">int</span> sock, <span class="type">char</span> *buf)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> sock;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_adr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(argc!=<span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage: %s &lt;IP&gt; &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_adr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_adr));</span><br><span class="line">    serv_adr.sin_family = AF_INET;</span><br><span class="line">    serv_adr.sin_addr.s_addr = inet_addr(argv[<span class="number">1</span>]);</span><br><span class="line">    serv_adr.sin_port = htons(atoi(argv[<span class="number">2</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(connect(sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_adr, <span class="keyword">sizeof</span>(serv_adr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;connect() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fork创建子进程，分割I/O</span></span><br><span class="line">    pid = fork();</span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">        write_routine(sock, buf);  <span class="comment">// 子进程：只负责写入（发送数据）</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        read_routine(sock, buf);   <span class="comment">// 父进程：只负责读取（接收数据）</span></span><br><span class="line"></span><br><span class="line">    close(sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 读流程：接收服务端回传的数据</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">read_routine</span><span class="params">(<span class="type">int</span> sock, <span class="type">char</span> *buf)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> str_len = read(sock, buf, BUF_SIZE);</span><br><span class="line">        <span class="keyword">if</span>(str_len == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        buf[str_len] = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Message from server: %s&quot;</span>, buf);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 写流程：向服务端发送数据，输入q/Q退出</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">write_routine</span><span class="params">(<span class="type">int</span> sock, <span class="type">char</span> *buf)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        fgets(buf, BUF_SIZE, <span class="built_in">stdin</span>);</span><br><span class="line">        <span class="comment">// 输入q/Q，关闭写流并退出</span></span><br><span class="line">        <span class="keyword">if</span>(!<span class="built_in">strcmp</span>(buf, <span class="string">&quot;q\n&quot;</span>) || !<span class="built_in">strcmp</span>(buf, <span class="string">&quot;Q\n&quot;</span>))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 核心：半关闭写流，发送EOF</span></span><br><span class="line">            shutdown(sock, SHUT_WR);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        write(sock, buf, <span class="built_in">strlen</span>(buf));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误处理</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./echo_mpclient 127.0.0.1 9527</span></span><br><span class="line">hello</span><br><span class="line">Message from server: hello</span><br><span class="line">hi</span><br><span class="line">Message from server: hi</span><br><span class="line">q</span><br></pre></td></tr></table></figure>

<p>上面的代码中我们用到了 <code>shutdown</code>，关闭了写流，对于这种半关闭的场景，其实用在复杂服务器中比较常见，比如我们上传文件，写流是打开的，发完文件后写流立即关闭，但是读流还在（父进程），我们依然可以收到服务器给我们返回的数据，他可能会统计一下我们发的文件信息给我们。这里的回声客户端输入q之后，立刻就退出了，逻辑处理太过简单验证不到这个场景。，而且这里客户端输入q，子进程退出父进程立马就退，也没必要处理僵尸子进程，基本不会产生。  </p>
<p>下面讲讲 <strong><code>shutdown</code>函数的用法、作用，以及为什么必须用它，而不能用<code>close</code></strong>。  </p>
<p>先给出<code>shutdown</code>函数的标准声明和参数解析：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shutdown</span><span class="params">(<span class="type">int</span> sock, <span class="type">int</span> howto)</span>;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>参数<code>sock</code></strong>：要关闭的套接字文件描述符</li>
<li><strong>参数<code>howto</code></strong>：关闭方式，有三个可选值<ol>
<li><code>SHUT_RD</code>：关闭<strong>读流</strong>，无法再接收数据</li>
<li><code>SHUT_WR</code>：关闭<strong>写流</strong>，无法再发送数据（本节使用）</li>
<li><code>SHUT_RDWR</code>：同时关闭读&#x2F;写流，完全断开连接</li>
</ol>
</li>
<li><strong>返回值</strong>：成功返回0，失败返回-1</li>
</ul>
<p><code>shutdown</code>的核心特性：<strong>它是「流关闭」，不是「文件描述符关闭」，无论套接字的引用计数是多少，都会立即关闭指定的数据流，并向对方  发送<code>EOF</code></strong>。  </p>
<p>现在结合代码和<code>fork</code>的文件描述符复制机制，讲清楚为什么不能用<code>close</code>：  </p>
<ol>
<li>调用<code>fork</code>创建子进程后，<strong>父进程和子进程会各持有一份套接字的文件描述符</strong>，内核中这个套接字的<strong>引用计数会变成2</strong>；</li>
<li>如果我们在子进程退出时调用<code>close(sock)</code>：只会关闭子进程的文件描述符，引用计数减为1，<strong>父进程的文件描述符依然存在</strong>，套接字不会被销毁，<strong>不会向服务端发送EOF</strong>，服务端的<code>read</code>会一直阻塞等待数据，无法断开连接；</li>
<li>而调用<code>shutdown(sock, SHUT_WR)</code>：直接关闭套接字的<strong>写数据流</strong>，无视引用计数，<strong>立刻向服务端发送EOF</strong>，服务端收到EOF后，<code>read</code>函数会返回0，就能正常识别客户端断开连接。</li>
</ol>
<p>简单总结：  </p>
<ul>
<li><code>close</code>：关闭<strong>文件描述符</strong>，依赖引用计数，计数为0才会销毁套接字、发EOF；</li>
<li><code>shutdown</code>：关闭<strong>数据流</strong>，立即生效，强制发送EOF，是TCP<strong>半关闭</strong>的标准用法。</li>
</ul>
<p>本节课的代码只是演示I&#x2F;O分割和<code>shutdown</code>的用法，回声客户端本身不需要这么复杂，但掌握这种<strong>读写分离+半关闭</strong>的思想，是编写高性能、稳定网络程序的关键基础。  </p>
<h2 id="Chapter11-进程间通信"><a href="#Chapter11-进程间通信" class="headerlink" title="Chapter11 进程间通信"></a>Chapter11 进程间通信</h2><p>每个进程在操作系统中都是资源隔离的独立个体，拥有各自专属内存空间，默认<strong>完全无法直接互相访问数据、传递信息</strong>。进程间通信（IPC），就是操作系统提供的、专门用于<strong>多个进程之间交换数据、完成协作交互</strong>的技术方案。  </p>
<p>本章主要讲解Linux下基础的进程间通信原理与实现方式，单纯看和基础多进程回声服务端开发联系不大，但它是复杂服务端架构的重要铺垫：后续想要实现多进程任务分发、子进程数据汇总、进程协同工作等进阶需求，都离不开IPC；掌握进程间通信，才能开发结构更多样、功能更复杂的各类服务端程序。  </p>
<h3 id="进程间通信的基本概念"><a href="#进程间通信的基本概念" class="headerlink" title="进程间通信的基本概念"></a>进程间通信的基本概念</h3><p>进程拥有完全独立的内存空间，即便通过<code>fork</code>创建的父子进程，内存也是完全隔离的，无法直接交换数据。想要实现进程间通信，核心就是借助<strong>操作系统提供的、多个进程都能访问的公共内存空间</strong>，最基础的实现方式就是<strong>管道（Pipe）</strong>，通过<code>pipe</code>函数创建内核级管道，完成父子进程的数据传输。  </p>
<p>pipe 函数详解:  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">pipe</span><span class="params">(<span class="type">int</span> fds[<span class="number">2</span>])</span>;</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>  </p>
<ul>
<li><code>int fds[2]</code>：传入整型数组，函数执行后会返回两个文件描述符<ul>
<li><code>fds[0]</code>：管道<strong>读端</strong>，仅用于从管道中读取数据</li>
<li><code>fds[1]</code>：管道<strong>写端</strong>，仅用于向管道中写入数据</li>
</ul>
</li>
</ul>
<p><strong>返回值</strong>  </p>
<ul>
<li>成功：返回 <code>0</code></li>
<li>失败：返回 <code>-1</code></li>
</ul>
<p><strong>核心特性</strong>：管道是<strong>半双工通信</strong>，数据只能单向流动（写端→读端）。  </p>
<p>代码示例：单向管道通信（父&lt;-&gt;子 单方向传输）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pipe1.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> fds[<span class="number">2</span>];</span><br><span class="line">    <span class="type">char</span> str[] = <span class="string">&quot;Who are you?&quot;</span>;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    pipe(fds);       <span class="comment">// 创建管道</span></span><br><span class="line">    pid = fork();    <span class="comment">// 创建子进程</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 子进程：向管道写端写入数据</span></span><br><span class="line">        write(fds[<span class="number">1</span>], str, <span class="keyword">sizeof</span>(str));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 父进程：从管道读端读取数据</span></span><br><span class="line">        read(fds[<span class="number">0</span>], buf, BUF_SIZE);</span><br><span class="line">        <span class="built_in">puts</span>(buf);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./pipe1</span></span><br><span class="line">Who are you?</span><br></pre></td></tr></table></figure>

<p>管道默认是<strong>单向半双工</strong>，<strong>无法用单个管道实现稳定的双向通信</strong>。<br>原因：单个管道只有一个缓冲区，父子进程同时读写会造成<strong>数据混乱、读取错误</strong>，数据流无法分离。<br>想要实现<strong>稳定的双向通信</strong>，必须创建<strong>两个独立管道</strong>，分别负责「父发子收」和「子发父收」。</p>
<p>单管道双向通信（演示缺陷）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pipe2.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> fds[<span class="number">2</span>];</span><br><span class="line">    <span class="type">char</span> str1[] = <span class="string">&quot;Who are you?&quot;</span>;</span><br><span class="line">    <span class="type">char</span> str2[] = <span class="string">&quot;Thank you for your message&quot;</span>;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    pipe(fds);</span><br><span class="line">    pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 子进程：先写后读</span></span><br><span class="line">        write(fds[<span class="number">1</span>], str1, <span class="keyword">sizeof</span>(str1));</span><br><span class="line">        sleep(<span class="number">2</span>);</span><br><span class="line">        read(fds[<span class="number">0</span>], buf, BUF_SIZE);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Child proc output: %s\n&quot;</span>, buf);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 父进程：先读后写</span></span><br><span class="line">        read(fds[<span class="number">0</span>], buf, BUF_SIZE);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Parent proc output: %s\n&quot;</span>, buf);</span><br><span class="line">        write(fds[<span class="number">1</span>], str2, <span class="keyword">sizeof</span>(str2));</span><br><span class="line">        sleep(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码必须依靠<code>sleep</code>强制等待避免数据冲突，如果去除会造成子进程自己写自己读，父进程读不到数据无限等待，实际开发中必须用双管道分离数据流。  </p>
<p>双管道双向通信：双管道分别负责「子进程发→父进程收」和「父进程发→子进程收」，数据流完全分离，<strong>无需 sleep 等待、不会数据冲突</strong>。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc,<span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> fds1[<span class="number">2</span>], fds2[<span class="number">2</span>];</span><br><span class="line">    <span class="type">char</span> str1[]=<span class="string">&quot;Who are you?&quot;</span>;</span><br><span class="line">    <span class="type">char</span> str2[]=<span class="string">&quot;Thank you for your message&quot;</span>;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建两个独立管道</span></span><br><span class="line">    pipe(fds1);</span><br><span class="line">    pipe(fds2);</span><br><span class="line">    pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(pid==<span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 子进程：管道1 写数据 → 发给父进程</span></span><br><span class="line">        write(fds1[<span class="number">1</span>], str1, <span class="keyword">sizeof</span>(str1));</span><br><span class="line">        <span class="comment">// 子进程：管道2 读数据 ← 接收父进程回复</span></span><br><span class="line">        read(fds2[<span class="number">0</span>], buf, BUF_SIZE);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Child proc output: %s \n&quot;</span>,buf);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 父进程：管道1 读数据 ← 接收子进程消息</span></span><br><span class="line">        read(fds1[<span class="number">0</span>],buf, BUF_SIZE);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Parent proc output: %s \n&quot;</span>,buf);</span><br><span class="line">        <span class="comment">// 父进程：管道2 写数据 → 回复子进程</span></span><br><span class="line">        write(fds2[<span class="number">1</span>],str2,<span class="keyword">sizeof</span>(str2));</span><br><span class="line">        sleep(<span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Parent proc output: Who are you? </span><br><span class="line">Child proc output: Thank you <span class="keyword">for</span> your message</span><br></pre></td></tr></table></figure>

<h3 id="pipe底层原理（自己添加内容）"><a href="#pipe底层原理（自己添加内容）" class="headerlink" title="pipe底层原理（自己添加内容）"></a>pipe底层原理（自己添加内容）</h3><ol>
<li>进程用户空间完全独立（32 位：0~3G；64 位：128T），无法直接互通数据；  </li>
<li>所有进程共享内核空间（32 位：3~4G；64 位：128T）；  </li>
<li>pipe 的本质：操作系统在内核共享空间创建一块缓冲区，作为进程间数据交换的中转站；  </li>
<li>数据通信流程：用户内存 → 拷贝到内核缓冲区 → 拷贝到另一个进程用户内存。</li>
</ol>
<p>说白了就是把数据从一个独立空间复制到共享空间（内核缓冲区），明明读写都是操作同一个内核缓冲区，为啥非要搞两个文件描述符（读 &#x2F; 写）？用一个文件描述符既能读又能写，难道不行吗？  </p>
<p>这是多方设计考量，并非舍近求远。  </p>
<p>首先，pipe 的设计定位是单向数据流，分读写端避免双向混读混写，从底层防止数据错乱。  </p>
<p>其次，分离读写端是管道实现 EOF 结束标记的核心前提（写端close才是EOF）。只用单个 fd，内核无法区分行为，不能判断通信是否彻底结束。  </p>
<p>最后，管道是动态流动的临时缓冲区；普通文件是静态持久存储（普通文件用一个fd就能读写），二者模型完全不同，不能套用一套读写逻辑。  </p>
<h3 id="运用进程间通信"><a href="#运用进程间通信" class="headerlink" title="运用进程间通信"></a>运用进程间通信</h3><p>下面是基于<a href="#%E5%9F%BA%E4%BA%8E%E5%A4%9A%E4%BB%BB%E5%8A%A1%E7%9A%84%E5%B9%B6%E5%8F%91%E6%9C%8D%E5%8A%A1%E5%99%A8">基于多任务的并发服务器</a>修改的示例，客户端使用之前任一个都可以，比如<a href="#%E5%88%86%E5%89%B2tcp%E7%9A%84io%E7%A8%8B%E5%BA%8F">分割tcp的io程序</a>的客户端，服务端利用管道把读取到客户端的信息写到文件中，其中读取信息和写文件在两个子进程中，父进程依然只是监听客户端连接。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo_storeserv.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">read_childproc</span><span class="params">(<span class="type">int</span> sig)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> serv_sock, clnt_sock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_adr</span>, <span class="title">clnt_adr</span>;</span></span><br><span class="line">    <span class="type">int</span> fds[<span class="number">2</span>];</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sigaction</span> <span class="title">act</span>;</span></span><br><span class="line">    <span class="type">socklen_t</span> adr_sz;</span><br><span class="line">    <span class="type">int</span> str_len;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(argc!=<span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage : %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 注册SIGCHLD信号处理函数，自动回收子进程</span></span><br><span class="line">    act.sa_handler = read_childproc;</span><br><span class="line">    sigemptyset(&amp;act.sa_mask);</span><br><span class="line">    act.sa_flags = <span class="number">0</span>;</span><br><span class="line">    sigaction(SIGCHLD, &amp;act, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建监听套接字</span></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_adr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_adr));</span><br><span class="line">    serv_adr.sin_family = AF_INET;</span><br><span class="line">    serv_adr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    serv_adr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 绑定+监听</span></span><br><span class="line">    <span class="keyword">if</span>(bind(serv_sock, (<span class="keyword">struct</span> sockaddr*) &amp;serv_adr, <span class="keyword">sizeof</span>(serv_adr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span>(listen(serv_sock, <span class="number">2</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    pipe(fds);</span><br><span class="line">    pid = fork();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">        FILE* fp = fopen(<span class="string">&quot;echo_storage.txt&quot;</span>, <span class="string">&quot;wt&quot;</span>);</span><br><span class="line">        <span class="type">char</span> msgbuf[BUF_SIZE];</span><br><span class="line">        <span class="type">int</span> i, len;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (fp == <span class="literal">NULL</span>) error_handling(<span class="string">&quot;fopen error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 写入任意数目数据（需要实时写入）</span></span><br><span class="line">        <span class="comment">// while (1) &#123;</span></span><br><span class="line">        <span class="comment">//   len = read(fds[0], msgbuf, BUF_SIZE);</span></span><br><span class="line">        <span class="comment">//   if (len &lt;= 0) break;</span></span><br><span class="line">        <span class="comment">//   fwrite((void*)msgbuf, 1, len, fp);  // 只写内存，不写磁盘</span></span><br><span class="line">        <span class="comment">//   fflush(fp);  // 立即写入磁盘，不缓存</span></span><br><span class="line">        <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 缓存三条数据</span></span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; <span class="number">3</span>; ++i) &#123;</span><br><span class="line">            len = read(fds[<span class="number">0</span>], msgbuf, BUF_SIZE);</span><br><span class="line">            fwrite((<span class="type">void</span>*)msgbuf, <span class="number">1</span>, len, fp);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        fclose(fp);  <span class="comment">// 此处执行一次 fflush(fp)</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 无限循环监听客户端连接</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        adr_sz = <span class="keyword">sizeof</span>(clnt_adr);</span><br><span class="line">        <span class="comment">// 接收客户端连接</span></span><br><span class="line">        clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_adr, &amp;adr_sz);</span><br><span class="line">        <span class="keyword">if</span>(clnt_sock == <span class="number">-1</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;new client connected...&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建子进程</span></span><br><span class="line">        pid = fork();</span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">-1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            close(clnt_sock);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 子进程：负责和客户端通信</span></span><br><span class="line">        <span class="keyword">if</span>(pid == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            close(serv_sock);  <span class="comment">// 子进程关闭监听套接字文件描述符</span></span><br><span class="line">            <span class="comment">// 回声服务：读取客户端数据并回传</span></span><br><span class="line">            <span class="keyword">while</span>((str_len = read(clnt_sock, buf, BUF_SIZE)) != <span class="number">0</span>) &#123;</span><br><span class="line">                write(clnt_sock, buf, str_len);</span><br><span class="line">                write(fds[<span class="number">1</span>], buf, str_len);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            close(clnt_sock);</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;client disconnected...&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 父进程：关闭客户端套接字文件描述符，继续监听</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            close(clnt_sock);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    close(serv_sock);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子进程退出信号处理函数：回收僵尸进程</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">read_childproc</span><span class="params">(<span class="type">int</span> sig)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">pid_t</span> pid;</span><br><span class="line">    <span class="type">int</span> status;</span><br><span class="line">    pid = waitpid(<span class="number">-1</span>, &amp;status, WNOHANG);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Removed proc id: %d \n&quot;</span>, pid);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误处理函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *message)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Chapter12-I-O复用"><a href="#Chapter12-I-O复用" class="headerlink" title="Chapter12 I&#x2F;O复用"></a>Chapter12 I&#x2F;O复用</h2><p>前文多进程服务端通过<strong>创建子进程</strong>处理多客户端并发，进程创建与调度开销大、并发能力有限，资源利用率低。  </p>
<p><strong>I&#x2F;O 复用</strong>是解决该问题的核心IO模型：允许<strong>单个进程</strong>同时监听<strong>多个文件描述符</strong>（客户端套接字、管道等）的读写、异常事件。  </p>
<p>由内核统一轮询监视所有IO，仅当文件描述符触发就绪事件时，程序才进行数据读写处理，无需阻塞等待单个IO、也无需频繁创建子进程。  </p>
<p>本章重点讲解 <code>select</code> 复用函数的原理、调用流程与服务端实战实现，同时兼顾 Windows 平台下的接口差异与代码适配，掌握 I&#x2F;O 复用是实现轻量、高并发服务器的基础。  </p>
<h3 id="基于I-O复用的服务器端"><a href="#基于I-O复用的服务器端" class="headerlink" title="基于I&#x2F;O复用的服务器端"></a>基于I&#x2F;O复用的服务器端</h3><p><strong>多进程服务器的核心缺陷</strong>：  </p>
<ul>
<li>此前多进程服务端，<strong>每接入一个客户端就创建一个子进程</strong>完成通信。频繁创建&#x2F;销毁进程会消耗大量内存与CPU调度资源；并发客户端数量上限低，连接量大时系统负载急剧升高，整体性能与稳定性都会下降。</li>
</ul>
<p><strong>I&#x2F;O 复用服务器的优势</strong>：  </p>
<ul>
<li>I&#x2F;O 复用仅依靠<strong>单个进程</strong>，即可同时监听、管理多个套接字IO事件。  </li>
<li>由内核统一批量监视所有文件描述符，只有客户端发生连接、可读、可写等就绪事件时，服务端才执行对应业务处理。  </li>
<li>无需创建大量子进程，资源开销极低，大幅提升并发承载能力，程序架构更轻量化。</li>
</ul>
<p><strong>适用场景（I&#x2F;O 复用并非万能）</strong>：  </p>
<ul>
<li>适合：长连接多、大部分连接处于空闲等待状态的服务，如聊天服务器、网关、消息转发服务。  </li>
<li>不适合：客户端请求伴随大量耗时计算、密集运算的业务，这类场景更适合多进程&#x2F;多线程架构分担压力。  </li>
<li>开发中需要根据业务特性选择模型。</li>
</ul>
<p><strong>通俗类比理解</strong>：  </p>
<ul>
<li>多进程模型：餐厅每来一位顾客，就单独聘用一名专属服务员，顾客越多，人力成本和管理压力越大。  </li>
<li>I&#x2F;O 复用模型：只用一名总管，同时照看全部餐桌，哪一桌有需求（点餐&#x2F;结账），就立刻针对性处理，以极低资源高效应对大量顾客。</li>
</ul>
<h3 id="理解select函数并实现服务器端"><a href="#理解select函数并实现服务器端" class="headerlink" title="理解select函数并实现服务器端"></a>理解select函数并实现服务器端</h3><h4 id="select-函数：I-O-复用的核心"><a href="#select-函数：I-O-复用的核心" class="headerlink" title="select 函数：I&#x2F;O 复用的核心"></a>select 函数：I&#x2F;O 复用的核心</h4><p>书中说 <strong>“select 函数是整个 I&#x2F;O 复用的内容也不为过”</strong>，这句话精准至极：<br><code>select</code> 是 Linux 下<strong>最基础、最经典</strong>的 I&#x2F;O 复用函数，它能让<strong>单个进程</strong>同时监视<strong>多个文件描述符</strong>（套接字、标准输入等），直到某个描述符触发<strong>I&#x2F;O 事件</strong>（有数据可读&#x2F;可写&#x2F;异常），再去处理，不用阻塞等待单个IO。  </p>
<p><strong>select 函数标准声明：</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"><span class="type">int</span> <span class="title function_">select</span><span class="params">(</span></span><br><span class="line"><span class="params">    <span class="type">int</span> nfds,            <span class="comment">// 监视的文件描述符范围</span></span></span><br><span class="line"><span class="params">    fd_set *readfds,     <span class="comment">// 监视「读事件」的fd集合</span></span></span><br><span class="line"><span class="params">    fd_set *writefds,    <span class="comment">// 监视「写事件」的fd集合</span></span></span><br><span class="line"><span class="params">    fd_set *exceptfds,   <span class="comment">// 监视「异常事件」的fd集合</span></span></span><br><span class="line"><span class="params">    <span class="keyword">struct</span> timeval *timeout <span class="comment">// 超时时间</span></span></span><br><span class="line"><span class="params">)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>参数解释：</strong>  </p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>nfds</code></td>
<td>监视的<strong>最大文件描述符 + 1</strong>（告诉内核需要检查的fd范围，提升效率，内核暴力遍历 [0, nfds-1] ）</td>
</tr>
<tr>
<td><code>readfds</code></td>
<td>关注<strong>读事件</strong>的fd集合（如：有数据到达、客户端连接）</td>
</tr>
<tr>
<td><code>writefds</code></td>
<td>关注<strong>写事件</strong>的fd集合（极少用，传<code>NULL</code>）</td>
</tr>
<tr>
<td><code>exceptfds</code></td>
<td>关注<strong>异常事件</strong>的fd集合（极少用，传<code>NULL</code>）</td>
</tr>
<tr>
<td><code>timeout</code></td>
<td>等待超时时间：传<code>NULL</code>&#x3D;永久阻塞；传结构体&#x3D;超时后返回</td>
</tr>
</tbody></table>
<p><strong>返回值（3种情况）：</strong>  </p>
<ul>
<li><code>-1</code>：函数调用<strong>出错</strong></li>
<li><code>0</code>：<strong>超时</strong>，没有任何fd发生事件</li>
<li><strong>正整数</strong>：发生事件的<strong>文件描述符数量</strong></li>
</ul>
<p><strong>select 核心配套工具：</strong>  </p>
<p><strong>1. <code>fd_set</code> 数组</strong>  </p>
<p>本质是<strong>位图（bit数组）</strong>，用来<strong>存放需要监视的文件描述符</strong>，是 I&#x2F;O 复用的「监视清单」。  </p>
<ul>
<li>我们把要监听的 fd 加入这个集合</li>
<li>select 函数会遍历这个集合，检查是否有事件发生</li>
</ul>
<p><strong>2. 四个操作宏</strong>  </p>
<p>专门用来操作 <code>fd_set</code> 集合，是固定用法：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">FD_ZERO(&amp;fdset);      <span class="comment">// 初始化集合，全部位清0</span></span><br><span class="line">FD_SET(fd, &amp;fdset);   <span class="comment">// 将指定fd加入集合（注册监视）</span></span><br><span class="line">FD_CLR(fd, &amp;fdset);   <span class="comment">// 将指定fd从集合删除</span></span><br><span class="line">FD_ISSET(fd, &amp;fdset); <span class="comment">// 判断fd是否发生了事件（返回1=就绪）</span></span><br></pre></td></tr></table></figure>

<p><strong>3. <code>struct timeval</code> 超时结构体</strong>  </p>
<p>设置 select 的等待超时时间：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> &#123;</span></span><br><span class="line">    <span class="type">long</span> tv_sec;   <span class="comment">// 秒</span></span><br><span class="line">    <span class="type">long</span> tv_usec;  <span class="comment">// 微秒</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>select 标准调用流程</strong>  </p>
<p><strong>select 调用必须遵循：初始化 → 备份 → 调用 → 校验 → 处理</strong>  </p>
<pre class="mermaid">flowchart TD
    A[1. 初始化 fd_set 监视集合] --> B[2. 注册需要监听的fd（相应位置为1）]
    B --> C[3. 设置超时时间]
    C --> D[4. 备份原始fd_set集合! 关键]
    D --> E[5. 调用 select 函数]
    E --> F{6. 检查返回值}
    F -->|返回-1| G[出错退出]
    F -->|返回0| H[超时，重新循环]
    F -->|返回正数| I[7. 找就绪fd]
    I --> J[8. 处理数据]
    J --> K[回到备份步骤]
    K --> D</pre>

<p>这里有必要备份 <code>fd_set</code>，因为调用 <code>select</code> 之后，除了有变化的fd对应的位，所有位都会被清零，如果不备份直接传原数据，我们的值就被改了，第二次调用 <code>select</code> 监听位可能就会错误。  </p>
<p>同理，超时时间也需要在循环内设置，select会改变它。  </p>
<table>
<thead>
<tr>
<th align="left">流程图步骤</th>
<th align="left">伪代码</th>
<th align="left">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="left">1. 初始化</td>
<td align="left"><code>FD_ZERO(&amp;reads);</code></td>
<td align="left">清空位图，所有位 &#x3D; 0</td>
</tr>
<tr>
<td align="left">2. 注册监听 fd</td>
<td align="left"><code>FD_SET(0, &amp;reads);</code></td>
<td align="left">把 fd&#x3D;0 标记为 1，加入监听</td>
</tr>
<tr>
<td align="left">3. 设置超时</td>
<td align="left"><code>timeout.tv_sec=5;</code></td>
<td align="left">等待 5 秒超时</td>
</tr>
<tr>
<td align="left">4. 备份集合</td>
<td align="left"><code>temps = reads;</code></td>
<td align="left">关键！复制一份给 select 用</td>
</tr>
<tr>
<td align="left">5. 调用 select</td>
<td align="left"><code>select(1, &amp;temps, ...)</code></td>
<td align="left">内核检查事件，会修改 temps</td>
</tr>
<tr>
<td align="left">6. 判断返回</td>
<td align="left"><code>if(result==0)</code></td>
<td align="left">超时 &#x2F; 就绪 &#x2F; 错误</td>
</tr>
<tr>
<td align="left">7. 检查就绪</td>
<td align="left"><code>FD_ISSET(0, &amp;temps)</code></td>
<td align="left">看哪个 fd 触发了</td>
</tr>
<tr>
<td align="left">8. 处理</td>
<td align="left"><code>read(0, buf...)</code></td>
<td align="left">读取控制台输入</td>
</tr>
<tr>
<td align="left">循环</td>
<td align="left">回到步骤 4</td>
<td align="left">下一轮重新备份、监听</td>
</tr>
</tbody></table>
<p><strong>代码示例（监视标准输入，5秒超时）：</strong>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// select.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 30</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    fd_set reads, temps;    <span class="comment">// reads=原始监视集合，temps=select调用用的备份集合</span></span><br><span class="line">    <span class="type">int</span> result, str_len;    </span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];     </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">timeout</span>;</span> <span class="comment">// 超时结构体</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 初始化监视集合（清空）</span></span><br><span class="line">    FD_ZERO(&amp;reads);</span><br><span class="line">    <span class="comment">// 2. 注册：监视 标准输入(文件描述符=0)</span></span><br><span class="line">    FD_SET(<span class="number">0</span>, &amp;reads);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 关键：每次调用select前，必须备份原始集合（select会修改集合）</span></span><br><span class="line">        temps = reads;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 每次循环都设置超时：5秒0微秒</span></span><br><span class="line">        timeout.tv_sec = <span class="number">5</span>;</span><br><span class="line">        timeout.tv_usec = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 调用select：监视读集合，超时5秒</span></span><br><span class="line">        <span class="comment">// 调用后会改变fd_set和timeout</span></span><br><span class="line">        result = select(<span class="number">1</span>, &amp;temps, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;timeout);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(result == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;select() error!&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(result == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">puts</span>(<span class="string">&quot;Time-out!&quot;</span>); <span class="comment">// 5秒无输入，超时</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 5. 判断：标准输入是否就绪（有数据输入）</span></span><br><span class="line">            <span class="keyword">if</span>(FD_ISSET(<span class="number">0</span>, &amp;temps)) &#123;</span><br><span class="line">                str_len = read(<span class="number">0</span>, buf, BUF_SIZE);</span><br><span class="line">                buf[str_len] = <span class="number">0</span>;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;message from console: %s&quot;</span>, buf);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ./select                     </span></span><br><span class="line">Time-out!</span><br><span class="line">Time-out!</span><br><span class="line">hello</span><br><span class="line">message from console: hello</span><br><span class="line">Time-out!</span><br><span class="line">鸡你太美</span><br><span class="line">message from console: 鸡你太美</span><br><span class="line">^C</span><br></pre></td></tr></table></figure>

<h4 id="实现I-O复用服务器端"><a href="#实现I-O复用服务器端" class="headerlink" title="实现I&#x2F;O复用服务器端"></a>实现I&#x2F;O复用服务器端</h4><p>因为文件描述符的创建是整数递增的，所以这里 fd_max 我们可以直接赋值为最新创建的文件描述符，供 select 函数的第一个参数使用。  </p>
<p>后面我们客户端连接服务端，创建了新的文件描述符，我们要更新fd_max，把客户端文件描述符加入监听队列。  </p>
<p>客户端我们使用<a href="#51-echo%E5%AE%A2%E6%88%B7%E7%AB%AF%E7%9A%84%E5%AE%8C%E7%BE%8E%E5%AE%9E%E7%8E%B0">5.1 echo客户端的完美实现</a>  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo_selectserv.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/select.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 100</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *buf)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> serv_sock, clnt_sock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">serv_adr</span>, <span class="title">clnt_adr</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">timeout</span>;</span></span><br><span class="line">    fd_set reads, cpy_reads;    <span class="comment">// 原始集合 + 备份集合（select核心）</span></span><br><span class="line">    <span class="type">socklen_t</span> adr_sz;</span><br><span class="line">    <span class="type">int</span> fd_max, str_len, fd_num, i;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 参数检查</span></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage : %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 创建服务端套接字 + 绑定 + 监听</span></span><br><span class="line">    serv_sock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">memset</span>(&amp;serv_adr, <span class="number">0</span>, <span class="keyword">sizeof</span>(serv_adr));</span><br><span class="line">    serv_adr.sin_family = AF_INET;</span><br><span class="line">    serv_adr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    serv_adr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bind(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;serv_adr, <span class="keyword">sizeof</span>(serv_adr)) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (listen(serv_sock, <span class="number">5</span>) == <span class="number">-1</span>)</span><br><span class="line">        error_handling(<span class="string">&quot;listen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 初始化 select 监视集合</span></span><br><span class="line">    FD_ZERO(&amp;reads);</span><br><span class="line">    FD_SET(serv_sock, &amp;reads);  <span class="comment">// 注册服务端socket（监视连接事件）</span></span><br><span class="line">    fd_max = serv_sock;         <span class="comment">// 记录当前最大文件描述符（select第一个参数用）</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. select 主循环（核心！）</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// ========== select 固定流程：备份原始集合 ==========</span></span><br><span class="line">        cpy_reads = reads;</span><br><span class="line">        <span class="comment">// 设置超时：5秒0微秒</span></span><br><span class="line">        timeout.tv_sec = <span class="number">5</span>;</span><br><span class="line">        timeout.tv_usec = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// ========== 调用 select 函数 ==========</span></span><br><span class="line">        <span class="comment">// 参数1：最大fd+1； 参数2：读集合； 超时时间</span></span><br><span class="line">        fd_num = select(fd_max + <span class="number">1</span>, &amp;cpy_reads, <span class="literal">NULL</span>, <span class="literal">NULL</span>, &amp;timeout);</span><br><span class="line">        <span class="keyword">if</span> (fd_num == <span class="number">-1</span>)  <span class="comment">// select 出错</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">if</span> (fd_num == <span class="number">0</span>)   <span class="comment">// 超时，无事件</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// ========== 遍历所有fd，查找就绪的fd ==========</span></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; fd_max + <span class="number">1</span>; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 判断当前fd是否有事件</span></span><br><span class="line">            <span class="keyword">if</span> (FD_ISSET(i, &amp;cpy_reads))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 情况1：服务端fd就绪 → 有新客户端连接</span></span><br><span class="line">                <span class="keyword">if</span> (i == serv_sock)</span><br><span class="line">                &#123;</span><br><span class="line">                    adr_sz = <span class="keyword">sizeof</span>(clnt_adr);</span><br><span class="line">                    clnt_sock = accept(serv_sock, (<span class="keyword">struct</span> sockaddr*)&amp;clnt_adr, &amp;adr_sz);</span><br><span class="line">                    </span><br><span class="line">                    <span class="comment">// 新客户端socket 注册到 select 监视集合</span></span><br><span class="line">                    FD_SET(clnt_sock, &amp;reads);</span><br><span class="line">                    <span class="comment">// 更新最大fd</span></span><br><span class="line">                    <span class="keyword">if</span> (fd_max &lt; clnt_sock)</span><br><span class="line">                        fd_max = clnt_sock;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;connected client: %d\n&quot;</span>, clnt_sock);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 情况2：客户端fd就绪 → 有数据可读</span></span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                &#123;</span><br><span class="line">                    str_len = read(i, buf, BUF_SIZE);</span><br><span class="line">                    <span class="comment">// 客户端断开连接</span></span><br><span class="line">                    <span class="keyword">if</span> (str_len == <span class="number">0</span>)</span><br><span class="line">                    &#123;</span><br><span class="line">                        FD_CLR(i, &amp;reads);  <span class="comment">// 从监视集合中删除</span></span><br><span class="line">                        close(i);          <span class="comment">// 关闭套接字</span></span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;closed client: %d\n&quot;</span>, i);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 回声：将数据回写给客户端</span></span><br><span class="line">                    <span class="keyword">else</span></span><br><span class="line">                    &#123;</span><br><span class="line">                        write(i, buf, str_len);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(serv_sock);  <span class="comment">// 关闭服务端socket</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误处理函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">error_handling</span><span class="params">(<span class="type">char</span> *buf)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fputs</span>(buf, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 服务端</span></span><br><span class="line"><span class="comment"># ./echo_selectserv 9527</span></span><br><span class="line">connected client: 4</span><br><span class="line">closed client: 4</span><br><span class="line"></span><br><span class="line"><span class="comment"># 客户端</span></span><br><span class="line"><span class="comment"># ./client_echo2 127.0.0.1 9527</span></span><br><span class="line">Connected........</span><br><span class="line">Input message(Q to quit): hello</span><br><span class="line">Message from server: hello</span><br><span class="line">Input message(Q to quit): how are you</span><br><span class="line">Message from server: how are you</span><br><span class="line">Input message(Q to quit): Q</span><br></pre></td></tr></table></figure>

<h3 id="基于Windows的实现"><a href="#基于Windows的实现" class="headerlink" title="基于Windows的实现"></a>基于Windows的实现</h3><p><strong>Windows 与 Linux 的 select 函数用法高度统一，但底层实现有关键差异</strong>。  </p>
<p><strong>select函数声明</strong>：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">select</span><span class="params">(</span></span><br><span class="line"><span class="params">    <span class="type">int</span> nfds,             <span class="comment">// 【无用参数】仅兼容Linux，固定填0</span></span></span><br><span class="line"><span class="params">    fd_set *readfds,      <span class="comment">// 读事件监视集合</span></span></span><br><span class="line"><span class="params">    fd_set *writefds,     <span class="comment">// 写事件监视集合</span></span></span><br><span class="line"><span class="params">    fd_set *exceptfds,    <span class="comment">// 异常事件监视集合</span></span></span><br><span class="line"><span class="params">    <span class="type">const</span> <span class="keyword">struct</span> timeval *timeout  <span class="comment">// 超时时间</span></span></span><br><span class="line"><span class="params">)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>  </p>
<table>
<thead>
<tr>
<th>参数</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>nfds</code></td>
<td><strong>Windows 下完全废弃</strong>，为了和Linux语法统一，<strong>固定传 0</strong></td>
</tr>
<tr>
<td><code>readfds/writefds/exceptfds</code></td>
<td>监视集合，用法和Linux一致</td>
</tr>
<tr>
<td><code>timeout</code></td>
<td>超时结构体，<strong>和Linux完全相同</strong></td>
</tr>
</tbody></table>
<p><strong>返回值</strong>  </p>
<ul>
<li><code>SOCKET_ERROR</code> (-1)：函数调用失败</li>
<li><code>0</code>：超时，无就绪套接字</li>
<li><strong>正整数</strong>：就绪的套接字数量（含义和Linux完全一致）</li>
</ul>
<p><strong>Windows 版 <code>fd_set</code>（和linux的核心差异点）</strong>：  </p>
<p>Windows 中 <code>fd_set</code> 不是位图，而是<strong>记录句柄数量 + 数组存储句柄</strong>（Windows 套接字句柄无规律、不从0开始）  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">fd_set</span> &#123;</span></span><br><span class="line">    u_int  fd_count;      <span class="comment">// 存储的套接字句柄 数量</span></span><br><span class="line">    SOCKET fd_array[FD_SETSIZE];  <span class="comment">// 存储套接字句柄的数组</span></span><br><span class="line">&#125; fd_set;</span><br></pre></td></tr></table></figure>

<p><strong><code>timeval</code> 结构体</strong>:  </p>
<p><strong>和 Linux 完全相同</strong>，无任何差异。  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> &#123;</span></span><br><span class="line">    <span class="type">long</span> tv_sec;   <span class="comment">// 秒</span></span><br><span class="line">    <span class="type">long</span> tv_usec;  <span class="comment">// 微秒</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>fd_set 操作宏</strong>：  </p>
<p><strong>宏名称、用法和 Linux 完全一致</strong>（底层实现不同，上层无感）：  </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">FD_ZERO(&amp;fdset);    <span class="comment">// 清空集合</span></span><br><span class="line">FD_SET(sock, &amp;fdset);   <span class="comment">// 添加套接字到监视集合</span></span><br><span class="line">FD_CLR(sock, &amp;fdset);   <span class="comment">// 从集合删除套接字</span></span><br><span class="line">FD_ISSET(sock, &amp;fdset); <span class="comment">// 判断套接字是否就绪</span></span><br></pre></td></tr></table></figure>

<p>Windows 下实现上述 select 回声服务器（客户端使用<a href="#52-windows-echo%E5%AE%A2%E6%88%B7%E7%AB%AF%E5%AE%9E%E7%8E%B0">windows下完美echo客户端</a>）</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo_selectserv_win.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> BUF_SIZE 1024</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span> *message)</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span></span><br><span class="line">&#123;</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    SOCKET hServSock, hClntSock;</span><br><span class="line">    SOCKADDR_IN servAdr, clntAdr;</span><br><span class="line">    TIMEVAL timeout;</span><br><span class="line">    fd_set reads, cpyReads;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> adrSz;</span><br><span class="line">    <span class="type">int</span> strLen, fdNum, i;</span><br><span class="line">    <span class="type">char</span> buf[BUF_SIZE];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Usage : %s &lt;port&gt;\n&quot;</span>, argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Windows 套接字初始化</span></span><br><span class="line">    <span class="keyword">if</span> (WSAStartup(MAKEWORD(<span class="number">2</span>, <span class="number">2</span>), &amp;wsaData) != <span class="number">0</span>)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;WSAStartup() error!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建服务端套接字</span></span><br><span class="line">    hServSock = socket(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">memset</span>(&amp;servAdr, <span class="number">0</span>, <span class="keyword">sizeof</span>(servAdr));</span><br><span class="line">    servAdr.sin_family = AF_INET;</span><br><span class="line">    servAdr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    servAdr.sin_port = htons(atoi(argv[<span class="number">1</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 绑定+监听</span></span><br><span class="line">    <span class="keyword">if</span> (bind(hServSock, (SOCKADDR*)&amp;servAdr, <span class="keyword">sizeof</span>(servAdr)) == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;bind() error&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (listen(hServSock, <span class="number">5</span>) == SOCKET_ERROR)</span><br><span class="line">        ErrorHandling(<span class="string">&quot;listen() error&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化 select 监视集合</span></span><br><span class="line">    FD_ZERO(&amp;reads);</span><br><span class="line">    FD_SET(hServSock, &amp;reads);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 备份集合（和Linux逻辑一致）</span></span><br><span class="line">        cpyReads = reads;</span><br><span class="line">        timeout.tv_sec = <span class="number">5</span>;</span><br><span class="line">        timeout.tv_usec = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Windows select：第一个参数固定填0</span></span><br><span class="line">        fdNum = select(<span class="number">0</span>, &amp;cpyReads, <span class="number">0</span>, <span class="number">0</span>, &amp;timeout);</span><br><span class="line">        <span class="keyword">if</span> (fdNum == SOCKET_ERROR)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">if</span> (fdNum == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Windows 遍历方式：遍历 fd_count（核心差异）</span></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; reads.fd_count; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (FD_ISSET(reads.fd_array[i], &amp;cpyReads))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">// 1. 服务端套接字：新客户端连接</span></span><br><span class="line">                <span class="keyword">if</span> (reads.fd_array[i] == hServSock)</span><br><span class="line">                &#123;</span><br><span class="line">                    adrSz = <span class="keyword">sizeof</span>(clntAdr);</span><br><span class="line">                    hClntSock = accept(hServSock, (SOCKADDR*)&amp;clntAdr, &amp;adrSz);</span><br><span class="line">                    FD_SET(hClntSock, &amp;reads);</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;connected client: %d\n&quot;</span>, hClntSock);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 2. 客户端套接字：读取数据并回声</span></span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                &#123;</span><br><span class="line">                    strLen = recv(reads.fd_array[i], buf, BUF_SIZE - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">                    <span class="comment">// 客户端断开连接</span></span><br><span class="line">                    <span class="keyword">if</span> (strLen == <span class="number">0</span>)</span><br><span class="line">                    &#123;</span><br><span class="line">                        <span class="comment">// 从集合删除+关闭套接字</span></span><br><span class="line">                        FD_CLR(reads.fd_array[i], &amp;reads);</span><br><span class="line">                        closesocket(reads.fd_array[i]);</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;closed client: %d\n&quot;</span>, reads.fd_array[i]);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">else</span></span><br><span class="line">                    &#123;</span><br><span class="line">                        <span class="comment">// 回声回传</span></span><br><span class="line">                        send(reads.fd_array[i], buf, strLen, <span class="number">0</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 资源释放</span></span><br><span class="line">    closesocket(hServSock);</span><br><span class="line">    WSACleanup();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 错误处理函数</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">ErrorHandling</span><span class="params">(<span class="type">char</span> *message)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">fputs</span>(message, <span class="built_in">stderr</span>);</span><br><span class="line">    fputc(<span class="string">&#x27;\n&#x27;</span>, <span class="built_in">stderr</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>示例结果：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 服务端</span></span><br><span class="line"><span class="comment"># .\echo_selectserv_win.exe 9527</span></span><br><span class="line">connected client: 240</span><br><span class="line">closed client: 240</span><br><span class="line"></span><br><span class="line"><span class="comment"># 客户端</span></span><br><span class="line"><span class="comment"># .\client_echo2_win.exe 127.0.0.1 9527</span></span><br><span class="line">Connected........</span><br><span class="line">Input message(Q to quit): hello</span><br><span class="line">Message from server: hello</span><br><span class="line">Input message(Q to quit): how are u?</span><br><span class="line">Message from server: how are u?</span><br><span class="line">Input message(Q to quit): 鸡你太美</span><br><span class="line">Message from server: 鸡你太美</span><br><span class="line">Input message(Q to quit): Q</span><br></pre></td></tr></table></figure>


<script type="module"> import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.esm.min.mjs';	mermaid.initialize({startOnLoad: true, flowchart: {curve: 'linear'}}); </script>]]></content>
      <tags>
        <tag>网络编程</tag>
      </tags>
  </entry>
  <entry>
    <title>模板</title>
    <url>/2025/12/04/%E6%A8%A1%E6%9D%BF/</url>
    <content><![CDATA[<h3 id="函数模板"><a href="#函数模板" class="headerlink" title="函数模板"></a>函数模板</h3><p>普通函数其参数是特定类型  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>, <span class="type">double</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>利用函数模板我们可以让编译器根据实参推断参数类型</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// typename 关键字换 class 也行</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> R&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T, R)</span></span>;</span><br></pre></td></tr></table></figure>

<p>比如写一个把传进参数+1的函数，我们可以对<strong>任意类型</strong>调用（如果可以+1的话）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function">T <span class="title">add_one</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>编译期看到调用点，才根据实参 <strong>自动生成</strong> 对应函数。  </li>
<li>如果 T 不支持 +1，编译直接报错，而不是运行期崩溃。</li>
</ul>
<p>我们试着调用<code>add_one</code>  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 打印2,3,3</span></span><br><span class="line">std::cout &lt;&lt; <span class="built_in">add_one</span>(<span class="number">1</span>) &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="built_in">add_one</span>(<span class="number">2.0f</span>) &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="built_in">add_one</span>(<span class="number">2.0</span>) &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译报错，string不支持+int</span></span><br><span class="line">std::cout &lt;&lt; <span class="built_in">add_one</span>(std::<span class="built_in">string</span>(<span class="string">&quot;hello&quot;</span>)) &lt;&lt; std::endl;</span><br></pre></td></tr></table></figure>

<h3 id="类模板"><a href="#类模板" class="headerlink" title="类模板"></a>类模板</h3><p>和函数模板如出一辙，可以泛化类型，让我们写一个能构造任意类型数组的一个类  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="comment">// 两个模板参数：类型 + 大小</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, std::<span class="type">size_t</span> N&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyArray</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyArray</span>() &#123; std::cout &lt;&lt; <span class="string">&quot;MyArray&lt;&quot;</span> &lt;&lt; <span class="built_in">typeid</span>(T).<span class="built_in">name</span>() &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; N &lt;&lt; <span class="string">&quot;&gt;\n&quot;</span>; &#125;</span><br><span class="line">    T data[N];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyArray&lt;<span class="type">int</span>, <span class="number">5</span>&gt;    a;   <span class="comment">// 生成一份 int[5] 的代码</span></span><br><span class="line">    MyArray&lt;<span class="type">double</span>, <span class="number">3</span>&gt; b;   <span class="comment">// 再生成一份 double[3] 的代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="模板参数包"><a href="#模板参数包" class="headerlink" title="模板参数包"></a>模板参数包</h3><p>模板参数包是接受 <strong>任意参数类型和任意参数个数</strong> 的一个特性，使用的时候涉及到打包和解包两个操作  </p>
<h4 id="函数模板-参数包"><a href="#函数模板-参数包" class="headerlink" title="函数模板+参数包"></a>函数模板+参数包</h4><p>我们写一个log函数，打印接收到的所有参数  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span>... Args&gt;  <span class="comment">// 打包</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">log</span><span class="params">(Args... args)</span>      <span class="comment">// 解包</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ((std::cout &lt;&lt; args &lt;&lt; <span class="string">&#x27; &#x27;</span>), ...);  <span class="comment">// fold expression, since c++17</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">log</span>(<span class="number">2025</span>, <span class="string">&quot;price=&quot;</span>, <span class="number">19.9</span>, <span class="string">&#x27;$&#x27;</span>);   <span class="comment">// 任意个数、任意类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的 <strong>右折表达式</strong> 会在编译期把参数包内容展开成前面操作的形式  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 一元右折表达式展开</span></span><br><span class="line">((std::cout &lt;&lt; arg1 &lt;&lt; <span class="string">&#x27; &#x27;</span>),</span><br><span class="line"> ((std::cout &lt;&lt; arg2 &lt;&lt; <span class="string">&#x27; &#x27;</span>),</span><br><span class="line">  ((std::cout &lt;&lt; arg3 &lt;&lt; <span class="string">&#x27; &#x27;</span>), ...)));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line">std::cout &lt;&lt; a1 &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">std::cout &lt;&lt; a2 &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">std::cout &lt;&lt; a3 &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>接下来我们使用递归调用来设计功能一样的log  </p>
<p>需要注意的事，设计递归，就需要终止条件，而递归函数模板也不例外  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 终止条件</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">log</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;[log end]&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Head, <span class="keyword">typename</span>... Args&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">log</span><span class="params">(<span class="type">const</span> Head&amp; h, Args... args)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; h &lt;&lt; std::endl;</span><br><span class="line">    <span class="built_in">log</span>(args...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>终止条件必须写在递归函数模板之前</strong>，必须让编译器先看到递归终止条件，在递归调用到0参数时，才会调用终止条件的函数；把终止条件写在后面和不写的效果是一样的，都会编译报错，因为编译器找不到0参数函数实例，就会照着模板生成一个，然后发现模板至少需要一个参数（typename Head限制至少需要一个参数），就会生成实例失败  </p>
<p>另外，如果存在多个可推导模板，比如上面两个log，一个用折叠表达式，一个用递归，编译器是看哪个模板更特殊决定使用哪个模板的，更特殊大致的意思是“约束更严格、匹配范围更小”，这里选用的是后面的递归形式，因为它限制了至少要有一个参数  </p>
<h4 id="类模板-参数包"><a href="#类模板-参数包" class="headerlink" title="类模板+参数包"></a>类模板+参数包</h4><p>我们来写一个简单的tuple类，用类模板递归把参数包拆成“头+尾”，这也是标准库std::tuple的核心思想，把数据一层层存储  </p>
<p>需要注意的是，设计递归，就需要终止条件，而递归类模板也不例外（重要的事情说两遍）  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 需要此前向声明，因为Tuple内部还使用了Tuple&lt;Tail...&gt;</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span>... Ts&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tuple</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 递归的终止条件（全特化），拆包结束</span></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;                   <span class="comment">// 0 个参数时的特化</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tuple</span>&lt;&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> <span class="type">const</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建tuple类</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Head, <span class="keyword">typename</span>... Tail&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tuple</span>&lt;Head, Tail...&gt;               <span class="comment">// 至少 1 个参数</span></span><br><span class="line">&#123;</span><br><span class="line">    Head value;                          <span class="comment">// 存为头部数据</span></span><br><span class="line">    Tuple&lt;Tail...&gt; tail;                 <span class="comment">// 剩下的再打包一层</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Tuple</span>(Head h, Tail... t)             <span class="comment">// 构造：把参数包一层层剥开</span></span><br><span class="line">        : <span class="built_in">value</span>(h), <span class="built_in">tail</span>(t...) &#123;&#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> <span class="type">const</span>                   <span class="comment">// 递归打印</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; value &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">        tail.<span class="built_in">print</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Tuple&lt;<span class="type">int</span>, <span class="type">double</span>, <span class="type">char</span>&gt; <span class="title">t</span><span class="params">(<span class="number">42</span>, <span class="number">3.14</span>, <span class="string">&#x27;X&#x27;</span>)</span></span>;</span><br><span class="line">    t.<span class="built_in">print</span>();                           <span class="comment">// 42 3.14 X</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="完美转发"><a href="#完美转发" class="headerlink" title="完美转发"></a>完美转发</h3><p>完美转发即把收到的实参<strong>原汁原味</strong>地转给另一段代码，既不掉引用，也不掉 const&#x2F;volatile&#96;，更不掉<strong>右值性</strong>  </p>
<p>我们先看看普通的传参  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T arg)</span>          <span class="comment">// 按值传</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">foo</span>(arg);                <span class="comment">// 这里 arg 永远是左值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果我们foo函数只有一个接收右值的定义，上面的代码就会编译报错了  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>&amp;&amp;)</span> </span>&#123;  &#125;  <span class="comment">// 右值引用</span></span><br></pre></td></tr></table></figure>

<p>使用<code>std::forward</code>进行完美转发，保留值性  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span>   <span class="comment">// std::forward</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 三个重载</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>&amp; x)</span>  </span>&#123; std::cout &lt;&lt; <span class="string">&quot;lvalue int&amp;        : &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>; &#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>&amp;&amp; x)</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;rvalue int&amp;&amp;       : &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>; &#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; x)</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;const lvalue const int&amp; : &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 完美转发包装器</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;          <span class="comment">// 万能引用，仅模板推导+&amp;&amp;才是万能，这里有个引用折叠的知识点就不多说了</span></span><br><span class="line">    <span class="built_in">foo</span>(std::forward&lt;T&gt;(arg));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span>  a = <span class="number">1</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> b = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">wrapper</span>(a);        <span class="comment">// lvalue</span></span><br><span class="line">    <span class="built_in">wrapper</span>(b);        <span class="comment">// const lvalue</span></span><br><span class="line">    <span class="built_in">wrapper</span>(<span class="number">3</span>);        <span class="comment">// rvalue</span></span><br><span class="line">    <span class="built_in">wrapper</span>(std::<span class="built_in">move</span>(a)); <span class="comment">// rvalue (forced transform)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>典型应用：工厂函数  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T, <span class="keyword">class</span>... Args&gt;</span></span><br><span class="line"><span class="function">T* <span class="title">create</span><span class="params">(Args&amp;&amp;... args)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">T</span>(std::forward&lt;Args&gt;(args)...);   <span class="comment">// 完美转发给构造函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="函数模板全特化"><a href="#函数模板全特化" class="headerlink" title="函数模板全特化"></a>函数模板全特化</h3><p>全特化和偏特化的概念很简单，前者是将全部模板参数定义为特定类型，后者是将部分定义为特定类型  </p>
<p>有人可能会好奇，编译器不是会自动推断吗？干嘛还要特化这种东西  </p>
<p>其实特化主要是针对特定类型，我们能做的事情不同，意思就是针对特定类型，函数的定义选为另一套，参数类型推导可不会改函数定义，所以这两个是完全不同的东西，别混淆了  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;could calculate&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">bool</span>)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;could judgement&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">char</span>*)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;could read&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span>    a  &#123; <span class="number">1</span> &#125;;</span><br><span class="line">    <span class="type">float</span>  b  &#123; <span class="number">2.0f</span>&#125;;</span><br><span class="line">    <span class="type">double</span> c  &#123; <span class="number">3.0</span> &#125;;</span><br><span class="line">    <span class="type">bool</span>   e  &#123; <span class="literal">false</span> &#125;;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span>* d &#123; <span class="string">&quot;hello&quot;</span> &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">foo</span>(a);  <span class="comment">// could calculate</span></span><br><span class="line">    <span class="built_in">foo</span>(b);  <span class="comment">// could calculate</span></span><br><span class="line">    <span class="built_in">foo</span>(c);  <span class="comment">// could calculate</span></span><br><span class="line">    <span class="built_in">foo</span>(d);  <span class="comment">// could read</span></span><br><span class="line">    <span class="built_in">foo</span>(e);  <span class="comment">// could judgement</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先提及两个注意点：  </p>
<ul>
<li><strong>全特化不参与重载</strong>，只是给已经生成的模板实例打补丁。  </li>
<li>如果把它写成重载函数而不是全特化，行为会不同（重载优先级更高）。</li>
</ul>
<p>这也是 “模板全特化 vs 重载” 经典面试题。</p>
<p>在上面的代码中加上一个重载函数，那么最终调用的是这个重载函数，打印出”hello”  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; s &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果你喜欢先看看目录再看文章，可能发现了函数模板只提到了全特化，没错，因为函数模板不允许偏特化，这和函数重载决议有关系  </p>
<p>说白了c++压根没考虑过两个完全一样的签名在重载决议时出现，我们的通用模板和偏特化模板可能会实例化出完全一样的函数签名，这时候就没法抉择了  </p>
<p>而全特化的函数模板，在编译器眼里就是一个普通函数，编译器给他改了函数签名让他不参与重载决议，所以函数模板全特化是允许的，在调用点处，编译器会先看有没有普通函数可以用，再用通用模板匹配，匹配推导类型成功后再看有没有全特化的函数，这样全特化函数即使改了签名还是能被调用到  </p>
<h3 id="类模板全特化与偏特化"><a href="#类模板全特化与偏特化" class="headerlink" title="类模板全特化与偏特化"></a>类模板全特化与偏特化</h3><p>类模板的全特化写法  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span> &#123;                      <span class="comment">// 主模板：两个参数</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Universal\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;                       <span class="comment">// 全特化：两个参数全部给出具体类型</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span>&lt;<span class="type">int</span>, <span class="type">double</span>&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;int+double Specialized\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Foo&lt;<span class="type">int</span>, <span class="type">char</span>&gt;   f1;</span><br><span class="line">    Foo&lt;<span class="type">int</span>, <span class="type">double</span>&gt; f2;</span><br><span class="line"></span><br><span class="line">    f<span class="number">1.</span><span class="built_in">print</span>();</span><br><span class="line">    f<span class="number">2.</span><span class="built_in">print</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类模板的偏特化写法  </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span> &#123;                      <span class="comment">// 主模板：两个参数</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Universal\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 只指定第一个，第二个保持开放</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">U</span>&gt;                <span class="comment">// 注意：这里还有一个模板参数 U</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span>&lt;<span class="type">int</span>, U&gt; &#123;              <span class="comment">// 模式：只要第一实参是 int 就命中</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;int+? Specialized\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 指定第二个为指针，第一个保持开放</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;                <span class="comment">// 仍有模板参数 T</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Foo</span>&lt;T, T*&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;?+T* Specialized\n&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Foo&lt;<span class="type">int</span>, <span class="type">char</span>&gt;   f1;</span><br><span class="line">    Foo&lt;<span class="type">int</span>, <span class="type">char</span>*&gt;  f2;</span><br><span class="line">    Foo&lt;<span class="type">float</span>, <span class="type">float</span>*&gt;   f3;</span><br><span class="line">    <span class="comment">// Foo&lt;int, int*&gt;   f4;</span></span><br><span class="line"></span><br><span class="line">    f<span class="number">1.</span><span class="built_in">print</span>();</span><br><span class="line">    f<span class="number">2.</span><span class="built_in">print</span>();</span><br><span class="line">    f<span class="number">3.</span><span class="built_in">print</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面注释了一个f4，如果取消注释就会发现有编译错误，原因是两个偏特化都能推导定义并且编译器无法判定谁更特殊，就不知道用哪个模板生成了，解决方案也很多，去掉一个偏特化或者加一个全特化之类的，这里只是说明一下问题希望用模板时多多思考，规则有点多  </p>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>泛型编程</tag>
      </tags>
  </entry>
  <entry>
    <title>打造QQ群聊机器人</title>
    <url>/2026/04/11/%E6%89%93%E9%80%A0QQ%E7%BE%A4%E8%81%8A%E6%9C%BA%E5%99%A8%E4%BA%BA/</url>
    <content><![CDATA[<p>你是否想拥有一个 24 小时在线的 QQ 智能助理，帮你自动回复消息、管理群聊，还能随时陪你聊天？本文记录了我个人在 Linux 云服务器上，使用 Docker Compose 快速部署 <strong>AstrBot</strong> + <strong>NapCat</strong> 并接入智谱 AI 大模型的全过程。整个过程只需几条命令，非常适合想快速上手的朋友。  </p>
<h2 id="Windows、Linux桌面版安装"><a href="#Windows、Linux桌面版安装" class="headerlink" title="Windows、Linux桌面版安装"></a>Windows、Linux桌面版安装</h2><p>我使用 Linux 云服务器部署QQ机器人，如果是 Windows 或者 Linux 桌面版系统，建议直接上B站查看教程，B站上教程众多，这里只推荐一个视频，如若失效自行查找即可：  </p>
<p><a href="https://www.bilibili.com/video/BV1zRfEBeEpj">QQ聊天机器人本地部署教程 - 幻_湮（BiliBili）</a>  </p>
<h2 id="Linux-云服务器部署配置"><a href="#Linux-云服务器部署配置" class="headerlink" title="Linux 云服务器部署配置"></a>Linux 云服务器部署配置</h2><p>ssh 登录到云服务器。  </p>
<h3 id="NapCatQQ、AstrBot和大模型"><a href="#NapCatQQ、AstrBot和大模型" class="headerlink" title="NapCatQQ、AstrBot和大模型"></a>NapCatQQ、AstrBot和大模型</h3><p><a href="https://napneko.github.io/guide/napcat">NapCatQQ 文档</a><br><a href="https://docs.astrbot.app/what-is-astrbot.html">AstrBot 文档</a><br><a href="https://bigmodel.cn/">智普AI开放平台</a>  </p>
<p>首先我们要在智普AI开放平台注册一个账号，新建一个 API Key 备用，选择智普AI只是我个人推荐，它注册有2000w免费Token使用，也有便宜好用的 Coding Plan 可用，对于大模型的选择完全自由，不限定于一家。  </p>
<p>还有，在AI开放平台的页面选项中找到开发文档，其中记录了调用API需要的步骤，最重要的是查找到 <code>BASE URL</code>，这必不可少。  </p>
<p>NapCat 是连接 QQ 的 MCP 工具，AstrBot 是协调一切的 Agent 大脑。AI应用的基本概念：<code>Agent</code>、<code>MCP</code> 和 <code>Skills</code>，如果想了解可以和 AI 大模型进行QA学习，简单来说，Agent 是软件应用，MCP 是本地机器和应用之间的媒介（比如读写文件，实际是MCP的功能），Skills 是特定内容文档，会被利用添加到与大模型沟通的上下文信息中。  </p>
<pre class="mermaid">flowchart LR
    subgraph A [你的 Linux 服务器]
        B[NapCat QQ<br>（MCP 工具）] -- 收发消息 --> C[QQ 服务器]
        B -- OneBot 协议 --> D[AstrBot<br>（Agent 大脑）]
    end

    C -- QQ 消息 --> B
    D -- API 调用 --> E[大模型<br>（DeepSeek 等）]
    E -- AI 回复 --> D

    F[QQ 用户] -- 发送消息 --> C
    D -- 控制指令/配置 --> G[Web 管理界面]</pre>

<table>
<thead>
<tr>
<th align="left">组件</th>
<th align="left">作用</th>
<th align="left">部署方式</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>NapCat</strong></td>
<td align="left">基于官方 QQ Linux 版，负责登录 QQ 并收发消息</td>
<td align="left">Docker 容器</td>
</tr>
<tr>
<td align="left"><strong>AstrBot</strong></td>
<td align="left">聊天机器人核心，负责处理消息、调用大模型、管理插件</td>
<td align="left">Docker 容器</td>
</tr>
<tr>
<td align="left"><strong>智谱 AI</strong></td>
<td align="left">提供大模型对话能力（也可替换为 DeepSeek、OpenAI 等）</td>
<td align="left">API 调用</td>
</tr>
</tbody></table>
<p>两者通过 <code>OneBot v11</code> 协议在容器内部通信，无需额外配置网络，开箱即用。  </p>
<p>下面正式开始讲解部署步骤。  </p>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><ol>
<li>一台运行着 Linux 的服务器（建议 2核2G 以上，我使用Ubuntu系统演示）。  </li>
<li>服务器上已安装 Docker 和 Docker Compose（下面步骤说明如何安装）。  </li>
<li>一个准备好的 QQ 小号（降低主号被风控的风险）。  </li>
<li>AI 大模型 API Key（在AI开放平台注册账号购买）。</li>
</ol>
<h3 id="安装-Docker-及-Docker-Compose-插件"><a href="#安装-Docker-及-Docker-Compose-插件" class="headerlink" title="安装 Docker 及 Docker Compose 插件"></a>安装 Docker 及 Docker Compose 插件</h3><p><a href="https://docs.docker.com/engine/install/ubuntu/">英文原版安装文档</a>  </p>
<p><a href="https://docker.github.net.cn/engine/install/ubuntu/">中文镜像安装文档</a>  </p>
<p>下面记录一下我的步骤  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 更新软件包索引</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装必要依赖</span></span><br><span class="line"><span class="built_in">sudo</span> apt install -y apt-transport-https ca-certificates curl software-properties-common</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加 Docker 官方 GPG 密钥</span></span><br><span class="line">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | <span class="built_in">sudo</span> gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg</span><br><span class="line"></span><br><span class="line"><span class="comment"># 添加 Docker APT 仓库</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;deb [arch=<span class="subst">$(dpkg --print-architecture)</span> signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu <span class="subst">$(lsb_release -cs)</span> stable&quot;</span> | <span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/apt/sources.list.d/docker.list &gt; /dev/null</span><br><span class="line"></span><br><span class="line"><span class="comment"># 更新并安装 Docker 引擎及 Compose 插件</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动 Docker 并设置开机自启</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl start docker</span><br><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> docker</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证安装</span></span><br><span class="line">docker --version</span><br><span class="line">docker compose version</span><br></pre></td></tr></table></figure>

<p>国内服务器拉取 Docker Hub 镜像可能较慢，可编辑 <code>/etc/docker/daemon.json</code> 添加镜像加速：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">tee</span> /etc/docker/daemon.json &lt;&lt;-<span class="string">&#x27;EOF&#x27;</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;registry-mirrors&quot;</span>: [<span class="string">&quot;https://docker.m.daocloud.io&quot;</span>]</span><br><span class="line">&#125;</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> systemctl daemon-reload</span><br><span class="line"><span class="built_in">sudo</span> systemctl restart docker</span><br></pre></td></tr></table></figure>

<h3 id="Docker-Compose-一键部署"><a href="#Docker-Compose-一键部署" class="headerlink" title="Docker Compose 一键部署"></a>Docker Compose 一键部署</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建并进入项目目录</span></span><br><span class="line"><span class="built_in">mkdir</span> astrbot &amp;&amp; <span class="built_in">cd</span> astrbot</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载官方提供的 Docker Compose 配置文件</span></span><br><span class="line">wget https://raw.githubusercontent.com/NapNeko/NapCat-Docker/main/compose/astrbot.yml</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>注</strong>：若 <code>wget</code> 下载失败，可手动创建 <code>astrbot.yml</code> 文件，内容如下：  </p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;3&#x27;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">napcat:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mlikiowa/napcat-docker:latest</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">napcat</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">NAPCAT_UID=1000</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">NAPCAT_GID=1000</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;6099:6099&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./napcat/data:/app/.napcat</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./napcat/config:/app/napcat/config</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">astrbot:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">soulter/astrbot:latest</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">astrbot</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;6185:6185&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./astrbot/data:/app/data</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ASTRBOT_WS_URL=ws://napcat:3001</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">napcat</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">unless-stopped</span></span><br></pre></td></tr></table></figure></blockquote>
<h3 id="启动所有服务"><a href="#启动所有服务" class="headerlink" title="启动所有服务"></a>启动所有服务</h3><p>在 <code>astrbot.yml</code> 文件所在目录下，执行：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> docker compose -f astrbot.yml up -d</span><br></pre></td></tr></table></figure>

<p>执行成功后，Docker 会自动拉取镜像并启动两个容器。可以通过 <code>sudo docker ps</code> 查看容器状态，确保 <code>napcat</code> 和 <code>astrbot</code> 均为 <code>Up</code> 状态。  </p>
<p>如果你的 Docker Compose 是独立安装的老版本，上面的命令执行会报错，先升级 Docker Compose：首选 docker compose（V2）插件。  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install docker-compose-plugin</span><br></pre></td></tr></table></figure>

<p>升级后再执行下面的命令重新启动服务。  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">NAPCAT_UID=$(<span class="built_in">id</span> -u) NAPCAT_GID=$(<span class="built_in">id</span> -g) <span class="built_in">sudo</span> -E docker compose -f astrbot.yml up -d</span><br></pre></td></tr></table></figure>

<p>正常情况下这里 tui 会输出一个登录二维码，用你的QQ小号扫码登录。  </p>
<h3 id="通过SSH隧道访问网页界面"><a href="#通过SSH隧道访问网页界面" class="headerlink" title="通过SSH隧道访问网页界面"></a>通过SSH隧道访问网页界面</h3><p>和桌面版配置差异的关键来了，服务器只有一个终端，并没有界面，我们需要利用 SSH隧道 使我们本地机器能访问到服务器端的 WebUI。  </p>
<p>本地开一个新终端，使用下面的命令登录服务器：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -L 6099:localhost:6099 -L 6185:localhost:6185 &lt;你的服务器用户名&gt;@&lt;你的服务器IP地址&gt;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>注意：请把命令中的 &lt;你的服务器用户名&gt; 和 &lt;你的服务器IP地址&gt; 替换成你自己的信息。这条命令会同时建立两条隧道，分别对应 NapCat 的 6099 端口和 AstrBot 的 6185 端口。  </p>
</blockquote>
<p>输入密码登录后，先不要关闭这个窗口。然后打开你电脑的浏览器，输入对应的地址就能访问管理面板了：  </p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">NapCat 面板: </span><br><span class="line">http://localhost:6099/webui</span><br><span class="line"></span><br><span class="line">AstrBot 面板: </span><br><span class="line">http://localhost:6185</span><br></pre></td></tr></table></figure>

<p>NapCat登录需要token，在服务器终端使用下面的命令获取，粘贴登录即可：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> docker logs napcat 2&gt;&amp;1 | grep -i token</span><br></pre></td></tr></table></figure>

<p>AstrBot登录，初始账户密码均为<code>astrbot</code>（忘了有没有大写了，在页面下端有提示），登录后会提醒你修改账户名和密码。  </p>
<h3 id="配置-NapCat-面板"><a href="#配置-NapCat-面板" class="headerlink" title="配置 NapCat 面板"></a>配置 NapCat 面板</h3><p>在上一步之后，后面的配置几乎和网络上的教程没有任何差别了（因为几乎都是通过浏览器界面配置的），如果想探索更多玩法，可以自行搜索教程配置，我就不赘述了，下面仅仅记录最基础的内容，让我们把QQ机器人用起来。  </p>
<p>……  </p>
<p>如你所见，只是用起来的话，NapCat并不再需要任何配置，实际上我们使用NapCat，主要的配置就是让他连接上AstrBot，由于上面我们是docker一体部署启动，所以这一步已经完成了（大概如此，或许只是创建连接接口，但是我们一体部署都做好了吧）。  </p>
<p>总之，后面配置检查不成功，再去搜索看看这一步怎么做。  </p>
<p>在实际的使用中，我们想要更多QQ机器人的功能，多半也是去配置AstrBot。  </p>
<h3 id="配置-AstrBot-面板"><a href="#配置-AstrBot-面板" class="headerlink" title="配置 AstrBot 面板"></a>配置 AstrBot 面板</h3><h4 id="创建机器人"><a href="#创建机器人" class="headerlink" title="创建机器人"></a>创建机器人</h4><ol>
<li>左侧 <code>机器人</code>；  </li>
<li>右上角 <code>创建机器人</code>；  </li>
<li>选择消息平台类别：<code>OneBot V11</code>；  </li>
<li><code>保存</code> -&gt; <code>无视警告并继续创建</code>。</li>
</ol>
<p>然后就出现了一个名为 <code>default</code> 的机器人名片，点击名片右上角开关启用它，查看下方 <code>平台日志</code>，出现 <code>适配器已连接</code> 即成功，如果不成功可能要去看看<strong>NapCat面板</strong>怎么配置。  </p>
<h4 id="接入大模型（以智谱-AI-为例）"><a href="#接入大模型（以智谱-AI-为例）" class="headerlink" title="接入大模型（以智谱 AI 为例）"></a>接入大模型（以智谱 AI 为例）</h4><ol>
<li>左侧 <code>模型提供商</code>；  </li>
<li>提供商源卡片 <code>新增</code>；</li>
<li>选择 <code>Zhipu</code>;  </li>
<li>填写 <code>API Key</code> 和 <code>API Base URL</code> （默认的应该也对），<code>保存配置</code>；  </li>
<li>下方 <code>获取模型列表</code>，按自己需求点击右侧 <code>+</code>，然后打开开关；  </li>
<li>左侧 <code>配置文件</code> -&gt; <code>普通配置</code>，在默认对话模型选择提供商，按喜好选一个即可；  </li>
<li>下方 <code>选择人格</code>，这里可以自行编写一些机器人的设定；  </li>
<li>最下面 <code>其他配置</code> 中 <code>身份识别</code> 以及 <code>显示群名称</code>，这两项建议开启，开启后可以在人格设定或者skill中用自然语言让你拥有绝对控制权；  </li>
<li>（还有其他功能就不多说了，比如联网搜索功能，打开开关就行，更多其他配置参考AstrBot文档）  </li>
<li>web 页面右下角图标保存配置。</li>
</ol>
<h3 id="测试与验证"><a href="#测试与验证" class="headerlink" title="测试与验证"></a>测试与验证</h3><p>（累啊。。我说手码字已经过时了）</p>
<p>现在，向你的机器人 QQ 号发送一条消息，收到回复，则说明部署成功！  </p>
<p>一般，我都会发一个：你是谁？  </p>
<h3 id="查看日志排查问题"><a href="#查看日志排查问题" class="headerlink" title="查看日志排查问题"></a>查看日志排查问题</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看 AstrBot 日志</span></span><br><span class="line"><span class="built_in">sudo</span> docker logs -f astrbot</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看 NapCat 日志</span></span><br><span class="line"><span class="built_in">sudo</span> docker logs -f napcat</span><br></pre></td></tr></table></figure>

<h3 id="常见问题速查"><a href="#常见问题速查" class="headerlink" title="常见问题速查"></a>常见问题速查</h3><table>
<thead>
<tr>
<th align="left">现象</th>
<th align="left">可能原因</th>
<th align="left">解决方法</th>
</tr>
</thead>
<tbody><tr>
<td align="left">无法访问 NapCat WebUI (6099)</td>
<td align="left">防火墙未开放端口</td>
<td align="left">在云控制台安全组&#x2F;系统防火墙中放行 <code>6099</code> 端口</td>
</tr>
<tr>
<td align="left">扫码后登录失败</td>
<td align="left">账号被风控</td>
<td align="left">尝试更换网络环境，或更换小号重试</td>
</tr>
<tr>
<td align="left">机器人不回复消息</td>
<td align="left">AI 配置有误或余额不足</td>
<td align="left">检查 API Key 是否正确、账户是否有额度；查看 AstrBot 日志</td>
</tr>
<tr>
<td align="left">AstrBot 面板无法访问 (6185)</td>
<td align="left">端口未开放或容器未启动</td>
<td align="left">检查防火墙规则，运行 <code>docker ps</code> 确认容器状态</td>
</tr>
</tbody></table>
<h3 id="我说-写完了"><a href="#我说-写完了" class="headerlink" title="我说 写完了"></a>我说 写完了</h3><p>至此，一个功能完整、24 小时在线的 QQ 机器人就部署完成了。你可以通过 AstrBot 的插件市场为其添加更多能力，比如天气查询、网页搜索、定时提醒等，把它打造成专属的智能助理。祝玩得开心。  </p>
<script type="module"> import mermaid from 'https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.esm.min.mjs';	mermaid.initialize({startOnLoad: true, flowchart: {curve: 'linear'}}); </script>]]></content>
      <tags>
        <tag>AI</tag>
      </tags>
  </entry>
  <entry>
    <title>法线矩阵的推导</title>
    <url>/2025/10/17/%E6%B3%95%E7%BA%BF%E7%9F%A9%E9%98%B5%E7%9A%84%E6%8E%A8%E5%AF%BC/</url>
    <content><![CDATA[<p>首先明确一点，法线矩阵是计算法线变换的矩阵，使得新的法线仍垂直于原来的平面。因为模型矩阵转化物体到世界坐标时，除了平移和均匀缩放，都可能破坏原法线的垂直性，比如将一个cube绕x轴旋转30度，原来的法线仍在y平面和z平面上，但物体表面已经不再原位置上了，这时我们需要把原法线也绕x轴旋转30度才能保持他们仍是原表面的法线。  </p>
<p>至于为什么提到是“变换”矩阵的逆转置，原则上只有模型矩阵会破坏法线垂直性，但有时我们需要在别的空间计算，比如观察空间，<code>view * model</code>此时就是我们的变换矩阵<strong>M</strong>，可以把他整体当做一个模型矩阵，因为模型矩阵包含其中，可能破坏法线垂直性。  </p>
<p>接下来我们推导法线矩阵（假设向量<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>n</mi></mrow><annotation encoding="application/x-tex">n</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal">n</span></span></span></span>和<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>v</mi></mrow><annotation encoding="application/x-tex">v</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span></span></span></span>都是3x1列向量）：  </p>
<p>(1) 我们有法线向量<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>n</mi></mrow><annotation encoding="application/x-tex">n</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal">n</span></span></span></span>和切线向量<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>v</mi></mrow><annotation encoding="application/x-tex">v</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span></span></span></span>  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mi>n</mi><mo>⋅</mo><mi>v</mi><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n \cdot v = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4445em;"></span><span class="mord mathnormal">n</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⋅</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>(2) 假设变换后的法线为n’，那么  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>⋅</mo><msup><mi>v</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n&#x27; \cdot v&#x27; = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⋅</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>这里需要提及一个转换，矩阵乘法和点乘的转换，靠转置实现：  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mi>n</mi><mo>⋅</mo><mi>v</mi><mo>=</mo><mn>0</mn><mo>=</mo><mo>&gt;</mo><msup><mi>n</mi><mi>T</mi></msup><mo>∗</mo><mi>v</mi><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n \cdot v = 0 =&gt;  n^T * v = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.4445em;"></span><span class="mord mathnormal">n</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⋅</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6835em;vertical-align:-0.0391em;"></span><span class="mord">0</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=&gt;</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>(3) 那么(2)可以转化为矩阵乘法  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mrow><mo mathvariant="normal">′</mo><mi>T</mi></mrow></msup><mo>∗</mo><msup><mi>v</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n&#x27;^T * v&#x27; = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>(4) 假设法线矩阵为<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><msup><mi>M</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup></mrow><annotation encoding="application/x-tex">M&#x27;</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7519em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.7519em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span></span></span></span>，那么  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><msup><mi>M</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>∗</mo><mi>n</mi></mrow><annotation encoding="application/x-tex">
n&#x27; = M&#x27; * n
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal">n</span></span></span></span></span>

<p>(5) 切线通过变换矩阵<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>M</mi></mrow><annotation encoding="application/x-tex">M</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.10903em;">M</span></span></span></span>变换得到<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><msup><mi>v</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup></mrow><annotation encoding="application/x-tex">v&#x27;</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7519em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.7519em;"><span style="top:-3.063em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span></span></span></span>  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>v</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mi>M</mi><mo>∗</mo><mi>v</mi></mrow><annotation encoding="application/x-tex">
v&#x27; = M * v
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.4306em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span></span></span></span></span>

<p>(6) 将上面的等式代入(3)，全部转化为矩阵乘法（后面不再写符号）  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mrow><mo mathvariant="normal">′</mo><mi>T</mi></mrow></msup><mo>∗</mo><msup><mi>v</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mo stretchy="false">(</mo><msup><mi>M</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>∗</mo><mi>n</mi><msup><mo stretchy="false">)</mo><mi>T</mi></msup><mo>∗</mo><mo stretchy="false">(</mo><mi>M</mi><mo>∗</mo><mi>v</mi><mo stretchy="false">)</mo><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n&#x27;^T * v&#x27; = (M&#x27; * n)^T * (M * v) = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:1.0519em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1.1413em;vertical-align:-0.25em;"></span><span class="mord mathnormal">n</span><span class="mclose"><span class="mclose">)</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mclose">)</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>把它完全展开：  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mi>T</mi></msup><msup><mi>M</mi><mrow><mo mathvariant="normal">′</mo><mi>T</mi></mrow></msup><mi>M</mi><mi>v</mi><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n^TM&#x27;^TMv = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>因为  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>n</mi><mi>T</mi></msup><mi>v</mi><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex">
n^Tv = 0
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal">n</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span><span class="mord mathnormal" style="margin-right:0.03588em;">v</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span></span>

<p>所以  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>M</mi><mrow><mo mathvariant="normal">′</mo><mi>T</mi></mrow></msup><mi>M</mi><mo>=</mo><mi>λ</mi><mi>I</mi></mrow><annotation encoding="application/x-tex">
M&#x27;^TM = \lambda I
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6944em;"></span><span class="mord mathnormal">λ</span><span class="mord mathnormal" style="margin-right:0.07847em;">I</span></span></span></span></span>

<p>我们最终只需要垂直关系，不受比例因子影响，将<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>λ</mi></mrow><annotation encoding="application/x-tex">\lambda</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6944em;"></span><span class="mord mathnormal">λ</span></span></span></span>代入1得  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>M</mi><mrow><mo mathvariant="normal">′</mo><mi>T</mi></mrow></msup><mo>=</mo><msup><mi>M</mi><mrow><mo>−</mo><mn>1</mn></mrow></msup><mo>=</mo><mo>&gt;</mo><msup><mi>M</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mo stretchy="false">(</mo><msup><mi>M</mi><mrow><mo>−</mo><mn>1</mn></mrow></msup><msup><mo stretchy="false">)</mo><mi>T</mi></msup></mrow><annotation encoding="application/x-tex">
M&#x27;^T = M^{-1} =&gt; M&#x27; = (M^{-1})^T
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8913em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.9032em;vertical-align:-0.0391em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">−</span><span class="mord mtight">1</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=&gt;</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:1.1413em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">−</span><span class="mord mtight">1</span></span></span></span></span></span></span></span></span><span class="mclose"><span class="mclose">)</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span></span></span></span>

<p>所以我们的法线矩阵就是变换矩阵逆的转置  </p>
<span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><msup><mi>M</mi><mo mathvariant="normal" lspace="0em" rspace="0em">′</mo></msup><mo>=</mo><mo stretchy="false">(</mo><msup><mi>M</mi><mrow><mo>−</mo><mn>1</mn></mrow></msup><msup><mo stretchy="false">)</mo><mi>T</mi></msup></mrow><annotation encoding="application/x-tex">
M&#x27; = (M^{-1})^T
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.8019em;"></span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8019em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">′</span></span></span></span></span></span></span></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:1.1413em;vertical-align:-0.25em;"></span><span class="mopen">(</span><span class="mord"><span class="mord mathnormal" style="margin-right:0.10903em;">M</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8641em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">−</span><span class="mord mtight">1</span></span></span></span></span></span></span></span></span><span class="mclose"><span class="mclose">)</span><span class="msupsub"><span class="vlist-t"><span class="vlist-r"><span class="vlist" style="height:0.8913em;"><span style="top:-3.113em;margin-right:0.05em;"><span class="pstrut" style="height:2.7em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mathnormal mtight" style="margin-right:0.13889em;">T</span></span></span></span></span></span></span></span></span></span></span></span>

<p>在OpenGL中，我们常常在着色器中这样计算法线矩阵：  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line">Normal = <span class="type">mat3</span>(<span class="built_in">transpose</span>(<span class="built_in">inverse</span>(model))) * aNormal;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>理解镜面反射反光度和风氏模型反射缺陷</title>
    <url>/2025/10/22/%E7%90%86%E8%A7%A3%E9%95%9C%E9%9D%A2%E5%8F%8D%E5%B0%84%E5%8F%8D%E5%85%89%E5%BA%A6/</url>
    <content><![CDATA[<p>在风氏模型中，计算镜面反射最后要用物体<strong>反光度</strong>作为指数，对视觉中看到的<strong>光强</strong>进行<strong>幂运算</strong>得到最终结果。  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> spec = <span class="built_in">pow</span>(<span class="built_in">max</span>(<span class="built_in">dot</span>(view_direction, light_reflect_direction), <span class="number">0.0</span>f), material.shininess);</span><br></pre></td></tr></table></figure>

<p>shininess（反光度）是控制镜面高光 “锐利程度” 的核心参数。  </p>
<p>我们把上面的语句拆开：  </p>
<ul>
<li><p><code>dot(view_direction, light_reflect_direction)</code>，这计算的是视线方向与反射光方向夹角的余弦值（范围 [-1, 1]），我们在外层加了个<code>max(..., 0)</code>是默认当反射光和视线成钝角时（余弦值为负数），相当于光线没进入人眼，反光强度为0（通过学习<strong>Blinn-Phong模型</strong>我们知道这并不自然）  </p>
</li>
<li><p><code>pow(..., shininess)</code>，pow函数作为一个衰减函数，在反光度shininess<strong>很小</strong>时会衰减得很慢，例如，<code>0.8^1 = 0.8</code>，<code>0.5^1 = 0.5</code>，意味着<strong>较大范围的角度都能贡献明显的高光</strong>（高光区域大而模糊）；而反光度shininess<strong>很大</strong>时会衰减得很快，例如，<code>8^100 ≈ 0，0.95^100 ≈ 0.6</code>，意味着<strong>只有视线与反射光方向非常接近时（夹角极小）才有高光</strong>（高光区域小而锐利）</p>
</li>
</ul>
<p>所以反光度某种意义上是一个用来模拟表面的粗糙 &#x2F; 光滑程度的参数，反光度越小，物体表面越粗糙，越大越光滑（下面两个参数资料来自AI回答）。  </p>
<ul>
<li>低 shininess（如 1～32）：模拟粗糙表面（如木头、石头、磨砂塑料）。<br>这类表面会把光线向四面八方散射，所以镜面高光范围大、边缘模糊，亮度随角度变化平缓。</li>
<li>高 shininess（如 64～1024）：模拟光滑表面（如金属、镜子、抛光塑料）。<br>这类表面的反射更集中，只有在视线接近反射光方向时才能看到高光，所以高光区域小、边缘锐利，亮度随角度变化剧烈。</li>
</ul>
<p>由此引申出风氏模型计算镜面光的不自然之处，在物体表面粗糙时（反光度小），反光的范围大，在远处表面即使光线和视线形成钝角，我们应该还是能看到一些反射光而不是直接为0。  </p>
<p>我们可以想象一下为什么风氏模型镜面反射表现不自然；比如我们面前放着一张砂纸，太阳在我们脑后，这时反射光和视线是为钝角的，但我们依然可以看到砂纸表面斑驳的亮点，究其原因就是砂纸表面粗糙，光线在某些地方还是能和视线形成锐角的，而不是超过一定角度就直接变暗。  </p>
<p>我们在<strong>Blinn-Phong</strong>模型中利用<strong>半程向量</strong>解决了这个问题，但实际上这也只是一种模拟罢了，只是比传统风氏模型更自然点，不会出现在某处光线突然变暗的情况。  </p>
<p>应该可以这么去理解，真正光滑的表面就是一个表面，而粗糙的表面是由无数细小杂乱的表面组合而成的，要计算<strong>真正的</strong>反射计算量太大，实现也过于复杂。  </p>
<p>所以微观上可以这样理解反光度shininess，它小，表示无数小平面朝向极乱；它大，表示无数小平面朝向趋于一致。  </p>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>简单CPU设计实践</title>
    <url>/2025/04/22/%E7%AE%80%E5%8D%95CPU%E8%AE%BE%E8%AE%A1%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<p>博客参考：<a href="https://lianera.github.io/post/2014/cpu-step-by-step/#:~:text=%E6%9C%AC%E6%96%87%E5%B0%86%E4%BB%8E%E9%9B%B6%E5%BC%80%E5%A7%8B%E4%BB%8B%E7%BB%8D%E5%A4%84%E7%90%86%E5%99%A8%E7%9A%84%E5%9F%BA%E6%9C%AC%E5%8E%9F%E7%90%86%E5%92%8C%E6%9E%B6%E6%9E%84%EF%BC%8C%E5%B9%B6%E4%B8%94%E7%94%A8%E7%AE%80%E5%8D%95%E7%9A%84%E7%94%B5%E8%B7%AF%E6%90%AD%E5%BB%BA%E6%96%B9%E5%BC%8F%E5%AE%9E%E7%8E%B0%E4%B8%80%E4%B8%AA%E9%9D%9E%E5%B8%B8%E7%AE%80%E5%8D%95%E7%9A%848%20bit,CPU%E3%80%82%20%E7%94%B1%E4%BA%8E%E6%95%B0%E5%AD%97%E7%94%B5%E8%B7%AF%E5%86%85%E5%AE%B9%E5%8F%82%E8%80%83%E8%B5%84%E6%96%99%E4%BC%97%E5%A4%9A%EF%BC%8C%E5%B9%B6%E4%B8%94%E5%A4%A7%E5%A4%9A%E6%95%B0%E4%B8%93%E4%B8%9A%E5%BC%80%E8%AE%BE%E8%BF%87%E6%95%B0%E5%AD%97%E7%94%B5%E8%B7%AF%EF%BC%8C%E6%89%80%E4%BB%A5%E5%9C%A8%E6%AD%A4%E7%95%A5%E8%BF%87%E6%95%B0%E7%94%B5%E7%9A%84%E7%9F%A5%E8%AF%86%EF%BC%8C%E5%A6%82%E6%9E%9C%E5%AF%B9%E6%95%B0%E7%94%B5%E6%9C%89%E4%BA%9B%E4%B8%8D%E6%98%8E%E7%99%BD%E7%9A%84%EF%BC%8C%E5%8F%AF%E4%BB%A5%E5%8F%82%E8%80%83%E5%85%B6%E4%BB%96%E6%95%B0%E7%94%B5%E6%95%99%E7%A8%8B%E3%80%82">简单CPU设计实践 - Lianera</a></p>
<p>感谢每一位无私分享知识的笔者。  </p>
<h2 id="一、基础"><a href="#一、基础" class="headerlink" title="一、基础"></a>一、基础</h2><h3 id="基本逻辑门"><a href="#基本逻辑门" class="headerlink" title="基本逻辑门"></a>基本逻辑门</h3><p>（严格意义上，只有不可分割的与、或、非门是基本逻辑门）  </p>
<p>AND(与)、OR(或)、NOT(非)、NAND(与非)、NOR(或非)、XOR(异或)、XNOR（同或）  </p>
<p><img src="/../images/post/cpu-1-%E5%9F%BA%E6%9C%AC%E9%80%BB%E8%BE%91%E9%97%A8.png" alt="基本逻辑门">  </p>
<p>以上门电路真值表可查阅：<a href="https://blog.csdn.net/weixin_52487896/article/details/125889966">常见七种逻辑门真值表 - Star、H</a>  </p>
<h3 id="组合逻辑电路分析设计方法"><a href="#组合逻辑电路分析设计方法" class="headerlink" title="组合逻辑电路分析设计方法"></a>组合逻辑电路分析设计方法</h3><p>分三步，需求分析-&gt;方案设计-&gt;电路实现  </p>
<p><strong>第一步：需求分析-&gt;真值表</strong>  </p>
<table>
<thead>
<tr>
<th align="center">A</th>
<th align="center">B</th>
<th align="center">C</th>
<th align="center">F</th>
</tr>
</thead>
<tbody><tr>
<td align="center">0</td>
<td align="center">0</td>
<td align="center">0</td>
<td align="center">0</td>
</tr>
<tr>
<td align="center">0</td>
<td align="center">0</td>
<td align="center">1</td>
<td align="center">0</td>
</tr>
<tr>
<td align="center">0</td>
<td align="center">1</td>
<td align="center">0</td>
<td align="center">0</td>
</tr>
<tr>
<td align="center">1</td>
<td align="center">0</td>
<td align="center">0</td>
<td align="center">0</td>
</tr>
<tr>
<td align="center">0</td>
<td align="center">1</td>
<td align="center">1</td>
<td align="center">1</td>
</tr>
<tr>
<td align="center">1</td>
<td align="center">0</td>
<td align="center">1</td>
<td align="center">1</td>
</tr>
<tr>
<td align="center">1</td>
<td align="center">1</td>
<td align="center">0</td>
<td align="center">1</td>
</tr>
<tr>
<td align="center">1</td>
<td align="center">1</td>
<td align="center">1</td>
<td align="center">1</td>
</tr>
</tbody></table>
<p>需要列举出所有可能（2^N行，N是输入参数个数），其中ABC是输入，F是输出  </p>
<p><strong>第二步：化简逻辑-&gt;卡诺图</strong>  </p>
<p>卡诺图是用来化简表达式的工具  </p>
<ol>
<li>把真值表中F&#x3D;1的项标在卡诺图里  </li>
<li>圈出最大的相邻1的矩形（2×2、1×4等）  </li>
<li>写出化简后的表达式</li>
</ol>
<p>比如上边真值表的例子，化简出来的表达式是：<br>F &#x3D; AB + AC + BC  </p>
<p>这就好像是有一个仅有三道题的考试（ABC），任意写对两道题，最后的结果就是通过考试  </p>
<p><strong>第三步：电路实现-&gt;表达式变电路</strong>  </p>
<p>现在把表达式变成电路，还是上面的例子，<strong>AB + AC + BC</strong>，这个表达式怎么处理？  </p>
<p>实际上这里的关系很清晰简单，只有AND和OR  </p>
<p>第一层：用AND门做AB、AC和BC<br>第二层：用OR门把三个AND结果合并  </p>
<h3 id="编码器"><a href="#编码器" class="headerlink" title="编码器"></a>编码器</h3><p>高低电平信号向二进制数的转换  </p>
<p>普通编码器：任何时刻只允许有一个信号输入<br>优先编码器：允许同时输入两个以上的编码信号，只对其中优先权最高的一个进行编码  </p>
<p>博客资料：<a href="https://zhuanlan.zhihu.com/p/569801290">自上而下理解优先编码器</a>  </p>
<h3 id="译码器"><a href="#译码器" class="headerlink" title="译码器"></a>译码器</h3><p>二进制数向高低电平信号的转换  </p>
<h3 id="数据选择器"><a href="#数据选择器" class="headerlink" title="数据选择器"></a>数据选择器</h3><h3 id="比较器"><a href="#比较器" class="headerlink" title="比较器"></a>比较器</h3><h3 id="加法器"><a href="#加法器" class="headerlink" title="加法器"></a>加法器</h3><h3 id="三态门"><a href="#三态门" class="headerlink" title="三态门"></a>三态门</h3><h3 id="基本触发器"><a href="#基本触发器" class="headerlink" title="基本触发器"></a>基本触发器</h3><h3 id="锁存器"><a href="#锁存器" class="headerlink" title="锁存器"></a>锁存器</h3><h3 id="寄存器"><a href="#寄存器" class="headerlink" title="寄存器"></a>寄存器</h3><h3 id="移位寄存器"><a href="#移位寄存器" class="headerlink" title="移位寄存器"></a>移位寄存器</h3><h3 id="计数器"><a href="#计数器" class="headerlink" title="计数器"></a>计数器</h3>]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>cpu</tag>
      </tags>
  </entry>
  <entry>
    <title>透视投影与深度</title>
    <url>/2025/10/28/%E9%80%8F%E8%A7%86%E6%8A%95%E5%BD%B1%E4%B8%8E%E6%B7%B1%E5%BA%A6/</url>
    <content><![CDATA[<p>先记录两篇博客：  </p>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/122411512">推导投影矩阵</a>  </li>
<li><a href="https://zhuanlan.zhihu.com/p/718895401">3DGS学习笔记-透视投影矩阵推导</a></li>
</ul>
<p>这两篇笔记都是推导透视投影矩阵的，通过第二篇文章可知第一篇文章的推导是基于<strong>左手坐标系</strong>的，最明显的区别就在于，透视投影矩阵的<strong>第四行</strong>是<code>(0,0,1,0)</code>还是<code>(0,0,-1,0)</code>，前者是左手坐标系，后者是右手坐标系。  </p>
<p>相机空间点 <code>(x_cam, y_cam, z_cam, 1)</code>（z_cam 为负，因为在相机前方）→ 透视投影矩阵变换 → 裁剪空间点 <code>(x_clip, y_clip, z_clip, w_clip=-z_cam)</code> → 透视除法 <code>(x_clip/w_clip, y_clip/w_clip, z_clip/w_clip)</code> → NDC。</p>
<p>经过透视投影之后，观察空间转换到裁剪空间，OpenGL采用右手坐标系，<code>w_clip = -z_cam</code>，如果是左手坐标系，<code>w_clip = z_cam</code>。  </p>
<p>注意是裁剪空间中的w与观察空间中z的关系。  </p>
<p>这个关系是透视除法的关键，因为 <strong>w_clip</strong> 恰好是物体到相机的距离，我们经过透视除法能营造出“<strong>近大远小</strong>”的效果。  </p>
<p>透视投影还会改变坐标系手性，注意看透视投影矩阵第三行，把z轴翻了个面，所以透视投影之后的裁剪空间，NDC，视口变换都在左手坐标系中。  </p>
<p>我们可以利用透视除法（齐次除法），将裁剪空间转换为NDC（Normalized Device Coordinates，标准化设备坐标），NDC中xyz的范围都是<code>[-1,1]</code>，其z值就是<strong>规范化的深度值</strong>，超出这个范围的顶点将被裁剪不做渲染。  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">vec3</span> ndc = <span class="built_in">gl_Position</span>.xyz / <span class="built_in">gl_Position</span>.w;</span><br></pre></td></tr></table></figure>

<p>手动计算 NDC 一般都是为了获取规范化的深度值做一些渲染工作。  </p>
<p>需要注意 NDC 的 z 轴方向符合左手系特性（+z指向屏幕内）。  </p>
<p>在OpenGL渲染管线中，裁剪之后会自动执行透视除法转换到NDC。这里要提及一件事，就是<strong>深度缓冲的取值范围是[0,1]</strong>，深度缓冲中的深度值才是真正的深度值，但是“值越小越近”的逻辑和前面是一样的，切忌和规范化的深度值产生混淆。  </p>
<p>计算深度缓冲：  </p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line">depth = (z_ndc + <span class="number">1</span>) / <span class="number">2</span>;    <span class="comment">// 将[-1,1]转换到[0, 1]</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>图形学</category>
      </categories>
      <tags>
        <tag>OpenGL</tag>
      </tags>
  </entry>
  <entry>
    <title>配置终端代理</title>
    <url>/2025/04/13/%E9%85%8D%E7%BD%AE%E7%BB%88%E7%AB%AF%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<h2 id="配置git终端代理（常用）"><a href="#配置git终端代理（常用）" class="headerlink" title="配置git终端代理（常用）"></a>配置git终端代理（常用）</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置代理</span></span><br><span class="line">git config --global http.proxy <span class="string">&quot;http://127.0.0.1:10809&quot;</span></span><br><span class="line">git config --global https.proxy <span class="string">&quot;https://127.0.0.1:10809&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 取消代理</span></span><br><span class="line">git config --global --<span class="built_in">unset</span> http.proxy</span><br><span class="line">git config --global --<span class="built_in">unset</span> https.proxy</span><br></pre></td></tr></table></figure>

<h2 id="Power-shell"><a href="#Power-shell" class="headerlink" title="Power shell"></a>Power shell</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置</span></span><br><span class="line"><span class="variable">$env</span>:http_proxy = <span class="string">&quot;http://代理服务器地址:端口&quot;</span></span><br><span class="line"><span class="variable">$env</span>:https_proxy = <span class="string">&quot;http://代理服务器地址:端口&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看</span></span><br><span class="line"><span class="variable">$env</span>:http_proxy</span><br><span class="line"><span class="variable">$env</span>:https_proxy</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清除</span></span><br><span class="line">Remove-Item Env:http_proxy</span><br><span class="line">Remove-Item Env:https_proxy</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>vpn</tag>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>配置解决Powershell输出中文乱码</title>
    <url>/2026/02/04/%E9%85%8D%E7%BD%AE%E8%A7%A3%E5%86%B3Powershell%E8%BE%93%E5%87%BA%E4%B8%AD%E6%96%87%E4%B9%B1%E7%A0%81/</url>
    <content><![CDATA[<p>把下面的配置添加到Powershell的配置文件中，每次打开Powershell都会自动生效了，当然先建议临时修改看看有没有用。</p>
<p>打开配置文件：  </p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">nvim <span class="variable">$PROFILE</span></span><br></pre></td></tr></table></figure>

<p>添加配置：  </p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># set powershell fileencoding UTF8</span></span><br><span class="line">[<span class="type">Console</span>]::OutputEncoding = [<span class="type">System.Text.Encoding</span>]::UTF8</span><br><span class="line">[<span class="type">Console</span>]::InputEncoding = [<span class="type">System.Text.Encoding</span>]::UTF8</span><br><span class="line"><span class="comment"># set Get-Content(cat) fileencoding UTF8</span></span><br><span class="line"><span class="variable">$PSDefaultParameterValues</span>[<span class="string">&#x27;Get-Content:Encoding&#x27;</span>] = <span class="string">&#x27;UTF8&#x27;</span></span><br></pre></td></tr></table></figure>

<p>我使用neovim编辑，如果没有这个文件可以新建一个：</p>
<p>powershell管理员模式下：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">nvim <span class="variable">$PROFILE</span></span><br></pre></td></tr></table></figure>

<p>或者去<code>C:\Users\[用户名]\Documents\WindowsPowerShell</code>目录下新建一个<code>Microsoft.PowerShell_profile.ps1</code>文件，用记事本编辑。</p>
<p>乱码多半是文件编码和shell输出编码不匹配的原因，我们统一选择使用<code>utf-8</code>编码，可以先临时配置先看看效果。  </p>
<p>先查看文件编码是不是utf-8编码，方法非常多，比如用记事本或者其他什么编辑器打开，一般右下角就会有显示。  </p>
<p>临时设置直接执行命令就好，设置cat(Get-Content)命令使用utf-8编码：  </p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="variable">$PSDefaultParameterValues</span>[<span class="string">&#x27;Get-Content:Encoding&#x27;</span>] = <span class="string">&#x27;UTF8&#x27;</span></span><br></pre></td></tr></table></figure>

<p>然后用cat命令检查一下乱码是否好了。</p>
<p>再就是可执行文件的中文输出，先执行下面的命令再执行可执行文件：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">[<span class="type">Console</span>]::OutputEncoding = [<span class="type">System.Text.Encoding</span>]::UTF8</span><br><span class="line">[<span class="type">Console</span>]::InputEncoding = [<span class="type">System.Text.Encoding</span>]::UTF8</span><br></pre></td></tr></table></figure>

<p>看到修改生效了再写到配置文件中，如果不生效重新启动一个Powershell窗口环境不变，寻找其他方法。  </p>
<p><strong><code>git status</code> 显示不出中文</strong>  </p>
<p>有时候可能会碰到这种情况：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">@cola ➜ blog git(main) git status</span><br><span class="line">On branch main</span><br><span class="line">Your branch is ahead of <span class="string">&#x27;origin/main&#x27;</span> by 1 commit.</span><br><span class="line">  (use <span class="string">&quot;git push&quot;</span> to publish your <span class="built_in">local</span> commits)</span><br><span class="line"></span><br><span class="line">Changes not staged <span class="keyword">for</span> commit:</span><br><span class="line">  (use <span class="string">&quot;git add &lt;file&gt;...&quot;</span> to update what will be committed)</span><br><span class="line">  (use <span class="string">&quot;git restore &lt;file&gt;...&quot;</span> to discard changes <span class="keyword">in</span> working directory)</span><br><span class="line">        modified:   <span class="string">&quot;source/_posts/tcp-ip\347\274\226\347\250\213.md&quot;</span></span><br></pre></td></tr></table></figure>

<p>这是因为 Git 默认会对非 ASCII 字符（比如中文）的文件名进行八进制转义，以确保在旧终端或跨平台场景下不会出现乱码。  </p>
<p>运行以下命令，关闭路径转义：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global core.quotePath <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p><code>core.quotePath</code> 默认为 true，Git 会把高字节字符（非 ASCII）转义为 \xxx 形式。  </p>
<p>将其改为false之后：  </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">@cola ➜ blog git(main) git status</span><br><span class="line">On branch main</span><br><span class="line">Your branch is ahead of <span class="string">&#x27;origin/main&#x27;</span> by 1 commit.</span><br><span class="line">  (use <span class="string">&quot;git push&quot;</span> to publish your <span class="built_in">local</span> commits)</span><br><span class="line"></span><br><span class="line">Changes not staged <span class="keyword">for</span> commit:</span><br><span class="line">  (use <span class="string">&quot;git add &lt;file&gt;...&quot;</span> to update what will be committed)</span><br><span class="line">  (use <span class="string">&quot;git restore &lt;file&gt;...&quot;</span> to discard changes <span class="keyword">in</span> working directory)</span><br><span class="line">        modified:   <span class="built_in">source</span>/_posts/tcp-ip编程.md</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>随想</category>
      </categories>
      <tags>
        <tag>环境配置</tag>
      </tags>
  </entry>
</search>
